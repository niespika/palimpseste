# PROMPT — Session Code : C6-L3 — « En faire plus »

> **À coller dans une session Claude Code fraîche.** Une session, un lot.
>
> Ce prompt est fabriqué selon la recette du `PLAN_DE_CHANTIER.md` §5. Ce que le lot construit, son manifeste et son « fait quand » font foi au `07-Implementation.md` §2 — ils sont recopiés ci-dessous avec **leurs versions au moment de l'écriture**.
>
> ⭐⭐ **TON LOT N'AJOUTE PAS UN CANAL : IL EN BRANCHE UN QUE TROIS LOTS ONT POSÉ SANS JAMAIS LE RELIER.** L'emplacement de ton offre **existe** — `C6-L2` a construit `/eleve/semaine` en deux temps et **s'est arrêté sur la phrase qui précède la tienne**. La règle du pull **existe**, écrite et gelée au `01-` §5. Le quota **existe** en base, avec son défaut à 30 minutes, et son commentaire de colonne **te nomme**. La marque `bonus` **a un lecteur de production** : la chaîne la recopie déjà sur la mesure. ⛔ **Ce qui manque est au milieu : personne ne pose jamais la marque, et personne ne sert jamais l'exercice de plus.** *Mesuré le 28/08 : **0 instance et 0 mesure marquées `bonus`, dans les deux bases.***

---

## ⭐⭐ CE QUI A ÉTÉ TRANCHÉ AVANT TOI — ne le rouvre pas

**Deux questions se posaient à la fabrication de ce prompt. Louis les a rendues le 28/08.**

| | La question | La réponse de Louis, 28/08 |
|---|---|---|
| **①** | **OÙ VIT LA MARQUE `bonus`.** Le `07-` §1.1 la loge sur **`exercices`** — et la chaîne l'y lit déjà. Mais `exercices` est **la banque** : *« entre élèves, une instance se ressert — une instance, plusieurs dépôts »* *(`utils/moteur/vivier.ts`)*. Un `true` posé là marquerait l'exercice comme bonus **pour tout élève qui le recevra ensuite, imposé compris**. Le fait est par **(élève × exercice)** ; la ligne est **partagée**. ⭐ Et le `01-` §5 dit autre chose — *« porte la marque `bonus` **au journal** (§11) »* —, **où aucune colonne ne l'attend** | ⭐ **LE DOMICILE EST À TOI, AVEC LE CONSTAT EN MAIN ET LA SOURCE SOUS LES YEUX.** Mesure, choisis, porte ta migration si tu en poses une. ⛔ **Et si tu conclus que le `07-` §1.1 est FAUX sur ce point — pas incomplet, faux —, tu poses `[faux]` au point de l'erreur et la dette `D10`** *(`07-` §2)*. **Tu ne récris pas la source.** *Le détail du constat est au §C.* |
| **②** | **LE MANIFESTE.** Ton entrée du `07-` §2 n'avait **ni manifeste ni « fait quand »** — quatrième fois, après `C4-L8-bis`, `C4-L6` et `C5-L4` | ✅ **LES QUATRE PIÈCES SONT CONFIRMÉES, ET ÉCRITES DANS LA SOURCE.** Le `07-` passe en **2.66**, et le geste de clôture a été joué dans la foulée : `derive-instruments.py --ecris` change **exactement quatre valeurs** *(`version` et `empreinte` du `07-` dans `MANIFESTE.ts` et `calame-retour.ts`)*, `--verifie` rend **`INSTRUMENTS : IDENTIQUE`**, `npm test` **1826 / 1826**, `npx tsc --noEmit` propre. **Tu n'as rien à porter dans le `07-` pour ce motif.** |

✅ **L'ALLUMAGE, MESURÉ LE 28/08 : LES SIX SONT À ON DES DEUX CÔTÉS, PRODUCTION COMPRISE** *(plus `rag_actif`)*. **Ta porte — `exercices_actif` — est ouverte partout.** ⚠️ *La fabrication de ce prompt les a trouvés `exercices_actif` et `rag_actif` **à OFF en production** en cours de séance ; **Louis les a rallumés le jour même**. Le `07-` §5 est donc à jour — mais l'épisode dit quelque chose que tu dois retenir.*

⛔⛔ **CE QU'IL FAUT EN RETENIR, ET C'EST UNE RÈGLE DE MÉTHODE, PAS UN CONSTAT : L'ALLUMAGE SE MESURE, IL NE SE RECOPIE PAS.** Ni la phrase du `07-` §5, ni un *« re-constaté à ON »* d'un relevé antérieur ne font foi : **un interrupteur est à Louis, il bouge entre deux séances, et il a bougé pendant celle-ci.** ⛔ **Et `scriptorium_params.updated_at` NE DATE RIEN** — *vérifié deux fois le 28/08 : la ligne de production portait `2026-08-25T01:53` **avant comme après** la bascule.* **Aucun trigger ne l'entretient : ne t'en sers jamais pour dater un changement d'allumage.**

⛔ **Tu ne bascules aucun interrupteur.** *« Une recette qui remet un interrupteur en écrivant une constante ne le remet pas : elle l'impose »* *(`07-` §5)* — **le patron est de MÉMORISER ce qu'on a trouvé et d'y REVENIR**, interruption comprise *(`scripts/recette/lecture-c5l2.mjs`)*. **Et un état de base ne se corrige jamais dans une source : il se dit au relevé.**

⭐ **Et ta boîte aux lettres a été vidée pour ce prompt** *(`PLAN_DE_CHANTIER.md` §5)* : **ses trois items sont entrés en pièges**, au §H. **Tu n'as aucun relevé à aller lire** — la règle de manifeste te l'interdit, et tout ce qui t'était adressé est ici.

---

## Le manifeste — recopié du `07-Implementation.md` §2

> *Manifeste* : **ce document, §1 et §5** · `01-routeur.md` §4, §5, §9, §11 — **relu et validé** · `02-exercices.md` **§6.C** · `06-Palimpseste.md` **§2**.

« Ce document » est le `07-Implementation.md`. **Quatre pièces.**

| Pièce | Où | Statut requis | Au moment de l'écriture |
|---|---|---|---|
| `07-Implementation.md`, **§1** *(le schéma)* et **§5** *(l'allumage)* | `/Users/louissagnieres/Documents/GitTest/palimpseste-conception/` | aucun — un lot n'exige pas un statut de la source qui le déclare *(`07-` §2)* | **VERSION 2.66** · RELU ET VALIDÉ · ⚠️ **le §4 est GELÉ** ; sont OUVERTS à l'implémentation le **§1**, le **§5** et **l'inventaire des lots du §2** |
| `01-routeur.md`, **§4** *(les cinq couches)*, **§5** *(la construction de la semaine — LE BUDGET OPTIONNEL)*, **§9** *(le plancher de mesure)*, **§11** *(le journal)* | même dépôt | ⛔ **relu et validé** — *le seul statut exigé de ce lot ; il implémente une règle du routeur, comme `C4-L2`* | **VERSION 5.7** · VALIDÉ ET GELÉ *(vaut relu et validé)* |
| `02-exercices.md`, **§6.C** *(« La passation, côté élève »)* — **il porte la phrase qui pose ton offre** | même dépôt | **déposé** *(entrée sans statut explicite — `07-` §2)* | **VERSION 5.9** · VALIDÉ ET GELÉ |
| `06-Palimpseste.md`, **§2** *(« Ce que l'élève voit du temps »)* — **le tableau de bord où vit le push, et l'interdiction du budget-temps hebdomadaire** | même dépôt | **déposé** *(entrée sans statut explicite)* | **VERSION 2.6** · VALIDÉ ET GELÉ |

### ⚠️ Ce que le manifeste ne nomme pas, et pourquoi tu le lis quand même

**Le statut porte sur le FICHIER, jamais sur la section** *(`07-` §2, en toutes lettres : « citer `01-routeur.md` §4 exige le statut de `01-routeur.md` ; la section dit seulement **où lire** »)*. **Quatre sections de tes fichiers portent de la matière que ton lot ne peut pas ignorer, et le pointeur ne les nomme pas. Lis-les :**

- **`01-routeur.md` §2, « Côté élève — une seule surface pour le cycle »** — *« **Le tableau de bord est le point d'entrée du cycle** : le héros "à faire maintenant" et la liste compacte "ensuite cette semaine", **chaque ligne portant la couleur du module concerné**. »* **C'est là que ton PUSH tombe**, et `C6-L2` y a déjà posé une tuile qui te sert de patron. ⛔ Et : *« Pendant le cycle, l'atelier est un **attribut visuel, jamais un lieu**. Il se **montre**, il ne se **visite** pas. »*
- **`01-routeur.md` §6, R0 et R5** — le routeur **ne cible que les `evaluee`**, et la dette de couverture compte **en exercices**, pas en cycles. **Ton bonus y entre** *(§5 : « il compte dans les tables de proportion, dans la couverture R5 et dans les compteurs d'escalade »)*.
- **`06-Palimpseste.md` §5** — *« **Un écran n'affiche un nombre que si ce nombre compte quelque chose.** »* C'est la règle de sobriété qui borne tout ce que ton offre montre à l'élève.
- **`02-exercices.md` §2.4 et §6, « Les principes de la banque »** — la **dérivation de la durée** *(sans laquelle ton quota en minutes ne se décompte pas)* et la **règle de non-emboîtement**, que ton exercice de plus doit respecter comme les autres.

⛔ **`00-referentiel.md`, `03-competences.md`, `04-Instances_Exercices.md`, `05-`, `08-` et les fiches de `competences/` ne sont PAS à ton manifeste, et ton lot n'en a pas besoin : il n'ouvre aucune compétence, il ne mesure rien, il n'assemble aucune instance, il n'importe rien.** ⚠️ **La seule exception à connaître** : le `08-FORMAT_IMPORT.md` §7.3 **signale** un `bonus` à `true` à l'import — *« le drapeau est une décision du routeur, pas de la conception »*. **Tu n'as pas à le lire pour travailler ; sache seulement que le port de l'import le tient déjà** *(`utils/fabrique/verifie-import.ts:594`)* **et que tu ne le touches pas.**

---

## Le contrôle d'entrée — en deux temps, et les deux ne font pas la même chose

> **Le statut bloque.** Un fichier du manifeste dont l'en-tête porte un statut inférieur à celui exigé **arrête le lot**, explicitement. Le barème est au `07-Implementation.md` §2 : *déposé* → *relu et validé* → *versé et bancé*, cumulatifs ; « VALIDÉ ET GELÉ » vaut *relu et validé*.
>
> **La version avertit.** Si la ligne VERSION d'un fichier du manifeste diffère de celle inscrite ici, **relis son en-tête avant de continuer** — une version qui bouge dit qu'un texte a changé, pas qu'il est devenu faux.
>
> **Le blocage est granulaire quand il peut l'être.** Une fiche de compétence absente ou non bancée bloque **cette compétence**, pas le lot.

Concrètement : vérifie que les quatre pièces **existent**, que le `07-` porte **VERSION 2.66**, le `01-` **5.7**, le `02-` **5.9** et le `06-` **2.6**. ⛔ **La clause granulaire n'a ici AUCUN objet** : ce manifeste ne compte **aucune fiche de compétence**, et **ton lot ne mesure rien** — il sert un exercice de plus, et la chaîne fait le reste.

### Ta dépendance est unique, et elle est jouée — mais ton chemin réel en traverse trois

*(`PLAN_DE_CHANTIER.md` §3 : « C6-L3 — « en faire plus » | **C4-L2** | pull et push sont des opérations du routeur ».)* **Vérifie-la par son chemin, pas par sa ligne au plan :**

- **C4-L2** — les règles pures du routeur : `utils/routeur/` *(`semaine.ts` pour les trois phases, `ciblage.ts` pour R0-R5, `budget.ts` pour le budget de l'élève, `config.ts` pour les constantes)*. *Jouée ; une migration au `SUIVI_SQL.md`, `c4_l2_routeur.sql`.*

⚠️⚠️ **MAIS TON PULL DOIT ÉCRIRE, ET C4-L2 N'ÉCRIT RIEN.** *« Ce lot a livré les règles en fonctions pures et les écrans en lecture, et RIEN QUI ÉCRIVE »* *(`07-` §2, C4-L2, clause bornée du 23/08)*. **Trois lots de plus sont sur ton chemin réel, et tous trois sont joués** — **vérifie chacun par son chemin, et si l'un manque, arrête-toi et signale-le, ne devine pas :**

- **C4-L12** — **l'écrivain**, `utils/moteur/` : `poserLesSemainesDuRouteur` *(`cycle-serveur.ts`)*, le vivier *(`vivier.ts`, `vivier-serveur.ts`)*, le journal de décision *(`decision.ts`)*, les minutes *(`minutes.ts`)*. **C'est lui que tu réutilises pour UN exercice ; ne le réécris pas.**
- **C4-L13** — `utils/assiduite/`, et surtout la règle qui range un dépôt dans sa semaine : `assigne_at` **lu dans le fuseau de l'école**.
- **C6-L2** — l'écran de la semaine et sa tuile : `utils/eleve/semaine.ts`, `utils/eleve/semaine-serveur.ts`, `app/eleve/semaine/page.tsx`, la tuile d'`app/eleve/page.tsx`. **C'est ton emplacement.**

⭐ *Ce n'est pas une contradiction du plan : la ligne du graphe dit que le pull et le push sont **des opérations du routeur**, et elle a raison. Elle a été écrite avant que `C4-L12` n'existe.* **Dis-le au relevé ; ne récris pas le plan.**

### Sept contrôles machine à jouer AVANT d'écrire une ligne

1. ⛔⛔ **`git status` ET `git log --oneline -5` — LE CONTRÔLE LE PLUS IMPORTANT DE CETTE LISTE, PARCE QUE CE DÉPÔT BOUGE PENDANT QU'ON Y TRAVAILLE.** ⚠️⚠️ **Une autre séance travaillait dans `utils/routeur/` et `utils/moteur/` PENDANT la fabrication de ce prompt** — au 28/08, l'arbre portait douze fichiers modifiés qui ne sont pas d'elle *(le tirage de la phase B, les bornes de grain et de cran sur l'entrée de priorité, `cransDuPalier`)*. ⛔ **NE DÉFAIS RIEN, NE COMMITE RIEN de ce qui n'est pas de toi** — *un lot qui commite le travail d'une autre séance le lui vole.* ⛔ **Et `git add -u` n'ajoute pas les fichiers neufs : nomme tes fichiers un par un.**
   - ⛔⛔ **TOUT NUMÉRO DE LIGNE ET TOUT NUMÉRO DE COMMIT DE CE PROMPT EST DATÉ DU 28/08 ET SERA PÉRIMÉ QUAND TU LE LIRAS.**
   - ⭐ **La parade est simple : retrouve chaque repère par `grep` sur son NOM** — `grep -rn "bonusNePortePasDeSonde" utils/`, `grep -rn "budget_optionnel_min" .` — **jamais par son numéro.** Les **chemins** et les **symboles** de ce prompt ont été vérifiés un par un et tiennent ; les **numéros** sont des indices, pas des adresses.
   - ⭐ **Prends TON propre point de départ et note-le au relevé** : le commit, l'état de l'arbre, et **ce qui n'est pas de toi**.
2. `npm test` — **il doit être vert avant que tu commences**, sinon tu ne sauras pas ce que tu as cassé. *Mesuré le 28/08 : **1826 / 1826**. **Ce chiffre bougera : prends TON propre point de départ**, c'est lui qui compte.*
3. `npx tsc --noEmit` — même motif. *(Mesuré le 28/08 : **propre**.)*
4. **`python3 scripts/derive-instruments.py --verifie`** — il doit rendre **`INSTRUMENTS : IDENTIQUE`** sur les **10 fichiers dérivés**. *(Vérifié le 28/08, après le passage du `07-` en 2.66.)*
5. **L'état des six interrupteurs, PAR REQUÊTE, DANS LES DEUX BASES** — ⛔ **et ne te fie ni à la phrase du `07-` §5, ni à ce prompt, ni à un « re-constaté à ON » d'un relevé : ils ont bougé PENDANT la fabrication de ce prompt** *(voir en tête)*. *Mesuré à la clôture, le 28/08 : **les six à ON des deux côtés**.* ⭐ **Un seul te commande : `exercices_actif`** — la porte que `lireLaPorte` lit déjà pour l'écran de la semaine. ⛔ **Tu n'en allumes ni n'en éteins aucun, et tu remets celui que tu empruntes comme tu l'as trouvé** — *« une recette qui remet un interrupteur en écrivant une constante ne le remet pas : elle l'impose »* *(`07-` §5)*. Le patron de mémorisation est `scripts/recette/lecture-c5l2.mjs`. ⚠️ **Et `updated_at` ne t'aidera pas** : il ne bouge pas quand un interrupteur bascule.
6. **`python3 scripts/derive-doctrine.py --verifie`** — **si et seulement si** tu lis une table de doctrine directement. Il doit dire **IDENTIQUE** sur les **douze** tables, les empreintes de source et la fixture. **S'il dit DIVERGE, rejoue `--sql` ; jamais corriger la base à la main.** ⭐ *Tu ne devrais en lire qu'une, et par un chemin existant : les durées, que le vivier de `C4-L12` dérive déjà.*
7. **L'état de la marque, avant d'y toucher.** Par requête, dans les deux bases : `select count(*) from exercices where bonus`, `select count(*) from competences_mesures where bonus`, `select count(*) from routeur_decisions`. *Mesuré le 28/08 — **sandbox** : 0 · 0 · 0. **production** : 0 · 0 · 0.* ⭐ **C'est ton constat d'entrée, et il tient en une ligne : le canal est câblé jusqu'à la mesure, et personne n'y a jamais rien mis.**

### Ce que tu peux écrire dans une source, et ce que tu ne peux pas

- **Le `07-` §1, le `07-` §5 et l'inventaire des lots du `07-` §2 sont ouverts à l'implémentation** : ce que la construction fait apparaître **s'y écrit depuis ton relevé, sans accord préalable**. ⚠️ **Le §2 (règle de manifeste), le §3, le §4 et le §6 sont GELÉS.**
- ⭐⭐ **AMENDER UNE SECTION OUVERTE DU `07-` FAIT MÉCANIQUEMENT DIVERGER LES DÉRIVÉS.** Le geste de clôture est de **rejouer `python3 scripts/derive-instruments.py --ecris`**, **EN DERNIER**, puis de vérifier que `--verifie` rend `INSTRUMENTS : IDENTIQUE` et que `npm test` repasse vert. ⛔ *Ne le joue pas au milieu de ta séance : tu ne saurais plus ce que tu as cassé.*
- **Partout ailleurs, une session Code ne corrige jamais une source** *(`07-` §2)*. Une source **fausse** — pas incomplète, fausse — reçoit **`[faux]`** au point de l'erreur, et une ligne à la section **DETTES** du `INVENTAIRE_Non_Tranches.md`, avec l'avant et l'après. **La prochaine dette est `D10`.**
- ⛔ **Le `01-`, le `02-` et le `06-` sont GELÉS.** Si tu conclus qu'il leur manque quelque chose, **tu le DIS À LOUIS, tu ne l'écris pas.**

---

## La mission — reprise du `07-Implementation.md` §2

> **C6-L3 — « En faire plus ».** Le **pull** — l'élève demande un exercice de plus sur son quota — et le **push** — une suggestion sur son tableau de bord, jamais une assignation. Les deux, avec le **marquage `bonus`**, font foi au `01-routeur.md` §5.

**Trois choses, donc, et la troisième n'est pas un ornement** : le **pull**, le **push**, et **la marque qui les distingue de ce qu'on a imposé**. *« Sans elle, l'analyse de fin d'année ne distingue plus ce que l'élève a demandé de ce qu'on lui a imposé — et le budget optionnel devient invisible dans ses propres chiffres »* *(`01-` §5)*.

---

## Les pièges — les décisions dont l'oubli coûte une session

*Chacun avec son renvoi. Ils se relisent contre la source, jamais contre le prompt précédent.*

### A. Les frontières — ce que ce lot ne construit pas *(et où chaque exclusion a été trouvée)*

1. ⛔ **Aucune route neuve, aucun onglet neuf, aucune entrée neuve à la navigation élève.** L'élève porte **quatre onglets** *(Tableau de bord · Calendrier · Intégrité · Modules)* plus la page « Moi », que `C4-L6` puis `C5-L4` ont fixés et que `C6-L2` n'a pas élargis. **Ton offre est le TROISIÈME TEMPS d'un écran qui existe**, et ton push une **tuile** du tableau de bord qui existe.
2. ⛔ **Aucun septième interrupteur** *(`07-` §5, en toutes lettres)*. **Un onglet, une liste, une porte ne sont pas des fonctionnalités à gater.** ⛔ **Et l'élève n'a JAMAIS à connaître le nom d'un interrupteur pour comprendre son écran** *(règle précisée par `C5-L4`)*.
3. ⛔ **Tu ne rejoues pas la semaine.** `C4-L12` la pose **une fois par cycle**, et *« tout se décide à la construction du cycle… aucune mesure du cycle en cours n'alimente sa propre construction »* *(`01-` §5)*. **Ton pull sert LE SUIVANT, il ne recompose rien.**
4. ⛔ **Tu ne touches pas au récapitulatif ni au bilan.** Ils sont `C6-L2`, joué, poussé et en production. *Tu lis `chargerLaSemaineDeLEleve` ; tu l'étends au besoin, tu ne la réécris pas.*
5. ⛔ **Tu ne touches pas à la valeur du budget.** *« Seule sa VALEUR se règle ici ; sa CONSOMMATION — le pull, la suggestion push, le marquage `bonus` — est C6-L3 »* — le commentaire est **en base**, sur `profiles.budget_optionnel_min`, posé par `C4-L2`. **L'écran des budgets du professeur est à lui.**
6. ⛔ **La préférence recueillie de l'élève n'est pas de toi** — `profiles.preference_recueillie_at` et `preference_reponse` existent, **et le `01-` §5 les déclare NON TRANCHÉES** : *« ce qui reste ouvert : la place qu'y prend la préférence recueillie de l'élève »*. `C4L2-14` au `SUIVI_tests_manuels.md` le redit : *« ni la question ni ses valeurs ne sont écrites dans les sources… ce n'est pas une recette à jouer, c'est une décision à prendre »*. *Mesuré le 28/08 : **0 ligne renseignée dans les deux bases**.*
7. ⛔ **Aucun écran professeur.** La matrice, les quatre drapeaux, la file N3 et le diagnostic de rétention sont **`C6-L1`**, joué. Les budgets par élève et l'écran d'assignation en lecture sont **`C4-L2`**.
8. ⛔ **Le branchement de l'essai de Fragments est `C6-L4`.** *Il n'a rien à voir avec le tien : c'est un signal diagnostique dont le `lieu` vaut `classe`, donc hors routage.*
9. ⛔ **Tu ne touches pas au calendrier de l'élève**, et le trou y est déjà déposé, à toi comme à personne : *« l'échéance d'un exercice assigné est invisible à l'agenda de l'élève — `utils/calendrier-evenements.ts` ne lit jamais `exercices_depots`. **Ce n'est ni `C6-L3` ni `C4-L12` : c'est le module Calendrier** »* *(`IDEES_post_rentree.md`, déposé par `C6-L2`)*. **Ne le joue pas ; ton passage le rendra tentant.**
10. ⛔ **Tu ne rouvres pas le domaine du compteur `aide_consommee`** *(`01-` §11)*, fermé par `C4-L3` le 22/08 et re-constaté par `C6-L2`. **Un bonus n'ajoute aucune sorte d'aide.**

### B. Ce qui existe déjà, et que tu ne réécris pas

11. ⭐⭐ **LA RÈGLE DU PULL EST ÉCRITE, ET ELLE TIENT EN QUATRE PHRASES. Recopiée du `01-` §5 pour que tu n'aies pas à décider de son sens :** *« Le quota "en faire plus" est un **pull** : l'élève demande **un** exercice à la fois. **PB5 s'y applique exercice par exercice** — chaque demande sert le suivant dans l'ordre que la phase B aurait produit. **Les minutes non utilisées sont perdues, sans report ni écrêtage.** **Le bonus est un exercice normal, mesures comprises** : il compte dans les tables de proportion, dans la couverture R5 et dans les compteurs d'escalade. **Il ne porte en revanche aucune sonde secondaire** : la phase C a placé les siennes à la construction, quand il n'existait pas. »*
12. ⭐ **ET LA RÈGLE DU PUSH AUSSI :** *« Un second canal, le **push**. Le quota ci-dessus se consomme à la demande de l'élève. Le push ne le remplace pas, **il l'amorce** : une compétence **sans mesure depuis la période du plancher de mesure** *(§9)*, chez un élève **à C ou moins**, apparaît en **suggestion** sur son tableau de bord. **C'est une suggestion, jamais une assignation** — l'élève la prend par le même pull, ou l'ignore. »*
13. ⭐⭐ **LA PHASE B EST PURE, ÉPROUVÉE, ET ELLE PREND DÉJÀ SON TIRAGE EN PARAMÈTRE.** `poserLaSemaine(liste, budget, candidatsPour, tirer)` — `utils/routeur/semaine.ts` — porte **PB1 à PB6**, y compris **PB5** *(« quand la liste est épuisée et qu'il reste du budget, on la reparcourt dans le même ordre »)* et **PB2** *(« jamais deux fois de suite la même compétence », contrainte dure)*. ⛔ **Ne la recopie pas** ; si elle ne sait pas servir un exercice unique, **c'est un paramètre qui lui manque, pas une seconde fonction**.
14. ⭐⭐ **`bonusNePortePasDeSonde()` EXISTE ET ATTEND SON APPELANT** — `utils/routeur/semaine.ts`, sous le titre `LE BUDGET OPTIONNEL`. Son commentaire te nomme : *« la CONSOMMATION du budget optionnel est C6-L3 — ici, seule la VALEUR se règle. Cette fonction existe pour que la règle soit lisible et éprouvée dès maintenant. »* **Elle rend `true`, et c'est tout ce qu'elle doit faire** : ton geste est de **ne pas appeler la phase C** sur un bonus.
15. ⭐ **LE BUDGET DE L'ÉLÈVE EST PUR ET TESTÉ.** `budgetDeLEleve(inscriptions, reglage)` — `utils/routeur/budget.ts` — rend `{ plancher, plafond, optionnel }` avec les défauts du `01-` §4 : **TC seul 45/60/+30 · HLP seul 60/90/+30 · bi-classe 90/120/+30**. ⛔ **Un élève sans parcours ne reçoit RIEN, et le professeur en est averti** — c'est le **piège de la vacuité**, condition de recette du `07-` §1.3 : *jamais un service silencieusement réduit aux seuls types génériques*. ⚠️ *Mesuré le 28/08 : **aucun réglage de budget en base**, ni plancher, ni plafond, ni optionnel, dans aucune des deux bases — les défauts de situation servent partout.*
16. ⭐⭐ **LE VIVIER FAIT DÉJÀ LES CINQ FILTRES, ET TU N'EN REFAIS AUCUN.** `constituerLeVivier` — `utils/moteur/vivier.ts` — écarte : le `lieu = 'classe'`, le statut hors `concu|assigne`, l'instance bloquée, **l'instance dont cet élève porte déjà un dépôt**, l'instance sans durée, **le filtre de parcours**, le matériau non validé, **le filtre du cours vu** *(les quatre états, `notions` compris)* et **le non-spoiler** avec sa `borne_amont`. ⛔ *Servir un bonus qui contourne l'un de ces filtres serait pire que de ne rien servir.*
17. ⭐ **`candidatsPour(retenus, competence, dejaPoses, expressionEnSecondaire)`** — `utils/moteur/vivier.ts` — est le rappel que la phase B interroge. **C'est par lui que ton pull trouve son exercice**, et il connaît déjà `dejaPoses`.
18. ⭐ **LE JOURNAL DE DÉCISION EST ÉCRIT PAR `lignesDeDecision`** *(`utils/moteur/decision.ts`)*, et `journalDuTirage()` porte les quatre canaux de tirage *(`R3`, `phase_b`, `sondes`, `phase_c`)*. ⛔ **`01-` §11, point 5 : trancher au hasard ET LOGGUER LE TIRAGE.** *Un départage non journalisé rend le routeur irreproductible.*
19. ⭐⭐ **LA PERSISTANCE EXISTE, ET SES DEUX GARDES SONT DES DÉFAUTS DÉJÀ PAYÉS.** La fonction `persister` de `utils/moteur/cycle-serveur.ts` écrit **la décision, PUIS le dépôt qui la porte** *(`exercices_depots.routeur_decision_id` — « un dépôt, une décision »)*, avec `origine = 'routeur'`. ⛔⛔ **`assigne_at` SE POSE, IL NE SE LAISSE PAS AU DÉFAUT, ET IL NE SE RÉÉCRIT JAMAIS** — voir le piège 63. ⛔ **Aucun `upsert`** : *« un upsert qui reposerait `statut` et `assigne_at` ferait repasser à `assigne` un élève déjà à `v1_remis`, et l'assiduité le recompterait ».*
    - ⚠️⚠️ **ET `exercices_depots` N'A QUE DEUX ÉCRIVAINS DANS TOUT LE DÉPÔT — vérifié par `grep` le 28/08** : `utils/moteur/cycle-serveur.ts` *(la voie du routeur)* et `app/prof/conception/actions.ts` *(la voie du professeur)*. **Ton pull sera le troisième, et le premier déclenché par un ÉLÈVE.**
    - ⛔⛔ **ET `origine` EST SOUS `CHECK (origine in ('routeur','prof'))` — DEUX VALEURS, PAS TROIS.** *« C'est ce qui distingue les deux voies DANS LES MÊMES TABLES »* *(`07-` §1.1 : « les deux voies coexistent, et aucune n'est le repli de l'autre »)*. ⭐ **Un bonus est **DEMANDÉ** par l'élève mais **DÉCIDÉ** par le routeur — c'est `origine = 'routeur'`**, et c'est cohérent : *« le bonus est un exercice normal »*, servi par la phase B. ⛔ **Une troisième valeur serait une migration ET un troisième régime pour tout ce qui lit `origine`** — l'assiduité, les écrans du professeur, la chaîne. **Ne l'ajoute pas ; et si tu penses qu'il le faut, dis pourquoi au relevé avant de l'écrire.**
20. ⭐ **`lundiDuCycle(instant, fuseau)`** — `utils/deroule/echeance.ts`. ⛔ **Un cycle est une semaine de lundi à dimanche DANS LE FUSEAU DE L'ÉCOLE. Ne recalcule pas un lundi à la main.**
21. ⭐ **`CYCLES_DU_PLANCHER_DE_MESURE = 3`** — `utils/routeur/config.ts`. **C'est la période que ton push lit**, et elle est là, et nulle part ailleurs. ⛔ **Jamais un `3` en dur dans ton code.**
22. ⭐ **LA LETTRE ET LE PROFIL SE LISENT PAR LES FONCTIONS EXISTANTES** : `jugerLaLettre` *(`utils/routeur/lettres.ts`)*, `mesuresQuiComptent` *(`mesure.ts`)*, `profilDeLaCompetence` et `fenetreDEvidence` *(`profil.ts`)*. ⚠️ **La lettre du push est celle de la COLONNE `competences_niveaux.lettre` — l'ÉTAT, pas l'affichage** : c'est ce que `C4-L12` lit pour R0, et c'est ce que « à C ou moins » veut dire. ⛔ *Ne lis pas la valeur plafonnée d'affichage pour décider d'un ciblage.*

### C. ⭐⭐ LA MARQUE `bonus` — un lecteur, aucun écrivain, et un domicile à choisir

23. ⛔⛔ **LE CANAL EST CÂBLÉ DE BOUT EN BOUT, SAUF SA SOURCE. Vérifie-le par `grep`, il tient en trois sauts** : `utils/chaine/contexte.ts` **sélectionne `bonus` sur `exercices`** et le pose dans le contexte *(`bonus: !!exercice.bonus`)* → `utils/chaine/chaine.ts` le passe à la mesure *(`bonus: ctx.bonus`)* → `competences_mesures.bonus` le porte, et `utils/routeur/donnees.ts` le relit. ⭐ **Le `07-` §1.2 le confirme** : *« le dépôt d'origine quand il y en a un, **le drapeau `bonus`**, l'`instrument_version`, la date »*. ⛔⛔ **ET RIEN, DANS TOUT LE DÉPÔT, NE POSE JAMAIS `true`** — vérifié par `grep` sur `bonus` dans `utils/`, `app/`, `components/` et `scripts/` : les seules occurrences sont des **lectures**, des **types**, des **fixtures de test**, le **signalement d'import**, et le `bonusVacances` de l'assiduité, **qui n'a rien à voir**.
    - ⚠️ **ET LE BOUT AVAL A UNE CONTRAINTE D'UNICITÉ QU'IL FAUT CONNAÎTRE AVANT DE PROUVER QUOI QUE CE SOIT : `uk_mesures_depot_competence` — UNE mesure par (dépôt × compétence)** *(constatée en base par `C6-L2`, qui l'a lue plutôt que devinée)*. **Un dépôt bonus qui mesure trois compétences produit donc trois lignes, chacune portant `bonus`** — et **tu ne peux pas en semer deux sur la même paire** pour éprouver ton canal.
24. ⛔⛔ **ET LE DOMICILE QUE LE `07-` §1.1 LUI DONNE NE PEUT PAS PORTER LE FAIT — c'est le constat central de ce lot, et il est mesuré.** Le §1.1 loge le drapeau sur **`exercices`** — *« le `statut` … et le drapeau `bonus` »* —, mais `exercices` est **la banque des instances**, et `utils/moteur/vivier.ts` l'écrit en toutes lettres : *« ⛔ **Resservir la même instance au MÊME élève** serait un défaut silencieux : toute instance dont il porte déjà un dépôt sort du vivier. **Entre élèves, en revanche, une instance se ressert — "une instance, plusieurs dépôts".** »* ⚠️ **Conséquence exacte** : un `true` posé sur l'instance que Léa a demandée en bonus **marquera aussi la mesure de Tom**, à qui la même instance aura été **imposée** la semaine suivante — et le journal de fin d'année dira l'inverse de ce que le `01-` §5 lui demande de dire.
25. ⭐ **ET LE `01-` §5 NE DIT PAS « SUR L'INSTANCE » : IL DIT « AU JOURNAL ».** *« Un exercice servi sur ce quota porte la marque `bonus` **au journal** *(§11)*. »* Le journal du §11 est **`routeur_decisions`** *(`07-` §1.5 : « `routeur_decisions` — le journal des décisions »)*, qui est par **(élève × cycle × exercice)**. ⛔ **Et il n'a AUCUNE colonne `bonus`** — vérifié sur `c4_l1_schema.sql` : il porte `cible_retenue`, `regle_declenchee`, `alternatives_ecartees`, `sondes_retenues`, `propositions_iso_duree`, `choix_eleve`, `borne_amont`, `override_prof`, `tirage_aleatoire`, `degrade`. **Rien d'autre.**
26. ⭐⭐ **TROIS DOMICILES SONT POSSIBLES, ET LE CHOIX EST À TOI — décision de Louis, 28/08.** *(a)* **`exercices.bonus`**, déjà lu par la chaîne — **aucune migration, et le défaut du piège 24** ; *(b)* **une colonne sur `routeur_decisions`** — le « journal » que le `01-` §5 nomme, per (élève × cycle × exercice), **une migration**, et la chaîne doit alors la joindre par `exercices_depots.routeur_decision_id` ; *(c)* **une colonne sur `exercices_depots`** — le seul objet par (élève × exercice) sur **les deux voies**, **une migration**, ⚠️ **mais le `07-` §1.1 écarte explicitement les colonnes de plus sur cette table** : *« une colonne de cycle sur le dépôt serait un second domicile de ce que `assigne_at` dit déjà, et deux domiciles finissent par diverger »* — l'argument porte sur le cycle, **pèse s'il porte aussi sur la marque**. ⛔ **Quel que soit ton choix, la chaîne doit continuer d'écrire `competences_mesures.bonus` avec la bonne valeur : c'est le seul bout du canal que la source déclare deux fois.**
27. ⛔⛔ **SI TU CONCLUS QUE LE `07-` §1.1 EST FAUX SUR CE POINT, TU LE MARQUES — TU NE LE RÉCRIS PAS.** Deux gestes, et deux seulement : **`[faux]`** au point de l'erreur, et **une ligne à la section DETTES** du `INVENTAIRE_Non_Tranches.md`, avec l'**avant / après**. **La prochaine dette est `D10`.** ⚠️ *Et pèse-le vraiment avant de le poser : « fausse » n'est pas « incomplète ». Le §1.1 déclare une colonne qui existe ; ce qui manque est la phrase qui dit qu'elle ne suffit pas. À toi de juger lequel des deux c'est, et de dire pourquoi.*
28. ⚠️ **LE `08-FORMAT_IMPORT.md` A TRANCHÉ QUI POSE LA MARQUE, ET CE N'EST PAS LA CONCEPTION.** Un `bonus` à `true` à l'import **se signale** : *« le drapeau est une décision du routeur, pas de la conception »* *(§7.3)*, et le port l'applique déjà *(`utils/fabrique/verifie-import.ts`, `generateur/verifie-import.py`)*. ⛔ **Tu ne touches ni au format, ni au port, ni au script.** ⭐ *Mais retiens la phrase : elle dit que l'écrivain de la marque est **le routeur**, donc **toi**.*

### D. Le PULL — « un exercice à la fois », et l'emplacement qui l'attend

29. ⭐⭐ **TON EMPLACEMENT EST LE TROISIÈME TEMPS DE `/eleve/semaine`, ET LA SOURCE LE DIT DANS L'ORDRE.** Le `02-` §6.C décrit **une seule séquence** : *« **Avant de commencer**, l'élève reçoit le récapitulatif de sa semaine […] **Il passe ensuite ses exercices.** […] **À la fin**, un bilan court […] **Puis on lui offre d'en faire plus s'il le veut.** C'est le budget optionnel du routeur : **un pull, un exercice demandé à la fois, jamais imposé, et jamais reporté d'une semaine sur l'autre.** »* ⭐ **`C6-L2` a construit les deux premiers temps et s'est arrêté là, volontairement.**
30. ⭐ **ET LE LOT T'A LAISSÉ DE QUOI T'Y BRANCHER.** `chargerLaSemaineDeLEleve(admin, eleveId, classeEnContexte, cycleLundi, fuseau)` — `utils/eleve/semaine-serveur.ts` — rend déjà `{ cycleLundi, porteOuverte, moment, exercices, frise, recapitulatif, bilan, manque, ecartAuTroisDuSixC, incidents }`. **Tu n'as ni à recompter la semaine, ni à redéfinir « à la fin ».**
31. ⚠️ **« À LA FIN » EST DÉFINI, ET C'EST `C6-L2` QUI L'A DÉFINI — ne le redéfinis pas.** `momentDeLaSemaine` rend **`vide` · `recapitulatif` · `bilan`**, et **`bilan` veut dire : plus aucun dépôt du cycle n'attend un geste de l'élève** — le geste attendu se lisant sur les tons d'`etatDeLExercice` *(`a_lire`, `a_faire`, `en_cours`)*. ⭐⭐ **Et l'obligation de lecture en fait partie** : un retour **publié mais non lu** attend encore un geste et **retient le bilan** *(`02-` §6.D, étape 17)*.
32. ⚠️⚠️ **MAIS « OFFRIR D'EN FAIRE PLUS » N'EST PAS FORCÉMENT « AU BILAN SEULEMENT », ET C'EST À TOI DE TRANCHER.** La source pose l'offre **après** le bilan dans sa séquence ; elle ne dit **pas** qu'un élève qui a fini mardi doive attendre d'avoir tout rendu pour demander plus. ⭐ **Choisis, dis-le au relevé, et dis ce que l'écran rend dans chacun des trois moments** — `vide`, `recapitulatif`, `bilan`. ⛔ *Et rappelle-toi le troisième vide de `C6-L2` : « la porte est fermée », « tu n'as rien à faire » et « la semaine est finie » ne se disent pas pareil.*
33. ⛔⛔ **UN SEUL À LA FOIS, ET C'EST UNE CONTRAINTE DURE.** *« L'élève demande **un** exercice à la fois »* — ⛔ **pas une liste, pas un choix entre trois, pas un lot de deux.** ⚠️ **À ne pas confondre avec les « 2-3 propositions iso-durée » de la couche 4** *(`01-` §4)*, qui sont le **choix DANS un exercice servi**, au méso et au macro, aux crans de `produire` et de `transformer` sauf `transformation_guidee`. **Ce sont deux mécaniques distinctes, et la seconde n'est pas de toi** *(`routeur_decisions.propositions_iso_duree` et `choix_eleve` sont à `C4-L12`)*.
34. ⛔⛔ **LE PLAFOND EST DUR, LE QUOTA NE SE REPORTE PAS, ET LE RELIQUAT EST PERDU.** *« Les minutes non utilisées sont perdues, sans report ni écrêtage »* *(`01-` §5)*, et *« jamais reporté d'une semaine sur l'autre »* *(`02-` §6.C)*. ⭐ **`RELIQUAT_PERDU_MIN = 5`** existe à `utils/routeur/config.ts` : *« le plus petit exercice du dispositif dure 5 minutes ; le reliquat sous ce seuil est perdu »* *(PB6)*. ⛔ **Un quota qui se reporte est un défaut silencieux : personne ne le verra avant l'analyse de fin d'année.**
35. ⭐⭐ **PB5 S'APPLIQUE EXERCICE PAR EXERCICE, ET C'EST LA PHRASE LA PLUS TECHNIQUE DE TA SOURCE.** *« Chaque demande sert **le suivant dans l'ordre que la phase B aurait produit**. »* ⚠️ **Cela veut dire que ton pull rejoue la phase B avec les exercices DÉJÀ POSÉS comme entrée** — c'est exactement ce que `poserLaSemaine` reçoit dans `candidatsPour(comp, dejaPoses)` —, **et qu'il s'arrête au premier posé**. ⛔ **PB2 vaut là aussi** : *« jamais deux fois de suite la même compétence, contrainte dure, sans exception »* — et **PB5 le redit pour la couture entre deux tours**.
36. ⛔ **LE BONUS EST UN EXERCICE NORMAL, MESURES COMPRISES.** *« Il compte dans les tables de proportion, dans la couverture R5 et dans les compteurs d'escalade »* *(`01-` §5)*. ⭐ **Conséquence pratique : tu ne fabriques aucun chemin parallèle.** Le dépôt qu'il pose entre dans le déroulé à six temps de `C4-L3`, sa v1 part en file, la chaîne le mesure, le retour lui revient. ⛔ *Un « bonus » qui ne serait pas mesuré ne serait pas un bonus : ce serait un exercice pour rien.*
37. ⛔⛔ **ET IL NE PORTE AUCUNE SONDE SECONDAIRE.** *« La phase C a placé les siennes à la construction, **quand il n'existait pas** »* *(`01-` §5)*. ⭐ **La règle est déjà en fonction pure — `bonusNePortePasDeSonde()` — et ton geste est de NE PAS appeler `poserLesSondes` sur un bonus.** ⚠️ *À ne pas confondre avec les **sondes de montée**, qui sont dans la case choisie en phase A et qui vivent sur l'exercice lui-même.*
38. ⚠️ **UN BONUS N'EST PAS UNE SECONDE ASSIGNATION D'UNE INSTANCE DÉJÀ SERVIE.** Le vivier écarte déjà *« toute instance dont il porte déjà un dépôt »* — et cette règle vaut pour ton pull **comme pour la semaine**. ⛔ **Ne la contourne pas pour avoir quelque chose à servir quand le vivier est vide** : *un vivier vide se DIT, il ne se remplit pas de force.*
39. ⚠️ **QUE FAIT LE PULL QUAND IL N'A RIEN À SERVIR ?** Trois cas distincts, et ils ne se disent pas pareil : **le quota est épuisé** · **le vivier est vide** *(aucune instance servable — et c'est le cas le plus probable à la rentrée)* · **la liste de priorité est vide** *(aucune compétence `evaluee`)*. ⛔ **Le silence est un mensonge** : dis lequel, en langue élève et sans nommer un interrupteur.
    - ⭐⭐ **ET LE PRÉCÉDENT EST EXEMPLAIRE, CHEZ `C6-L2` : IL A ÉCRIT UNE PHRASE POUR CHAQUE MANQUE, ET AUCUN SILENCE.** *« travaillé N fois » rend **`jamais travaillé`** — jamais « 0 fois », qui se lirait comme un score ; « en progrès » rend **`pas encore assez d'exercices pour le dire (2 sur 4)`** — **le décompte est DIT** ; « prochaine étape » rend **`Tu n'as pas encore reçu de retour : ta prochaine étape viendra avec lui.`*** ⛔ **Jamais un silence, et jamais la valeur flatteuse par défaut.** ⭐ **Écris tes trois phrases au relevé, mot pour mot**, comme il a écrit les siennes.

### E. Le PUSH — une suggestion, jamais une assignation

40. ⛔⛔ **LE PUSH NE POSE AUCUN DÉPÔT, ET C'EST TOUTE SA DÉFINITION.** *« C'est une suggestion, jamais une assignation — l'élève la prend **par le même pull**, ou l'ignore »* *(`01-` §5)*. ⭐ **Il n'y a donc qu'UN écrivain dans ton lot : le pull.** Le push est **une lecture qui produit une phrase et un bouton**.
41. ⭐ **SES DEUX CONDITIONS SONT CUMULATIVES, ET TOUTES DEUX SE CALCULENT.** *(a)* **une compétence sans mesure depuis la période du plancher de mesure** — trois cycles, `CYCLES_DU_PLANCHER_DE_MESURE`, et le §9 dit *« au moins une mesure tous les 3 cycles sur toute compétence **ciblée** »* ; *(b)* **chez un élève à C ou moins** — la lettre de `competences_niveaux`, pas la valeur d'affichage *(piège 22)*.
42. ⛔ **UNE COMPÉTENCE SANS LETTRE N'EST NI CIBLABLE NI SONDABLE** *(`01-` §3 : « une compétence sans lettre n'est ni ciblable, ni sondable, ni plafonnée, et n'entre dans aucun départage »)*. ⛔ **`NULL` n'est pas « E », et « à C ou moins » ne l'attrape pas.** ⚠️ *Mesuré le 28/08 : en **bac à sable**, 102 niveaux dont la lettre est **NULLE** ; en **production**, 149 lettres réelles sur 178 mesures et trois compétences mesurées.* **Ton push sera donc muet en bac à sable et parlant en production — et c'est l'inverse de ta tuile.**
43. ⛔ **LE ROUTEUR NE CIBLE QUE LES `evaluee`** *(`01-` §3, R0)*. Une compétence `mesuree_silencieusement` ou `differee` **ne se suggère pas** : *« le verdict à l'élève : non »*. ⭐ **`lireLesStatutsAvecDate` et `mesuresQuiComptent` existent** — le second écarte déjà les mesures antérieures à la recette.
44. ⚠️ **LA SUGGESTION EST UNE TUILE, ET `C6-L2` T'A LAISSÉ SON PATRON.** `app/eleve/page.tsx` construit une liste de `Tache` priorisées, **chaque ligne portant la couleur de son module** *(`01-` §2)*, et `signalDeLaSemaine` *(`utils/eleve/semaine-serveur.ts`)* montre comment une tuile naît d'une lecture serveur. ⛔ **Ne fabrique pas une septième surface** : c'est une tuile de plus dans une liste qui en porte sept.
45. ⛔ **ET LA TUILE PORTE UN LIEN, DONC ELLE LIT SA PORTE.** *« Un lien vers un écran fermé est une promesse cassée »* — le commentaire est déjà dans `app/eleve/page.tsx`, deux fois. ⭐ **Lis `exercices_actif` DANS le module partagé, jamais au site d'appel** : *« une garde qu'on peut oublier en écrivant un second écran n'est pas une garde »* *(`07-` §5, précisé par `C5-L4`)*.
46. ⚠️ **« À INTERVALLE RÉGULIER » N'EST PAS DIT POUR LE PUSH.** La source ne dit **ni** à quelle fréquence la suggestion se renouvelle, **ni** ce qui se passe quand l'élève l'ignore. ⭐ **Choisis le plus sobre — une suggestion qui se recalcule à chaque lecture et disparaît dès que sa condition tombe — et dis-le au relevé.** ⛔ **Ne persiste pas un état de suggestion sans nécessité** : une colonne de plus est une migration, et *« une trace n'est pas un état »*.

### F. La frise, le quota, et le budget qui ne se montre pas

47. ⛔⛔ **LA FRISE COMPTE AUJOURD'HUI TOUS LES DÉPÔTS MAISON DU CYCLE, `bonus` COMPRIS — ET C'EST TON ITEM DE BOÎTE LE PLUS CONCRET.** `friseDeLaSemaine` *(`utils/eleve/semaine.ts`)* fait une case par exercice de la liste, sans distinction. **Elle est juste aujourd'hui parce qu'aucun dépôt n'est bonus** ; **le jour où ton pull en pose, « 3 sur 5 » mélangera l'assigné et le demandé, et l'élève lira comme un retard ce qu'il a choisi en plus.** ⭐ **C'est `C6-L2` qui te l'a déposé, nommément.**
48. ⚠️ **ET LA LECTURE QUI ALIMENTE LA FRISE NE SÉLECTIONNE PAS LA MARQUE.** `exercicesMaisonDeLEleve` *(`utils/codex-onglets/liste.ts`)* fait `select('id, statut, echeance, assigne_at, exercices!inner(id, lieu, classe_id, consigne_instanciee, modes_par_competence)')` — **`bonus` n'y est pas**. ⭐ **`C6-L2` lui a déjà ajouté deux champs** *(`assigneAt` et `competences`, tous deux déjà lus par la requête)* : **c'est l'élargissement de ce qu'elle REND, jamais une seconde liste.** ⛔ **Fais de même ; n'écris pas une requête parallèle.**
49. ⛔⛔ **ET L'ASSIDUITÉ COMPTE LA MÊME CHOSE — VÉRIFIE CE QUE TON BONUS Y FAIT.** Une semaine est *« faite »* quand l'élève a rendu **au moins trois quarts de ses exercices assignés** *(`06-` §5)*, et `utils/assiduite/collecte.ts` fait la jonction « dépôts → (assignés, terminés) ». ⚠️ **Un bonus rendu au numérateur ET au dénominateur ne change rien ; un bonus NON rendu abaisse l'assiduité d'un élève qui en a fait PLUS que les autres.** ⭐ *Le code le sait déjà à moitié : `utils/routeur/assiduite.ts` porte « le bonus peut pousser au-delà de 100 % : on borne l'affichage, pas le compte » — mais **ce bonus-là est celui des VACANCES**, qui n'a rien à voir avec le tien et qui est **fermé** *(`07-` §1.5, décision du 24/08)*. **Ne les confonds pas.*** ⛔ **Mesure, décide, et dis-le au relevé** — `06-` §5 est GELÉ et ne parle que d'« exercices assignés ».
50. ⛔⛔ **PAS DE BUDGET-TEMPS HEBDOMADAIRE — `06-` §2, EN TOUTES LETTRES.** *« **Pas de budget-temps hebdomadaire.** En début de semaine, il voit **la quantité d'exercices** que le routeur lui a prévue ; sur chaque exercice, **une durée indicative**. »* ⚠️⚠️ **Ton quota est en MINUTES, et c'est précisément le piège** : `profiles.budget_optionnel_min` vaut 30 par défaut, la phase B décompte en minutes, **et rien de tout cela ne se montre à l'élève**. ⭐ **Le pull se compte en minutes CÔTÉ SERVEUR ; côté élève, il se dit en exercices, ou il ne se dit pas.** ⛔ *« Il te reste 12 minutes de bonus » est exactement ce que la source interdit.*
51. ⛔ **`06-` §5, ET C'EST LA RÈGLE DE SOBRIÉTÉ DE TOUT CE QUE TU MONTRES** : *« **Un écran n'affiche un nombre que si ce nombre compte quelque chose.** »* ⭐ **Les seuls nombres que `C6-L2` a autorisés chez l'élève sont `n` — « travaillé N fois » — et le nombre d'exercices de la semaine.** ⛔ **Aucun pourcentage, aucun compte de minutes, aucune moyenne.**
52. ⭐ **LA DURÉE INDICATIVE PAR EXERCICE EXISTE, ET ELLE EST PERMISE** — `dureeDeLInstance` / `dureeIndicativeLisible` *(`utils/deroule/`)*, dérivée d'`exercices_durees`. ⚠️ **Mais `C6-L2` a délibérément écarté la durée de l'écran de la semaine** *(« deux choses, et deux seulement » — `02-` §6.C)*, et **`dureeDeLInstance` n'est pas exportée**. ⛔ **Si ton offre doit dire une durée, dis-la sur l'exercice offert, jamais en somme hebdomadaire — et ne recopie pas la fonction.**

### G. Les gardes qui traversent — elles ont toutes coûté une séance à quelqu'un

53. ⛔⛔ **AUCUNE POLICY ÉLÈVE SUR LES TABLES DU MOTEUR.** *« Lecture élève : ses propres lignes, strictement ; **toutes les écritures passent par le serveur** »* *(`07-` §1)* — et `C4-L3`, `C4-L4`, `C4-L9` et `C4-L6` l'ont re-vérifié par requête : **zéro policy élève sur les 39 tables**. ⛔⛔ **Ton pull est une ÉCRITURE déclenchée par un ÉLÈVE : c'est le premier du dépôt.** Elle se fait **côté serveur, par le client admin, avec l'identité vérifiée dans le code** — le patron de garde est `garderEleveDeroule` *(`utils/deroule/acces.ts`)*, **qui vérifie le rôle**, et le motif y est écrit : *« le client admin qui suit **contourne la RLS** : sans ce contrôle, la seule chose qui distingue un élève d'un professeur sur ces écrans serait l'URL »*. ⛔ **Tu n'ouvres AUCUNE policy pour rendre une écriture plus simple à écrire.**
54. ⛔⛔ **ET UNE ÉCRITURE DÉCLENCHÉE PAR UN CLIC A UN MODE DE PANNE QUE LA POSE HEBDOMADAIRE N'AVAIT PAS : LE DOUBLE CLIC.** Deux appels concurrents servent **deux** exercices sur un quota d'un. ⭐ **Le dépôt a déjà le patron d'une garde mécanique** : *« l'insertion de l'instance EST le claim de la ligne de plan — deux conceptions concurrentes, la seconde perd **avant** d'avoir rien écrit, et aucun `UPDATE` conditionnel n'est nécessaire »* *(`07-` §1.1)*. **Cherche l'équivalent ; ne compte pas sur un booléen d'écran.**
55. ⚠️⚠️ **`supabase-js` NE LÈVE PAS** — il rend `{ error }`. Une écriture ratée n'est pas *« rien à servir »* : **on le dit**, plutôt que de laisser un écran affirmer *« tu n'as plus de bonus »*. ⚠️ **Il PLAFONNE aussi toute réponse à 1000 lignes sans rien dire** — `exercices` en porte 251 en production, `competences_mesures` grandira ; le patron est `lirePagine` *(`utils/routeur/donnees.ts`)*, **et un décompte indisponible est un incident, pas un succès**. ⚠️ **Et son constructeur est PARESSEUX** : une requête non `await`ée ne part jamais.
56. ⚠️ **`import 'server-only'` REND UN MODULE INTESTABLE SOUS `npm test`.** Le glob est `utils/**/*.test.ts`. ⭐ **Le partage est celui de tout le dépôt** : les **règles pures** dans un module sans `server-only`, avec ses tests ; l'**écrivain / lecteur** à côté, en `*-serveur.ts`. *`utils/eleve/semaine.ts` + `semaine-serveur.ts` en est le modèle le plus proche du tien.* ⛔ **Une règle posée sous `app/` ne serait JAMAIS éprouvée, sans qu'aucun message ne le dise.**
57. ⛔⛔ **`export type` DANS UN MODULE `'use server'` TUE TOUT LE MODULE À L'EXÉCUTION**, et `tsc`, `npm test` et les recettes passent tous. **Ton pull sera très probablement une server action : ses types vivent ailleurs.**
58. ⛔ **LE CRLF DES `<textarea>`, ET IL A MORDU TROIS FOIS.** Un formulaire rend du **CRLF**, la base stocke du **LF** ; `new FormData()` ne le montre pas, **et cela vaut aussi pour les server actions React**. ⚠️ *Ton lot a peu de saisie — un bouton, sans doute —, mais si tu en écris une, normalise.*
59. ⚠️ **LA CLASSE EN CONTEXTE BORNE LA LISTE, PAS LE PROFIL, ET IL Y A TROIS ÉTATS, PAS DEUX.** `contexteClasseEleve` rend `{ inscriptions, active, toutes }` : en état **« Toutes »**, `active` est **null** sans que l'élève soit sans classe. ⛔ **Le budget, lui, est UNIFIÉ PAR ÉLÈVE** — *« un élève inscrit dans deux classes a **un seul** budget »* *(`01-` §4)* : **ton quota ne se compte jamais par classe.** ⚠️ *C'est une asymétrie réelle avec l'écran de la semaine, qui, lui, agrège par inscription. Regarde comment `app/eleve/semaine/page.tsx` s'en tire avant d'écrire.*
60. ⭐ **LES JETONS DE `globals.css`, JAMAIS DE HEX EN DUR** *(`AGENTS.md`)*. **Un module = 2-3 onglets.** ⚠️ **Chaque ligne du tableau de bord porte la couleur de son module**, et **l'atelier suit le mode** : `codex` s'il porte `composer`, `aletheia` sinon *(`atelierDUnFormatif`)*. ⛔ **« Pendant le cycle, l'atelier est un attribut visuel, jamais un lieu »** *(`01-` §2)*.
61. ⛔ **DEUX TABLES NE SE JOIGNENT JAMAIS DANS UNE LECTURE ÉLÈVE — les SQUELETTES et la MÉTACOGNITION** *(`07-` §1)*. *« C'est la garde la plus facile à casser et la plus coûteuse : elle donne la grille et les réponses. »* ⚠️ **Et une troisième s'y ajoute de fait pour tout écran élève : `competences_mesures.observables`.**

### H. ⭐ CE QUE TA BOÎTE AUX LETTRES T'A LAISSÉ — les trois items de `C6-L2`, entrés en pièges

*Ils sont au `PLAN_DE_CHANTIER.md` §5, boîte `C6-L3`, déposés le 28/08. **Ils sont ici en entier : tu n'as pas à aller les lire.***

62. ✅ **ITEM 1 — l'emplacement.** *« L'écran de la semaine existe, et il est l'endroit de ton "en faire plus". `/eleve/semaine` rend la séquence du `02-` §6.C en deux temps, et le §6.C se termine sur TA phrase. `C6-L2` s'est arrêté juste avant, et volontairement. Ce qui t'attend est donc un emplacement, pas une page. »* **→ pièges 29 à 32.**
63. ✅ **ITEM 2 — le rattachement au cycle.** *« `ExerciceMaison` a gagné `assigneAt` et `competences`, tous deux déjà lus par la requête existante. **Le rattachement d'un dépôt à sa semaine passe par `assigne_at` lu dans le FUSEAU DE L'ÉCOLE**, jamais en UTC : un exercice `bonus` demandé le dimanche soir tomberait sinon dans la semaine suivante, et sortirait du quota qu'il consomme. »* ⭐⭐ **ET C'EST UN PIÈGE PLUS PROFOND QU'IL N'EN A L'AIR** : `C4-L12` pose `assigne_at` à **midi UTC du lundi du cycle**, et **pas à l'instant de l'écriture** — *« à minuit UTC, un lundi UTC est encore le DIMANCHE à Toronto, et le dépôt ouvrirait la semaine PRÉCÉDENTE »*. ⛔ **Ton pull écrit au moment du clic : décide ce qu'il pose, et pose-le explicitement.** ⚠️ *Le `07-` §1.1 est formel sur la conséquence : laissé au `default now()`, un dépôt tombe dans la semaine d'assiduité **du jour d'écriture**, `exercices_assignes` reste à 0, et la semaine se lit **« faite par construction »**.*
64. ✅ **ITEM 3 — la frise.** *« La frise compte AUJOURD'HUI TOUS les dépôts maison du cycle, `bonus` compris — parce qu'aucun n'est `bonus` en base. Le jour où le pull en pose, "3 sur 5" mélangera l'assigné et le demandé. »* **→ pièges 47 à 49.**

⚠️ **Et le corollaire, qui vaut pour toi comme il valait pour `C6-L2`** : *« "je ne fais pas X, c'est ton lot" n'est pas un dépôt »* — **une couture que tu déclares hors périmètre se nomme À SON DESTINATAIRE, dans SA boîte du `PLAN_DE_CHANTIER.md` §5**, ou elle n'existe pour personne. **Tes destinataires probables : `C4-L12`, `C6-L4`, ou « aucun lot », qui est une réponse et se dit à Louis.**

---

## Le « fait quand » — recopié du `07-Implementation.md` §2

> *Fait quand* : l'élève **peut demander un exercice de plus et le reçoit** — **un seul à la fois**, dans son quota et **jamais au-delà**, servi par les mêmes règles que sa semaine ; une **suggestion** apparaît sur son tableau de bord quand une compétence l'appelle, et **elle ne s'assigne jamais toute seule** ; ⭐ **et ce qui est servi sur ce quota se distingue de ce qui a été imposé** — la marque `bonus` **se pose, se relit, et atteint la mesure** —, si bien que **l'écran de la semaine ne compte plus un bonus comme un retard**.

**C'est la condition de recette, et elle ne se négocie pas en séance.** Cinq précisions sur la manière de la prouver :

- ⛔⛔ **« IL LE REÇOIT » SE PROUVE PAR LE DÉPÔT, PUIS PAR LE CLIC — jamais par un message de succès.** Un pull qui rend « c'est fait » sans qu'`exercices_depots` porte une ligne neuve **n'a rien servi**. **Constate en base**, puis **ouvre le dépôt** : le déroulé à six temps de `C4-L3` doit s'ouvrir, comme pour n'importe quel exercice.
- ⛔ **« UN SEUL À LA FOIS » ET « JAMAIS AU-DELÀ » SE PROUVENT PAR L'ABUS, PAS PAR L'USAGE.** Demande **deux fois de suite**, puis **jusqu'à épuisement du quota**, puis **une fois de trop** : le compte doit s'arrêter, et l'écran doit **dire pourquoi**. ⚠️ **Et éprouve le double clic** *(piège 54)* : deux appels concurrents ne servent qu'un exercice.
- ⭐ **« SERVI PAR LES MÊMES RÈGLES QUE SA SEMAINE » SE PROUVE SUR LE JOURNAL.** La ligne de `routeur_decisions` du bonus doit porter **sa cible, sa règle déclenchée et son tirage**, comme celles de la semaine — ⛔ **et AUCUNE sonde** *(piège 37)*. **Écris au relevé la ligne que ton pull a produite, champ par champ.**
- ⛔⛔ **« ELLE NE S'ASSIGNE JAMAIS TOUTE SEULE » SE PROUVE PAR LA NÉGATIVE, ET C'EST LA CLAUSE LA PLUS FACILE À CROIRE TENUE.** Sème un élève qui remplit **les deux conditions du push**, affiche la suggestion, **et constate qu'aucune ligne n'est apparue dans `exercices_depots`** tant qu'il n'a pas cliqué. *Une suggestion qui assigne est exactement le défaut que la source nomme.*
- ⭐⭐ **« LA MARQUE SE POSE, SE RELIT, ET ATTEINT LA MESURE » SE PROUVE SUR TROIS SAUTS, EN BASE.** *(1)* la marque est posée là où tu as décidé de la loger ; *(2)* la lecture de la semaine la relit et **la frise distingue l'assigné du demandé** ; *(3)* ⛔ **une mesure écrite depuis ce dépôt porte `competences_mesures.bonus = true`** — c'est le bout du canal que la source déclare, et **c'est le seul qui prouve que ton domicile marche**. ⚠️ **Et prouve le contraire aussi** : un exercice **imposé** sur **la même instance**, chez **un autre élève**, doit rendre `bonus = false` *(piège 24)*.

⭐⭐ **ET LE SMOKE ÉLÈVE N'EST PAS UNE FORMALITÉ : CHEZ `C6-L2`, L'ŒIL A TROUVÉ QUATRE DÉFAUTS QUE RIEN D'AUTRE NE VOYAIT** — `tsc` propre, **60 tests** verts, **51 contrôles de couture** verts, et **aucun des quatre n'était atteignable autrement**. *(Un motif de silence répété cinq fois · un « jamais travaillé · pas encore travaillé » qui se bégayait · **huit dimensions alignées en une phrase-fleuve à virgules**, corrigées en une liste, une par ligne · un titre collé à son paragraphe, un `<span>` inline que `space-y` ne sépare pas.)* ⛔ **Tu ajoutes un TROISIÈME TEMPS à un écran et une TUILE de plus : c'est exactement la même classe de défaut. Regarde-le, ne le déduis pas.**

⚠️ *Une session Code ne s'authentifie pas d'elle-même. Le patron de smoke élève existe et `C6-L2` l'a joué : **session mintée par l'API admin, `generateLink` + la route `/auth/confirm`, aucun mot de passe saisi**. `TEST_ELEVE_EMAIL` et `TEST_ELEVE_PASSWORD` sont au `.env.local`.*

⚠️ **Et une observation déposée par `C6-L2` et NON corrigée, qui frappera ta tuile de push** : à largeur mobile, **les titres de tuiles longs se tronquent** *(« Les six com… », « Lecture à pours… »)* — c'est le comportement existant de la liste « ensuite cette semaine » *(`truncate`)*, **partagé par toutes les tuiles**. ⛔ **Le corriger serait toucher le mécanisme de `C7-L2` : ce n'est pas ton lot.** ⭐ **Mais nomme ta tuile COURT**, et vérifie-la à largeur mobile.

---

## Les conventions — `PLAN_DE_CHANTIER.md` §5

### ⭐⭐ De couture — et ta couture est la question même de ton lot

**Pour tout lot qui succède à un autre** : le lot **éprouve la couture avec le ou les lots dont il dépend**, et **pas seulement sa propre frontière**. **Trois gestes.**

1. **Nommer les coutures qu'il traverse**, une par une, sous la seule forme qui les rend vérifiables — *qui écrit cette colonne · qui la lit · **un chemin réel y mène-t-il ?***
2. **Les éprouver par EXÉCUTION, jamais par lecture** : faire passer une donnée d'un bout à l'autre et **constater en base**, comme les **30** `scripts/recette/*.mjs` du dépôt le font déjà.
3. **Laisser le script au dépôt**, avec son **mode de retrait** — *c'est lui, pas le relevé, qui se rejouera à la prochaine revue.*

⭐ **Elle est écrite parce que c'est le seul défaut qu'aucun lot ne peut voir depuis sa place.** Le défaut central de `C4-L7` — *un retour de la maison ne pouvait jamais atteindre l'élève* — a traversé **dix lots** : *« chacun est juste à sa frontière, et **le trou est ENTRE eux** ».*

⭐⭐ **TA COUTURE SE NOMME EN UNE PHRASE :**

> **Ce que l'élève DEMANDE lui revient-il comme un exercice normal — et le dispositif sait-il ensuite que c'est lui qui l'a demandé ?**

**Elle traverse ta dépendance et trois lots de plus.** *Cinq coutures à nommer, et quatre sont aujourd'hui coupées :*

| # | Qui écrit | Qui lit | Chemin réel ? |
|---|---|---|---|
| **①** | `C4-L2` → `profiles.budget_optionnel_min` *(défaut 30)* | un consommateur de quota | ⛔ **coupé** — *la valeur se règle, personne ne la consomme ; **0 réglage en base des deux côtés*** |
| **②** | ton pull → `exercices_depots` + `routeur_decisions` | le déroulé à six temps de `C4-L3` | ⛔ **coupé** — *aucune écriture de dépôt déclenchée par un ÉLÈVE n'existe dans le dépôt* |
| **③** | ton pull → la marque `bonus` | `utils/chaine/contexte.ts` → `competences_mesures.bonus` | ⚠️ **la MOITIÉ AVAL passe, l'amont n'existe pas** — *0 instance et 0 mesure marquées, les deux bases* |
| **④** | `exercicesMaisonDeLEleve` → la liste du cycle | `friseDeLaSemaine` | ⚠️ **passe, mais elle ne SAIT PAS distinguer** — *la requête ne sélectionne pas `bonus`* |
| **⑤** | ton push *(une lecture)* | la liste des tuiles d'`app/eleve/page.tsx` | ⛔ **coupé** — *aucune tuile ne naît d'une compétence sans mesure* |

⭐ **Le modèle du script existe et il est récent** : `scripts/recette/couture-c6l2.mjs`, avec son `--retire` et son `--garde-le-decor` ; `scripts/recette/routeur-c4l12.mjs` porte le patron de **faire tourner le moteur sur un décor semé** ; `scripts/recette/lecture-c5l2.mjs` porte celui de **mémoriser un interrupteur trouvé et d'y revenir**. **Le tien sème un décor** *(un élève avec ses inscriptions, un vivier d'instances servables, une semaine posée qui n'épuise pas le plafond, une compétence `evaluee` à C ou moins sans mesure depuis trois cycles)*, **appelle les lectures et l'écriture que tes deux canaux appellent**, et **constate en base** — jusqu'à `competences_mesures.bonus`. ⛔ **Pas de lecture de code en guise de preuve.** ⛔ **Aucun appel de modèle payé : tout le décor se sème en base.**

⚠️⚠️ **QUATRE PIÈGES DE DÉCOR QUE `C6-L2` A PAYÉS EN JOUANT, ET QUI T'ATTENDENT TOUS LES QUATRE.** *Aucun ne se voyait dans le code ; chacun a fait échouer une recette là où elle s'attendait verte. Ils sont ici parce qu'ils sont à toi aussi — ton décor sème des mesures et emprunte un élève réel, exactement comme le sien.*

1. ⛔⛔ **UN DÉCOR QU'ON NE SAIT RETROUVER QUE PAR UN FICHIER LOCAL N'EST PAS RETIRABLE.** Deux runs de `C6-L2` ont laissé **onze mesures orphelines** — l'un tué par un `| head` *(SIGPIPE, avant le retrait)*, l'autre interrompu par **une contrainte** avant que le registre ne soit écrit. ⭐ **La parade qu'il a posée, et qui se reprend telle quelle** : chaque ligne semée porte **la MARQUE dans `instrument_version`**, le registre s'écrit **à chaque ligne**, et `--retire` **balaie par la marque même sans registre**. ⚠️ *C'est aussi ce qui a permis de séparer proprement les 11 semées des 3 réelles : les vraies portent un `instrument_version` et neuf clés d'observables, les semées n'avaient ni l'un ni l'autre.*
2. ⛔⛔ **LA BORNE DE RECETTE ÉCARTE SILENCIEUSEMENT UN DÉCOR SEMÉ TROP TÔT.** `mesuresQuiComptent` retire **tout ce qui précède `statut_recette_pose_le`** : `C6-L2` a semé six mesures et n'en a vu **compter que trois**, la borne d'`argumentation` étant au **23/08 21 h 19**. ⭐ **Le correctif est de LIRE LA BORNE EN BASE et de semer dedans** — et d'**échouer avec un motif nommé** si le créneau n'existe pas. ⚠️⚠️ **Et pour toi, c'est pire qu'un décor invisible** : **ton PUSH lit « aucune mesure depuis trois cycles »**. Des mesures réelles mais **pré-recette** ne comptent pas — ta suggestion se lèverait donc **à juste titre ou à tort selon la borne**, et tu dois savoir laquelle des deux tu prouves.
3. ⛔ **UN DÉCOR POSÉ SUR UNE DONNÉE RÉELLE QU'IL NE COMPTE PAS NE PROUVE RIEN.** Le premier élève que `C6-L2` a choisi **portait déjà une mesure** sur la compétence semée : `n` valait 7, les deux fenêtres se décalaient d'un cran, et le verdict basculait. ⭐ **Choisis un élève SANS AUCUNE MESURE sur la compétence que tu sèmes** — et pour toi, **sans aucun dépôt du cycle** : ton pull compte les exercices déjà posés *(PB5)*, et un dépôt réel oublié fausse le compte du quota.
4. ⭐ **LIS LES CONTRAINTES DE BASE, NE LES DEVINE PAS.** `C6-L2` en a nommé quatre qui l'ont surpris, et **trois te concernent directement** : **`exercices_statut_check` porte QUATRE valeurs — `a_concevoir`, `concu`, `assigne`, `clos` — et « valide » n'en est pas une** ; **`exercices_cran_chk`** *(une instance conçue porte son cran — donc un décor d'instance en a besoin)* ; et **`uk_mesures_depot_competence`** *(piège 23)*. ⚠️ **Et `exercices_depots.origine` est sous `CHECK` à deux valeurs** *(piège 19)*.

⚠️ **La base est la SANDBOX, et des élèves réels y travaillent** : ne touche QUE ce que tu as semé, et **vérifie le retour par requête**, jamais par la sortie de ton script.

⚠️ **Elle ne demande pas de réparer ce qu'elle révèle.** Ce que le passage montre **se dépose** au `SUIVI_tests_manuels.md` avec sa condition de reprise.

### Du dépôt

⚠️ **UNE MIGRATION EST PROBABLE, ET ELLE N'EST PAS CERTAINE** — le domicile de la marque *(piège 26)*. **Il n'y en a aucune autre : ton lot lit, il assigne, il ne recalcule rien.**

**Si tu en fais une** : **une ligne au `SUIVI_SQL.md` AVANT exécution**, jamais après — date, fichier, zone, cases **Sandbox** et **Prod** ; un fichier `.sql` à la racine **plus son rollback, écrit avant l'exécution et présent sur le disque** ; **additive et gatée** ; **bac à sable d'abord, prod ensuite**. ⛔⛔ **`exercices`, `exercices_depots` ET `routeur_decisions` SONT DES TABLES VIVANTES** — 251 instances et 86 dépôts en production : **le protocole renforcé de la règle R6 s'applique**, lis-la en tête du `SUIVI_SQL.md` avant d'écrire une ligne. **Sandbox d'abord et jamais l'inverse · ne jamais rejouer un fichier de l'Archive · répétition à blanc sur le CORPS du fichier**, jamais sur le fichier entier *(son `commit;` validerait ta transaction d'essai)*.

**Les six interrupteurs restent où ils sont, et comme tu les trouves** — `exercices_actif`, `routeur_actif`, `competences_affichage_actif`, `fabrique_actif`, `chaine_actif`, `passation_classe_actif`. ⛔ **Aucun septième** *(`07-` §5, en toutes lettres)*. ⚠️ **Et rappelle-toi qu'ils ont bougé pendant la fabrication de ce prompt** : **mesure-les toi-même**, constate, ne bascule pas.

**La doctrine en base est dérivée, jamais tapée, et il n'y a qu'un dériveur.** ⭐ *Ton lot ne devrait lire aucune table de doctrine directement : les durées lui viennent du vivier de `C4-L12`.* **Si tu en lis une, joue `python3 scripts/derive-doctrine.py --verifie` avant** — **IDENTIQUE** sur les **douze** tables, les empreintes de source et la fixture. **S'il dit DIVERGE, rejoue `--sql` ; jamais corriger la base à la main.**

### De clôture

**Ta section au `SUIVI_tests_manuels.md`**, au moment où le lot se clôt : ce qui a été **prouvé en séance, coché avec sa preuve**, et ce qui **reste à jouer en recette, décoché, avec sa condition de reprise nommée**. *Un reste de recette qui ne vit que dans un relevé ne se rappelle à personne.*

⚠️ **Et trois conditions de reprise sont écrites d'avance par la base elle-même** : *(a)* **le pull ne se prouve qu'en bac à sable** — la production ne porte **aucun dépôt maison** *(86 dépôts, `lieu = 'classe'` sur les 86)*, et ses **251 instances sont non servables** ; ⭐ *la porte, elle, est ouverte des deux côtés* ; *(b)* **le push sera muet en bac à sable** — 102 niveaux dont la lettre est NULLE — **et parlant en production**, où 149 lettres réelles existent ; *(c)* **`competences_mesures.bonus` ne se prouve en vrai qu'avec la chaîne qui tourne** — dis si tu l'as fait tourner ou si tu as constaté le contexte sans payer d'appel.

⚠️ **Et un renvoi de périmètre laisse sa trace, AVEC SA LISTE ET SON DESTINATAIRE** *(la leçon de `C4-L6`, à sa quatrième récidive)*. ⭐ **Chacun se nomme dans SA boîte du `PLAN_DE_CHANTIER.md` §5.** ⛔ *Deux ne sont PAS de nouveaux renvois et ne se redéposent pas : l'**échéance absente de l'agenda élève** est déjà à `IDEES_post_rentree.md`, et **`ilYAStagnation` sans appelant** est déjà à la boîte de `C4-L12`.*

### De dette

**Une source trouvée fausse se MARQUE, elle ne se corrige pas** *(`07-` §2)*. Deux gestes : **`[faux]`** au point de l'erreur, et **une ligne à la section DETTES** du `INVENTAIRE_Non_Tranches.md`, qui porte l'**avant / après**. **La prochaine dette est `D10`.** ⭐ **Ton candidat le plus sérieux est le `07-` §1.1 sur le domicile de `bonus`** *(piège 27)* — **pèse-le, ne le pose pas d'office.**

⛔⛔ **ET DEUX CANDIDATS ÉVIDENTS N'EN SONT PAS.** *(1)* **Un interrupteur que tu trouverais à OFF** contre la phrase du `07-` §5 : c'est un **état de base**, pas une règle fausse — **il se dit à Louis, et il se remet comme on l'a trouvé.** *La fabrication en a trouvé deux à OFF en production le 28/08 ; Louis les a rallumés le jour même, et rien n'a été corrigé dans la source — c'est exactement le bon geste.* *(2)* Le **`PLAN_DE_CHANTIER.md` §3**, qui te donne `C4-L2` pour dépendance unique quand ton chemin réel traverse aussi `C4-L12`, `C4-L13` et `C6-L2` : **le plan n'est pas une des neuf sources**, et *« une dette n'est pas un plan périmé »* — **dis-le au relevé.**

### D'ouverture de compétence

⛔ **Sans objet — ce lot n'ouvre aucune compétence dans la chaîne**, et les six sont déjà ouvertes. ⚠️ **Ne joue `derive-instruments.py --ecris` QUE si tu as amendé une section ouverte du `07-`**, et **EN DERNIER**.

### Et le hors-périmètre

Toute idée ou découverte hors du lot → **une ligne dans `IDEES_post_rentree.md`**, pas dans le code *(`AGENTS.md`)*. **Note-les, ne les joue pas.**

---

## ⭐ Ce que tu dois rendre à Louis

**Quoi qu'il arrive dans la séance — lot fini, lot partiel, lot arrêté au constat —, tu rends d'abord ce tableau, dans le terminal, en tête de ta réponse finale :**

| | Ce qu'il fallait | État | La preuve |
|---|---|---|---|
| **①** | le **PULL** — l'élève demande, **un seul à la fois**, et **reçoit un dépôt réel** | … | … |
| **②** | le **quota** — **jamais au-delà**, **rien de reporté**, et l'écran **dit pourquoi** quand il s'arrête | … | … |
| **③** | le **PUSH** — une suggestion sur le tableau de bord, **qui n'assigne rien** | … | … |
| **④** | la **MARQUE `bonus`** — son **domicile choisi et motivé**, posée, relue | … | … |
| **⑤** | ⛔ **elle atteint `competences_mesures.bonus`** — et **reste fausse pour l'exercice imposé** | … | … |
| **⑥** | la **frise** — l'assigné et le demandé **ne se confondent plus** | … | … |
| **⑦** | ⛔ **aucune sonde secondaire sur un bonus** | … | … |
| **⑧** | ⭐ **la couture** — les cinq canaux du tableau, éprouvés **par exécution** | … | … |

⚠️ **Trois états seulement, et le mot se choisit sans détour** : **FERMÉ** *(construit et prouvé — dis par quoi)* · **PARTIEL** *(construit, non prouvé — dis ce qui manque à la preuve)* · **NON FAIT** *(dis pourquoi, et à qui ça revient)*. ⛔ **« En cours » n'est pas un état.** ⛔ *Pour la couture, **FERMÉ** veut dire « éprouvée par exécution, script au dépôt » — jamais « lue et jugée correcte ».*

### Et ce que ton relevé doit porter, après lui

Le nom du fichier : **`RELEVE_C6_L3_2026-XX-XX.md`**, à la racine du dépôt `palimpseste`. **Après le tableau, dix choses au minimum**, en plus du récit :

1. **Ton point de départ** — le commit, l'état de l'arbre, **ce qui n'est pas de toi**, et les chiffres de `npm test` et de `tsc` **avant** que tu commences ;
2. ⭐⭐ **LE DOMICILE DE LA MARQUE**, et **le raisonnement complet** : ce que tu as mesuré, les trois options pesées, celle que tu retiens, **et ce qui devient faux dans le `07-` §1.1 si l'une d'elles l'est** ;
3. ⭐⭐ **LES CINQ CANAUX DU TABLEAU DE COUTURE**, un par un : qui écrit, qui lit, **et ce que tu as branché** ;
4. **La ligne de `routeur_decisions` qu'un pull produit**, champ par champ, **à côté d'une ligne de semaine ordinaire** — c'est la preuve la plus courte que « le bonus est un exercice normal » ;
5. **Ce que ton pull pose comme `assigne_at`**, et **pourquoi** *(piège 63)* ;
6. **Les trois vides du pull** — quota épuisé · vivier vide · liste vide — avec **la phrase exacte que l'écran rend pour chacun**, et **ce qu'il rend porte fermée** ;
7. **Ce que le bonus fait au compte d'assiduité** *(piège 49)* — mesuré, pas raisonné ;
8. **La migration**, s'il y en a une : sa ligne au `SUIVI_SQL.md`, son rollback, et **le protocole renforcé qui a été suivi** ;
9. **Les renvois de périmètre, AVEC LEUR LISTE ET LEUR DESTINATAIRE** — fichiers et lignes, et **la boîte où tu les as déposés** ;
10. **Ce qui reste à jouer en recette**, avec sa condition de reprise — parce que c'est cela qui part au `SUIVI_tests_manuels.md`.

⭐ **Et deux amendements t'appartiennent sans accord préalable** : l'**inventaire des lots du `07-` §2** *(ton état de clôture)* et le **`07-` §1** *(ce que la construction aura précisé du domicile de la marque et de l'écriture déclenchée par un élève)*. ⚠️ **Si tu y touches, rejoue `derive-instruments.py --ecris` EN DERNIER**, et vérifie que `--verifie` rend `INSTRUMENTS : IDENTIQUE` et que `npm test` repasse vert.
