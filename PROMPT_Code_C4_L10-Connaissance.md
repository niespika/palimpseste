# PROMPT — Session Code : C4-L10 · Connaissance — l'ouverture de la Connaissance dans la chaîne

> **À coller dans une session Claude Code fraîche.** Une session, une compétence.
>
> Ce prompt est fabriqué selon la recette du `PLAN_DE_CHANTIER.md` §5. Ce que le lot construit, son manifeste et son « fait quand » font foi au `07-Implementation.md` §2 — ils sont recopiés ci-dessous avec leurs versions au moment de l'écriture.
>
> ⛔ **CE PROMPT SUPPOSE `C4-L10 · Expression` JOUÉ — ET IL L'EST, DEPUIS LE 22/08.** Cette séance-là a aligné le verrou de la dérivation et posé **tous** les canaux qui manquaient à l'interface du branchement — **une fois, pour les six**. Ici, tu **vérifies** que ce socle est là *(§ « Le socle »)*, puis tu ne portes qu'une chose : **le branchement de la Connaissance**. Si un point du socle manque, **arrête-toi et dis-le** : ce n'est pas ton lot.
> *(Les six dérivés existent déjà : `derive-instruments.py --resume` rend les six compétences OUVERTES. Le premier des trois gestes d'ouverture est donc fait pour toi — vérifie-le, ne le referais pas à l'aveugle.)*
> ⚠️⚠️ **UN DÉFAUT DE TON MODULE, TROUVÉ PAR LA SÉANCE EXPRESSION — lis-le avant de commencer.** `connaissance.pre_p1(texte, params=None)` prend **le TEXTE**, quand le `CONTRAT-MODULES.md` §2 et `banc.py` passent **le CONTEXTE** — vérifié en appelant : `AttributeError: 'dict' object has no attribute 'strip'`. **Le portage suit LE CONTRAT** — c'est lui qui est au manifeste — **et lit la copie dans le contexte**, ce que le module veut dire. *L'Expression porte exactement le même défaut et l'a traité ainsi : suis son patron.* ⚠️ **C'est une dette de source côté conception — marque-la, ne corrige pas le module.**

---

## Le manifeste — recopié du `07-Implementation.md` §2

> *Manifeste* : **ce document, §1 et §2** · `03-competences.md` §1, §2 et §9 — **relu et validé**, le gabarit d'une fiche, ce que porte son bloc machine, l'état de chacune et la **porte de recette** · `01-routeur.md` §3, §8.2 et §11 — **relu et validé**, les trois statuts de recette, le **seuil de réussite** d'un observable, et les **quatre temps** de la chaîne · `competences/<nom>.md` — **relu et validé**, la fiche de la compétence ouverte ; *une compétence dont la fiche est absente ou seulement déposée reste hors du lot, pas le lot hors du chantier* · `copies-tests/_commun/CONTRAT-MODULES.md` — les **six crochets**, les **trois clés** et l'**extraction** · `copies-tests/<nom>/code.py` — le module de la compétence, et **ses vecteurs**.

« Ce document » est le `07-Implementation.md`, et `<nom>` vaut ici **`connaissance`**. Les six pièces :

| Pièce | Où | Statut requis | Au moment de l'écriture |
|---|---|---|---|
| `07-Implementation.md`, **§1 et §2** | `/Users/louissagnieres/Documents/GitTest/palimpseste-conception/` | aucun — un lot n'exige pas un statut de la source qui le déclare *(`07-` §2)* | **VERSION 2.34** · RELU ET VALIDÉ · **le §1 et l'inventaire du §2 sont OUVERTS À L'IMPLÉMENTATION** ; la **règle de manifeste** du §2 est GELÉE |
| `03-competences.md`, **§1, §2 et §9** | même dépôt | **relu et validé** *(explicite au manifeste)* | **VERSION 2.1** · VALIDÉ ET GELÉ |
| `01-routeur.md`, **§3, §8.2 et §11** | même dépôt | **relu et validé** *(explicite au manifeste)* | **VERSION 5.5** · VALIDÉ ET GELÉ |
| `competences/connaissance.md` | même dépôt | **relu et validé** *(explicite au manifeste — ⚠️ **pas** *versé et bancé* : voir le socle, point 1)* | **VERSION 2.2** · RELUE ET VALIDÉE |
| `copies-tests/_commun/CONTRAT-MODULES.md` | même dépôt | **déposé** *(entrée sans statut explicite — `07-` §2)* | pas de ligne VERSION · **validé par Louis item par item, 1er-2 août 2026** |
| `copies-tests/connaissance/code.py` | même dépôt | **déposé** *(entrée sans statut explicite)* | pas d'en-tête · `TESTS_P2_PARFAIT` **18 vecteurs** · `TESTS_CODE1_PARFAIT` **4 vecteurs** · `VERSION_GOLDS_TESTEE = None` |

Ce que chaque pièce fait ici. Le **`07-` §1** déclare les tables que la chaîne écrit — `exercices_squelettes` et son index unique, `competences_mesures` et ses colonnes, `competences_niveaux` et son `statut_recette` *(§1.2, §1.3)* ; le **§2** porte la règle de manifeste, la clause granulaire et la convention de dette. Le **`03-` §1** donne le **gabarit d'une fiche** — ses dix sections, ce que porte son bloc machine, et le **vocabulaire d'`observables_mesure`** ; le **§2** dit l'état de chacune ; le **§9** est la **porte de recette** et ses deux planchers mécaniques. Le **`01-` §3** porte les trois statuts de recette et qui les choisit ; le **§8.2** dit qu'un verdict d'observable se lit en confrontant sa valeur au seuil de la fiche, jamais en base ; le **§11** pose les **quatre temps** de la chaîne et tout ce qu'une mesure journalise. **La fiche de la Connaissance** porte son squelette P1, ses règles de notation P2, son bloc machine, ses observables de télémétrie et **ses prompts** — c'est elle qui fait foi sur le détail de sa chaîne. Le **`CONTRAT-MODULES.md`** dit ce qu'un module rend et par quels crochets. Le **`code.py`** est l'implémentation existante de ce contrat, avec **ses vecteurs de test embarqués**.

**Rien de plus : la règle de manifeste veut que ce qui n'y figure pas ne se lise pas** *(`07-` §2)*. Cinq précisions :

- l'`AGENTS.md` du dépôt n'est pas une source : Claude Code le charge d'office, il porte les conventions du repo ;
- ⛔ **`SPEC_C3_exercices_competences.md` est archivée : elle ne fait foi sur rien et ne se lit pas** — elle traîne à la racine du dépôt `palimpseste` et son nom ressemble à ton sujet ;
- ⛔ **aucun relevé de lot ne se lit.** Une seule exception, et elle est nommée au socle : **le relevé de `C4-L10 · Expression`**, que tu ne lis pas non plus — ce qu'il avait à te dire est **à la boîte aux lettres du `PLAN_DE_CHANTIER.md` §5**, entrée `C4-L10` ;
- ⛔ **`NOTE-CALCUL-HORS-MODELE.md` ne se lit pas** : son en-tête se défend lui-même *(« acté pour l'Expression … `[à valider]` pour les autres compétences »)*. **Le principe qu'elle porte est écrit dans une source gelée** — `01-` §11 : *« le modèle ne rend ni niveau, ni dimension, ni décompte »* ;
- **le statut porte sur le fichier, la section dit seulement où lire** *(`07-` §2)*. Tu suivras les citations que tes sections font : le `01-` **§8.3** *(ce que l'escalade lit d'un observable)*, **que le §8.2 cite**. ⚠️ Le `01-` **§12** *(le schéma de sortie strict et la défense contre l'injection)* **n'est cité par aucune section de ton manifeste** ; **ce prompt l'ouvre quand même, et le dit** — la chaîne passe par `appel.ts`, qui l'applique déjà. **Les cinq autres fiches de compétence ne se lisent pas.**

## Le contrôle d'entrée — en deux temps, et les deux ne font pas la même chose

> **Le statut bloque.** Un fichier du manifeste dont l'en-tête porte un statut inférieur à celui exigé **arrête le lot**, explicitement. Le barème est au `07-Implementation.md` §2 : *déposé* → *relu et validé* → *versé et bancé*, cumulatifs ; « VALIDÉ ET GELÉ » vaut *relu et validé*.
>
> **La version avertit.** Si la ligne VERSION d'un fichier du manifeste diffère de celle inscrite ici, **relis son en-tête avant de continuer** — une version qui bouge dit qu'un texte a changé, pas qu'il est devenu faux.
>
> **Le blocage est granulaire quand il peut l'être.** Une fiche de compétence absente ou non bancée bloque **cette compétence**, pas le lot.

Concrètement : vérifie que les six pièces **existent**, que le `07-` porte **VERSION 2.34**, le `03-` **2.1**, le `01-` **5.5** et `competences/connaissance.md` **2.2**, et que les trois sources de conception valent au moins *relu et validé*. ⚠️ **Le statut requis de ta fiche est *relu et validé*, pas *versé et bancé*** — c'est ce que la séance Expression a inscrit dans le code, et c'est le socle, point 1.

⚠️⚠️ **UNE PORTE, ET ELLE SE VÉRIFIE AVANT DE COMMENCER — `C4-L11` DOIT ÊTRE JOUÉ.** La `cible_primaire` de l'exercice conçu par le professeur n'était **pas construite** : la chaîne prenait **l'ordre alphabétique**, assumé comme convention, **et levait une alerte dès que ce repli servait sur plus d'une compétence**. Tant qu'une seule était branchée, il ne mordait pas — **il mord à la deuxième, donc à toi**. Sa condition de fermeture est *« AVANT LA DEUXIÈME COMPÉTENCE BRANCHÉE »*, et **Louis a placé `C4-L11` — le lot des correctifs — juste après l'Expression, précisément pour la tenir.** ⛔ **Vérifie qu'il est joué avant de brancher** : la `cible_primaire` se déclare à l'écran de conception et `cibleDuRetour` la lit avant tout défaut. **S'il ne l'est pas, arrête-toi et dis-le à Louis** — brancher quand même ne casse rien de visible, et c'est exactement le problème : le retour et le squelette de version finale s'attacheraient à la compétence dont le nom vient en premier dans l'alphabet, pas à celle qui était visée.

**Ce que tu peux écrire dans une source, et ce que tu ne peux pas.** Le **`07-` §1 et l'inventaire des lots du §2 sont ouverts à l'implémentation** : ce que la construction fait apparaître **s'y écrit depuis ton relevé, sans accord préalable**. ⚠️ **La règle de manifeste du §2, le §3, le §4 et le §6 sont GELÉS.** **Partout ailleurs, une session Code ne corrige jamais une source** : tu poses **`[faux]`** au point de l'erreur et tu portes la correction au **registre des ouverts**, section **DETTES** du `INVENTAIRE_Non_Tranches.md`, avec l'avant et l'après.
⚠️ **Ce lot a plus de chances qu'un autre d'en rencontrer** : porter une grille, c'est la lire à la lettre. **Marque, ne corrige pas** — et surtout ne « répare » pas la fiche pour faire passer ton portage.

## La mission — reprise du `07-Implementation.md` §2

> **C4-L10 — L'ouverture d'une compétence dans la chaîne.** La chaîne froide est bâtie et éprouvée *(C4-L5)*, et elle n'ouvre **aucune compétence** : aucun instrument n'est dérivé, `INSTRUMENTS` et `BRANCHEMENTS` sont vides, et un dépôt traverse la chaîne sans qu'aucun squelette, aucune mesure ni aucun retour ne s'écrive. **Ce lot ouvre UNE compétence, et il se rejoue tel quel pour chacune des cinq autres** — *le blocage est granulaire ; une compétence de plus est une compétence de plus à la rentrée, pas un lot de plus.*
>
> Il porte quatre choses, et la première ne se fait qu'une fois :
>
> - **Le verrou de la dérivation s'aligne sur ce que les sources disent.** L'instrument se dérive d'une fiche **relue et validée** ; ce qui ouvre une compétence à la chaîne redevient son **statut de recette en base**, posé par le professeur à l'écran de la fabrique *(§1.3, C4-L8)*. **Les deux planchers mécaniques du `03-competences.md` §9 restent** — fiche non déposée → `differee`, correspondance observable → formulation non uploadée → non déclarable `evaluee`. *Motif : le seuil de banc n'est une règle dans aucune source — il n'existe que comme exigence d'un lot, à la ligne de manifeste de C4-L5. Le `03-competences.md` §9 dit que les trois conditions de banc « ne sont gardées par aucun mécanisme … c'est le professeur qui les vérifie », et le `01-routeur.md` §3 que « c'est le professeur qui choisit, sans automatisme ». Un contrôle plus strict que sa source refuse du licite, et il le refuse en silence.*
> - **L'interface du branchement reçoit ce que le contrat des modules lui envoie.** Le `copies-tests/_commun/CONTRAT-MODULES.md` déclare **six crochets** et fait passer à `code2` **la sortie de `code1`** ; l'interface de la chaîne porte **cinq champs** et ne la lui passe pas — or l'agrégation *est* ce croisement. Ce lot pose les canaux manquants **une fois, pour les six**, et il les pose d'après ce que les modules utilisent réellement, jamais d'après ce qui pourrait servir.
> - **Le branchement de la compétence, porté depuis son module.** `extractions`, `jugement`, `code1`, `code2`, `delta` — **écrits à la main, la fiche sous les yeux** *(`01-routeur.md` §11 : « Le détail de chaque chaîne vit dans la fiche de sa compétence »)*, et le module de calibration à côté : `copies-tests/<nom>/code.py` implémente déjà le même contrat, et il **porte ses propres vecteurs**. *Ce n'est pas une réécriture : c'est une extraction, au sens du `CONTRAT-MODULES.md` §7 — ce que le module encode a été mesuré, le régénérer depuis la fiche le jetterait.*
> - **Les observables de télémétrie de la compétence.** Ceux du **§5 de sa fiche**, qui commandent l'escalade N1/N2, le levier du retour et la question « se juger ». **Ce ne sont pas ceux du module** — celui-ci ne rend que les observables que le banc compare aux golds — et le `03-competences.md` §1 dit qui les applique : *« il n'est pas appliqué par le module mais par la chaîne froide »*. `code2` les rend au relevé, **ou dit par une alerte nommée pourquoi il ne le peut pas.** *Sans eux la chaîne n'échoue pas : elle rend `n/a`, la valeur sort du dénominateur du taux, et tout le §8 du routeur devient aveugle sur cet observable — sans symptôme.*
>
> *Ce lot ne touche ni au banc, ni au module `code.py`, ni aux prompts — qui vivent dans la fiche et se dérivent. Il ne pose aucun statut de recette, et il n'en propose aucun : le professeur choisit. Et il ne juge pas la fiche — une grille trouvée fausse en la portant se **marque**, elle ne se corrige pas.*

**Et rien d'autre.** ⭐ **Pour toi, le PREMIER des quatre tirets est déjà fait** — le verrou est aligné, la séance Expression s'en est chargée. Il te reste **les trois autres, sur ta seule compétence** : dériver et importer l'instrument, porter le branchement, et rendre les observables de télémétrie. Tu n'ajoutes **aucun écran**, **aucune migration**, et tu ne changes **rien** au comportement visible tant que le professeur n'a pas posé de statut.

## Le socle — ce que `C4-L10 · Expression` a posé le 22/08, et que tu VÉRIFIES sans le refaire

**Huit points, tous nommés. Chacun se vérifie en un `grep` ou une commande. Si l'un manque, arrête-toi et dis-le.**

1. **Le verrou est aligné** — `scripts/derive-instruments.py` dérive d'une fiche *relue et validée*. ⚠️ **`degre < RELU` est écrit à DEUX endroits**, et la séance a dû corriger **onze** formulations de *« versé et bancé »* là où son prompt en comptait neuf. **Contrôle** : `python3 scripts/derive-instruments.py --resume` doit rendre *« compétences OUVERTES à la chaîne »* avec **les six**, la tienne comprise et son compte d'observables de télémétrie.
2. **La substitution des slots existe** — `utils/chaine/slots.ts`. Elle lit les slots **à la forme du banc**, sépare la **tête invariante** *(qui devient le préfixe cacheable — 9008 caractères sur l'Expression)* de la queue, substitue **sous balise de matériau**, et porte **le refus des deux sens**, joué **au chargement**. ⛔ **Le gabarit brut ne part jamais à côté du substitué.** ⭐ **`{sujet}` est la consigne instanciée** — la table `exercices` porte la consigne, jamais le texte d'un sujet distinct ; le fournisseur est nommé en **un seul endroit**, `FOURNISSEURS_NATIFS`.
3. **`code2` reçoit la sortie de `code1`** — la signature est `code2(artefactP2, sortieCode1, ctx)`, et `code1` rend **`mesures` + `document_p2`**.
4. **`conformite` est déclaré au branchement et appelé à chaque passage** ; ses alertes rejoignent celles de `code2`.
5. **Les crochets pré-phase ont leur canal** — `prepareCopie` · `pre` **par étage**, avec ses **`slotsFournis` déclarés** · `preP2`, dont **un slot servi à `null` arrête la mesure en le nommant**. ⭐ **Ceux que TU utilises : `pre_p1`, `pre_p2`, `code1`, `code2`, `conformite`.**
6. **Le canal privé `_audit`** — les clés à tiret bas passent à `conformite` et **ne sont lues nulle part ailleurs** ; `refusFormeCode2` porte le refus dur du contrat sur les **trois clés publiques**. ⛔ **`injection_p2` n'a PAS été construit, et ne doit pas l'être** : vérifié module par module, **aucun des six ne l'utilise** — *on ne bâtit pas un canal sans client.*
7. **`utils/chaine/arrondi.ts` existe, écrit une fois pour les six** — voir le piège 23.
8. **`valeursDesParametres()` — un seul domicile pour lire les paramètres dérivés.** ⚠️ **La forme dérivée est un BLOC par paramètre** *(`defaut`, `bornes`, `statut`)*, jamais une valeur à plat. Le typage l'ignorait, et *« le jour où les fiches dérivent, un `seuil_parametre` lu sur le bloc rend un objet, la comparaison au seuil devient impossible, et l'observable sort en silence du dénominateur »* — exactement le trou que ce lot existe pour fermer. ⚠️ **Tu es directement concernée** : ta fiche est l'une des **deux** qui portent l'essentiel de ces paramètres en blocs. **Lis-les par `valeursDesParametres()`, jamais à plat.**

> ⭐ **Le patron d'un slot ouvert existe, et c'est l'Expression.** Son import, son branchement et ses tests dans `utils/chaine/instruments.ts` sont ton gabarit. *Copie la forme, jamais le contenu — le détail de chaque chaîne fait foi à sa fiche.*
>
> ⭐⭐ **Et vide la boîte aux lettres de C4-L10** au `PLAN_DE_CHANTIER.md` §5 : **elle est remplie — huit items déposés par la séance Expression, et c'est le dépôt le plus utile du lot.** **C'est le seul endroit où les lire** — la règle de manifeste t'interdit son relevé. Chaque item entre en piège, puis se raye d'un ✅ et de sa date.

## Les pièges — les décisions dont l'oubli coûte une migration

*Les renvois nomment leur document ; « §1 », « §1.2 », « §1.3 » nus pointent au `07-Implementation.md`. En cas de doute entre ce prompt et la source : **la source a raison**.*

### A — Ce que ce lot ne construit pas

1. **La chaîne est bâtie, éprouvée, et elle ne se refait pas.** `utils/chaine/` porte ses quatre temps, sa file, son bail, son idempotence, ses plafonds, son anti-injection, son schéma strict, son étage de Monitoring et son retour segmenté. **Ta réussite se mesure à un diff quasi nul hors d'`instruments.ts`.**
2. **Le banc, les golds, les critères et le protocole ne se touchent pas.** Ils vivent au dépôt de **conception** et servent la calibration. Tu **lis** `code.py` et le contrat ; tu **n'édites** ni l'un ni l'autre. *Un module modifié pour faciliter un portage casse le banc qui l'a validé.*
3. **Aucun prompt ne se tape.** Les prompts vivent **dans ta fiche**, entre leurs marqueurs, et `derive-instruments.py` les verse dans `utils/chaine/derive/`. *« FICHIER DÉRIVÉ — NE S'ÉDITE JAMAIS À LA MAIN. »* Si un prompt te paraît faux, c'est une **dette de source**.
4. **Ce lot n'écrit rien en base et n'attend aucune migration.** ⚠️ Si tu découvres qu'une migration est nécessaire, **arrête-toi et dis-le** — c'est un signe que le périmètre a glissé. Si elle l'est vraiment : **une ligne au `SUIVI_SQL.md` avant exécution**, jamais après, et la migration est **additive et gatée**.
5. **Tu ne poses aucun statut de recette, et tu n'en proposes aucun.** Il se pose à l'écran de C4-L8, par `poser_statut_recette`, **et c'est le professeur qui choisit** *(`01-` §3 ; `03-` §9)*. Une compétence naît `mesuree_silencieusement` : *« l'oubli n'envoie jamais un verdict faux à un élève »*. ⛔ **N'écris jamais `evaluee` en base, même « pour tester ».**
6. **Le lot de correctifs n'est pas le tien.** `exercices_squelettes.prompt_version` *(écrite deux fois, et qui disparaît)*, la `cible_primaire` *(remplacée par l'ordre alphabétique, avec alerte)*, et le **déclencheur** de la chaîne, qui n'existe pas. **Relève-les, ne les corrige pas.**

### B — Les observables de télémétrie, et le silence qu'ils font

7. ⭐⭐ **DEUX LISTES D'OBSERVABLES, QUI NE SE CROISENT QU'UNE FOIS SUR TOUT LE CORPUS.** Les `OBSERVABLES` du module sont ceux que **le banc compare aux golds** : **24** sur les six fiches. Ceux du **§5** des fiches sont ceux de la **télémétrie du routeur** : **56**. *Un seul observable croise les deux listes* — `question_specifique`, au Questionnement *(`03-` §9 ; ⚠️ il y écrit encore « 55 », la Synthèse en a gagné un depuis : **compte les lignes de ta fiche, jamais le chiffre de la prose**)*.
    ⭐ **Ton compte à toi.** Le module rend `niveau`, `diversite`, `justesse`, `profil_moyen`, `etendue`. Le §5 de ta fiche en déclare **8** : `mobilisation`, `diversite_registres`, `diversite_sources`, `taux_justesse`, `contresens`, `unite_plaquee`, `inverifiable`, `etendue_rappel`. **Porter le module ne suffit donc pas** : c'est la moitié du travail, et l'autre moitié ne se voit pas.
8. **Qui les calcule est TRANCHÉ, et pas là où on l'attendrait.** Le `CONTRAT-MODULES.md` §8 pose encore la question — *« Module ou app ? `[à valider]` »* — mais le **`03-` §1, gelé, répond** : *« `observables_mesure` n'est pas de son ressort — il n'est pas appliqué par le module mais par **la chaîne froide** »*. **La source a raison contre le contrat** : une annexe ne contredit jamais une source. C'est donc **`code2`** qui rend leurs quantités au `releve`.
9. ⚠️⚠️ **UN OBSERVABLE OUBLIÉ NE LÈVE AUCUNE ERREUR — IL REND `n/a`.** `utils/chaine/observables.ts` : *« Une entrée ABSENTE veut dire “pas d'occasion” et rend `n/a` ; une entrée à `null` veut dire la même chose »*. Et `n/a` **sort du dénominateur** du taux *(`01-` §8.2)*. **Une compétence ouverte dont `code2` oublie trois observables tourne, écrit, rend une lettre — et l'escalade N1/N2 est aveugle sur ces trois-là, pour toujours, sans un symptôme.** ⭐ La parade est dans le « fait quand » : **chaque observable a sa valeur au relevé, ou son alerte nommée.**
10. **Le vocabulaire d'`observables_mesure` fait foi au `03-` §1 — et le script en contrôle DAVANTAGE que la source n'en nomme.** Le **`03-` §1** énumère les six familles — `proportion`, `densité`, `comptage rapporté`, `comptage`, `binaire`, `ordinal` —, puis `rapporte_a`, `reussie`, **`seuil` ou `valeur_reussie`**, `sans_objet_si`, `statut`, `sens`. **`derive-instruments.py` contrôle en plus** : `echelle` obligatoire sur un ordinal *(et le seuil doit être **sur** l'échelle)*, `seuil` **XOR** `seuil_parametre`, `valeur_reussie` exigée par `vaut`. ⭐ **Une seule famille compose** : `comptage rapporté` **divise** l'entrée du même nom par le dénominateur que `rapporte_a` désigne ; les autres lisent l'entrée telle quelle. *Le nom de l'entrée du relevé est donc le **code de l'observable**, exactement.*
11. **Le verdict ne se stocke jamais, et le seuil ne se recopie jamais.** *« Le verdict — réussie ou ratée — ne se stocke pas : il se lit en confrontant cette valeur au seuil de réussite déclaré à la fiche »* *(`07-` §2, qui renvoie au `01-` §8.2)*. **Ne crée aucune colonne de verdict**, et **ne mets aucun seuil en dur** : il est dans l'instrument dérivé. *Tes paramètres déclarés : **six** — `min_registres`, `haut_registres`, `haut_sources`, `plafond_inverifiable_haut`, `seuil_ratio_haut`, `restitution_de_cours`.*

11 bis. ⚠️⚠️ **LA POPULATION D'UNE `proportion` NE VIT PAS AU BLOC MACHINE — ELLE VIT À TON §5.** Vérifié sur les six fiches le 23/08 : `comptage rapporté` nomme son `rapporte_a` **15 fois sur 15** ; `proportion` et `densité`, **0 fois sur 18**. **Ce n'est pas un oubli, c'est la règle** : `rapporte_a` est **le paramètre du diviseur de la chaîne** — *« `comptage rapporté` est la seule famille qui compose »* *(`observables.ts`)* —, et pour une `proportion` c'est **`code2` qui divise en entier**. Le dénominateur est donc affaire de **prose**, et les fiches le nomment, **exclusion comprise** : l'Expression *« de phrases, **hors ouvertures de paragraphe** »*, la Structure *« de coutures, **hors seuils** »*, la Connaissance *« les `inverifiable` **hors du dénominateur** »*.

    ⚠️ **L'Argumentation, elle, ne l'avait pas nommée** — *« proportion d'unités »*, sans dire lesquelles — et sa séance a dû arbitrer. **Elle a bien fait, et sur un critère solide** : le `sens` d'un observable l'adosse souvent à une règle du §4, et *un observable calculé sur une autre population que la règle à laquelle son `sens` l'adosse peut dire l'inverse d'elle*. ⭐ **Plus l'argument décisif, qu'elle a trouvé toute seule** : *« prendre toutes les unités du relevé ferait qu'un statut mal orthographié abaisserait `garant_present` sans toucher `preuve_circulaire` »* — deux familles à deux populations, et **une seule entrée malformée déplacerait l'un sans toucher l'autre**. *(Sa fiche a été complétée le 23/08, pour que la question ne s'y repose plus.)*

    ⛔ **Et ce que tu fais si la tienne ne la nomme pas : tu t'arrêtes et tu le dis à Louis.** C'est une **décision de source**, pas un choix d'implémentation — et une population choisie en passant ne laisse **aucun symptôme**.

    ⭐ **Ton cas : ta SEULE `proportion` est nommée**, exclusion comprise — `taux_justesse`, *« proportion d'unités `juste`, **les `inverifiable` hors du dénominateur** »*. **Vérifie-le et passe** : tu n'as rien à arbitrer ici.

### C — Ce que la chaîne fait déjà, et qu'il ne faut pas refaire

12. **La v1 seule écrit les mesures ; la vf n'attache que son delta.** *« Les observables d'une mesure viennent de la v1 seule »*, et *« le jugement porté sur la version finale sert uniquement à écrire le retour final — il n'écrit jamais dans les mesures »* *(`01-` §11)*. `chaine.ts` l'applique déjà. **Ton `code2` ne doit pas le savoir.**
13. **`delta_v1_vf` compare les SQUELETTES, jamais les verdicts, et NULL n'est pas 0.** *« Ce que “comparer deux squelettes” veut dire dépend de la grille : la source ne le définit pas hors de la fiche, donc le branchement le porte. »* ⚠️ Si tu ne déclares pas `delta`, la chaîne le dit par une alerte et laisse NULL — c'est voulu. **Ne rends jamais 0 pour « rien changé »** : une passation en classe n'a pas de version finale.
14. **Un squelette par (dépôt × version × compétence), et l'index unique le garde.** Une reprise **met à jour** la ligne. ⚠️ `chaine.ts` porte déjà l'alerte du **rejeu sous un autre instrument** : ne la contourne pas.
15. **`instrument_version` est la ligne VERSION de ta fiche, et rien d'autre.** *« C'est elle qui bouge dès qu'un prompt bouge, puisqu'un prompt vit dans sa fiche. »* Elle vient du dérivé. **Ne la calcule pas, ne la compose pas.**
16. **Le modèle ne rend ni niveau, ni dimension, ni décompte.** Les systèmes sont écrits — *« Tu es un extracteur. Tu relèves, tu ne juges pas »*, *« Tu es un juge. Tu ne rends ni niveau, ni dimension, ni décompte »*. **Si ton portage a besoin que P2 rende un nombre, c'est un bug** : *« si un nombre sort du modèle, soit c'est un extrait que le code re-vérifie, soit c'est un bug »*.
17. **Les deux temps de code ne journalisent rien.** `code1` et `code2` ne sont pas des appels : aucune ligne à `api_couts`, aucune `phase`. *« La `phase` dit l'étage, pas le nombre d'appels. »*

### D — Le portage, et ce qu'il retire sans le dire

18. **C'est une EXTRACTION, jamais une régénération.** Le contrat §7 : *« le v1.6 d'Expression encode six erreurs réellement mesurées en juillet — les régénérer depuis la fiche les jetterait »*. **Le module encode ce que la fiche ne dit pas encore.** Lis la fiche pour comprendre, lis le module pour porter.
19. ⭐⭐ **PORTER RETIRE LES RÉPARATIONS QUE LE MODÈLE FAISAIT EN SILENCE, ET LE CONTRÔLE NE COÛTE RIEN.** La preuve, mesurée sur la Structure le 30/07 : son P2 ouvrait la route de la promesse sur **103 des 118 cellules**, et **aucune des cent cellules du banc ne l'a prise** — les deux modèles **réparaient le « et/ou » sans le dire**. *Un code qui applique le texte à la lettre ne réparera rien.* Le contrat §7 en fait une **règle du chantier** — *« contrôle gratuit à faire pendant l'extraction »* — et **ce prompt te demande de le faire** : liste ce que le modèle réparait — valeurs mal orthographiées reçues quand même, champs devinés —, et **vérifie que chaque cas devient une ALERTE, pas un trou**. ⚠️ *La fréquence observée d'un trou n'est pas son importance a priori.*
20. ⚠️ **`document_p2` est obligatoire dès que `code1` existe, et c'est le seul défaut dont RIEN ne témoigne.** Contrat §2 : *« Un module qui l'omet fait servir `null` à son juge, qui juge alors à vide — et c'est le seul défaut de ce contrat dont rien ne témoigne : les verdicts sortent, l'autotest est vert, la trace est propre. »* ⭐ **Sa valeur peut être `None`** — un relevé illisible n'a pas de document — ; **c'est l'ABSENCE de la clé qui est l'erreur.**
21. **`code2` rend exactement trois clés publiques.** `verdicts` — les clés sont **exactement** les observables déclarés —, `trace` — *« depuis que le code calcule juste, une erreur de jugement n'a plus de symptôme visible : la trace est ce qui la rend lisible »* — et `alertes`. **Une seule source de vérité : ce qui récapitule recopie la trace, il ne la recalcule jamais.** *Le canal privé `_audit` se marque d'un tiret bas ; sans la marque, une quatrième clé est une violation.*
22. **Une alerte, jamais une valeur par défaut.** *« Un rejet d'audit non apparié n'est jamais soustrait ; une valeur illisible rend une alerte, pas une valeur par défaut. »* **Et le parsage tolérant appartient à la chaîne** : `appel.ts` valide déjà la forme. Tes crochets reçoivent des objets propres — **ne remets pas de la réparation dedans.**

23. ⚠️⚠️ **L'ARRONDI N'EST PAS LE MÊME DANS LES DEUX LANGAGES, ET AUCUN VECTEUR NE LE VOIT.** `round()` de Python tranche les égalités **au pair**, `Math.round` **vers le haut** : une densité de **0,125** vaut **0,12** d'un côté et **0,13** de l'autre. *Les vecteurs tombent sur des valeurs rondes ; une copie réelle, non.* **`utils/chaine/arrondi.ts` est écrit — sers-t'en, ne le réimplémente pas.**
24. ⭐⭐ **UN VECTEUR GOLD NE PROUVE QUE CE QU'IL COUVRE — FAIS L'ÉPREUVE NÉGATIVE.** Les sept golds de l'Expression portent **tous** un écart de grades ≤ 1 : **inverser la règle du profil dissocié les passe tous les sept**, *vérifié en cassant le seuil exprès*. **Casse ton portage volontairement, règle par règle, et regarde si le contrôle tombe.** S'il ne tombe pas, ton contrôle ne contrôle rien. ⚠️ **Et toi, tu n'as **18 vecteurs, mais ton autotest les déclare lui-même CONSTRUITS : ils prouvent l'arithmétique, jamais la validité**** — l'épreuve négative t'est donc encore plus nécessaire qu'à elle.
25. **LE BALAYAGE, quand les vecteurs ne couvrent pas.** Appeler **la même fonction du même module** sur plus d'entrées **n'invente aucune règle** — c'est le seul moyen de couvrir ce qu'aucun gold ne renseigne. *L'Expression l'a fait, et c'est ce qui a trouvé ses trous.*
26. **LA FICHE NE DIT PAS TOUT, ET CE QU'ELLE TAIT NE S'INVENTE PAS.** L'Expression ne définit **nulle part** ce que *« comparer deux squelettes »* veut dire pour elle : **`delta` n'est donc pas déclaré**, la chaîne le dit par une alerte et laisse **NULL**. *N2 reste aveugle au signal de réceptivité tant que la fiche se tait.* **Regarde si la tienne le définit ; si non, ne l'invente pas — relève-le.**
27. **Les observables du §5 demandent parfois un CALCUL PROPRE, pas une lecture.** Sur les neuf de l'Expression, **trois ne se lisaient nulle part** : l'un est une **sous-partie** de ce que le module compte, l'autre une **somme de deux étiquettes**, et quatre se comptent **par étiquette et APRÈS AUDIT** — ce qui a demandé d'enrichir l'audit lui-même, le module ne comptant ses retraits que par famille. **Ne présume pas que ton relevé les porte déjà.**
28. **Déclare tes `slotsFournis` : `verifierCoherence()` te dira ce qui manque AVANT le premier appel payé.** Le contrôle tombe **au chargement**, dans les deux sens.

### E — Connaissance : ce qui lui est propre

**Ta chaîne, en un coup d'œil.** Crochets définis par ton module : `pre_p1`, `pre_p2`, `code1`, `code2`, `conformite`. Slots de tes prompts — **P1** `{consigne}` `{pre_releve}` `{production}` · **P2** `{consigne}` `{corpus_cours}` `{releve_phase_1}` `{restitution_de_cours}`.
    ⭐ **Ton module DÉCLARE `SLOT_DOCUMENT_P2 = "releve_phase_1"`** — obligatoire, ton P2 porte plus d'un slot.

29. ⭐⭐ **TU ES LA COMPÉTENCE LA PLUS EXIGEANTE EN SLOTS — SEPT, ET DEUX CROCHETS LES SERVENT.** Trois en P1 *(`{consigne}`, `{pre_releve}`, `{production}`)* et **quatre en P2** *(`{consigne}`, `{corpus_cours}`, `{releve_phase_1}`, `{restitution_de_cours}`)*. `pre_p1` sert les premiers, **`pre_p2` sert le corpus du cours, la consigne et le drapeau de restitution**. ⚠️ **Le contrat est net : `pre_p2` tourne AVANT P1, et ses slots se vérifient AU CHARGEMENT.** Si le contexte de la chaîne ne porte pas le corpus du cours, ton `pre_p2` doit **servir `None`** — c'est ainsi qu'un module dit *« le contexte ne porte pas ce qu'il me faut »* sans lever d'exception ni inventer une valeur.
30. ⭐ **TU DÉCLARES `SLOT_DOCUMENT_P2 = "releve_phase_1"`, ET C'EST OBLIGATOIRE CHEZ TOI.** *« Le slot du document se déclare dès que le prompt P2 en porte plus d'un »* — le tien en porte quatre. ⛔ **Ne le devine jamais par soustraction** : le contrat dit exactement pourquoi — *« jusqu'au jour où un `pre_p2` incomplet ferait passer le relevé entier dans le slot du référent, sans que rien ne le voie »*.
31. ⚠️ **`restitution_de_cours` est À LA FOIS un paramètre et un slot de P2.** Ne le calcule pas deux fois, et ne laisse pas le paramètre et le slot diverger : c'est le patron même d'un domicile double.
32. **Tes 18 vecteurs sont des cas CONSTRUITS, et ton autotest le dit lui-même.** Ils prouvent l'arithmétique de `code2`, **jamais** que la grille lit les élèves. **Ne les présente pas comme une preuve de validité** dans ta section au `SUIVI_tests_manuels.md` — ils prouvent que le portage est fidèle, ce qui est exactement ce qu'on te demande.
33. **Ton `conformite` porte le contrôle d'existence des citations, en ALERTE SEULE.** Tu es l'un des **deux** modules sur six à en avoir un *(l'autre est l'Expression, et il le porte dans `code1`)*. ⚠️ **Deux modules, deux crochets différents — c'est un `[à valider]` du contrat §8.** Porte le tien où il est, relève l'écart, **ne l'uniformise pas.**
34. **Ton référent est le corpus de cours, et tu es mono-mode `composer`.** *« La Connaissance mesure ce que l'élève mobilise de mémoire, non ce qu'il reconnaît chez un auteur, et le déclare »* — **elle n'a donc aucune grille réceptive, et ce n'est pas un manque.** Ne construis aucun chemin de réception.

## Le « fait quand » — recopié du `07-Implementation.md` §2

> *Fait quand* : `derive-instruments.py --resume` déclare la compétence **ouverte** et `--verifie` dit **IDENTIQUE** ; `verifierCoherence()` ne rend **aucun écart** ; **le branchement reproduit, sur les vecteurs embarqués du module — `TESTS_CODE1_PARFAIT` et `TESTS_P2_PARFAIT` —, exactement ce que `python3 code.py --autotest` produit, et sans aucun appel de modèle** ; **chaque observable du §5 de la fiche a sa valeur au relevé, ou son alerte nommée** ; un dépôt réel traverse la chaîne et écrit **un squelette, une mesure et sa lettre-équivalente** ; et `npm test` reste vert.

**Elle ne se négocie pas en séance.** Trois précisions d'exécution :

- **Les vecteurs sont la preuve centrale, et ils ne coûtent rien.** `python3 copies-tests/connaissance/code.py --autotest` les joue en une seconde, **sans aucun appel de modèle**. **Ton état : `TESTS_P2_PARFAIT` **18 vecteurs** · `TESTS_CODE1_PARFAIT` **4 vecteurs** · `VERSION_GOLDS_TESTEE = None`.** L'autotest rend aujourd'hui : **tout passe** — et il déclare lui-même : *« ces 18 vecteurs sont des cas CONSTRUITS, ils prouvent l'arithmétique, jamais la validité »*, plus *« la branche “aucun inverifiable” est redondante avec les deux portes sur tout le domaine des bornes — déclarée telle, gardée pour la trace »*. ⚠️ **Ne compte jamais une constante sans vérifier son TYPE** — chez l'Expression, `TESTS_CODE1_PARFAIT` est une **chaîne**, pas une liste, et un `len()` y a rendu « 52 vecteurs » qui étaient 52 caractères. **Rejoue-les côté TypeScript sur les mêmes entrées, et compare LES TROIS CLÉS** — pas seulement `verdicts`. *Une trace qui diverge dit qu'un chemin de calcul a changé, même quand le verdict tombe juste.*
- **Ce que ton dossier porte en plus, ou en moins** : **aucun run stocké, aucun gold**.
- **La dérivation ne se joue pas depuis un montage.** `MANIFESTE.ts` embarque `racine_conception` **en chemin absolu** : lancée d'ailleurs que la machine de Louis, elle fait dire **DIVERGE** à tout. *(C'est un angle mort déjà relevé au `PLAN_DE_CHANTIER.md` §6 — il appartient au lot de correctifs.)*
- **Le dépôt réel se joue derrière les interrupteurs, sans jamais poser `evaluee`.** Le patron existe : `scripts/recette/chaine-c4l5.mjs`, et son `--sans-appel` pour la partie qui ne dépense pas. **Un décor semé se retire** — le geste symétrique, comme `decor-c4l6.mjs --retire`.

## Les conventions — elles valent pour tout lot

**Deux conventions de dépôt, pour tout lot qui touche la base** : une **ligne au `SUIVI_SQL.md` avant exécution**, jamais après ; et les migrations sont **additives et gatées**. *Ce lot n'en attend aucune — piège 4.*

**Une convention pour tout lot qui s'appuie sur la doctrine en base** : elle est **dérivée, jamais tapée, et il n'y a qu'un dériveur**. *Ce lot ne lit pas la doctrine, mais la même règle vaut mot pour mot pour `derive-instruments.py` : **s'il dit DIVERGE, rejouer `--ecris` ; jamais corriger un dérivé à la main.***

⭐ **La convention d'ouverture d'une compétence — trois gestes, dans cet ordre** *(`PLAN_DE_CHANTIER.md` §5 ; `utils/chaine/LISEZ-MOI.md`)* :

1. `python3 scripts/derive-instruments.py --ecris` — le dérivé apparaît dans `utils/chaine/derive/competences/` ;
2. importer `INSTRUMENT_CONNAISSANCE` dans `instruments.ts` et le poser dans `INSTRUMENTS` ;
3. écrire son **branchement** dans `BRANCHEMENTS` — quel prompt est P1, lequel est P2, ce que Code1 prépare, ce que Code2 agrège, et le calcul du `delta_v1_vf`. **Cela ne se devine pas depuis les titres des blocs** : *« Le détail de chaque chaîne vit dans la fiche de sa compétence »* *(`01-` §11, qui renvoie au `03-` §1)*.

**Une convention de clôture, pour tout lot : sa section au `SUIVI_tests_manuels.md`** — ce qui a été prouvé en séance, **coché avec sa preuve**, et ce qui **reste à jouer en recette**, décoché, **avec sa condition de reprise en toutes lettres**. *C'est ainsi que les restes de C4-L2 à C4-L5 ont trouvé leur destinataire.*

**Une convention de dette : une source trouvée fausse se marque, elle ne se corrige pas** — **`[faux]`** au point de l'erreur, et **une ligne à la section DETTES** du `INVENTAIRE_Non_Tranches.md`, qui porte l'avant / après.

---

## Ce que ce lot laisse à celui d'après

Écris-le à la **boîte aux lettres du `PLAN_DE_CHANTIER.md` §5**, par destinataire — *« un relevé ne se transmet pas tout seul, et il ne DOIT pas »*.

- ⭐ **`C4-L10`, pour les compétences qui restent.** Ce que ton portage a appris : ce que le modèle réparait en silence, ce qui manquait à ta fiche pour porter, et **tout canal que tu as dû ajouter au socle**. C'est le dépôt le plus utile que tu feras.
- **`C4-L7` — la recette du flux.** ⚠️ **Sa boîte aux lettres est le `SUIVI_tests_manuels.md`**, pas le plan. ⭐ **Et depuis le 22/08, sa condition commune n'existe plus** : `C4-L10 · Expression` a relu et **requalifié une par une** les onze entrées qui disaient *« la première fiche versée et bancée »*. Elles portent désormais **des conditions distinctes, qui ne se lèveront pas ensemble** — la **deuxième compétence branchée** *(`C4L5-2`)*, une fiche qui **définit son delta** *(`C4L5-4`)*, une copie qui **porte une réussite au relevé** *(`C4L5-1bis`, `C4L4-4`)*, un **statut `evaluee` posé par le professeur** *(`C4L4-5`, `C4L3-15`, `C4L2-12`)*, une **fenêtre d'évidence remplie** *(`C4L2-11`)*. **Relis-les à ta clôture : ton lot vient peut-être d'en lever une.**
- **Le lot de correctifs**, pour ce que le piège 6 t'aura fait croiser.
