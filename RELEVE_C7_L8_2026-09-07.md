# RELEVÉ — C7-L8 « La chaîne à l'heure de la clé » (séance du 07/09/2026, branche `feat/ecrans-passation`)

**Prompt** : `PROMPT_Code_C7_L8.md` (31 pièges, fabriqué le 06/09 au soir ; lu dans le dépôt principal — il n'est pas dans le worktree). **Manifeste vérifié** : `01-` **v5.12** *(le manifeste dit 5.11 — la version avertit ; en-tête relu : « sous le gabarit, la bande indexe, le registre ordonne », rien ne touche la chaîne)* · `02-` v6.7 VALIDÉ ET GELÉ · `10-` v0.12 `[arrêté le 03/09/2026]` *(le manifeste dit 0.11 — §⑥, les deux textes recopiés au prompt, relus)* · `06-` v2.6 VALIDÉ ET GELÉ · `07-` v2.80 RELU ET VALIDÉ, **entrée `C7-L8` conforme au §I mot pour mot** *(mission, « fait quand », manifeste, « arbitrage à confirmer : `chaine_cle_actif` »)*. **Branche** : `feat/ecrans-passation`, tête `bcd1367` à l'arrivée, 24 commits d'avance sur `main`, non fusionnée, **pas sur `origin`** ; rien poussé, rien fusionné. ⚠️ Machine : `Sun Sep 6 15:54 EDT 2026` — le prompt et les commits de L7 datent la séance du 07/09 ; je garde leur datation.

**Une migration, la seule** : `c7_l8_chaine_cle.sql` — **jouée en bac à sable le 07/09** *(répétition à blanc sur le corps d'abord, puis le fichier ; `porte_posee = t`, `porte_a_off = t`, `params_intacts = 1` ; vérifiée par psql et PostgREST)*, **en attente en prod** *(le geste de Louis, avec le push)*. Ligne au `SUIVI_SQL.md` écrite AVANT. **Le lot est derrière `chaine_cle_actif`, à OFF des deux côtés** ; aucun autre interrupteur touché ; rien écrit en production.

---

## 0. Où en est le lot

| | |
|---|---|
| **Code** | `utils/chaine/cle.ts` *(la part pure : l'observable de la clé, la cible, les compétences froides, la restriction de la mesure, la tête du « se juger », le signal de recopie, le passage à corriger, le nom de l'observable)* · `utils/chaine/porte-cle.ts` *(le lecteur tolérant et la bascule)* · `utils/chaine/contexte.ts` *(`ctx.chaineCleActif`, `ctx.cle` — `exercices_cas.probleme` → `exercices_problemes` en une ligne —, `CasServiAuRetour.probleme` et `.passageMarque` ; et les trois hunks de `main` qui mettent `observable_code` dans `servable`)* · `utils/chaine/chaine.ts` *(la clé servie, la cible, `competencesFroides`, la restriction de la mesure, le signal, le retour et son rejeu bornés, l'élagage nourri)* · `utils/chaine/retour.ts` *(`observableIsole`, la borne en clair, `materiaux` / `passagesACorriger` jusqu'à l'élagage)* · `utils/chaine/citation-verifiee.ts` *(`jugerLAncrage` : la troisième confrontation, au matériau, sauf le passage)* · `utils/deroule/vue.ts` *(`construireSeJuger` : la compétence de la clé, l'observable en tête ; et le hunk de `main` — le code, pas le nom)* · `app/prof/scriptorium/PorteChaineCle.tsx`, `actions-chaine-cle.ts`, `SectionParametresScriptorium.tsx` *(la bascule)*. |
| **Tests** | `utils/chaine/cle.test.ts` (20) et `utils/chaine/retour-cle.test.ts` (9) — **porte fermée à l'octet** pour les quatre sorties du piège 2 ; suite entière **2393 → 2422**, 1 rouge hors lot dans les deux cas *(`verifie-import-gabarit.test.ts`, piège 29)* ; `tsc` propre ; eslint : un avertissement antérieur au lot. |
| **Éprouvé par exécution** | `scripts/recette/couture-c7l8.mjs` *(bac à sable ; `--constat` lecture seule, `--essai` écrit un décor marqué et le dit, `--retire` repose tout — joué trois fois, retiré trois fois, vérifié par requête)* · `scripts/recette/banc-citations-origine.mjs --prod` *(lecture seule, avec la colonne « dans le passage »)* · la requête de coût sur `api_couts` *(prod, lecture seule)* · la lecture à l'œil à 375 px. |
| **Le « fait quand »** | **(1)** sur le décor ✅ *(0 citation copie ET matériau hors passage sur les retours de A et D ; 1 citation élaguée comme recopiée sur D)* — **en prod : à jouer la première semaine après l'ouverture** (`C7L8-x1`) · **(2)** ✅ *(A : 1 compétence mesurée, 4 écartées avec motif, 1 squelette, 8 observables à `n/a`, 2 points tous d'argumentation)* · **(3)** ✅ *(C, cran 2 méso : 1 question, `argumentation|lien_explicite`, aucune autre compétence)* · **(4)** sur le décor ✅ *(9 → 4 appels, 0,095 → 0,0445 $)* — **en prod : à mesurer après l'ouverture** (`C7L8-x2`). Détail au `SUIVI_tests_manuels.md`, section `C7-L8`. |
| **Commit** | sur la branche, fichier par fichier, sans push. |

---

## 1. ⚠️ EN TÊTE DE SÉANCE — l'arbitrage que le prompt me faisait dire à Louis, et ce que le code fait en attendant

**La borne « CE CRAN ISOLE » du cran 2** *(séance des écrans, 06/09, `retour.ts`)* est née derrière `juge_documents_actif`. Le piège 3 propose qu'elle **rejoigne `chaine_cle_actif`** avec le filtre des squelettes — une seule porte pour « le retour ne parle que de l'observable de la clé », à tous les crans qui isolent ; le juge garde la sienne pour le verdict ; **pas les deux portes sur la même phrase**.

**Ce que le code fait** — la lecture la plus conservatrice, qui satisfait le piège 2 (« porte fermée, la chaîne d'hier à l'octet ») ET le piège 3 (« pas deux portes sur la même phrase ») :
- **porte `chaine_cle_actif` FERMÉE** : rien ne bouge — la borne du cran 2 reste sous `juge_documents_actif`, comme hier, à l'octet (test « porte FERMÉE » de `retour-cle.test.ts`) ;
- **porte OUVERTE, sur un exercice qui isole avec une clé** : la borne du lot *(« CET EXERCICE ISOLE UN SEUL OBSERVABLE — « Le lien » (argumentation), ce que l'élève entend comme « … » ; ton retour ne parle QUE de lui… »)* s'écrit à **tous** les crans qui isolent, et **au cran 2 elle REMPLACE celle de la séance des écrans** — le bloc des pièces reste servi, son paragraphe « ⛔ CE CRAN ISOLE » ne s'écrit plus (test « UNE borne, pas deux »).

**Ce que je n'ai pas fait, et qui est à Louis** : retirer la borne du cran 2 de `juge_documents_actif` *(la faire dépendre de `chaine_cle_actif` seule)*. Ce serait deux lignes dans `retour.ts` — mais porte fermée, le retour du cran 2 d'aujourd'hui en bac à sable (juge ON) perdrait sa borne, ce qui contredit « la chaîne d'hier à l'octet ». **Je propose de laisser tel quel** : la borne d'hier tient tant que le lot est fermé, celle du lot prend sa place dès qu'il s'ouvre. *(Condition de reprise `C7L8-x4`.)*

---

## 2. Ce que la fabrication a mesuré, et ce que j'ai remesuré

| | Le prompt (06/09) | Remesuré (07/09) |
|---|---|---|
| **Branche** | tête `bcd1367`, 24 d'avance, L7 joué | idem ; **⚠️ `main` porte 22 commits que la branche n'a pas** — la « fusion de minuit » n'a pas eu lieu (§3). |
| **Bac à sable** | `gabarit_actif` ON, `juge_documents_actif` ON, `chaine_actif` ON ; 328 exercices 1.5 `a_concevoir` ; verdicts aux crans 2 et 5 | idem ; `scriptorium_params` **32 colonnes, aucune `chaine_cle_actif`** à l'arrivée (33 après la migration) ; 51 dépôts, **1 seul `verdicts_cran`** (v1) ; 518 cas avec `probleme` ; 206 problèmes dérivés, 19 sans observable routé ; 0 dépôt avec décision. |
| **Production** | `gabarit_actif` OFF, `juge_documents_actif` OFF, `chaine_actif` ON ; 0 exercice 1.5 | idem ; 32 colonnes, aucune `chaine_cle_actif` ; **0 cas avec `probleme`** (la bascule n'est pas jouée) ; 205 problèmes dérivés, 19 sans observable ; 565 dépôts, 480 avec décision, 0 verdict. **Rien écrit, aucun interrupteur touché.** |
| **Le banc `--prod`** | 54 retours, 122 points, 27 « copie ET matériau » | **62 retours, 141 points, 18 sans citation ; 86 copie seule ; 33 copie ET matériau — 21 HORS du passage à corriger, 12 DANS ; 2 matériau seul ; 2 nulle part.** Hors passage par cran : 4 · 10 · 6 · 1 aux crans 4·5·7·9. |
| **Le coût, prod** | 103 dépôts, 634 appels, 6,16/dépôt, 0,091 $/dépôt, 2,23 compétences | **118 dépôts, 710 appels (6,02), 10 689 jetons et 0,0881 $ par dépôt ; p1 281 · p2 265 · retour 130 · juge 34 ; 2,20 compétences mesurées (1 : 56 · 3 : 43 · 4 : 18).** Colonne `cout`, jamais `cout_usd` ; `tokens_entree` / `tokens_sortie`. |
| **`derive-doctrine.py --verifie`** | — | **17/17 IDENTIQUE** en bac à sable avant toute lecture de la doctrine. |
| **`retours_texte_segmente_chk`** | n'exige plus l'ancrage (31/08) | vérifié par `pg_get_constraintdef` sur les DEUX bases : `retour_segmente_bien_forme(texte, true) and retour_segmente_bien_forme(texte_edite_par_prof, false)` — un point élagué passe (D : 2 points, 1 sans citation, écrit). |
| **La borne du cran 2** (piège 3) | vit derrière `juge_documents_actif` | confirmé (`retour.ts`, bloc des pièces) — §1. |
| **Le cran 5 en texte à trou** (piège 6) | `texte_v1` = le passage seul | confirmé sur le décor : 145 car. contre 503 ; `reassemble` porté (0 / 405 car.). |

---

## 3. ⚠️ Ce qui n'était pas comme le prompt le disait

1. **La « fusion de minuit » de `main` dans la branche n'avait pas eu lieu** *(pièges 19 et « capture-page.mjs … y arrive avec la fusion »)* : `main` porte 22 commits absents de la branche, dont `banc-citations-origine.mjs`, `capture-page.mjs`, et le changement de `contexte.ts` / `vue.ts` qui fait apparier le « se juger » **par code**. Sans lui, la pièce (3) n'a rien à apparier *(la banque de questions est par `competence|observable_code`, la couverture portait des NOMS)*. **Ce que j'ai fait, sans fusionner** : les trois hunks de `main` (commit `bdcae6e`) recopiés **à l'identique** dans `contexte.ts` et `vue.ts` *(commentés comme tels)*, et les deux scripts rapatriés par `git checkout main -- …`. ⚠️ **À vérifier à la fusion** : elle doit passer sans conflit (mêmes lignes, même contenu) — sinon garder la version de `main` et rejouer `cle.test.ts` / `retour-cle.test.ts`.
2. **La ligne `IDEES` « restreindre P2 à l'observable de la clé » (06/09) n'existait sur aucune branche** : écrite le 07/09, datée, avec ce que (a) coûte par rapport à (b) (§5).
3. **Un dépôt par (élève × exercice)** — `uk_depots_eleve_exercice` : la couture a dû poser son dépôt « porte ouverte » sur le cran 5 d'une **seconde** clé du même objet (`argument.garant.connecteur`, observable `garant_present`), le premier sur `argument.garant.absent` (`lien_explicite`).
4. **`next dev` (Turbopack) refuse le worktree** : « Symlink node_modules … points out of the filesystem root ». `.claude/launch.json` gagne une entrée `palimpseste-webpack` (`next dev --webpack --port 3200`) — c'est elle qui a servi la lecture à 375 px.
5. **Porte fermée, sur la branche, le « se juger » du cran 2 est VIDE** *(0 question sur `ex-gab-argument-garant-circulaire-c2`)* : la couche type des crans de production ne porte rien dans `servable`, et l'offre vide est sautée par `seJugerAServir`. Sur `main` le 06/09, le même cran servait 3 questions dont 2 de Structure. **Porte ouverte : 1 question, `argumentation|lien_explicite`** — la clé fournit l'observable que la couche type n'a pas.

---

## 4. Les arbitrages de la séance — à confirmer par Louis

| # | L'arbitrage | Ce que le code fait | Où |
|---|---|---|---|
| **S1** | **La borne du cran 2** (piège 3) | reste sous `juge_documents_actif` porte fermée ; remplacée par celle du lot porte ouverte — §1. | `retour.ts` |
| **S2** | **RR4 surveille TOUS les codes de la compétence servie**, pas le seul code de la clé. Le piège 12 disait « `codesObservables` réduit à ce code » — mais cette liste n'est PAS envoyée à Calame, elle ne sert qu'à REFUSER un code nommé dans le texte : la réduire relâcherait RR4 sur les autres codes. | la liste entière ; la restriction de ce que Calame REÇOIT passe par les squelettes (un seul) et la borne en clair. | `chaine.ts`, `engendrerLeRetour` |
| **S3** | **Le squelette de jugement (P2) part ENTIER** : le piège 12 supposait « son jugement ne porte qu'un observable (les autres à `n/a`) » — c'est vrai de `competences_mesures.observables`, pas de `artefact_jugement`, dont la forme fait foi à la fiche et ne se filtre pas génériquement. | P1 et P2 entiers, la borne en clair dit à Calame de n'en garder qu'un ; ce que ça coûte est au §5. | `chaine.ts` |
| **S4** | **Le lecteur de clé lit `exercices_problemes` en UNE ligne**, pas `chargerDoctrineDepuisBase` (dix-sept tables à chaque dépôt). Même domicile — la table dérivée que `doctrine.problemes[cle]` lit. | `cleDuCas`, tolérant : `ok` · `sans_observable` · `absente_de_la_doctrine` · `illisible` (⇒ mesure large, alerte). | `contexte.ts` |
| **S5** | **Le passage à corriger = les segments MARQUÉS** (`marquerLeMateriau`, « le passage qui porte le problème », bornes de phrase), calculé pour TOUT cas qui a un diff, à tous les crans — le même domicile que l'écran et le trou du cran 5 ; à défaut `passageFautif`. | `CasServiAuRetour.passageMarque` ; `passagesACorriger` ; le banc lit le même champ. | `contexte.ts`, `cle.ts` |
| **S6** | **Le signal de recopie est journalisé à CHAQUE v1 des crans 3·5·7** porte ouverte (ratio + phrases retrouvées), sans seuil — « le seuil se lit sur corpus ». Il entre dans `alertes`, donc dans `dernier_message` du job. | `signalDeRecopie` ; ⚠️ un dépôt du gabarit au cran 5 le porte aussi (0,37 ×, 0 phrase) : un bruit à borner le jour où Louis fixe la barre. | `cle.ts`, `chaine.ts` |
| **S7** | **La cible avec la clé = la compétence de l'observable, sans « indéterminée »** ; décision divergente ⇒ alerte, la clé l'emporte. La règle de `competencesVisees` (`vue.ts`) est **recopiée, pas son code** — un seul domicile serait mieux : à remonter dans `chaine.ts` et faire importer par `vue.ts` (proposition, pas fait). | `cibleAvecLaCle` | `cle.ts` |
| **S8** | **La compétence de la clé écartée en amont** (non branchée, `differee`, mode non couvert) ⇒ **rien à mesurer**, aucun squelette, aucun retour, et l'alerte le dit — la clause granulaire s'applique à une compétence au lieu de six. Non rencontré en base. | `competencesFroidesDe` | `cle.ts` |
| **S9** | **`sondesMontee` reste** (c'est la cible, M-e) ; les sondes secondaires sont écartées AVEC LE MOTIF « sonde de la décision NON mesurée … » et une alerte compte-les. Non rencontré : aucun dépôt avec décision en bac à sable. | idem | `cle.ts` |

---

## 5. Le piège 11 — ce que `code2` rend sur un seul observable, MESURÉ

Sur le dépôt A *(cran 5, `argument.garant.connecteur`, observable `garant_present`)* : `competences_mesures.observables = { garant_present: 0, les huit autres: "n/a" }`, `lettre_equivalente = C` *(et `E` au premier essai, sur une copie que le juge avait dite ratée)*. **La lettre vient de `branchement.lettre(agrege)` sur TOUT le jugement de P2** — la forme (a) mesure un observable et laisse P2 juger les huit autres, dont la lettre garde la trace. Ce que le moteur fait ensuite de quatre mesures à un observable *(`jugerLaLettre`, la fenêtre d'évidence, l'escalade)* : **non mesuré** — une seule mesure de décor, et le moteur n'écrit pas de première lettre sans ancre (`competences_niveaux` reposé tel quel). **Question à Louis** : la `lettre_equivalente` d'une mesure « juste l'observable » doit-elle rester celle de P2 entier, ou passer à `null` tant que (b) n'existe pas ? *(Condition de reprise `C7L8-x3` ; la forme (b) à `IDEES`, 07/09.)*

**Ce que (a) coûte encore par rapport à (b)** : un P2 qui juge des observables qu'on jette — des jetons de sortie, pas un appel *(A : 7 899 jetons pour 4 appels ; la sortie de P2 est la part compressible)*.

---

## 6. Les nombres du banc et du coût

- **Banc prod, avant l'ouverture** (§2) : **21 citations sur 141 seraient élaguées** par la pièce (4) *(copie ET matériau hors passage)*, **12 gardées** par l'exception du passage. Les citations courtes (« bien posé », 2 mots) : 2 des 12 gardées le sont par le passage, aucune des 21 hors passage n'a moins de 4 mots *(vérifié à l'œil sur la liste)*.
- **Coût prod, avant** : 0,0881 $ et 6,02 appels par dépôt, 2,20 compétences. **Sur le décor** : 9 → 4 appels *(B porte fermée : p1 3 · p2 4 · retour 1 · juge 1 ; A porte ouverte : 1 · 1 · 1 · 1)*, 0,095 → 0,0445 $, 14 128 → 7 899 jetons. À 2,20 compétences par dépôt, l'économie attendue en prod est de ~1,2 chaîne froide, ~2,5 appels sur 6 — **un constat, jamais une règle** (`01-` §11).
- **Après l'ouverture, en prod** : `C7L8-x1` (le banc) et `C7L8-x2` (le coût), la première semaine.

---

## 7. Ce que ce relevé porte au `07-` §2 (entrée `C7-L8`) — à écrire dans la source par Louis ou par la prochaine fabrication

1. **Pièce (2)** : « sur un exercice qui isole, la chaîne mesure **la cible et les sondes de la décision**, pas les six compétences, et se concentre sur l'observable de la clé : […] les autres observables de la compétence **restent au journal sans retour** » → **« sur un exercice qui isole avec une clé, la chaîne ne mesure QUE L'OBSERVABLE ISOLÉ — ni toutes les compétences, ni la compétence entière, ni les sondes de la décision ; seuls les crans 6 et 8 exercent, et s'y mesure ce qui s'y mesure aujourd'hui »** *(Louis, 06/09 au soir)*. Le « fait quand » 2 perd « et les sondes de la décision ».
2. **`chaine_cle_actif`** passe de « ⚠️ arbitrage à confirmer » à **décidé** *(Louis, 06/09 au soir)* ; migration `c7_l8_chaine_cle.sql`.
3. **Manifeste** : `01-` v5.12 *(non 5.11)*, `10-` v0.12 *(non 0.11)*.
4. **La borne du cran 2** : §1, si Louis tranche.

---

## 8. Questions à Louis

- **Q1 (§1)** — la borne du cran 2 : laisser sous `juge_documents_actif` porte fermée et remplacée porte ouverte (ce que le code fait), ou la sortir de `juge_documents_actif` ?
- **Q2 (§5)** — `lettre_equivalente` d'une mesure « juste l'observable » : celle de P2 entier, ou `null` ?
- **Q3** — la prose de Calame peut désigner une citation élaguée (« comme ici », vu sur D à 375 px) : le point se lit, mais son « ici » ne montre plus rien. Un mot de plus dans la règle 1 *(« ne renvoie pas à une citation que tu n'as pas donnée »)* ou l'accepter ? *(Piège 24 : la prose ne s'élague pas, et un refus de plus rejoue des retours.)*
- **Q4 (S6)** — le signal de recopie journalisé sur chaque v1 des crans 3·5·7 : quelle barre, une fois lu sur corpus ?
- **Q5 (S7)** — remonter la règle de `competencesVisees` dans `chaine.ts` et la faire importer par `vue.ts` ?

---

## 9. Ce qui est pour d'autres lots — déposé dans leur boîte au `PLAN_DE_CHANTIER.md` §5

- **C7-L7** : les sondes secondaires posées sur un exercice qui isole ne sont plus mesurées (journalisées, motif servi) ; le lecteur de clé de la chaîne (`ctx.cle`, `observableDeLaCle`).
- **C9-L3** : le motif 1 « cible INDÉTERMINÉE » est fermé pour le gabarit, porte ouverte ; porte fermée il reste (vu sur B).
- **La fabrique** : rien de plus que l'item de L7 (le rouge de `verifie-import-gabarit.test.ts`, piège 29).

## 10. Hors du lot, nommément, et non touché

P1/P2 et les fiches · le juge du cran *(lu, servi `piece` / `reassemble` comme hier)* · le routeur et la décision · les écrans *(le cran 7 n'a pas d'écran à trou : le signal de D — 1,18 ×, 4 phrases sur 5 — est la mesure que Louis demandait avant de décider)* · les retours non écrits · toute vague, tout import · toute migration hors celle de l'interrupteur · `delta_v1_vf` · le tirage semé.
