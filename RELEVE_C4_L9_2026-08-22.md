# RELEVÉ — Session Code C4-L9 · La conception des examens diagnostiques *(22/08/2026)*

> Ce relevé porte **ce que la séance a construit**, **ce qu'elle a tranché et pourquoi**,
> **ce qu'elle a amendé au `07-`**, **ce qu'elle a mesuré**, et **les questions qu'elle laisse
> ouvertes à Louis**. Le journal SQL porte les migrations ; `SUIVI_tests_manuels.md` porte ce qui
> est prouvé et ce qui reste à jouer en recette.

---

## 1. Le contrôle d'entrée — en deux temps, et les deux ont passé

**Les six pièces du manifeste, aux versions attendues :**

| Pièce | Attendue par le prompt | Constatée le 22/08 | |
|---|---|---|---|
| `07-Implementation.md` §1 | VERSION 2.28 · RELU ET VALIDÉ · §1 ouvert | **2.28** · RELU ET VALIDÉ | ✅ *(passée à **2.29** par l'amendement de cette séance, §4)* |
| `01-routeur.md` §9 et §10 | VERSION 5.5 · relu et validé | **5.5** · VALIDÉ ET GELÉ | ✅ |
| `02-exercices.md` §1.3, §4, §6 A, §6 B, §6.D | VERSION 5.4 · relu et validé | **5.4** · VALIDÉ ET GELÉ | ✅ |
| `05-GENERATEUR_Reference_Decomposee.md` §4 | VERSION 2.0 · relu et validé | **2.0** · VALIDÉ ET GELÉ | ✅ |
| `06-Palimpseste.md` §1 | VERSION 2.6 · déposé | **2.6** · VALIDÉ ET GELÉ | ✅ |
| `SUIVI_SQL.md` | déposé | présent, journal actif | ✅ |

**La clause granulaire n'avait ici aucun objet** : ce manifeste ne compte aucune fiche de
compétence, et ce lot n'ouvre aucune compétence — *il ne mesure rien, il conçoit ce qui sera
mesuré*.

**Les trois dépendances, vérifiées PAR REQUÊTE et non par confiance :**

- **C4-L1 joué** — `exercices_types` porte ses **15 lignes** *(13 objets + 2 de nature `complet`)*,
  les deux types diagnostiques y sont et sont **entièrement vides** *(`grain` null, `crans_admis`
  `{}`, `genres_admis` null, `libelle` null, `mode_saisie` null)*. `exercices.exercice_planifie_id`
  existe, **nullable**, `on delete restrict`, vers `scriptorium_exercices_planifies(id)`.
  `idx_exercices_planifie` existe et **n'est PAS unique**. `exercices_cran_chk` est en place.
  **Aucune colonne `cible_primaire`** — le report tient.
- **C4-L8 joué** — `app/prof/conception/` *(10 fichiers)*, `utils/fabrique/` *(14 modules)*,
  `exercices_sujets` / `exercices_textes` / `exercices_references` en base, avec du corpus dedans.
- **C4-L4 joué** — `utils/passation/` *(18 modules)*, `components/passation/EcranEleve` et
  `EcranProf`, et **les quatre routes**.
- **Le plan d'évaluation** *(qui n'est pas un lot)* — `scriptorium_exercices_planifies` avec sa
  typologie verrouillée, son cycle `a_concevoir → concu → annule`, `uk_exercices_diagnostic` et
  `exercices_diag_fenetre_chk`. **Un plan validé courant existe** *(classe **Test**, 7 élèves)*.
- ⚠️ **`plan_evaluation_actif` est déjà à `true`** — c'est l'interrupteur du professeur, il ne
  m'appartient pas, et je ne l'ai pas touché. Les six autres sont restés à **OFF**.

---

## 2. Ce que la séance a construit

**Une migration**, `c4_l9_examens_diagnostiques.sql` *(+ son rollback, écrit AVANT exécution et
laissé sur le disque)*, **quatre gestes** :

1. **le renommage des deux codes de type** — `diagnostic_essai` → `examen_diagnostique_essai`,
   `diagnostic_explication_texte` → `examen_diagnostique_explication_texte` ;
2. **ce que la mission nomme** sur ces deux lignes : `genres_admis`, `exclusions_parcours = '{}'`
   *(écrit explicitement)*, plus `mode_saisie = 'manuscrit'` et `libelle` ;
3. **`uk_exercices_planifie`** — l'index unique partiel : *une ligne de plan ⇔ au plus une instance* ;
4. **le mur** — `exercices_cran_chk` remplacée par le trigger `trg_exercices_cran_selon_le_type`.

**Du code**, et il tient en un principe : **un seul chargeur, un seul composant, deux entrées.**

| Où | Quoi |
|---|---|
| `utils/examens/types.ts` | **pur** — les deux codes, la typologie du plan, et **l'arrêté du `01-` §10 recopié** |
| `utils/examens/consigne.ts` | **pur** — ce que l'élève lit, et la normalisation CRLF |
| `utils/examens/plan.ts` | ce que le professeur a à concevoir **dans son module**, et la lecture inverse |
| `utils/examens/conception.ts` | le chargeur commun, **le geste**, et le chemin inverse |
| `utils/examens/signal.ts` | le signal de l'élève **au lancement**, derrière les deux portes |
| `components/examens/` | l'écran de conception, l'encart prof, la carte élève |
| `app/prof/examens-diagnostiques/actions.ts` | le geste serveur — **un dossier sans page, donc sans route** |
| `app/prof/{codex,aletheia}/examen-diagnostique/[planifieId]/` | **les deux entrées**, minces |

**Cinq greffes ailleurs, toutes petites** : l'encart sur `/prof/codex` et `/prof/aletheia`, le signal
sur les deux pages de module de l'élève, le **deep-link du calendrier** *(piège 25 — le troisième
site)*, et **les trois fonctions de libellé** *(piège 31)*.

---

## 3. Ce que la séance a TRANCHÉ, et pourquoi

### 3.1 ⭐ Le mur : un TRIGGER, pas un discriminant sur la ligne

Le prompt ouvrait deux voies *(« soit un trigger qui joint le type, soit un discriminant porté par
la ligne »)* et rendait la forme physique à la session Code *(`07-` §1)*.

**Retenu : le trigger.** Le motif est celui que le §1 répète d'un bout à l'autre — *« deux copies
d'un même chiffre finissent par diverger »*. `nature` vit sur `exercices_types`, et
`exercices.type_id` est `not null` : recopier `complet` sur chaque instance fabriquerait **un second
domicile pour une valeur dérivable**. `paire_diagnostic` n'est pas un contre-exemple — elle n'est
dérivable de rien, c'est un **choix de conception**, et c'est pour cela qu'elle a dû vivre sur la
ligne.

⭐ **Et le trigger est strictement plus fort** que ce qu'un discriminant aurait acheté : il tient
**les deux sens**. Le second — *« une instance d'un type `complet` ne porte JAMAIS de cran »* —
n'était **exprimable par aucun CHECK**, et **rien ne l'interdisait jusqu'ici**.

`security invoker` *(le défaut, et le bon)*, `search_path` épinglé, `errcode = '23514'` pour que les
appelants voient toujours une violation de contrainte.

### 3.2 L'unicité sans découpe par statut

`uk_exercices_quiz` et `uk_exercices_codex_session` portent `where … and supprime_at is null`.
**`exercices` n'a ni soft-delete ni statut `retire`** — ses quatre statuts sont `a_concevoir → concu
→ assigne → clos` : **il n'y a aucun tombstone à exclure**. Et exclure `clos` **rendrait vide la
lecture inverse** dès l'examen terminé — or le « fait quand » exige que *« les deux se retrouvent
l'une l'autre »*, **par une seule colonne et deux lectures**. Donc : **aucune découpe**.

### 3.3 La consigne est PRÉ-COMPOSÉE et ÉDITABLE, jamais imposée

Aucune source n'arrête le texte d'une consigne d'examen diagnostique, et `consigne_gabarit` est NULL
sur les deux types. Plutôt que d'inventer une formulation pédagogique, l'écran **compose un point de
départ** *(l'intitulé du type, puis le matériau)* et **le professeur l'arrête** — *« c'est le texte
qu'il arrête que l'élève lit »* *(`02-` §6 B.1, point 7)*.

⭐ **Et côté Aletheia, le texte entier entre dans la consigne**, ce qui n'allait pas de soi. Le flux
de C4-L4 sert à l'élève `consigne_instanciee` **et rien d'autre** : un texte laissé au seul
`materiau_source_texte_id` **ne s'afficherait nulle part**, et l'élève aurait à expliquer un texte
qu'il ne voit pas. *« Le flux de C4-L4 prend l'instance sans rien changer »* : c'est donc à
l'instance de porter ce qu'il faut lire. **1 270 caractères servis, vérifiés à la recette.**

### 3.4 Le module de l'élève se lit à la LIGNE DE PLAN, pas au mode

*« L'élève entre par SON module. »* La règle du `01-` §2 — *« Codex s'il porte `composer`, Aletheia
sinon »* — **enverrait l'explication de texte dans Codex**, puisqu'elle mesure l'Expression **en
`composer`** *(`01-` §10)*, quand le `06-` §1 la range en **lecture diagnostique, dans Aletheia**.
Cette règle est écrite pour les **formatifs**, dont l'atelier suit le mode ; *« les sommatifs se
conçoivent chacun dans son module »*, et **leur module est au plan**. D'où l'ordre : **la ligne de
plan d'abord**, la règle du mode **en repli** pour une passation de classe hors plan.

### 3.5 Les refusés sont MONTRÉS, avec leur motif

Un texte sans référence validée **n'est pas retiré de la liste** : il s'affiche dans un repli, avec
le motif qui **nomme la référence**. Le cacher laisserait le professeur chercher pourquoi son texte
a disparu. ⚠️ Et **le geste refuse aussi** — *un écran n'est pas une garde*.

### 3.6 Le chemin inverse existe, sans bouton

`retirerExamenDiagnostique()` ramène la ligne de plan à `a_concevoir` **avant** de supprimer
l'instance *(tant que le lien pointe encore)*, ne ressuscite jamais un `annule`, et refuse une
instance déjà `assigne`. ⚠️ **Elle n'est exposée à aucun bouton** : la mission compte quatre choses,
et un geste « retirer » n'en est pas une. Elle sert le retour arrière du geste de conception et la
recette. **Le jour où un écran de retrait naîtra, il l'appellera.**

---

## 4. Ce que la séance a AMENDÉ au `07-` — §1, ouvert à l'implémentation

**VERSION 2.28 → 2.29.** Deux paragraphes ajoutés au §1.1, tous deux marqués *« Amendé par C4-L9 »* :

1. **« L'exercice planifié dont elle vient est UNIQUE, et le lien n'a qu'un domicile »** — la clé
   vit sur l'instance, elle est nullable et **unique quand elle est renseignée** ; l'insertion **est
   le claim** ; **aucune découpe par statut**, et pourquoi ; *une seule colonne, deux lectures*.
2. **« La garde du `cran` ne peut pas être un `CHECK`, et voici pourquoi »** — les deux sens de la
   règle, l'impossibilité d'un CHECK inter-tables, le contournement à ne jamais faire *(inventer un
   cran)*, et le choix du trigger contre le discriminant recopié.

*Rien d'autre n'a été touché : ni le §2, ni le §3, ni le §4, ni le §6.*

---

## 5. LES QUESTIONS OUVERTES — ce que la séance n'a PAS tranché

> Elles sont **notées et non tranchées**, comme demandé. Chacune est aussi au
> `SUIVI_tests_manuels.md`, décochée.

### 5.1 ⚠️⚠️ « Se juger » ne se sert PAS sur un examen diagnostique — et deux sources se tendent

L'écran lève bien le drapeau, la **confiance de remise est servie** — mais `offreSeJuger` (C4-L4)
exige un **geste `produire`** et un **grain `meso`/`macro`** *(`02-` §5)*, quand un examen
diagnostique **n'a ni cran ni grain** : son type est `complet`, et
`types_complet_sans_objet_ni_cran_chk` le lui **interdit**.

**La tension est entre les sources, pas dans le code** :

| Ce que dit… | …et ce que ça implique |
|---|---|
| `02-` §5 | « se juger » n'est servi qu'en geste **`produire`**, au grain **`meso`/`macro`** |
| `02-` §6.D, étape 9 | « si le professeur a levé le premier drapeau, l'élève **se juge** » — **dans le flux de la passation en classe**, où vit l'examen |
| `01-` §10 | un examen diagnostique est **« macro par construction »** |
| `07-` §1 | un type `complet` **n'a ni grain ni cran** — la garde l'interdit |

**Conséquence, si rien n'est tranché** : les deux examens de la semaine 1 produisent **la confiance
de remise** mais **aucun signal de « se juger »** — et *« une année de collecte manquée ne se
rattrape pas »*.

⚠️ **Non contourné** : `utils/passation/` est resté intact *(le diff n'y porte que des
commentaires)*, et **aucun cran n'a été inventé** pour débloquer le chemin. **À trancher.**

### 5.2 L'assignation reste hors du module — le chemin traverse deux modules

Ce lot **conçoit** *(instance `concu`, ligne de plan `concu`)* et **n'assigne pas** : la mission
compte quatre choses, et l'assignation n'en est pas — elle est nommée à **C4-L8** *(« la création
d'exercices communs à toute une classe »)*. Le professeur passe donc par
`/prof/conception/{id}` — **hors de son module** — pour que les dépôts naissent, avant de revenir à
`/prof/{module}/passation/{id}` pour ouvrir.

**L'écran le dit et l'y mène** *(deux liens sur l'écran de succès)*. Mais *« le professeur voit ce
qu'il a à concevoir DANS SON MODULE »*, et ce détour n'y est pas. **À trancher : l'assignation
doit-elle rejoindre l'écran du module ?**

### 5.3 Deux dérivations du dépôt divergent — et aucune n'est de ce lot

Jouées **avant et après** le geste de ce lot, sans changement :

- **`derive-doctrine.py --verifie`** — **les onze tables sont IDENTIQUES**, mais
  `SOURCES : DIVERGE — 06-Palimpseste.md` et `FIXTURE : DIVERGE`. Le `06-` a bougé le 22/08 ; ce
  qu'il alimente *(`demonstrations_formes`, 3 lignes)* est **inchangé**. Ce n'est pas l'angle mort
  « chemin absolu » que le prompt annonce : le chemin est le bon, **c'est bien une empreinte de
  source qui a bougé**.
- **`derive-instruments.py --verifie`** — DIVERGE sur `MANIFESTE.ts` et `calame-retour.ts`, parce
  que le `07-` est passé de **2.25 à 2.28**. ✓ **Vérifié à l'octet** *(dérivation dans un répertoire
  jetable, puis diff)* : **le gabarit Calame est identique** ; seuls `empreinte_source` et
  `version_source` diffèrent. Le §4 est gelé et n'a pas bougé. **C'est le seul échec de `npm test`,
  et il précède cette séance** *(déjà relevé à C4-L3)*.

⚠️ **Ni l'un ni l'autre n'a été « réparé »** : re-jouer `--sql` ou `--ecris` réécrirait les tables et
les fichiers dérivés d'**autres lots**, sur une divergence que ce lot n'a pas causée. **La
re-dérivation appartient à qui a fait bouger la source.** *(Et ma version 2.29 l'éloigne encore d'un
cran : c'est une raison de plus de la jouer, pas de la jouer ici.)*

### 5.4 Le libellé du calendrier lit avec deux tirets

Le prompt arrête le libellé cible : **« Examen diagnostique — écriture (classe) »**, en minuscules
pour `plan-cadence.ts`. Or `calendrier-a-faire.ts` compose `« Exercice à concevoir — {libellé} »` :
la tâche lit donc **« Exercice à concevoir — examen diagnostique — écriture »**. **Fidèle au prompt,
et un peu lourd à lire.** Signalé, non modifié.

---

## 6. Les dettes constatées — pour le lot de correctifs, PAS pour cette séance

- **`/prof/conception/{id}` rend « cran NaN »** sur une instance d'examen diagnostique, et son bloc
  **Édition** refuse *(il lit `exercices_cas`, qu'un examen n'a pas — son appui ne dépend que du
  cran)*. Le bloc **Assignation** fonctionne, et c'est par lui que passe la suite. **Cosmétique,
  écran de C4-L8.**
- **`idx_exercices_planifie` est devenu redondant** *(même clé, même prédicat, non unique)*. Laissé
  en place : il appartient à C4-L1, les migrations de ce lot sont additives, et aucun piège n'en
  demandait le retrait.
- **La garde « référence validée » vit à deux endroits** — `app/prof/conception/actions.ts:130` et
  `utils/examens/conception.ts`, même colonne, même prédicat. L'extraire toucherait un fichier de
  C4-L8.
- **`c4_l1_seed.sql` porte une vérification qui devient fausse sur cette base** — son
  `trois_objets_terminaux` comptera **cinq** après le geste 2. ⛔ **Non réparée**, comme le piège 14
  l'exige : c'est un `select` de fin de fichier, pas une contrainte, **et il reste vrai à son propre
  moment** *(sur une base neuve, le seed passe avant la migration de ce lot)*. Le contrôle
  équivalent est écrit **à ma migration**.

---

## 7. CE QUE LA SÉANCE A MESURÉ — le « fait quand », ligne à ligne

| Le « fait quand » exige | Prouvé par | ✓ |
|---|---|---|
| une ligne de plan *à concevoir* s'affiche **dans son module**, avec sa date | recette B — et **elle ne s'affiche pas dans l'autre** | ✅ |
| le professeur la conçoit — **sujet** dans Codex, **texte** dans Aletheia | recette D | ✅ |
| **sans qu'aucune date lui soit demandée** | recette D — `fenetre_debut` et `fenetre_fin` **nulles** ; aucun champ de date à l'écran | ✅ |
| un texte **sans référence validée est refusé avec son motif** | recette C — à l'affichage **et au geste**, et **rien ne reste derrière le refus** | ✅ |
| l'instance porte **`lieu = classe`** | recette D | ✅ |
| la ligne de plan passe **conçue** | recette D — avec son `concu_at` | ✅ |
| **les deux se retrouvent l'une l'autre** | recette E — **dans les deux sens**, et **aucune colonne neuve** sur la ligne de plan | ✅ |
| la **`forme`** vaut **`sommatif`** | recette F — par `utils/chaine/contexte.ts`, **sur l'instance réelle** | ✅ |
| **le flux de C4-L4 prend l'instance sans rien changer** | recette H — et `git diff` sur `utils/passation/` + `components/passation/` : **commentaires seuls** | ✅ |
| au lancement, **l'élève voit son signal** et entre par son module | recette I — sur un dépôt **réellement ouvert** par `ouvrirLesDepots()` | ✅ *(par requête)* / ⏳ *(à l'écran)* |
| **les deux examens de la semaine 1 sont montés, un par module** | recette J — et `uk_exercices_diagnostic` **servi comme preuve** | ✅ |

**Les chiffres** : migration **15 drapeaux + 4 épreuves du mur**, tous à `t` ; recette **114
vérifications, 0 échec** ; **18 tests purs neufs** *(`npm test` : 981 / 980 — l'unique échec est
celui du §5.3)* ; `tsc --noEmit` et `eslint` : **rien**.

⏳ **Ce qui n'a PAS été vu à l'écran, et pourquoi** : la séance n'avait **pas de compte
professeur**, et **je ne saisis pas de mot de passe** — le smoke élève n'a donc pas été joué non
plus. **Le décor est laissé en place exprès** pour que les deux smokes soient jouables tels quels
*(C4L9-13 et C4L9-14)*. Ce qui a pu être vérifié sans authentification l'a été : **les six routes
compilent et rendent `307 → /login` sans erreur**.

---

## 8. Le périmètre — ce que ce lot n'a PAS fait, délibérément

- **Aucune table créée** — le schéma est joué *(C4-L1)*, la fabrique aussi *(C4-L8)*, la passation
  aussi *(C4-L4)*.
- **Aucune colonne sur `scriptorium_exercices_planifies`**, et **aucun renommage** dedans :
  `diagnostique`, `fenetre_diagnostique` et `origine = 'diagnostic'` **restent** *(≈ 50 sites, table
  vivante, aucune ambiguïté — renoncement chiffré et assumé)*.
- **Aucun `paire_diagnostic`**, **aucun `genre`**, **aucun `cran`**, **aucun `bonus`**, **aucune
  `cible_primaire`** sur l'instance.
- **Aucune policy élève ouverte**, aucune fonction `security definer`, **aucun appel de modèle**.
- **Aucun interrupteur créé ni détourné** ; les six re-constatés à **OFF** après la migration, après
  la recette et après le smoke.
- **Ni les onglets** *(C4-L6, C5-L4)*, **ni le dépôt du corpus** *(C4-L8)*, **ni le plan
  d'évaluation** lui-même.
- **Les six fiches de compétence n'ont pas été lues** : ce lot ne touche à aucun instrument.
