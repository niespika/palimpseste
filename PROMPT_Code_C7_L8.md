# PROMPT — Session Code : C7-L8 — « La chaîne à l'heure de la clé »

> **À coller dans une session Claude Code fraîche.** Une session, un lot.
>
> Ce prompt est fabriqué selon la recette du `PLAN_DE_CHANTIER.md` §5, le **2026-09-06** au soir, à partir des quatre décisions de Louis du 06/09 après-midi, prises sur un signalement d'élèves — *« le retour cite le texte original et non ma réponse »* — et sur trois mesures. Ce que le lot construit, son manifeste et son « fait quand » sont recopiés ci-dessous **avec leurs versions au moment de l'écriture**. ⚠️ **Lis d'abord la section qui suit : elle dit où vit le code, ce que la fabrication a mesuré, et l'arbitrage que tu fais confirmer avant d'écrire une ligne.**
>
> ⭐⭐ **TON LOT NE CHANGE PAS CE QUE LA CHAÎNE SAIT FAIRE : IL CHANGE CE QU'ELLE REGARDE.** Mesuré le 06/09 dans le code de la branche : `competencesFroides = mesurees` en v1 *(`chaine.ts`)* — **toutes les compétences que l'instance déclare** passent P1 et P2, isole ou non ; le retour reçoit **les squelettes de toutes** ; le « se juger » élit ses questions **par fragilité**, sans regarder la clé ; et l'élagage des citations ne confronte la citation **qu'à la copie** — une phrase du devoir que l'élève a recopiée y est, donc elle passe. Sous le gabarit, un exercice isole **un observable, celui de sa clé**, et la copie est **ce que l'élève a écrit dans le trou**. Les quatre pièces du lot font suivre la chaîne.
>
> ⛔⛔ **DÉCISION DE LOUIS, 06/09 AU SOIR : LE LOT EST DERRIÈRE SON PROPRE INTERRUPTEUR, `chaine_cle_actif`, À OFF.** Le `07-` §2 l'écrivait « à confirmer » ; c'est confirmé — *« le lot change le retour de tous les dépôts, un lot lit le sien »*. Un interrupteur est une colonne de `scriptorium_params`, donc **une migration additive**, la seule de ce lot *(§⑤)*.

---

## ⚠️⚠️ CE QUE LA FABRICATION A MESURÉ, ET CE QUE TU VÉRIFIES AVANT TOUT

| | Le fait | Ce que tu en fais |
|---|---|---|
| **①** | ✅ **L'entrée `C7-L8` du `07-Implementation.md` §2 EST ÉCRITE le 06/09 après-midi** *(chapitre `### C7`, après `C7-L7` ; le `07-` passe en **2.80**, `derive-instruments --ecris` rejoué — version et empreinte seules)*. Le §I en garde la copie. Le dépôt de conception est à **`/Users/louissagnieres/Documents/GitTest/palimpseste-conception/`** — on n'y commite que sur demande. | ⛔ **Ton contrôle d'entrée commence là** : ouvre le `07-` §2, chapitre `C7`, et vérifie que l'entrée porte **mission, « fait quand », manifeste et l'arbitrage `chaine_cle_actif`** tels qu'au §I. Un écart se signale, il ne se corrige pas. |
| **②** | ⛔⛔ **DÉCISION DE LOUIS, 06/09 AU SOIR : LES LOTS C7 SE JOUENT SUR LA BRANCHE `feat/ecrans-passation`, PAS SUR `main`** — worktree **`../palimpseste-passation`**. **Remesuré le 07/09 au soir : tête `bcd1367`, 24 commits d'avance sur `main`, et `C7-L7` Y EST JOUÉ** *(`RELEVE_C7_L7_2026-09-06.md`, section `C7-L7` au `SUIVI_tests_manuels.md`)*. C'est là que vivent le cran 2 en texte à trou, **le cran 5 en texte à trou** *(commit `0b1a42e` : le juge reçoit le devoir réassemblé)*, la couverture qui suit le cran *(`a938efb`)*, `C7-L6` et `C7-L7`. **Louis pousse la branche le soir du 07/09.** | **Ouvre le worktree et vérifie que tu y es** : `git branch --show-current` ⇒ `feat/ecrans-passation` ; `git status -sb` dit si la branche est sur `origin` et si `main` l'a absorbée. **Tu commites sur elle, tu ne pousses rien, tu ne fusionnes rien** — si la branche est déjà fusionnée dans `main` à ton arrivée, **demande à Louis où travailler** avant d'écrire. ⚠️ `git status --short` avant chaque commit, **tes fichiers seulement** — tu touches `utils/chaine/*`, `utils/deroule/juger.ts`, `utils/deroule/vue.ts`, `scripts/recette/*`. L7 a écrit `utils/moteur/*`, `utils/registre/*` et `InstanceDuVivier.observable` / `alternatives_ecartees.objet.observable`, **dont tu n'as pas besoin** *(piège 9)*. |
| **③** | ✅ **L'allumage, mesuré PAR REQUÊTE le 06/09 au soir.** **Bac à sable** : `gabarit_actif` **ON**, `juge_documents_actif` **ON** *(allumé le 06/09 en fin d'après-midi ; des verdicts existent aux crans 2 et 5, posés par les smokes)*, `chaine_actif` ON ; 328 exercices 1.5 tous `a_concevoir`, 0 dépôt du routeur. **Production** : `gabarit_actif` **OFF**, `juge_documents_actif` **OFF**, `chaine_actif` ON ; **0 exercice 1.5** — la bascule de dimanche n'est pas jouée à l'heure de l'écriture. `scriptorium_params` porte **32 colonnes**, dont `juge_documents_actif` et `gabarit_actif` ; **aucune `chaine_cle_actif`**. | **Remesure les deux bases.** ⛔ Tu ne bascules aucun interrupteur, tu n'écris rien en production. ⚠️ Sans le juge, aucun verdict de cran ; **la borne « CE CRAN ISOLE » du retour au cran 2 est déjà derrière `juge_documents_actif`** *(séance des écrans, 06/09)* — piège 3. |
| **④** | ✅ **Le banc REJOUÉ le 06/09 au soir, en production, lecture seule** *(`scripts/recette/banc-citations-origine.mjs --prod`)* : **54 retours chauds depuis le 31/08, 122 points, 13 sans citation ; 78 « copie seule », 27 « copie ET matériau », 2 « matériau seul », 2 « nulle part »** — au cran 5 : 14 recopiées sur 43 points ; au 4 : 7 sur 29 ; au 7 : 5 sur 27. 6 dépôts sans matériau en contexte. *(Le `07-` dit 27 sur 118 : même compte, les 4 points de cran inconnu en plus.)* **Le coût, même jour, sur les 103 dépôts du routeur qui ont des appels** : **634 appels — 6,16 par dépôt ; 10 858 jetons et 0,091 $ par dépôt** ; par phase p1 251 · p2 234 · retour 115 · juge 34 *(`phase` NULL, module `exercices-chaine-juge`)* ; **2,23 compétences mesurées par dépôt** *(1 : 48 dépôts · 3 : 37 · 4 : 17)*. | **C'est ta ligne de base pour le « fait quand » 4** — et la colonne s'appelle **`api_couts.cout`**, pas `cout_usd` *(mesuré : la seconde n'existe pas)*. Rejoue le banc et la requête de coût **avant** ton lot, **après** en bac à sable sur des dépôts de décor, et **après l'ouverture** en prod. |
| **⑤** | ✅ **`chaine_cle_actif` — DÉCIDÉ PAR LOUIS le 06/09 au soir.** Une colonne `scriptorium_params.chaine_cle_actif boolean not null default false` — **une migration additive**, `c7_l8_chaine_cle.sql` + rollback, **une ligne au `SUIVI_SQL.md` AVANT exécution**, bac à sable d'abord, prod sur le geste de Louis. Ce qu'il protège : les pièces **(1)** et **(4)** changent le retour **de tous les dépôts, banque 1.4 comprise** ; les pièces **(2)** et **(3)** ne mordent que sur un exercice **qui isole avec une clé** *(`exercices_cas.probleme` non nul)*, donc sous le gabarit seulement — **mais les quatre lisent la même porte**. | Le patron est **`c7_l1_juge_documents.sql`** + **`utils/juge/porte.ts`** *(lecteur tolérant : colonne absente ⇒ OFF)* + `PorteJugeDocuments.tsx` dans Paramètres de Scriptorium ; **porte fermée, la chaîne d'hier à l'octet**, et un test le prouve *(patron `retour-juge.test.ts:40`)*. ⛔ **Rien ne s'emprunte à `chaine_actif`** *(la coupure de facture)*, ni à `gabarit_actif`, ni à `juge_documents_actif` : un lot lit le sien. ⚠️ **Une pièce reste à trancher, et c'est toi qui proposes** : la borne « CE CRAN ISOLE » du cran 2, née derrière `juge_documents_actif` — piège 3. |
| **⑥** | ⚠️ **Le manifeste épingle le `10-` en v0.11 ; le fichier est en v0.12.** La 0.12 touche **§2 bis.1** *(« Le plan — le trou est un ordre »)* et **§6** *(le cran 5 : « et le devoir RÉASSEMBLÉ »)* — deux de tes sections. | **La version avertit.** Les deux textes sont recopiés ci-dessous tels qu'en v0.12 ; relis l'en-tête, note l'écart au relevé, continue. |
| **⑦** | ⭐ **Ta boîte aux lettres est VIDE — le lot est né le 06/09.** Aucun `RELEVE_C7_L1` ni `RELEVE_C7_L6` : leur relevé est leur entrée au `07-` §2 et leurs commits. | **Tout ce qui t'était adressé est ici, aux pièges.** |

---

## Le manifeste — *(recopié du `07-` §2, entrée `C7-L8` ; versions du 06/09)*

> *Manifeste* : `01-` v5.11 §4 · `02-` v6.7 §2.2, §5 · `10-` v0.11 §2 bis.1, §6 · `06-` §4. Dépend de **C7-L1**, **C7-L6**, C4-L5.

**Quatre pièces, plus « ce document ».**

| Pièce | Où | Statut requis | Au moment de l'écriture *(06/09)* |
|---|---|---|---|
| `01-routeur.md`, **§4** *(couche 3, l'amendement du 06/09 : « un exercice isole UN observable, celui de sa clé, et choisir la clé, c'est choisir l'observable » ; aux crans 6 et 8 l'exercice exerce)* | `/Users/louissagnieres/Documents/GitTest/palimpseste-conception/` | **déposé** *(aucun statut explicite au manifeste — le minimum)* | **VERSION 5.12** *(07/09, commit `cd3e019` — « sous le gabarit, la bande indexe, le registre ordonne » : le cran d'en dessous qu'une échelle exige se sert même sous la bande ; PB2 lit l'observable de la clé, la méthode en est l'exception ; R2 sert tout le trio — **rien de ceci ne touche la chaîne**)* · VALIDÉ ET GELÉ · ⚠️ **le manifeste du `07-` dit v5.11 : la version avertit** |
| `02-exercices.md`, **§2.2** *(l'échelle des crans ; « `couverture_observables` vaut `isole` au cran 2 : la pièce écrite n'engage que les observables rattachés à son constituant, et le retour ne parle que d'eux »)*, **§5** *(la crédence et « se juger » — « n'est servi qu'en geste `produire`, au méso ou au macro, compétence cible `evaluee` » ; l'élève DÉSIGNE aux 4·7·9 ; les quatre épreuves)* | même dépôt | **déposé** | **VERSION 6.7** · VALIDÉ ET GELÉ |
| `10-Gabarit.md`, **§2 bis.1** *(le cran 2 isole ; « le prompt du retour au cran 2 ne mesure et ne commente que cet observable — jamais la compétence entière » ; la clé « pièce absente » en `probleme`)*, **§6** *(ce que le juge reçoit, cran par cran ; au 5, le devoir réassemblé)* | même dépôt | **déposé** | **VERSION 0.12** — `[arrêté le 03/09/2026]` ; le chapitre `C7` du `07-` le tient pour relu et validé · ⚠️ **§⑥** |
| `06-Palimpseste.md`, **§4** *(la transcription : « une littéralité absolue » — c'est sur elle que se mesure une copie photographiée)* | même dépôt | **déposé** | **VERSION 2.6** · VALIDÉ ET GELÉ |
| `07-Implementation.md`, **§2** *(chapitre `C7` — `C7-L1`, `C7-L6`, `C7-L8`)*, **§4** *(Calame — la règle 1 : « cite ses mots, au caractère près » ; « une citation que le code ne retrouve pas telle quelle dans la copie est RETIRÉE »)*, **§1.2** *(la mesure)*, **§5** *(l'allumage — « un lot lit LE SIEN »)* | même dépôt | aucun | **VERSION 2.80** · RELU ET VALIDÉ |

### ⚠️ Ce que le manifeste ne nomme pas, et pourquoi tu le lis quand même

- **`01-` §1, principe 4** *(« mesurer large, retourner étroit » ; « une compétence sondée n'est jamais une cible de cet exercice »)*, **§8.8** *(M-e : la sonde de montée ne compte nulle part)*, **§11** *(« il y a un squelette par dépôt × version × compétence MESURÉE » ; « les observables d'une mesure viennent de la v1 seule » ; le coût 2N + 4 « ne pilote rien »)*, **§12** *(RR1-RR4)*. **La pièce (2) est une lecture de ces quatre sections.**
- **`10-` §2 bis.6** *(le cran 5 en texte à trou, validé le 06/09 : « la copie de l'élève EST le passage réécrit »)* et **§5** *(le marquage : « le passage qui porte le problème », les bornes de phrase)*. **La pièce (1) et « le passage à corriger » de la pièce (4) s'y lisent.**
- ⛔ **Le `09-Objets.md` N'EST PAS à ton manifeste** : l'observable d'une clé se lit dans **la doctrine dérivée** — `exercices_problemes` *(`observable_code`, `observable_competence`)*, `doctrine.problemes[cle]` *(`utils/fabrique/doctrine.ts`)*.

⛔ **`00-`, `03-`, `04-`, `05-`, `08-` et les fiches de `competences/` ne sont PAS à ton manifeste** : tu n'ouvres aucune compétence, tu ne touches à **aucun instrument** *(les prompts P1/P2 vivent dans les fiches, dérivés par `derive-instruments.py` — C4-L10 —, et **la décision du 31/08 tient : P1 et P2 ne changent pas**)*, tu n'importes rien, tu ne touches ni au routeur ni à un écran.

---

## Le contrôle d'entrée — en deux temps, et les deux ne font pas la même chose

> **Le statut bloque.** Un fichier du manifeste dont l'en-tête porte un statut inférieur à celui exigé **arrête le lot**, explicitement. Le barème est au `07-Implementation.md` §2 : *déposé* → *relu et validé* → *versé et bancé*, cumulatifs ; « VALIDÉ ET GELÉ » vaut *relu et validé*.
>
> **La version avertit.** Si la ligne VERSION d'un fichier du manifeste diffère de celle inscrite ici, **relis son en-tête avant de continuer** — une version qui bouge dit qu'un texte a changé, pas qu'il est devenu faux.
>
> **Le blocage est granulaire quand il peut l'être.** Une fiche de compétence absente ou non bancée bloque **cette compétence**, pas le lot.

Concrètement : vérifie que les cinq pièces **existent**, que le `01-` porte **VERSION 5.12** *(le manifeste dit 5.11 — avertit, ne bloque pas)*, le `02-` **6.7**, le `10-` **0.12** *(§⑥)*, le `06-` **2.6**, le `07-` **2.80** — et **fais les gestes du §①, du §② et du §⑤**. La clause granulaire joue à sa façon : **une compétence non branchée à la chaîne est écartée par `competencesDeLExercice`** *(`etatCompetence(c).ouverte`)*, et tu ne changes pas cela.

### Tes dépendances sont jouées — vérifie-les par leur chemin, pas par leur ligne au plan

- **C4-L5** — la chaîne de mesure : `utils/chaine/chaine.ts` *(`traiterDepot` ; `competencesDeLExercice` ; `cibleDuRetour` — décision → `cible_primaire` → alphabétique ; `sondesDeLExercice` ; `competencesFroides` ; le retour, `engendrerLeRetour` / `rejouerLeRetour`)*, `contexte.ts` *(`lireContexte` — `production()` : `texte_v1` sinon `transcription_v1` ; `decision` : `cibleRetenue`, `sondes`, `sondesMontee` ; `casPourLeRetour` : matériau, `reponseAttendue`, `versionCorrigee`, `defaut`, `passageFautif`, `zone`, `choix`, `piece`, `reassemble`)*, `retour.ts` *(l'assemblage du prompt de Calame, `controlerRR3`, `jugerLAncrage`, `elaguerLesAncrages`, `fuitesRR4`)*, `citation-verifiee.ts` *(`citationTient`)*, `citation-approchee.ts`, `anti-injection.ts` *(`citationsIntrouvables`)*, `fidelite-p1.ts`, `mesures.ts`, `file.ts`.
- **C7-L1** — le juge du cran : `utils/chaine/juge-cran.ts` *(`JUGE_AUX_CRANS = {2, 4, 5, 7, 9}` sur la branche ; `assemblerLeJuge`, `questionDuCran` — au 2 : « tu ne mesures et ne commentes QUE les observables de ce constituant »)*, `juge-cran-serveur.ts` *(`jugerLeCran`, en parallèle des chaînes, attendu avant le retour ; ⛔ un cran 2 sans pièces n'est jamais jugé)*, `utils/juge/porte.ts`, `exercices_depots.verdicts_cran`, `utils/registre/reussites.ts`.
- **C7-L6** — le routeur par devoir *(branche)* : ce que tu en lis, c'est **la décision** — `exercices_depots.routeur_decision_id` → `routeur_decisions(cible_retenue, sondes_retenues)`, que `contexte.ts` lit déjà. *Tu ne lis ni le devoir ni la quarantaine.*
- **La séance des écrans, 06/09** *(branche)* : `utils/gabarit/pieces.ts` *(`separerLeTrou`, `morceauxDuPassage`, `assemblerLObjet`)*, `utils/gabarit/lecture.ts` *(`lireLeCran2`)*, `utils/deroule/vue.ts` *(`competencesVisees` — cible retenue, sinon `cible_primaire`, sinon `observable_isole_competence` ; `construireSeJuger`)*, `utils/deroule/juger.ts` *(`candidates` — la fragilité, puis le tirage semé)*, `utils/passation/metacognition.ts` *(`offreSeJuger`)*.

**Si l'une manque, arrête-toi et signale-le.**

---

## La mission — ce que le lot construit, et rien d'autre

**Quatre pièces, décidées par Louis le 06/09 :**

1. **La production envoyée à la chaîne est ce que l'élève a écrit** — au cran 2 la pièce *(le juge du cran garde l'objet assemblé)*, aux crans 5 et 7 le passage réécrit, jamais le devoir recopié ; l'écran fait du passage en gras **le champ**, dans le fil du devoir, comme le trou du cran 2 *(`10-` v0.12 §2 bis.6, validé)* ; et **la chaîne signale une v1 qui reproduit le devoir**.
2. **Sur un exercice qui isole, la chaîne mesure la cible et les sondes de la décision, pas les six compétences**, et **se concentre sur l'observable de la clé** : le retour ne parle que de lui *(`02-` §2.2)* ; les autres observables de la compétence restent au journal sans retour. ⛔⛔ **REPRISE PAR LOUIS, 06/09 AU SOIR, ET C'EST ELLE QUI VAUT : sur un exercice qui isole, la chaîne ne mesure QUE L'OBSERVABLE ISOLÉ — ni toutes les compétences, ni la compétence entière, juste l'observable. Seuls les crans 6 et 8 exercent, et s'y mesure ce qui s'y mesure aujourd'hui.** *(La phrase du `07-` « les autres observables restent au journal » est donc dépassée ; le relevé le portera au `07-`.)* Comment, sans toucher à un instrument : piège 11.
3. **Le « se juger » pose ses questions sur l'observable de la clé d'abord.** ⛔ **Précisé par Louis, 06/09 au soir : il porte UNIQUEMENT sur la compétence liée à l'observable servi** — jamais une autre compétence, même testée par l'objet ; **aux crans 6 et 8**, il peut porter sur plusieurs compétences *(les cibles)*.
4. **Une citation présente dans le matériau ET dans la copie s'écarte** comme les citations introuvables, **sauf sur le passage à corriger** *(la zone du diff)*.

**Hors du lot, nommément** : les prompts P1/P2 et les instruments *(les fiches, C4-L10)* · le juge du cran lui-même *(C7-L1 : tu le lis, tu ne le réécris pas)* · le routeur et la décision *(C7-L7)* · les écrans *(le cran 5 en texte à trou est codé sur la branche ; le cran 7 n'a pas d'écran à trou, et ce n'est pas toi qui le décides)* · les retours non écrits *(`C9`)* · toute vague, tout import · toute migration **hors** celle de l'interrupteur.

---

## Les pièges — tirés des sources et du code, chacun avec son renvoi

### A. La porte, et ce qui mord derrière elle

1. ⛔⛔ **UN LOT LIT LE SIEN — `chaine_cle_actif`, décidé** *(§⑤)*. Lecteur **séparé et tolérant** *(`utils/juge/porte.ts` : `.select('chaine_cle_actif').eq('id', 1).maybeSingle()`, `error` ⇒ OFF)*, lu **une fois** dans `lireContexte` et porté par `ContexteDepot` *(comme `jugeDocumentsActif`)* ; **jamais** dans une boucle par compétence. La bascule dans Paramètres de Scriptorium, patron `PorteJugeDocuments.tsx`. ⛔ Pas `chaine_actif` *(la coupure de facture)*, pas `routeur_actif`.
2. ⭐⭐ **PORTE FERMÉE, LA CHAÎNE D'HIER À L'OCTET — et un test le prouve.** `retour-juge.test.ts:40` fige le message de Calame à porte fermée ; fais de même pour **la liste des compétences froides**, **les squelettes servis au retour**, **l'ordre des candidates du « se juger »** et **l'élagage** : porte fermée, quatre sorties identiques à celles d'aujourd'hui, sur les mêmes entrées.
3. ⚠️ **La borne du cran 2 existe déjà, et elle vit derrière `juge_documents_actif`** — *« la borne »* est le paragraphe ajouté au prompt de Calame par la séance des écrans, qui lui **dit** de ne parler que de la pièce ; elle ne **retire** rien de ce qu'il reçoit — *(séance des écrans, 06/09 : `retour.ts` l. ~528, « ⛔ CE CRAN ISOLE. Ton retour ne parle QUE de ce que sa pièce engage »)* — et **elle ne suffit pas** : mesuré le 06/09 en bac à sable, *« le retour franchit encore la borne au cran 2 malgré le paragraphe : un point Structure (“pas d'introduction”) sur une pièce seule — Calame reçoit toujours les squelettes P2 de TOUTES les compétences »*. **Ta pièce (2) est la réparation de ce constat, généralisée à tous les crans qui isolent** : la borne par le prompt reste, et **Calame ne reçoit plus que l'observable de la clé** *(pièges 11 et 12)* — quand on ne lui donne pas la Structure, il n'en parle pas. ⚠️ **Arbitrage de fabrication, à dire à Louis en tête de séance** : la borne en clair du cran 2 **rejoint `chaine_cle_actif`** avec le filtre des squelettes qui la complète — une seule porte pour « le retour ne parle que de l'observable de la clé », à tous les crans qui isolent ; le juge garde la sienne pour le verdict. Pas les deux portes sur la même phrase.
4. ⭐ **Ce qui distingue « un exercice qui isole avec une clé » d'un autre, c'est `exercices_cas.probleme`** *(non nul ⇒ gabarit 1.5)*, jamais le cran ni la porte : un cran 5 de la banque 1.4 isole sans clé, et **les pièces (2) et (3) n'y ont rien à lire** — la chaîne le sert comme hier. `casPourLeRetour` sélectionne `ordre, reponse_attendue, defaut, exercices_materiaux(...)` : **ajoute `probleme` à ce `select`**, et résous l'observable par `doctrine.problemes[cle]` *(`observableCode`, `observableCompetence`)*. ⚠️ **19 clés sur 205 n'ont AUCUN observable** *(`observable_route = false`, `observable_code` nul — ex. `argument.garant.trop_large`, `argument.preuve.affirmation`)*. **Mesuré par la séance L7 le 07/09 : aucune des 328 instances 1.5 du bac à sable n'en porte** — rien ne casse aujourd'hui, mais une vague peut en verser. Une telle instance **isole sans observable** : la chaîne n'a rien à mesurer « sur la cible » — **elle mesure la compétence de `observable_isole_competence` comme hier, le retour n'est pas borné, et l'alerte le dit** *(« clé sans observable routé »)*. **Ne plante pas, n'invente pas un observable.** Côté routeur, L7 classe cette instance après les autres *(`sans_observable`)*.
5. ⚠️ **La compétence de la clé EST `exercices.observable_isole_competence`** *(304 sur 304 en bac à sable)*, et `couvertureDeLInstance` la met seule en `isole`, les autres en `observable_seul`. **Ne dérive pas une seconde fois ce que la couverture dit déjà** ; la clé t'apporte **le code de l'observable**, c'est tout ce qui manque.

### B. La pièce (1) — la copie est ce que l'élève a écrit

6. ⭐⭐ **SUR LA BRANCHE, L'ÉCRAN FAIT DÉJÀ LA MOITIÉ DE (1).** Au cran 2 et au cran 5 du gabarit, le champ est **le trou** *(`ChampDeRedaction forme='trou'`, le même `<textarea>`)* : `texte_v1` porte **la pièce**, ou **le passage réécrit**, et rien d'autre ; le juge reçoit l'objet assemblé *(`piece`)* ou le devoir réassemblé *(`reassemble`)*. **Mesure-le** : un dépôt de décor au cran 5 en bac à sable, `texte_v1` = le passage seul. **P1 et P2 reçoivent donc déjà `production()` = ce que l'élève a écrit** — la pièce (1) n'a rien à changer à la copie, **sous le gabarit, aux crans 2 et 5**.
7. ⚠️ **Le cran 7 n'a pas d'écran à trou** *(`10-` §2 bis.6 : le 5 seul ; « les crans 3 et 4(a) gardent l'écran d'hier »)*, et la banque 1.4 n'en a à aucun cran : **c'est là que la v1 reproduit le devoir** — mesuré en prod, v1 ≈ 1,01 × la longueur du matériau au 5, 0,93 au 7. **Le signal** : sur un cran de transformation *(3·5·7)*, compare la production au matériau du cas — longueur, et **les phrases du matériau retrouvées telles quelles** *(`citationTient(production, phrase)`, pas un second tokeniseur)* — et **alerte** dans le bilan du job *(`alertes`, `exercices_jobs.dernier_message`, ce que `attention-serveur.ts` lit)*. ⛔ **Un signal, jamais un refus, jamais un strike** : « écarter, jamais refuser » vaut ici aussi, et le canal de signalement d'intégrité *(`07-` §1.2)* n'est pas fait pour un élève qui a recopié son texte de départ. Le seuil se lit sur corpus, pas d'avance : journalise le ratio, et laisse Louis fixer la barre.
8. ⛔ **`production()` = `texte_v1`, sinon `transcription_v1`** *(`contexte.ts`)*. Les dépôts d'avant le 31/08 sont **des photos** : `texte_v1` vide, la copie vit dans la transcription *(mémoire du 31/08 : une mesure qui ne copie pas `production()` rend « 0 sur 135 », un zéro faux)*. **Toute mesure de ce lot copie `production()`** — le banc le fait déjà par `lireContexteDuDepot`.
9. ⚠️ **Tu ne lis pas la décision de C7-L7.** L7 journalise `alternatives_ecartees.objet.observable` ; **ton domicile de l'observable est la clé du cas** *(piège 4)*, présente sur tout exercice 1.5, décision ou pas *(la voie du professeur, les décors)*. Si les deux existent et divergent, **alerte**, ne tranche pas.

### C. La pièce (2) — la cible et les sondes, et l'observable de la clé

10. ⭐⭐ **`competencesFroides` — c'est LA ligne.** `chaine.ts` : `competencesFroides = version === 'v1' ? mesurees : (cible ? [cible] : [])`. Sur un exercice qui isole avec une clé, porte ouverte : **v1 = `{la compétence de l'observable de la clé}`**, et cette compétence est aussi la cible — `cibleDuRetour(ctx, mesurees)` *(décision → `cible_primaire` → alphabétique)* doit la rendre ; **si la décision dit une autre cible, alerte, et la clé l'emporte** *(c'est l'observable servi qui est mesuré)*. ⛔⛔ **Les sondes de la décision — DÉCIDÉ PAR LOUIS, 06/09 au soir : NON.** Sur un exercice qui isole, **aucune sonde secondaire n'est mesurée** — *« à l'exception des crans 6 et 8, ça n'a plus aucun sens »* : `ctx.decision.sondes` **n'entre pas** dans `competencesFroides` *(⛔ `sondesMontee` n'est pas une sonde secondaire : c'est la cible elle-même, marquée M-e — elle reste)*. **Aux crans 6 et 8**, les sondes se mesurent comme aujourd'hui — et il n'y a aucun exercice 6·8 dans la banque 1.5. ⚠️ La phrase du `07-` « la cible et les sondes de la décision » et le « fait quand » 2 sont donc **dépassés d'un mot** : la mesure ne porte que **la cible** — le relevé le porte au `07-`. ⚠️ Le routeur continue de **poser** des sondes secondaires sur ces exercices *(phase C, `poserLesSondes`)* : elles sont journalisées et **non mesurées** ; dis-le dans la boîte de `C7-L7`, qui pourra cesser d'en poser sur un exercice qui isole. **Sans décision** *(voie du professeur, décor)* : la cible seule — et sur le gabarit `cible_primaire` est nulle, donc **la compétence de l'observable isolé** : c'est le patron `competencesVisees` de `vue.ts` *(05/09)*, **recopie sa règle, pas son code** — un seul domicile serait mieux : propose de la remonter dans `chaine.ts` et de la faire importer par `vue.ts`. ⚠️ **`mesurees` reste calculé et journalisé** : les compétences écartées par ce lot se disent *(`ecartees.push({ competence, motif: 'hors cible et sondes de la décision (C7-L8)' })`)*, comme celles que la porte de mode écarte — **un motif servi, jamais un silence**.
11. ⛔⛔ **« JUSTE L'OBSERVABLE » — COMMENT, SANS TOUCHER À UN INSTRUMENT.** P2 est **un appel par compétence**, dont le prompt vient de la fiche *(`derive-instruments.py`, C4-L10)* et porte la liste des observables du §5 ; **aucun slot ne le restreint à un observable** *(`FOURNISSEURS_NATIFS` : sujet · copie · consigne · mode · reference · source)*. Deux formes, et **la fabrication retient la première pour ce lot** : *(a)* **un seul P1 et un seul P2, sur la compétence de la clé, et la mesure NE GARDE que l'observable de la clé** — dans `competences_mesures.observables`, les autres codes passent à **`n/a`** *(« `n/a` n'est jamais 0 » : ils sortent du dénominateur de la fenêtre d'évidence, de l'escalade et de la lettre — c'est exactement « ne pas mesurer »)* ; *(b)* **restreindre P2 à l'observable** — c'est un prompt neuf, donc un instrument neuf, donc les fiches : **hors de ton lot** ; ✅ **Louis a tranché le 06/09 au soir : (a) pour ce lot, (b) « à terme »** — la ligne est à `IDEES_post_rentree.md` *(2026-09-06, « restreindre P2 à l'observable de la clé »)* ; dis au relevé ce que (a) coûte encore par rapport à (b) *(un P2 qui juge des observables qu'on jette)*. ⚠️ **Une conséquence à MESURER, pas à supposer** : `code2` agrège les observables en une **lettre-équivalente** par mesure ; avec un seul observable présent, vérifie ce qu'il rend *(une lettre, ou `null` — les six branchements ne réagissent pas pareil aux `n/a`)*, et ce que `jugerLaLettre` en fait sur quatre mesures à un observable chacune. **Des nombres, sur un décor, avant de conclure.** Aux crans **6 et 8** *(`exerce`)*, rien ne change : la chaîne mesure comme aujourd'hui.
12. ⭐⭐ **LE RETOUR NE PARLE QUE DE L'OBSERVABLE DE LA CLÉ — par ce qu'il REÇOIT, pas par le prompt seul.** `engendrerLeRetour` reçoit `squelettes` *(extraction + jugement, par compétence)* et en fait `couches` et `codesObservables`. Avec le piège 11 (a), **une seule compétence arrive, et son jugement ne porte qu'un observable** *(les autres à `n/a`)* : sers à Calame ce squelette-là, `codesObservables` réduit à ce code *(c'est aussi ce que RR4 surveille)*, et la borne en clair du cran 2 **étendue à tous les crans qui isolent**, avec le nom de l'observable *(le nom de la fiche, pas le code — RR4)*. ⚠️ **Le squelette d'extraction (P1) reste entier** : c'est là que Calame prend ses citations, et une citation n'est pas un observable. ⚠️ Le plafond de la règle 2 du gabarit *(deux au micro, trois au méso)* reste : le retour parle d'**un** observable, il peut en dire deux ou trois choses.
13. ⚠️ **Le rejeu du retour lit des squelettes STOCKÉS** *(`rejouerLeRetour`, `exercices_squelettes`)* : un dépôt mesuré sous l'ancien régime porte les squelettes de toutes les compétences, et un rejeu après ton lot doit **filtrer au retour, pas seulement à la mesure** — sinon le retour rejoué d'un dépôt d'hier diffère de son retour d'hier. Applique le filtre **au même endroit** pour les deux chemins. `scripts/recette/retour-a-blanc.mjs` rejoue Calame **sans écrire** ; il refait la mesure — sers-t'en pour éprouver.
14. ⚠️ **`sondesDeLExercice`** écarte les secondaires du retour ; sur un exercice qui isole, elles ne sont plus mesurées non plus *(piège 10)* — l'alerte « compétence(s) sondée(s) écartée(s) du retour » n'a alors plus d'objet : ne la laisse pas mentir. ⚠️ **La vf** : `competencesFroides = [cible]` déjà ; ne touche pas au `delta_v1_vf`.
15. ⚠️ **Le juge du cran ne bouge pas** *(C7-L1)* : il part en parallèle, attendu avant le retour, et son verdict cite le passage. Ta borne du retour **ne retire pas le verdict** ni le passage cité : ils portent sur la tâche du cran, pas sur un observable de grille.
16. ⭐ **Le coût — fait quand 4.** Ligne de base au §④. Ce que (2) économise avec le piège 11 (a) : **N − 1 chaînes froides** par dépôt qui isole *(à 2,23 compétences par dépôt en moyenne, ~1,2 chaîne, soit ~2,5 appels sur 6,16)* ; la forme (b) économiserait en plus des jetons de sortie sur le seul P2, pas un appel. Mesure **avant / après** par la même requête *(`api_couts` par `depot_id`, `phase`, `cout`)* — en bac à sable sur des décors, puis en prod la première semaine après l'ouverture. ⛔ **Aucune règle ne s'appuie sur ce chiffre** *(`01-` §11)* : ni plafond, ni arbitrage — c'est un constat.

### D. La pièce (3) — le « se juger »

17. ⭐ **L'élection vit dans `utils/deroule/juger.ts`, `candidates()`** : les observables **testés** des compétences **ciblées**, triés par fragilité *(`etatDesObservables` sur la fenêtre, les sans-taux en queue)*, puis `departage()` au tirage **semé sur le dépôt**. **(3), précisé par Louis** : sur un exercice qui isole avec une clé, **la `couverture` reçue par `candidates()` ne contient QUE la compétence de l'observable de la clé** — aucune autre, même si l'objet la teste —, **et dans cette compétence l'observable de la clé passe en tête** ; la fragilité puis le tirage ordonnent le reste. **Aux crans 6 et 8**, la couverture reste celle d'aujourd'hui : les compétences ciblées, plusieurs. ⚠️ Le tirage semé **reste** *(`Math.imul`, les bits hauts — mémoire du 2⁵³)* : ne le réécris pas.
18. ⚠️ **Le « se juger » n'est servi qu'en `produire` × méso/macro × `evaluee`** *(`02-` §5)* : sous le gabarit, **au cran 2 seulement**, sur `argument`, `exemple`, `objection`, `paragraphe`, `plan` — **la transition et la phrase n'en ont pas**, même au 2. Le fait quand 3 se recette sur un **objet méso**.
19. ⛔ **Mesuré le 06/09 sur `main`, en bac à sable, dépôt `b41a1257` (argument, cran 2) : 3 questions servies — `lien_explicite`, `bloc_unite`, `jointure_presente`.** La clé `argument.garant.absent` porte `lien_explicite` *(argumentation)* ; **les deux autres sont de la Structure**, posées sur une pièce seule — **c'est exactement ce que (3) corrige**. ⚠️ Cette mesure est faite avec le `contexte.ts`/`vue.ts` de `main` *(le « se juger » apparie désormais par CODE, plus par nom)* ; **sur la branche, vérifie après la fusion de minuit que `ctx.servable` porte des codes** — sinon l'offre est vide *(mémoire : « servi VIDE bloquait la vf », réparé par `seJugerAServir`)*.
20. ⚠️ **Les questions viennent de la correspondance observable → formulation** *(`exercices_metacognition`, déposée par C4-L8 : dimension, question, liste fermée de réponses)* : un observable de la clé **sans question déposée** n'a rien à servir — l'ordre passe au suivant, et l'alerte le dit. **Tu n'écris aucune question.**

### E. La pièce (4) — la citation présente dans le matériau ET la copie

21. ⭐⭐ **`elaguerLesAncrages` / `jugerLAncrage`** *(`retour.ts` ~l. 895)* confrontent l'ancrage à `{ production, texteSupport }` — **la copie, et le texte d'auteur servi**. **Le matériau — le devoir d'élève — n'y est pas** : une citation recopiée du devoir est dans la copie, donc elle tient. (4) ajoute **une troisième confrontation, au matériau du cas** *(`casPourLeRetour[].materiau`, tous les cas)* : **présente dans la copie ET dans le matériau ⇒ écartée**, même geste que l'introuvable — **le point garde son texte, il perd son bloc** ; motif au bilan. ⛔ **Écarter, jamais refuser** : le refus a été mesuré à 40 retours sur 67 rejoués pour la même garantie.
22. ⭐⭐ **« SAUF SUR LE PASSAGE À CORRIGER » — un seul domicile pour la zone.** Aux crans 3·5·7 la copie **est** une réécriture du passage : l'élève en garde des mots, et les citer est légitime. La zone est **`passageFautif`** de `CasServiAuRetour` *(`cibleDansLeMateriau(contenu, version_corrigee)`)*, **étendue par les règles du marquage** *(`marquerLeMateriau`, « le passage qui porte le problème », bornes de phrase — `10-` §5)*, comme l'écran et le juge : **une citation qui tient dans le passage marqué n'est pas écartée**. ⛔ Ne recalcule pas un diff à toi.
23. ⚠️ **Un seul tokeniseur** : `citationTient` *(`citation-verifiee.ts`)* → `citationsIntrouvables` *(`anti-injection.ts`)* — apostrophes, guillemets, blancs aplatis ; **l'élision** découpée sur `[...]` / `…` ; l'approché au mot *(`citation-approchee.ts`, seuil 0,90, ≥ 4 mots)*. **Tu ne réécris rien de cela** ; tu appelles `citationTient(materiau, citation)` une fois de plus. ⚠️ Les citations courtes — « les gens », « me convainc », 2 mots, vues au banc — passent l'exact ; la règle les écarte si elles sont dans les deux. **Compte-les** et dis au relevé combien de points perdent leur citation *(27 sur 122 aujourd'hui, avant l'exception du passage)*.
24. ⚠️ **La PROSE ne s'élague pas** — `controlerRR3` la **refuse** quand elle attribue à l'élève une phrase du texte support ou du co-texte. **N'ajoute pas un refus « matériau » à RR3** : la règle 1 amendée *(« NE RÉPÈTE PAS la citation dans ta phrase »)* et le cas « matériau seul » *(2 sur 122, déjà « RR3-citation »)* suffisent, et un refus de plus rejoue des retours. Si Louis veut la prose aussi, c'est une décision, pas un geste.
25. ⭐ **`banc-citations-origine.mjs` est ton fait quand 1, et il doit apprendre l'exception.** Aujourd'hui il classe « copie ET matériau » sans regarder le passage ; **ajoute-lui la colonne « dans le passage à corriger »**, par le même `passageFautif`, pour que « aucune citation copie ET matériau **hors** du passage » soit mesurable. Lecture seule, `--prod`, la première semaine après l'ouverture.

### F. Ce que la fabrication a mesuré, et que tu remesures

26. **Les deux bases, 06/09 au soir** — §③ et §④. ⭐ **En bac à sable, rien ne se sert tel quel** : 328 exercices 1.5 `a_concevoir` ; les smokes de la séance des écrans ont posé leurs dépôts par décor *(`scripts/recette/smoke-cran2.mjs`, `--cran 5`, `decor-gabarit-eleve.mjs` — marque en base, `--retire`)*. **Le juge est ON en bac à sable** : des `verdicts_cran` existent aux crans 2 et 5. ⚠️ Un dépôt de décor **n'a pas de décision** : ta cible y est la compétence de l'observable isolé, et **aucune sonde** — dis-le dans la recette, ne fabrique pas une décision à la main.
27. ⚠️ **IDEES, 04/09** : *« retour non écrit sur trois remises de cran 5 en bac à sable (deux `argument.garant.absent`, un `argument.garant.connecteur`, dépôts assignés par décor sans décision) — cible INDÉTERMINÉE »*. **C'est le motif 1 de `C9-L3`**, et **ta règle du piège 10 le ferme pour le gabarit** *(sans décision, la cible est la compétence de l'observable isolé, jamais indéterminée)*. **Dis-le dans la boîte de `C9-L3`** ; ne rouvre pas les trois dépôts.
28. ⛔ **`supabase-js` ne lève pas** ; `{ error }` partout ; une lecture ratée de `probleme` ou du matériau **rend « sans clé »** — la chaîne sert comme hier, et l'alerte le dit. **Une lecture ratée n'est jamais « isole avec une clé »** : dans le doute, on mesure large.
29. ⚠️ **`api_couts.phase`** n'admet que `p1`, `p2`, `retour` *(et NULL pour le juge)* : aucune phase neuve. `server-only` : `--import ./scripts/register-calibration-resolver.mjs` pour tout script. **Le glob de `npm test` est `utils/**/*.test.ts`**. Mesure le compte total avant, rends-le après *(la suite portait 2210 tests le 03/09)*. ⚠️ **Un rouge hors lot, connu le 07/09** : `utils/fabrique/verifie-import-gabarit.test.ts` *(« le cran 2 exige un constituant… »)* est rouge depuis la régénération de `doctrine.fixture.json` par L7 — l'exemple du test date d'avant le 06/09, pas le contrôle ; il est dans la boîte de la fabrique au plan. **Ce n'est pas toi** : ne le répare pas, note-le au relevé.
30. ⚠️ **Le plafond d'appels par dépôt** *(`depotAAtteintSonPlafond`, `config.plafondAppelsParDepot`)* se lit **au nombre de lignes en base** : moins de chaînes, c'est plus de marge de rejeu — pas une raison d'en ajouter.
31. ⚠️ **`retours_texte_segmente_chk`** *(la garde en base du retour)* n'exige plus l'ancrage depuis `retour_ancrage_elague.sql` *(31/08, les deux bases)* : un point élagué par (4) passe la contrainte. **Vérifie par `pg_get_functiondef`, pas par un commentaire** — la mémoire du 31/08 dit pourquoi.

---

## La convention de couture — ce que tu éprouves par EXÉCUTION

Ton lot succède à `C7-L1`, `C7-L6` et `C4-L5`, et la séance des écrans lui a préparé le champ. **Cinq coutures, nommées sous la forme vérifiable** *(qui écrit · qui lit · un chemin réel y mène-t-il ?)* :

| couture | qui écrit | qui lit *(toi)* | l'épreuve |
|---|---|---|---|
| **la copie** `exercices_depots.texte_v1` au cran 2 et au cran 5 du gabarit | l'écran à trou *(branche)* | `production()` → P1/P2, le juge, l'élagage | un dépôt de décor au 5 : `texte_v1` = le passage seul, et le juge reçoit `reassemble` ; au 2 : la pièce seule, et `piece` |
| **la clé du cas** `exercices_cas.probleme` → `exercices_problemes.observable_code` | l'import 1.5 · `derive-doctrine.py` | `casPourLeRetour` → l'observable de la clé | sur les 328 exercices 1.5 : combien de cas portent une clé, combien de clés rendent un observable, combien n'en rendent aucun — **par objet, des nombres** |
| **la décision** `routeur_decisions(cible_retenue, sondes_retenues)` | C4-L12 / C7-L6 / C7-L7 | `ctx.decision` → `competencesFroides` | un dépôt **avec** décision *(`essai-cron-hebdo.ts --essai` en bac à sable, ou `poserLesSemainesDuRouteur` sur un cycle demandé)* : la cible et ses sondes mesurées, les autres écartées avec motif ; un dépôt **sans** : la cible seule |
| **les squelettes** `exercices_squelettes` → le prompt de Calame | la mesure | `engendrerLeRetour` / `rejouerLeRetour` | sur un dépôt qui isole : les points du retour ne portent que l'observable de la clé, **à la mesure et au rejeu** ; porte fermée, le retour d'hier à l'octet |
| **l'ancrage** `exercices_retours` (points, `ancrage.citation`) contre `casPourLeRetour[].materiau` et `passageFautif` | Calame · `elaguerLesAncrages` | l'écran *(`RetourSegmente`)*, le banc | un point qui cite une phrase du devoir hors du passage : élagué, motif au bilan ; un point qui cite des mots du passage réécrit : gardé ; `banc-citations-origine.mjs` sur les dépôts de décor : 0 « copie ET matériau » hors passage |

**Le script se laisse au dépôt** : `scripts/recette/couture-c7l8.mjs`, mode constat, registre AVANT, retrait par marque, contrôles comptés et **confrontés en base par requête**. **Bac à sable uniquement** — aucune écriture en production par ce lot. Plus le banc `--prod` en lecture seule et **la requête de coût, avant et après**.

**Puis le « fait quand », dans l'ordre de ses quatre points** — chaque point avec **sa requête et son résultat**. Et **une lecture à l'œil d'un retour de cran 5 sur téléphone** *(375 px, `scripts/recette/capture-page.mjs` émule par CDP — ⚠️ il est sur `main` et **absent de la branche** au 06/09 au soir : il y arrive avec la fusion de minuit ; ⛔ `chrome --window-size` ne fait pas un écran de 375 px)* : le point sans citation doit se lire comme un point, pas comme un trou.

---

## Le « fait quand » — *(recopié du `07-` §2 ; ne se négocie pas en séance)*

> *Fait quand* : **(1)** `banc-citations-origine.mjs --prod`, sur la première semaine après l'ouverture : aucune citation « copie ET matériau » hors du passage à corriger ; **(2)** sur un dépôt de décor au cran 5 en bac à sable, la mesure ne porte que la cible et les sondes de la décision, et les points du retour ne concernent que l'observable de la clé ; **(3)** au cran 2 sur un objet méso, le « se juger » sert des questions de l'observable de la clé ; **(4)** le coût par dépôt baisse dans `api_couts` *(N compétences → une plus les sondes)*, mesuré avant et après.

---

## Les conventions de dépôt et de clôture

- **La migration de l'interrupteur — décidée** *(§⑤)* : `c7_l8_chaine_cle.sql` + `c7_l8_chaine_cle_rollback.sql`, **une ligne au `SUIVI_SQL.md` AVANT exécution** *(date, fichier, zone, cases Sandbox / Prod)*, **bac à sable d'abord** ; additive et gatée, elle s'applique normalement *(R6, point 5)* ; **la prod est le geste de Louis**, avec le push de la branche. ⛔ **Jamais un fichier de l'Archive.** ⚠️ Le lecteur est tolérant : **le code peut partir avant le SQL** *(colonne absente ⇒ OFF)*, et c'est l'ordre voulu ici.
- **`derive-doctrine.py --verifie`** avant de lire la doctrine : IDENTIQUE sur 17 tables ; DIVERGE ⇒ `--sql`, jamais la base à la main.
- **Ta section au `SUIVI_tests_manuels.md`** : `## C7-L8 — La chaîne à l'heure de la clé (séance du …)` — prouvé coché **avec sa preuve**, décoché **avec sa condition de reprise** *(au moins : **`C7L8-x` — le banc `--prod` sur la première semaine après l'ouverture**, et **le coût en prod avant / après**)*. ⚠️ Ni `C7-L1` ni `C7-L6` n'ont de section.
- **Une source trouvée fausse se marque, elle ne se corrige pas** : `[faux]` au point de l'erreur, une ligne à DETTES de `INVENTAIRE_Non_Tranches.md`.
- **Ton relevé** : `RELEVE_C7_L8_<date>.md` — **la migration en tête** *(jouée en bac à sable, en attente en prod)*, **ce que `code2` rend sur un seul observable** *(piège 11)*, **ce que la borne du cran 2 est devenue** *(piège 3)*, puis les nombres du banc et du coût. **Et l'amendement du `07-` §2 que ton relevé porte** : la pièce (2) dit « juste l'observable, aucune sonde hors des crans 6 et 8 », plus « la cible et les sondes, les autres observables restent au journal » ; et `chaine_cle_actif` passe de « à confirmer » à décidé. Ce qui est pour `C9-L3` *(piège 27)*, pour `C7-L7` ou pour la fabrique va **dans leur boîte** au `PLAN_DE_CHANTIER.md` §5.
- ⛔ **`git add -A` est interdit** ; deux lots vivent sur la branche. Fichier par fichier, `git status --short` avant chaque commit. **Tu commites sur la branche, tu ne pousses pas** *(§②)*.

---

## §H — Ce qui est hors de toi, et où ça va

- **`C7-L7` — le routeur par objet** *(même branche, même soir)* : il journalise l'observable élu sur la décision ; **si ton lecteur de clé peut lui servir**, dis-le dans sa boîte. Tu ne lis pas son journal *(piège 9)*. **Et les sondes secondaires posées sur un exercice qui isole ne sont plus mesurées** *(piège 10)* : une ligne dans sa boîte, pour qu'il cesse d'en poser là.
- **`C9-L3` — les causes amont du retour non écrit** : le motif « cible INDÉTERMINÉE » sur le gabarit *(piège 27)*.
- **Le cran 7 en texte à trou** : pas décidé ; si le signal du piège 7 montre que le 7 recopie autant que le 5 recopiait, **c'est une mesure pour Louis**, pas un écran à faire.
- **La prose et le matériau** *(piège 24)* : une décision, si Louis la veut.
- **Renommer les neuf observables qui sont des mots français** *(RR4)* : les sources, pas toi.

---

## §I — Les entrées `C7-L1` *(en abrégé)* et `C7-L8` du `07-Implementation.md` §2 — copie de ce qui est écrit dans la source

*(Chapitre **`### C7 — le gabarit des exercices`**, `07-` **2.80**, 06/09/2026. La source fait foi ; cette copie est là pour que tu n'aies rien à chercher.)*

> **C7-L1 — Le juge reçoit le texte, et ce qu'on tient pour vrai.** *(…)* Le lot pose donc **un quatrième appel, « le juge du cran », aux crans 4·5·7·9** *(`phase` NULL, comme la transcription — la contrainte d'`api_couts` n'admet que trois étages)*, qui reçoit les documents balisés — le devoir d'élève, **l'énoncé du problème** *(`exercices_cas.defaut`)*, **le passage qui le porte**, **la version corrigée**, la réponse attendue, **la zone désignée et son verdict de zone**, le candidat choisi au 4 — et rend un verdict contraint *(réussi, problème vu, passage cité **vérifié verbatim dans la copie**, motif)*, écrit sur le dépôt *(`exercices_depots.verdicts_cran`, par version)* ; **Calame reçoit les mêmes documents et ce verdict**, et cite le passage. ⛔ **P1 et P2 ne changent pas** : la décision du 31/08 tient pour eux. *(…)* son interrupteur est **le sien, `juge_documents_actif`** *(§5 : « un lot lit LE SIEN, jamais celui d'un voisin »)*, à OFF ; porte fermée, le message du retour est **celui d'hier à l'octet**, et un test le prouve. **Le registre des réussites** est **dérivé, sans table neuve** *(`utils/registre/reussites.ts`)*. *(…)*
>
> **C7-L8 — La chaîne à l'heure de la clé.** *(Ouvert le 06/09/2026, après-midi, sur un signalement d'élèves relayé par Louis — « le retour cite le texte original et non ma réponse » — et trois mesures : sur les 121 retours chauds de production, aux crans 4·5·7·9, **27 citations « Tu écris » sur 118 sont des phrases du devoir servi que l'élève avait recopiées** — au cran 5 la v1 fait 1,01 fois la longueur du matériau, 0,93 au 7 — et 2 ne sont que dans le matériau ; le contrôle du 31/08 ne peut pas les voir, le texte EST dans la copie ; la chaîne mesure **toutes les compétences ouvertes** sur chaque dépôt, isole ou non *(`competencesDeLExercice`)* ; le « se juger » élit ses questions par fragilité, pas par la clé.)* Quatre pièces, décidées par Louis le 06/09 : **(1) la production envoyée à la chaîne est ce que l'élève a écrit** — au cran 2 la pièce *(le juge du cran garde l'objet assemblé)*, aux crans 5 et 7 le passage réécrit, jamais le devoir recopié ; l'écran fait du passage en gras **le champ**, dans le fil du devoir, comme le trou du cran 2 *(proposition de la séance des écrans, `10-` v0.12 §2 bis.6, à valider)*, et la chaîne **signale** une v1 qui reproduit le devoir ; **(2) sur un exercice qui isole, la chaîne mesure la cible et les sondes de la décision, pas les six compétences**, et **se concentre sur l'observable de la clé** : le retour ne parle que de lui *(`02-` §2.2 : « la pièce écrite n'engage que les observables rattachés à son constituant »)*, les autres observables de la compétence restent au journal sans retour ; **(3) le « se juger » pose ses questions sur l'observable de la clé d'abord** ; **(4) une citation présente dans le matériau ET dans la copie s'écarte** comme les citations introuvables, sauf sur le passage à corriger *(la zone du diff)*. ⚠️ **Arbitrage à confirmer** : derrière **son propre interrupteur**, `chaine_cle_actif` à OFF — le lot change le retour de tous les dépôts, un lot lit le sien. *Fait quand* : **[le « fait quand » ci-dessus, verbatim]**. *Manifeste* : `01-` v5.11 §4 · `02-` v6.7 §2.2, §5 · `10-` v0.11 §2 bis.1, §6 · `06-` §4. Dépend de **C7-L1**, **C7-L6**, C4-L5.
>
> *(Notes de fabrication : le §2 bis.6 du `10-` est marqué « à valider » dans cette entrée ; il a été **validé par Louis le 06/09 après-midi** et codé sur la branche — `10-` v0.12. **L'interrupteur `chaine_cle_actif`, « à confirmer » dans l'entrée, est DÉCIDÉ par Louis le 06/09 au soir** ; l'entrée du `07-` n'est pas amendée par la fabrication — c'est ton relevé qui le portera, le §2 étant ouvert à l'implémentation depuis un relevé.)*
