# PROMPT — Session Code : C7-L7 — « Le routeur par objet »

> **À coller dans une session Claude Code fraîche.** Une session, un lot.
>
> Ce prompt est fabriqué selon la recette du `PLAN_DE_CHANTIER.md` §5, le **2026-09-06** au soir, à partir des décisions de Louis du 06/09 après-midi *(`01-` v5.11 §4, couche 3, « L'objet : quand il se sert »)*. Ce que le lot construit, son manifeste et son « fait quand » sont recopiés ci-dessous **avec leurs versions au moment de l'écriture**. ⚠️ **Lis d'abord la section qui suit : elle dit où vit le code dont tu dépends, ce que la fabrication a mesuré, et ce que tu vérifies avant tout.**
>
> ⭐⭐ **TON LOT NE CHOISIT PAS UNE CLÉ DE PLUS : IL DONNE AU ROUTEUR L'UNITÉ QU'IL N'A PAS — L'OBJET.** Mesuré le 06/09 : la couche 2 élit une compétence, la couche 4 filtre des instances, et **rien entre les deux ne dit sur quel objet on travaille**. Aux crans qui isolent, l'instance porte la compétence de l'observable de sa clé *(304 sur 304 en bac à sable)*, et **rien ne lit ni `exercices_cas.probleme` ni `observable_code`** : après la porte du registre *(C7-L5)* et la quarantaine des devoirs *(C7-L6)*, **la clé sort de PB1, de PB3 et du tirage** — c'est-à-dire par accident. Sous le gabarit, tous les crans isolent sauf le 6 et le 8 : **choisir la clé, c'est choisir l'observable**, et c'est une décision, pas un reste.
>
> ⛔ **Aucune migration attendue, derrière `gabarit_actif`.** L'état d'un objet est **dérivé**, jamais stocké ; le journal est le jsonb de `routeur_decisions` ; les deux compteurs sont des requêtes. Si ton lot croit avoir besoin d'une colonne, il s'arrête et le dit.

---

## ⚠️⚠️ CE QUE LA FABRICATION A MESURÉ, ET CE QUE TU VÉRIFIES AVANT TOUT

| | Le fait | Ce que tu en fais |
|---|---|---|
| **①** | ✅ **L'entrée `C7-L7` du `07-Implementation.md` §2 EST ÉCRITE le 06/09 après-midi** *(chapitre `### C7`, après `C7-L6` ; le `07-` passe en **2.80**, `derive-instruments --ecris` rejoué — version et empreinte seules, commit `b1f7dc8` sur `main`)*. Le §I en fin de prompt en garde la copie. ⚠️ Le dépôt de conception est à **`/Users/louissagnieres/Documents/GitTest/palimpseste-conception/`** *(commit `2b07e8d`)* — on n'y commite que sur demande. | ⛔ **Ton contrôle d'entrée commence là** : ouvre le `07-` §2, chapitre `C7`, et vérifie que l'entrée porte **mission, « fait quand » et manifeste** tels qu'au §I. Un écart se signale, il ne se corrige pas. Si `derive-instruments.py --verifie` dit DIVERGE, une séance a bougé une source après le 06/09 : lis son diff avant de rejouer `--ecris`. |
| **②** | ⛔⛔ **DÉCISION DE LOUIS, 06/09 AU SOIR : CE LOT SE JOUE SUR LA BRANCHE `feat/ecrans-passation`, PAS SUR `main`.** C'est là que vivent `C7-L6` *(le routeur par devoir, commits `45fddfa` et `a938efb`)* et le cran 2 en texte à trou — worktree **`../palimpseste-passation`**, tête `e66c4d4`, **16 commits d'avance** sur `main` le 06/09 au soir. **La branche ne part sur `main` que quand tous les lots qu'elle porte ont été joués** ; ce n'est pas toi qui la pousses. | **Ouvre le worktree de la branche, et vérifie que tu y es** : `git branch --show-current` doit dire `feat/ecrans-passation`, et `git log --oneline -1` porter `e66c4d4` ou un commit plus récent de la même branche. **Tu ne commites que sur elle, tu ne pousses rien, tu ne fusionnes rien.** Si `main` a avancé entre-temps, ne rebase pas de ton chef : dis-le. ⚠️ `vivier.ts` fait **950 lignes** sur la branche contre 707 sur `main` : c'est le fichier que tu touches le plus. |
| **③** | ✅ **L'allumage, mesuré PAR REQUÊTE le 06/09 au soir.** **Bac à sable** : `gabarit_actif` **ON**, `juge_documents_actif` **ON** *(il était OFF le 05/09)*, `routeur_actif` ON ; **328 exercices 1.5** *(41 clés ; transition 87 · argument 53 · objection 52 · paragraphe 52 · exemple 35 · phrase 25 · plan 24)*, **tous `a_concevoir`** ; **0 `routeur_decisions`, 0 dépôt du routeur**. **Production** : `gabarit_actif` **OFF**, `juge_documents_actif` **OFF**, `routeur_actif` ON ; **480 décisions, toutes sur le cycle `2026-08-31`** *(un seul passage du routeur)* ; **0 exercice 1.5** — la bascule de dimanche *(`scripts/recette/BASCULE_gabarit_lundi_7_septembre.md`)* **n'est pas encore jouée** à l'heure de l'écriture. | **Remesure les deux bases** *(l'allumage se mesure, il ne se recopie pas)*. ⛔ Tu ne bascules aucun interrupteur, tu n'écris rien en production. ⭐ **Si la bascule a été jouée entre-temps**, la prod porte ~328 exercices 1.5 `concu`, `gabarit_actif` ON — et **le routeur passe lundi 07/09 à 18:00 UTC** *(`/api/assiduite/hebdo`)* : cette première semaine est **la semaine 1 de tout le monde** *(deux objets en méthode pour tous)*, que **`C7-L6` sert déjà** *(`bornerLaMethode`, deux objets)*. **Tes règles mordent à partir du cycle du 14/09** : pour qu'elles tournent ce lundi-là, **la branche doit être sur `main` avant lundi 14/09, 18:00 UTC** — c'est l'échéance de la branche, et la décision de la pousser est celle de Louis, pas la tienne. |
| **④** | ⭐ **Ta boîte aux lettres est VIDE — le lot est né le 06/09.** Il n'existe **aucun `RELEVE_C7_L5` ni `RELEVE_C7_L6`** : ces deux lots ont été codés en séance de conception, et leur relevé est **leur entrée au `07-` §2** *(recopiée au §I pour L6)* plus leurs commits. | **Tout ce qui t'était adressé est ici, aux pièges.** Tu n'as aucun relevé à ouvrir. |
| **⑤** | ⛔⛔ **DEUX « PAS FAIT » DE `C7-L6` SONT SUR TON CHEMIN, ET LE PREMIER DÉFINIT TON ÉTAT « TENU ».** *(a)* **« Deux et deux » se compte sur deux DEVOIRS différents, à un cycle d'écart au moins** *(`01-` §8.10 ; `10-` §7 amendé le 06/09)* — `utils/registre/reussites.ts` **compte encore sans devoir** *(`cransDebloques` additionne les réussites d'un cran, `DepotPourLeRegistre` n'a pas de champ devoir)*. *(b)* **Les sondes partent dès DEUX ÉCHECS, consécutifs ou non** — `cransOuLEleveStagne` lit encore **la fin de la série** *(`serie.slice(-2)`)*. Le `07-` les déclare « pas fait » à l'entrée `C7-L6`, « avec la session de l'écran » — un oubli entre deux sessions parallèles, dit par Louis le 06/09 au soir. | ⭐⭐ **DÉCISION DE LOUIS, 06/09 AU SOIR : C7-L7 LES FAIT, TOUS LES DEUX.** Ton « tenu » *(règle 5)* n'existe pas sans *(a)* : un objet serait « tenu » sur deux réussites du même texte le même jour. La lecture existe déjà — `lireLesDepotsPourLeRegistre` sélectionne `exercices_cas(...)` : ajoute `materiau_id`, et au cran 2 la souche de l'`id_import` *(`devoirsDeLInstance`, `vivier-serveur.ts`)* ; `DepotPourLeRegistre` gagne `devoirs: string[]`. *(b)* est un `filter` au lieu d'un `slice`. **Les 154 tests du moteur et du registre restent verts**, et le point (4) du « fait quand » de `C7-L6` *(§I : deux réussites le même jour sur le même devoir n'ouvrent pas ; sur deux devoirs à un cycle d'écart, oui)* **devient une épreuve de ton lot**. Dis au relevé que tu as fermé ces deux « pas fait » de L6, pour que le `07-` puisse le porter. |
| **⑥** | ⚠️ **Le manifeste du `07-` épingle le `10-` en v0.11 ; le fichier est en v0.12.** La 0.12 *(06/09 midi)* touche §2 bis.1, §2 bis.6, §3, §5, §6 — **pas le §7**, qui a bougé pour la dernière fois en v0.10 *(« deux et deux » sur deux devoirs)*. | **La version avertit, elle ne bloque pas** : relis l'en-tête, note l'écart au relevé, continue. |
| **⑦** | ⚠️ **Les deux arbres portent du travail non commité d'autres séances** *(le 06/09 au soir, sur `main` : `SUIVI_SQL.md`, `utils/fabrique/doctrine.fixture.json` ; sur la branche, mesure-le)*, et **la branche est partagée avec la session des écrans**. | `git status --short` avant chaque commit ; **`git add -A` interdit** ; fichier par fichier, et seulement les tiens. **Tu ne pousses pas** *(§②)*. |

---

## Le manifeste — *(recopié du `07-` §2, entrée `C7-L7` ; versions du 06/09)*

> *Manifeste* : `01-` v5.11 §4 *(couches 2 et 3)*, §5, §11 · `02-` v6.7 §2.2 · `10-` v0.11 §7. Dépend de **C7-L6**, C7-L5, C4-L12.

**Trois pièces, plus « ce document ».**

| Pièce | Où | Statut requis | Au moment de l'écriture *(06/09)* |
|---|---|---|---|
| `01-routeur.md`, **§4** *(couche 2 — la cible ; **couche 3 — « L'objet : quand il se sert », les six règles du 06/09** ; couche 4 — la porte du registre et l'espacement des devoirs, en dernier)*, **§5** *(les trois phases ; la semaine de méthode dans la pose — deux objets, la séquence par palier ; l'amendement du 06/09 après-midi : « deux objets la première semaine, puis le budget décide »)*, **§11** *(ce que la décision journalise ; les compteurs)* | `/Users/louissagnieres/Documents/GitTest/palimpseste-conception/` | **déposé** *(aucun statut explicite au manifeste — le minimum)* | **VERSION 5.11** · VALIDÉ ET GELÉ *(vaut relu et validé)* |
| `02-exercices.md`, **§2.2** *(l'échelle des crans ; **le cran 2 `isole`** — l'observable de la clé « pièce absente » de son trou ; les crans 6 et 8 `exerce`)* | même dépôt | **déposé** | **VERSION 6.7** · VALIDÉ ET GELÉ |
| `10-Gabarit.md`, **§7** *(réussi, raté — et l'ordre des crans : le registre par élève × objet × cran × variante, « deux et deux » sur deux devoirs à un cycle d'écart, les sondes dès deux échecs, la semaine de méthode)* | même dépôt | **déposé** | **VERSION 0.12** — `[arrêté le 03/09/2026]` ; le chapitre `C7` du `07-` le tient pour **relu et validé** · ⚠️ **§⑥ : le manifeste dit v0.11** |
| `07-Implementation.md`, **§2** *(chapitre `C7` — les entrées `C7-L5`, `C7-L6`, `C7-L7` ; la règle de manifeste)*, **§1.5** *(le journal `routeur_decisions` : « la forme physique appartient à la session Code »)* | même dépôt | aucun — un lot n'exige pas un statut de la source qui le déclare | **VERSION 2.80** · RELU ET VALIDÉ *(le §2 est GELÉ pour la règle de manifeste seule, OUVERT pour l'inventaire des lots)* |

### ⚠️ Ce que le manifeste ne nomme pas, et pourquoi tu le lis quand même

- **`01-` §3** — les dérivés « jamais stockés » *(la fenêtre d'évidence, l'historique des cibles)*, **le registre des réussites et les crans débloqués** *(amendement du 03/09)*, **« le devoir est l'unité de service, la clé l'unité de mesure »** *(amendement du 06/09)*. **Les six règles du §4 y renvoient trois fois.**
- **`01-` §6** — R5 *(« la dette de couverture », que la règle 4 lit « au grain de l'observable »)* et **la branche d'échec** *(`degrade`)*.
- **`01-` §8.8 et §8.10** — les sondes de montée *(M-b, M-e : la sonde ne compte nulle part)* et l'espacement des devoirs.
- ⛔ **Le `09-Objets.md` N'EST PAS à ton manifeste, et tu ne le lis pas** : l'observable d'une clé se lit dans **la doctrine dérivée en base** — `exercices_problemes` *(`observable_code`, `observable_competence`, `observable_route`)*, chargée par `utils/fabrique/doctrine.ts` *(`doctrine.problemes[cle]`)*. Le `09-` est `[à valider]` ; la table en est la seule lecture licite, après `derive-doctrine.py --verifie` **IDENTIQUE**.

⛔ **`00-`, `03-`, `04-`, `05-`, `06-`, `08-` et les fiches de `competences/` ne sont PAS à ton manifeste** : tu n'ouvres aucune compétence, tu n'importes rien, tu ne touches ni au juge, ni à la chaîne, ni à un écran.

---

## Le contrôle d'entrée — en deux temps, et les deux ne font pas la même chose

> **Le statut bloque.** Un fichier du manifeste dont l'en-tête porte un statut inférieur à celui exigé **arrête le lot**, explicitement. Le barème est au `07-Implementation.md` §2 : *déposé* → *relu et validé* → *versé et bancé*, cumulatifs ; « VALIDÉ ET GELÉ » vaut *relu et validé*.
>
> **La version avertit.** Si la ligne VERSION d'un fichier du manifeste diffère de celle inscrite ici, **relis son en-tête avant de continuer** — une version qui bouge dit qu'un texte a changé, pas qu'il est devenu faux.
>
> **Le blocage est granulaire quand il peut l'être.** Une fiche de compétence absente ou non bancée bloque **cette compétence**, pas le lot.

Concrètement : vérifie que les quatre pièces **existent**, que le `01-` porte **VERSION 5.11**, le `02-` **6.7**, le `10-` **0.12** *(§⑥)*, le `07-` **2.80** — et **fais les gestes du §① et du §②**. La clause granulaire est sans objet : tu n'ouvres rien.

### Tes dépendances sont jouées — vérifie-les par leur chemin, pas par leur ligne au plan

- **C4-L12** — l'écrivain du routeur : `utils/moteur/cycle-serveur.ts` *(`poserLesSemainesDuRouteur` ; `composerPourUnEleve` — l'état, le vivier et la liste, **partagés avec le pull** ; `poserLaSemaineDUnEleve` ; `persister`)*, `utils/routeur/semaine.ts` *(`poserLaSemaine`, PB1-PB6, **pur** ; `RepriseDeLaPose` pour le pull)*, `utils/routeur/ciblage.ts` *(`listeDePriorite` — PA2 ; `EntreeDePriorite` porte `grains` et `crans`)*, `utils/moteur/decision.ts` *(`lignesDeDecision`, `journalDuTirage`)*. **Chaque dépôt du routeur porte `exercices_depots.routeur_decision_id`.**
- **C7-L5** — le routeur par registre : `utils/registre/reussites.ts` *(le registre dérivé, `cransDebloques`, `cransOuLEleveStagne`, `ECHELLES`)*, `porte.ts` *(`porteDeLObjet` → `ouverts` / `sondes` / `methode` ; `statutDeService` ; `CRANS_DE_LA_METHODE = [1, 3, 4]`)*, `porte-serveur.ts` *(`lireLaPorteDesCrans` : `dejaServis` **sur les seuls exercices du gabarit**, les sondes déjà reçues ; **inerte à `gabarit_actif` OFF**)*. Dans `vivier.ts`, la porte est **le dernier filtre** *(écart `porte_registre`)* ; `InstanceRetenue.porte`.
- **C7-L6** — le routeur par devoir *(sur la branche, §②)* : `vivier.ts` *(`InstanceDuVivier.devoirs`, `devoirEnQuarantaine`, `QUARANTAINE_CYCLES = 2`, le second passage `degrade` + `devoirsManquants`, **`bornerLaMethode`** — deux objets, un devoir, `sequenceDeMethode` par palier, écart `methode_hors_quota`)*, `vivier-serveur.ts` *(`devoirsDeLInstance` — le devoir du cran 2 par la souche de l'`id_import`)*, `cycle-serveur.ts` *(`lireLesDevoirsServis`, `bornerLaMethode(…, true)` — le cran 2 servi)*, `decision.ts` *(`devoir`, `methode`, `devoir_manquant` dans `alternatives_ecartees`)*, `scripts/recette/banc-devoir-quarantaine.mjs`.
- **C6-L3** — le pull *(« en faire plus »)* : `utils/moteur/bonus-serveur.ts`, qui appelle **`composerPourUnEleve` puis `poserLaSemaine` avec `reprise`**. *Il n'est pas au manifeste, mais ton ordre par objet passe par lui — piège 12.*

**Si l'une manque, arrête-toi et signale-le.**

---

## La mission — ce que le lot construit, et rien d'autre

**La couche 2 élit la compétence ; la couche 3 élit l'objet, le cran, puis l'observable, et la couche 4 le devoir.** L'objet a **un état par élève** — *en méthode*, *ouvert*, *tenu* —, **dérivé** du registre des réussites et des décisions, jamais stocké : il entre par sa semaine de méthode ; ouvert, il reçoit **un exercice par cycle**, sur le cran non tenu le plus bas, sur un autre devoir ; tenu *(« deux et deux » sur toute la bande du palier)*, il sort du centre et ne revient qu'en sonde. **La phase B pose dans l'ordre** : la méthode *(sa séquence entière)*, un exercice par objet ouvert, puis **un objet neuf seulement si le budget le permet encore** — deux objets la première semaine pour tous, ensuite **un par cycle en TC, deux en HLP et en bi-classe**. **Entre objets ouverts, puis entre clés d'un objet** : l'observable non acquis *(fenêtre d'évidence)*, puis le moins mesuré *(R5 au grain de l'observable)*, puis le tirage journalisé. **Aux crans 6 et 8** l'instance exerce, PB4 pose ses cibles, on y monte par le registre du cran 2. Départage : « pas deux exercices de suite sur le même objet », PB2 garde la compétence. **Journal** *(`01-` §11)* : sur chaque décision l'état de l'objet, l'observable élu et son motif ; deux compteurs — objets ouverts par élève × cycle, temps de passage méthode → ouvert → tenu. Aucune migration attendue ; derrière `gabarit_actif`.

**Hors du lot, nommément** : la chaîne, le retour, le « se juger » et la citation recopiée *(`C7-L8`)* · les crans 6 et 8 au registre *(les seuils — « pas fait » de C7-L5, et il le reste)* · le PLAN au cran 2 et les contre-exemples du `09-` *(jeudi 10/09)* · les retours non écrits *(`C9`)* · les durées par cran *(fin de semaine 2, décision de Louis)* · la fabrique et toute vague · tout écran · toute migration · tout interrupteur.

---

## Les pièges — tirés des sources et du code, chacun avec son renvoi

### A. L'état de l'objet — DÉRIVÉ, jamais stocké

1. ⛔⛔ **TROIS ÉTATS, AUCUNE COLONNE.** *En méthode*, *ouvert*, *tenu* se **dérivent** du registre des réussites *(`reussites.ts`)* et des décisions *(`routeur_decisions`)* — « dérivé — jamais stocké », comme le registre lui-même *(`01-` §3, amendement du 03/09 ; §4 couche 3, « un objet a une vie par élève, en quatre états »)*. Un module **pur** à côté de `porte.ts` — propose `utils/registre/objet.ts` : `etatDeLObjet(registre, objet, dejaServi, bande) → 'methode' | 'ouvert' | 'tenu'`, testé sous `utils/**` *(le glob de `npm test` est `utils/**/*.test.ts` — une règle sous `app/` ne serait jamais éprouvée)*. **Un lecteur serveur qui relit ce que `lireLaPorteDesCrans` lit déjà est un second domicile** : étends `PorteDesCrans` *(`de(objet)` mémorise déjà par objet)*, ne relis pas les dépôts.
2. ⭐ **« En méthode » = jamais servi SOUS LE GABARIT**, pas « jamais servi ». Décision de Louis du 04/09, codée à `porte-serveur.ts` : `dejaServis` ne compte que les dépôts dont **un cas porte `probleme`**. Un élève qui a fait dix `phrase` sous la banque 1.4 entre en semaine de méthode sur `phrase`. Ne recode pas cette lecture, appelle-la.
3. ⚠️ **« Ouvert » = déjà servi ET un cran non tenu dans la bande du palier ; « tenu » = tous les crans de la bande tenus par « deux et deux » — et LA BANDE N'EST PAS `cransDuPalier` TOUT ENTIER.** `cransDuPalier` *(`config.ts`)* concatène `sous_la_bande`, `centre` **et `au_dessus`** ; or « tenu, l'objet **sort du centre** et ne revient qu'en **sonde au-dessus** *(M-b)* » *(règle 5)*. **Arbitrage de fabrication : « tenu » se compte sur `sous_la_bande ∪ centre` ; la zone `au_dessus` reste celle des sondes**, sinon un objet ne serait jamais tenu chez E-D *(le 6·7·8 y sont au-dessus, et le registre ne dérive pas le 6 ni le 8 — piège 20)*. Journalise l'arbitrage et dis-le au relevé.
4. ⚠️ **Un cran de la bande que la BANQUE ne porte pas pour cet objet sort de la bande.** Chez E-D, le centre contient `production_guidee` *(le 2)* ; **la banque 1.5 en production n'a pas de cran 2** *(24 exercices en bac à sable, déposés en prod seulement si l'écran est mergé — `BASCULE_…md`, étape 2)* ; `mot` et `reference` sont fermés aux 6 et 8 par exception *(`IDEES_post_rentree.md`)*. Sans cet arbitrage, **« le cran non tenu le plus bas » serait toujours le 2, que personne ne peut servir**, et l'objet consommerait sa place pour rien à chaque cycle. **Compte la bande sur les crans que le vivier porte pour l'objet**, journalise « bande réduite : crans absents de la banque », et dis-le au relevé.
5. ⚠️ **Le palier qui indexe la bande est celui de la COMPÉTENCE CIBLE, jamais « le palier de l'élève »** *(`01-` §4 : « un élève est à B en Structure et à E en Questionnement »)*. Le registre, lui, est **par objet × cran, sans compétence**. Un même objet porte des clés de plusieurs compétences *(mesuré sur la grille : `argument.garant.connecteur` → `garant_present` / argumentation, `argument.conclusion.absente` → `bloc_unite` / structure, `argument.garant.non_nomme` → `fonction_moments` / structure)*. **L'état se dérive donc par (élève × objet × palier de la cible)** : le même objet peut être *tenu* pour l'Argumentation à B et *ouvert* pour la Structure à D. `composerPourUnEleve` porte déjà `paliers: Map<Competence, Lettre>`.
6. ⛔ **« Deux et deux » sur deux devoirs à un cycle d'écart — §⑤.** Deux réussites le même jour sur le même texte n'en sont qu'une ; la paire compte déjà pour une *(le second cas seul, `issueDuDepot`)*. Le devoir d'un dépôt = `exercices_cas.materiau_id`, ou la souche de l'`id_import` au cran 2. **Le cycle d'écart se compte comme la quarantaine** — `lundiDe`, `cyclesEcoules` *(`vivier.ts`)* —, pas en jours.
7. ⚠️ **Un objet ne se ferme jamais, un objet « tenu » non plus** : « il ne coûte plus rien » signifie qu'il **ne prend plus de place au centre** ; il revient **en sonde** *(`porte.sondes`, M-b — la part chiffrée par palier)* ou **quand la lettre monte et rouvre une bande** — ce second cas est mécanique : la bande du nouveau palier porte des crans non tenus. Ne pose aucun drapeau « fermé ».

### B. La phase B par objet — un seul ordre, et le budget borne l'entrée

8. ⭐⭐ **L'ORDRE (i) → (ii) → (iii) EST LA RÈGLE 3, ET LA MOITIÉ EXISTE.** *(i)* la méthode, sa séquence entière : **`bornerLaMethode` le fait** — deux objets au plus *(`OBJETS_EN_METHODE_MAX = 2`)*, les deux premiers de la liste de priorité *(`rangDe`)*, un seul devoir, la séquence du palier *(`sequenceDeMethode`)*. *(ii)* **un exercice par objet ouvert** et *(iii)* **l'entrée bornée** n'existent pas. ⛔ **Ce que « la semaine 1 » veut dire** — arbitrage de fabrication : **l'élève n'a AUCUN objet déjà servi sous le gabarit** *(`dejaServis` vide)*, pas « le premier cycle du gabarit en production » — un élève inscrit en octobre vit sa semaine 1 en octobre. Semaine 1 ⇒ `max = 2` pour tous ; ensuite **`max` = 1 en `tc_seul`, 2 en `hlp_seul` et `bi_classe`** *(`budgetDeLEleve(...).situation`, `utils/routeur/budget.ts`)*, **et seulement après (i) et (ii)**.
9. ⚠️ **« Si le budget le permet encore » se juge sur la SÉQUENCE ENTIÈRE, pas exercice par exercice.** Mesuré le 05/09 : 1 ≈ 7 min la paire, 3 ≈ 2, 4 ≈ 12, 2 ≈ 10 sans mesure — **une trentaine de minutes par objet** *(`01-` §4, règle 3)*. PB6 pose « tant qu'il en existe un qui tienne » : sans garde, il poserait la fiche et la paire d'un troisième objet et s'arrêterait au 3 — **une méthode à moitié posée n'est pas une entrée**. Un objet neuf entre **si sa séquence tient sous le plafond après (i) et (ii)**, sinon il attend, **et la décision le dit** *(fait quand 2 : « le budget épuisé, aucun objet neuf n'entre et la décision le dit »)*. ⚠️ Les durées sont celles de la doctrine *(`dureeExercice`, indexées sur l'objet)*, pas les durées réelles : Louis a décidé de ne pas les recaler avant la fin de la semaine 2. **Tu ne les touches pas.**
10. ⛔⛔ **UNE SEULE PHASE B.** `poserLaSemaine` *(`semaine.ts`)* est **pure**, et **le pull de C6-L3 la rejoue avec `reprise`** pour servir « le suivant dans l'ordre que la phase B aurait produit ». Une seconde fonction de pose « pour le gabarit » serait une seconde lecture de PB1-PB6, et *« deux lectures d'une même règle finissent par diverger »*. **Ton ordre par objet entre par `candidatsPour`** *(qui reçoit déjà `dejaPoses` et le vivier)* et par ce que `poserLaSemaine` reçoit — un rang ou un ordre explicite sur le `Candidat` —, jamais par une boucle à côté.
11. ⭐ **Ce que la règle 4 fait à PB1 et à PB3 sous le gabarit — arbitrage de fabrication, à confirmer.** Aujourd'hui, entre les candidats d'une même compétence, **PB1** *(le plus petit grain)* puis **PB3** *(change de cran, de mode, de grain)* puis le tirage choisissent — et sous le gabarit le grain **est** l'objet *(phrase = micro, argument = méso)* : PB1 servirait toujours `phrase`. La règle 4 dit autre chose : **l'objet dont l'observable est non acquis, puis le moins mesuré, puis le tirage** ; et la règle 6 pose « pas deux exercices de suite sur le même objet » **à côté de PB3**. Lecture retenue : **la règle 4 classe les objets ouverts et les clés d'un objet ; PB1 ne joue plus entre objets sous le gabarit ; PB3 gagne un quatrième terme, l'objet ; PB2 reste dure ; le tirage journalisé garde le dernier mot** *(`journal.tirer('phase_b')` existe — ne tire pas seul, §11 point 5)*. **Porte fermée** *(`gabarit_actif` OFF, ou instance sans clé)*, **PB1-PB3 servent comme hier, à l'octet — un test le prouve.**
12. ⚠️ **« Un exercice par objet ouvert et par cycle » borne la POSE, pas le vivier.** Le vivier garde toutes les instances de l'objet *(la porte et la quarantaine ont déjà filtré)* ; c'est la phase B qui s'arrête à un par objet ouvert — **PB5 compris** *(au second tour, l'objet déjà servi ce cycle ne reçoit pas un second exercice ; le budget qui reste va à (iii), puis à la voie mixte)*. ⚠️ **Et le pull ?** Un bonus est « un exercice normal » *(`01-` §5)* : arbitrage de fabrication — **il respecte la même borne** *(même phase B, `reprise`)*, et quand plus rien ne reste, le pull le dit *(le motif d'arrêt de `SemainePosee.journal`)*. Dis-le au relevé ; si Louis veut que le bonus rouvre l'objet, c'est une ligne à changer, pas une seconde règle.
13. ⭐ **Le cran servi à un objet ouvert = « le cran non tenu le plus bas », PARMI CE QUE LA PORTE OUVRE.** La porte du registre *(C7-L5)* reste le dernier filtre de la couche 4 — **tu ne la rouvres pas**. Le 1 tenu et le 4 ouvert non tenu ⇒ le 4 ; un cran en `sonde` *(deux échecs au cran d'en dessous)* se sert comme sonde de montée, marqué `sonde_montee` *(M-e, `avecLaSondeDuRegistre`)* — **rien de neuf** dans cette mécanique.
14. ⭐ **Règle 1 — « entrent les objets dont une clé porte l'observable de la compétence en tête de la liste »** : l'entrée se lit **par la clé**, pas par `competences[]` de l'objet. La compétence de la clé est `exercices_problemes.observable_competence`, et elle est aussi `exercices.observable_isole_competence` sur l'instance *(304 sur 304 mesurés)*. `bornerLaMethode` ordonne déjà par `ciblables`, qui sous le gabarit se réduit à cette compétence : **vérifie-le sur la banque**, ne le suppose pas.
15. ⚠️ **En segment 2, « la compétence en tête de la liste » est celle de la CALIBRATION** — `listeDePriorite` rend `regle: 'calibration'`, la moins mesurée d'abord, avec `grains` *(micro et méso pour l'Argumentation et la Structure, méso seul pour l'Expression)* et `crans` *(la bande dure)*. La prod y est jusqu'au **14/09** *(le segment 3 commence à ce cycle — mesure-le par `segmentDuLundi`, `utils/routeur/segments.ts` : `SEMAINES_SEGMENT_2 = 2`)*. **Tes règles doivent tenir dans les deux régimes**, et le 14/09 est précisément le cycle où elles mordent pour la première fois — R1, R2, R3 s'allument le même jour.
16. ⚠️ **La semaine de méthode reste l'exception à l'espacement** *(`01-` §3, §5)* : le même devoir aux crans de la séquence. `constituerLeVivier` le sait *(`porte !== 'methode'`)*. Ne l'étends pas à (ii) : un objet **ouvert** reçoit son exercice **sur un autre devoir** — c'est le retour espacé, « et ce n'est pas une lenteur » *(règle 2)*. Sans devoir frais, `degrade` et `devoirsManquants` — **déjà là**.

### C. L'observable — élire, compter, apparier

17. ⛔⛔ **LA CLÉ D'UNE INSTANCE N'EST PAS LUE PAR LE VIVIER.** `vivier-serveur.ts` sélectionne `exercices_cas(materiau_id)` — **ajoute `probleme` à cette même jointure**, pas une seconde requête *(1000 lignes, `lirePagine`)*. `InstanceDuVivier` gagne `cle: string | null` et `observable: { code, competence } | null`, résolus par `doctrine.problemes[cle]` *(`ProblemeDoctrine.observableCode`, `.observableCompetence`, `.observableRoute`)* — **la doctrine est déjà chargée une fois pour tous** *(`chargerDoctrineDepuisBase` dans `poserLesSemainesDuRouteur`)*. Une instance de la banque 1.4 n'a pas de clé : elle passe **comme hier**.
18. ⚠️ **19 clés sur 205 n'ont AUCUN observable** *(`observable_route = false`, `observable_code` nul — ex. `argument.garant.trop_large`, `argument.preuve.affirmation`)*. La fabrique les écarte avant tout appel, mais **une instance qui en porterait une n'a rien à élire** : la règle 4 ne peut pas la classer — elle passe **après** celles qui ont un observable, avec un motif journalisé. **Ne plante pas, n'invente pas un observable.**
19. ⭐⭐ **LES CODES DE LA GRILLE SONT CEUX DES INSTRUMENTS — VÉRIFIE-LE AVANT D'APPARIER.** Mesuré sur la fixture : 52 codes distincts *(`garant_present`, `lien_explicite`, `bloc_unite`, `question_specifique`…)*, le même espace de noms que `competences_mesures.observables` *(jsonb par code)* et que `instrumentDuRouteur(competence).observablesMesure` *(`utils/moteur/etat-serveur.ts`)*. ⚠️ **Mais un code de la grille absent de l'instrument est un code que la chaîne ne mesure pas** : « le moins mesuré » vaudrait **0 pour toujours** et le mettrait en tête à chaque cycle. **Compte-les à part, journalise-les, et dis au relevé combien il y en a par compétence.** *Le « se juger » a payé le même piège le 06/09 : il appariait un NOM à un CODE et n'appariait rien nulle part.*
20. ⭐ **« Non acquis (fenêtre d'évidence) » existe déjà, ne le recalcule pas.** `etatDesObservables(fenetreDEvidence(comptent), instrument, requis)` *(`utils/routeur/observables.ts` ; appelé à `etat-serveur.ts` l. 234)* — `mesuresQuiComptent` retire ce qui précède `statut_recette_pose_le` **et les sondes de montée** *(M-e)* ; `estAcquis` = taux **strictement** > 2/3 ; « tout observable est réputé non acquis au départ » — **au premier cycle, tout est non acquis**, et le départage passe au « moins mesuré », puis au tirage. ⚠️ Le calcul demande la **fiche** *(`observablesRequis(fiches.get(competence))`)* : `composerPourUnEleve` ne les lit pas — `lireLesFiches(admin)` existe *(`donnees.ts`)*, **une fois pour tous**, pas par élève.
21. ⚠️ **« Le moins mesuré (R5 au grain de l'observable) »** : le nombre de mesures de l'élève dont `observables[code]` porte une valeur — **`n/a` n'est jamais 0** : il sort du compte *(`tauxDeReussite`, la chaîne le tient)*. Arbitrage de fabrication : **les sondes de montée comptent** — elles mesurent l'observable, M-e ne les neutralise que pour la fenêtre et la stagnation. R5 « se compte sur TOUS les exercices » *(`01-` §6)*. Dis-le.
22. ⚠️ **Le registre ne dérive ni le 6 ni le 8** *(`issueDuDepot` rend `null` — « pas fait » de C7-L5, et il le reste)* : un objet ne peut pas être « tenu » sur une bande qui contient le 6 ou le 8 *(C, B, A)* — piège 4 les fait sortir de la bande **tant que la banque 1.5 n'en porte pas** *(0 exercice 6·8 en 1.5)*. **Le jour où la banque en aura, le registre devra les dériver** : une ligne dans la boîte de « C7-L5 · crans 6·8 au registre ».
23. ⭐ **Aux crans 6 et 8, rien à réécrire** : l'instance `exerce` *(`couvertureDeLInstance`, `isole = false`)*, PB4 pose ses cibles secondaires *(`candidatsPour`, `PLAFOND_CIBLES_PAR_GRAIN` : 2 au méso, 3 au macro)*, et on y monte par le registre du cran 2 *(`ECHELLES`, `[2, 6, 8]`)*. **Fait quand 5** *(« au cran 6, l'exercice porte une cible secondaire quand le grain l'autorise »)* se recette sur la banque **1.4** *(mesuré en prod le 06/09 : **14 instances `concu` non bloquées aux crans 6·8 — 7 et 7 —, plus 14 `a_concevoir` bloquées** ; ⚠️ `retirer-banque-14.mjs --prod --applique`, étape 4 de la bascule, les met `bloque` — après dimanche, cette recette se fait en bac à sable ou sur un décor)* — pas sur la 1.5, qui n'en a pas.

### D. Le journal et les compteurs — le jsonb, jamais une colonne

24. ⛔ **`routeur_decisions` a 17 colonnes, mesurées le 06/09, et tu n'en ajoutes aucune** : `id, eleve_id, cycle_lundi, exercice_id, cible_retenue, regle_declenchee, alternatives_ecartees, sondes_retenues, propositions_iso_duree, choix_eleve, borne_amont, override_prof, tirage_aleatoire, degrade, created_at, etat_escalade, bonus`. **L'état de l'objet, l'observable élu et son motif** vont dans `alternatives_ecartees` *(`lignesDeDecision`, `decision.ts`)*, à côté de `porte_registre`, `devoir`, `methode`, `devoir_manquant` — propose `objet: { etat, entree, observable: { code, competence, motif } }`, plus la liste des objets **écartés** et pourquoi *(« tenu », « un par cycle », « entrée hors budget », « sans observable »)*. ⚠️ **La forme de `sondes_retenues` est figée par ses lecteurs** *(`utils/chaine/contexte.ts`, `app/prof/routeur/serveur.ts`)* : tu n'y touches pas.
25. ⭐ **Les deux compteurs se DÉRIVENT du journal, en requête** *(`01-` §11, point 7 : « l'agrégation se fait en requête »)* : **objets ouverts par élève × cycle** *(les décisions du cycle, groupées par objet et par état)* et **temps de passage méthode → ouvert → tenu** *(la première décision `methode` de l'objet, la première `ouvert`, la première où il est absent parce que `tenu`)*. **Un banc, pas une table** : `scripts/recette/banc-objet.mjs [--prod]`, lecture seule, sur le patron de `banc-devoir-quarantaine.mjs` *(`--import ./scripts/register-calibration-resolver.mjs`, clés `PROD_*` sur `--prod`)*. C'est lui qui rend le **fait quand 6** : sur une semaine réelle, ce que la règle aurait posé à la place de ce qui l'a été.
26. ⚠️ **Les écarts se NOMMENT** — `MotifDEcart` gagne ce qu'il faut *(`objet_tenu`, `objet_un_par_cycle`, `objet_entree_hors_budget`, `sans_observable`…)* et `EcartDuVivier.detail` dit le motif en clair : « un vide expliqué, jamais un onglet qui clignote ». ⚠️ **Mesuré le 06/09 : seuls `vivier.ts` et `cycle-serveur.ts` nomment `EcartDuVivier`** — les écarts vivent au bilan du routeur *(`BilanDuRouteur.ecartsDuVivier`)*, et la console du professeur *(`app/prof/routeur/serveur.ts`)* lit `routeur_decisions`, pas le bilan. **Ne promets aucun affichage** : ton journal `alternatives_ecartees.objet` *(piège 24)* est ce que la console pourra lire, et c'est là que le motif doit être en clair.

### E. Ce que la fabrication a mesuré, et que tu remesures

27. **Les deux bases, 06/09 au soir** *(PostgREST, lecture seule)* — §③. ⭐ **En bac à sable, RIEN ne peut être servi tel quel** : 328 exercices 1.5 en `a_concevoir`, et `STATUTS_SERVABLES = ['concu', 'assigne']`. Deux voies : `scripts/recette/passer-concu.mjs` *(sans `--prod` ; relevé d'abord, `--applique` ensuite)*, ou le patron de `epreuve-porte-registre.mjs`, qui **force `concu` en mémoire** sur les instances lues et appelle `constituerLeVivier` **sans rien écrire**. Le registre du bac à sable est **vide** *(0 dépôt du routeur)* : `epreuve-porte-registre.mjs` dérive des **registres de décor** *(deux réussites, une seule, deux échecs)* — c'est ton modèle pour l'état de l'objet.
28. ⚠️ **La garde d'idempotence** : un élève qui porte **déjà une décision imposée sur le cycle** n'est pas reposé *(`dejaServi`, `poserLaSemaineDUnEleve`)*. Pour rejouer une pose en bac à sable : `scripts/recette/essai-cron-hebdo.ts` *(`--constat` / `--essai` / `--retire`, registre AVANT, bac à sable seulement)* ou `poserLesSemainesDuRouteur(admin, fuseau, jour, { cycleDemande, elevesDemandes })`. **Retire ce que tu poses** : le décor va en base avec sa marque, et se retrouve par elle.
29. ⛔ **`supabase-js` ne lève pas** : `{ error }` partout ; une lecture mal posée se lit comme une base vide. `lirePagine` **pagine, ordonne sur une clé unique, confronte au `count`** — PostgREST plafonne à 1000 lignes **sans rien signaler**. Tout lecteur nouveau passe par lui. Et **une lecture ratée n'est pas un état** : registre illisible ⇒ `incidents`, et l'objet reste **en méthode** *(la lecture la plus prudente — on ne ferme rien sur une erreur)*. Éprouve ce sens-là.
30. ⚠️ **`server-only`** : tout script de recette s'appelle avec `node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --import ./scripts/register-calibration-resolver.mjs …` ; un module pur n'importe **jamais** un `-serveur.ts`. **154 tests du moteur et du registre** verts à ton arrivée — mesure le compte total de `npm test` avant, et rends-le après.
31. ⚠️ **Le lot lit LE SIEN — `gabarit_actif`** *(chapitre `C7` du `07-` : chaque lot derrière lui ; §5 : « un lot lit le sien, jamais celui d'un voisin »)*. `lireLaPorteDesCrans` rend `actif: false` à OFF, et `composerPourUnEleve` en dérive déjà `devoirsServis: null`. **Ton état de l'objet suit le même drapeau** : porte fermée, la pose est **celle d'hier à l'octet**, et un test le prouve sur une même liste et un même vivier. ⛔ Pas de septième interrupteur, pas d'emprunt à `routeur_actif`.
32. ⚠️ **Le bac à sable a `juge_documents_actif` ON depuis le 06/09** ; la prod l'a à OFF, et **sans le juge, le registre ne porte de réussites qu'aux crans 1 et 3** *(les jetons — mesuré le 04/09 : 0 « ouvert » au-dessus du 3 sur 480 dépôts)*. Un objet ne devient donc jamais « tenu » en prod tant que le juge est fermé, et **ce n'est pas un défaut de ton lot** : la bascule allume le juge **avant** `gabarit_actif` *(`BASCULE_…md`, étape 5)*. Dis-le au relevé, ne le contourne pas.

---

## La convention de couture — ce que tu éprouves par EXÉCUTION

Ton lot succède à `C7-L6`, `C7-L5` et `C4-L12`, et le pull de `C6-L3` traverse ton ordre. **Cinq coutures, nommées sous la forme vérifiable** *(qui écrit · qui lit · un chemin réel y mène-t-il ?)* :

| couture | qui écrit | qui lit *(toi)* | l'épreuve |
|---|---|---|---|
| **la clé de l'instance** `exercices_cas.probleme` → `exercices_problemes.observable_code` | l'import 1.5 *(C7-L2)* · `derive-doctrine.py` | `lireLesInstances` → `InstanceDuVivier.observable` | sur les 328 exercices 1.5 du bac à sable : combien portent une clé, combien de clés rendent un observable, combien n'en rendent aucun *(piège 18)*, combien d'observables sont absents de l'instrument *(piège 19)* — **des nombres, par objet et par compétence** |
| **le registre** `verdicts_cran` + crédence + zones → `LigneRegistre` *(avec le devoir, §⑤)* | la chaîne et le juge *(C7-L1)*, l'élève *(jetons)* | `etatDeLObjet` | un registre de décor *(patron `epreuve-porte-registre.mjs`)* : deux réussites sur **le même** devoir le même jour ⇒ **pas** tenu ; deux réussites sur **deux** devoirs à un cycle d'écart ⇒ le cran suivant s'ouvre ; toute la bande tenue ⇒ `tenu` |
| **la porte et la quarantaine** `porte.de(objet)` · `devoirsServis` | `lireLaPorteDesCrans` · `lireLesDevoirsServis` | ta phase B | un objet **ouvert** reçoit **un** exercice, sur un **autre** devoir *(écart `devoir_en_quarantaine` sur les instances du devoir déjà servi)* ; en **méthode**, le même devoir aux crans de la séquence |
| **la pose et le journal** `poserLaSemaine` → `lignesDeDecision` → `persister` | C4-L12 | `alternatives_ecartees.objet` | une pose complète en bac à sable *(`essai-cron-hebdo`)* : chaque décision porte l'état de l'objet, l'observable élu et son motif ; **constaté en base par requête**, pas dans un log |
| **le pull** `bonus-serveur.ts` → `composerPourUnEleve` → `poserLaSemaine(…, reprise)` | C6-L3 | le même ordre | après la semaine posée, un pull sert **le suivant dans l'ordre par objet** ; un objet déjà servi ce cycle n'en reçoit pas un second ; plus rien ⇒ le motif le dit |

**Le script se laisse au dépôt** : `scripts/recette/couture-c7l7.mjs`, mode constat, registre AVANT, retrait par marque, contrôles comptés et **confrontés en base par requête**. **Bac à sable uniquement** — aucune écriture en production par ce lot. Plus `banc-objet.mjs --prod` *(piège 25)*, lecture seule.

**Puis le « fait quand », sur un élève de décor en bac à sable, dans l'ordre de ses six points** — chaque point avec **sa requête et son résultat**, pas « les tests passent ».

---

## Le « fait quand » — *(recopié du `07-` §2 ; ne se négocie pas en séance)*

> *Fait quand* : sur un élève de décor en bac à sable, **(1)** la semaine 1 pose deux objets en méthode, la semaine 2 pose un exercice sur chacun des deux, sur un autre devoir, et fait entrer un troisième objet en méthode *(un seul, en TC)* ; **(2)** le budget épuisé, aucun objet neuf n'entre et la décision le dit ; **(3)** entre deux objets ouverts, celui dont l'observable est non acquis passe d'abord, motif journalisé ; **(4)** un objet tenu n'est plus posé au centre et n'apparaît qu'en sonde ; **(5)** au cran 6, l'exercice porte une cible secondaire quand le grain l'autorise ; **(6)** un banc en lecture seule sur la production dit, sur une semaine réelle, ce que la règle aurait posé à la place de ce qui l'a été.

---

## Les conventions de dépôt et de clôture

- **Aucune migration, aucun interrupteur** : rien au `SUIVI_SQL.md`. Avant de lire la doctrine en base, `python3 scripts/derive-doctrine.py --verifie` doit dire **IDENTIQUE** *(17 tables, `exercices_problemes` comprise)* ; s'il dit DIVERGE, **rejouer `--sql`, jamais corriger la base à la main** — et le `--fixture` du dériveur **écrit sur stdout** : rediriger vers `utils/fabrique/doctrine.fixture.json`.
- **Ta section au `SUIVI_tests_manuels.md`** : `## C7-L7 — Le routeur par objet (séance du …)` — prouvé en séance coché **avec sa preuve**, ce qui reste décoché **avec sa condition de reprise** *(au moins : **`C7L7-x` — la première pose réelle sous la règle, lundi 14/09 après 18:00 UTC, lue par `banc-objet.mjs --prod`**, et **le premier objet « tenu » en production**, qui suppose le juge ouvert)*. ⚠️ Ni `C7-L5` ni `C7-L6` n'ont de section : n'en fabrique pas pour eux, mais dis dans la tienne ce que tu as éprouvé de leurs coutures.
- **Une source trouvée fausse se marque, elle ne se corrige pas** : `[faux]` au point de l'erreur, une ligne à DETTES de `INVENTAIRE_Non_Tranches.md` *(dépôt de conception)*.
- **Ton relevé** : `RELEVE_C7_L7_<date>.md` — **les arbitrages de fabrication en tête** *(pièges 3, 4, 8, 9, 11, 12, 21)*, confirmés ou renversés par Louis, et **les deux « pas fait » de C7-L6 que tu as fermés** *(§⑤ — décidé, pas un arbitrage)*. Ce qui est pour `C7-L8`, pour « C7-L5 · crans 6·8 au registre » ou pour la fabrique va **dans leur boîte** au `PLAN_DE_CHANTIER.md` §5, par destinataire.
- ⛔ **`git add -A` est interdit** ; l'arbre porte le travail d'autres séances. Fichier par fichier, `git status --short` avant chaque commit. **Tu commites sur la branche, tu ne pousses pas** *(§②)* : la branche part sur `main` quand tous ses lots sont joués, sur la décision de Louis — et sur `main`, elle ne change rien au cycle du 07/09 *(semaine 1, déjà servie par C7-L6)* ; elle doit y être avant le 14/09 18:00 UTC pour le cycle suivant.

---

## §H — Ce qui est hors de toi, et où ça va

- **`C7-L8` — la chaîne à l'heure de la clé** *(prompt à fabriquer après le tien)* : la mesure sur la cible et les sondes seulement, le retour sur l'observable de la clé, le « se juger » par la clé, la citation recopiée écartée. **Si ton `InstanceDuVivier.observable` ou ton journal `objet.observable` peut lui servir de canal** *(la chaîne lit déjà la décision par `exercices_depots.routeur_decision_id` — `utils/chaine/contexte.ts`)*, **dis-le dans sa boîte**, ne le construis pas.
- **Les crans 6·8 au registre** *(les seuils — « pas fait » de C7-L5)* : une ligne dans sa boîte, avec ce que ton piège 4 en fait en attendant.
- **Le PLAN au cran 2** *(jeudi 10/09, relecture de Louis)* et **les contre-exemples dans le `09-`** : la fabrique.
- **Les durées indexées sur le cran** *(rapport du 05/09 §3)* : fin de semaine 2, décision de Louis.
- **La table micro/méso/macro en objets × crans** *(rapport §4, question H)* : non tranchée — si ta règle 4 la rend inutile ou fausse, **dis-le au relevé** avec les nombres, ne la réécris pas.
- **`C9`** — les retours non écrits ; **`C10`** — la fermeture de la semaine.

---

## §I — Les entrées `C7-L6` et `C7-L7` du `07-Implementation.md` §2 — copie de ce qui est écrit dans la source

*(Chapitre **`### C7 — le gabarit des exercices`**, `07-` **2.80**, 06/09/2026. La source fait foi ; cette copie est là pour que tu n'aies rien à chercher. L'entrée `C7-L6` tient lieu de relevé pour ce lot, qui n'en a pas d'autre.)*

> **C7-L6 — Le routeur par devoir.** *(Ouvert le 06/09/2026, nuit, sur les décisions de Louis des 05 et 06/09 et les mesures du `RAPPORT_Refonte_routeur_cle_2026-09-05.md` : 37 paires « même texte deux fois au même élève » sur 480 dépôts de production ; le `09-` au plafond de ses clés — le volume vient des devoirs.)* Le vivier connaît **le devoir de chaque instance** *(`exercices_cas.materiau_id` ; au cran 2, sans matériau, la souche de l'`id_import` `ex-gab-<objet>-<clé>-c2` et le devoir du cas A de la clé)* et, par élève, **la date du dernier dépôt sur chaque devoir** ; il écarte `devoir_en_quarantaine` *(deux cycles, `01-` §8.10 ; un cycle après une erreur faite avec confiance)*, **sauf en semaine de méthode** ; la phase B pose la **séquence de méthode par palier** *(`01-` §5 : E-D fiche → paire 1 → 3 → 2 → 4 ; C-B paire 1 → 3 → fiche → 2 ; A sonde au 4 ou 9)* sur **deux objets au plus** par élève et par cycle, le cran 2 seulement si son écran est servi ; **« deux et deux » se compte sur deux devoirs à un cycle d'écart** et **les sondes partent dès deux échecs** *(`utils/registre/reussites.ts`)* ; un objet sans devoir frais **se sert quand même** *(`degrade`)* et **compte un manque par objet**, lu par la console de la fabrique ; tout se journalise dans `routeur_decisions` *(le devoir, la date du dernier dépôt, l'écart et son motif)*. Aucune migration ; derrière `gabarit_actif` ; **sur la branche `feat/ecrans-passation`**. *Fait quand* : sur un élève de décor en bac à sable, (1) un devoir déposé au cycle N n'est pas resservi au cycle N + 1 et l'est au cycle N + 2, la décision portant la date du dernier dépôt ; (2) la même clé sur un autre devoir est servie au cycle N + 1 ; (3) en semaine de méthode, l'élève reçoit deux objets et, sur chacun, la séquence de son palier sur un seul devoir ; (4) deux réussites le même jour sur le même devoir n'ouvrent pas le cran suivant, deux réussites sur deux devoirs à un cycle d'écart l'ouvrent ; (5) le banc `banc-devoir-quarantaine.mjs --prod` dit, en lecture seule, ce que la règle aurait fermé sur la semaine réelle. *Manifeste* : `01-` v5.9 §3, §5, §8.10, §11 · `10-` v0.10 §7 · `08-` v1.10 §5. ✅ **Codé le 06/09 (nuit) sur la branche `feat/ecrans-passation`** *(commit du lot, 0 migration, derrière `gabarit_actif`)* : `devoirs` sur l'instance, `lireLesDevoirsServis`, écart `devoir_en_quarantaine`, second passage « sans devoir frais » (`degrade` + `devoirsManquants`), `bornerLaMethode` (deux objets, un devoir, séquence par palier), journal `devoir` / `methode` / `devoir_manquant`, `banc-devoir-quarantaine.mjs` ; 154 tests du moteur et du registre verts. **Mesuré en production (lecture seule)** : sur la semaine du 31/08, la règle aurait écarté **36 dépôts sur 443** (22 sur `phrase`, aux crans 4 et 5 surtout), tous la même semaine ; aucun objet n'aurait été sans devoir frais. ⏳ **Pas fait** : « deux et deux » sur deux devoirs et les sondes dès deux échecs (`reussites.ts`, avec la session de l'écran), le retour à un cycle après une erreur confiante, le smoke sur un élève de décor. ⭐ **06/09 (après-midi)** : la couverture d'une instance suit le CRAN, plus le geste — le cran 2 produit et isole *(`02-` v6.7 ; commit `a938efb` sur la branche)*.
>
> **C7-L7 — Le routeur par objet.** *(Ouvert le 06/09/2026, après-midi, sur les décisions de Louis du même jour — `01-` v5.11 §4, couche 3, « L'objet : quand il se sert » — et sur une mesure : le routeur ne choisit PAS la clé. La cible est une compétence ; aux crans qui isolent, l'instance porte la compétence de l'observable de sa clé, 304 sur 304 en bac à sable, et rien ne lit ni `probleme` ni `observable_code` ; après la porte et la quarantaine, la clé sort de PB1, PB3 et du tirage. Sous le gabarit tous les crans isolent sauf le 6 et le 8 : choisir la clé, c'est choisir l'observable.)* **La couche 2 élit la compétence ; la couche 3 élit l'objet, le cran, puis l'observable, et la couche 4 le devoir.** L'objet a **un état par élève** — *en méthode*, *ouvert*, *tenu* —, **dérivé** du registre des réussites et des décisions, jamais stocké : il entre par sa semaine de méthode ; ouvert, il reçoit **un exercice par cycle**, sur le cran non tenu le plus bas, sur un autre devoir ; tenu *(« deux et deux » sur toute la bande du palier)*, il sort du centre et ne revient qu'en sonde. **La phase B pose dans l'ordre** : la méthode *(sa séquence entière)*, un exercice par objet ouvert, puis **un objet neuf seulement si le budget le permet encore** — deux objets la première semaine pour tous, ensuite **un par cycle en TC, deux en HLP et en bi-classe**. **Entre objets ouverts, puis entre clés d'un objet** : l'observable non acquis *(fenêtre d'évidence)*, puis le moins mesuré *(R5 au grain de l'observable)*, puis le tirage journalisé. **Aux crans 6 et 8** l'instance exerce, PB4 pose ses cibles, on y monte par le registre du cran 2. Départage : « pas deux exercices de suite sur le même objet », PB2 garde la compétence. **Journal** *(`01-` §11)* : sur chaque décision l'état de l'objet, l'observable élu et son motif ; deux compteurs — objets ouverts par élève × cycle, temps de passage méthode → ouvert → tenu. Aucune migration attendue *(le journal est le jsonb de `routeur_decisions`)* ; derrière `gabarit_actif`. *Fait quand* : **[le « fait quand » ci-dessus, verbatim]**. *Manifeste* : `01-` v5.11 §4 *(couches 2 et 3)*, §5, §11 · `02-` v6.7 §2.2 · `10-` v0.11 §7. Dépend de **C7-L6**, C7-L5, C4-L12.
