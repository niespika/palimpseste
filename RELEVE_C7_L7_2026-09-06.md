# RELEVÉ — C7-L7 « Le routeur par objet » (séance du 06/09/2026, branche `feat/ecrans-passation`)

**Prompt** : `PROMPT_Code_C7_L7.md` (32 pièges, fabriqué le 06/09 au soir). **Manifeste vérifié** : `01-` v5.11 · `02-` v6.7 · `10-` v0.12 *(le manifeste dit v0.11 — l'écart est le §⑥ du prompt, lu et sans effet sur le §7)* · `07-` v2.80, entrée `C7-L7` conforme au §I mot pour mot. **Branche** : `feat/ecrans-passation`, tête `0d01928` à l'arrivée ; rien poussé, rien fusionné. ⚠️ `origin/main` porte **12 commits** que la branche n'a pas *(Quazian, pilotage, C10)* — pas de rebase de mon fait. Le `main` **local** porte de plus `b1f7dc8` et `df62cde` *(non poussés)*.

**Aucune migration, aucun interrupteur touché.** Derrière `gabarit_actif`. Rien écrit en production.

---

## 0. Où en est le lot

| | |
|---|---|
| **Code** | `utils/registre/objet.ts` *(l'état, pur)* · `utils/moteur/objets.ts` *(l'ordre par objet, pur)* · `utils/routeur/cycles.ts` *(lundis et cycles, sortis de `vivier.ts` pour que le registre les lise sans boucler)* · `reussites.ts` / `reussites-serveur.ts` *(le devoir au registre)* · `vivier.ts` / `vivier-serveur.ts` *(la clé et l'observable de l'instance ; `candidatsPour` ordonne ; `bornerLaMethode` un objet par compétence)* · `semaine.ts` *(`Candidat.ordre` — le rang remplace PB1, PB3 gagne l'objet)* · `decision.ts` *(`alternatives_ecartees.objet`)* · `cycle-serveur.ts` *(le contexte des objets, `retenusPourLaPose` partagé)* · `bonus-serveur.ts` *(la même borne)*. |
| **Tests** | moteur + registre **154 → 188** *(33 neufs : `objet.test.ts` 11, `objets.test.ts` 19, `reussites.test.ts` +2 réécrits +2, `vivier.test.ts` +1)* ; avec le routeur **493 / 493** *(`ciblage.test.ts` +1)* ; `tsc` propre. **Suite entière** : `npm test` **2357 à l'arrivée → 2391 après**, **1 rouge** dans les deux cas dès la fixture régénérée, hors lot *(§3, Q6)*. |
| **Éprouvé par exécution** | `scripts/recette/couture-c7l7.mjs` *(bac à sable, `--constat` / `--essai` / `--retire`, registre AVANT, retrait vérifié par requête — joué deux fois, retiré deux fois)* · `scripts/recette/banc-objet.mjs --prod` *(lecture seule)*. |
| **Le « fait quand »** | **(1a) ✅ · (1b) ✅ · (2) ✅ · (3) ✅ · (4) ✅ · (5) ✅ · (6) ✅** — les six, après les trois décisions de Louis du 06/09 (nuit), **aux vraies lettres de l'élève de test (C en Argumentation et en Structure)**. Détail au `SUIVI_tests_manuels.md`, section `C7-L7`. |
| **Commit** | sur la branche, fichier par fichier, sans push. |

---

## 1. Les arbitrages de fabrication, confirmés ou renversés — à confirmer par Louis

Ceux du prompt *(pièges 3, 4, 8, 9, 11, 12, 21)*, et ceux que la séance a dû rendre.

| # | L'arbitrage | Ce que le code fait | Où |
|---|---|---|---|
| **P3** | « Tenu » se compte sur `sous_la_bande ∪ centre` ; `au_dessus` reste aux sondes. | `bandeDuPalier` : E-D → 1·2·3·4·5 (sondes 6·7·8) ; C → 4·5·6·7 (sonde 8) ; B → 7·8 (sonde 9) ; A → 8·9. Sans lettre : la bande d'E-D, comme `sequenceDeMethode`. | `utils/registre/objet.ts` |
| **P4** | Un cran que la banque ne porte pas pour l'objet sort de la bande. | `etatDeLObjet(…, cransDisponibles)` — la banque se lit sur toutes les instances servables (`cransPortesParLaBanque`) ; `cransAbsents` journalisé (« bande réduite : crans 2 absents de la banque »). Éprouvé : E-D sans 2 ⇒ bande 1·3·4·5, cran à servir 1. | `objet.ts`, `objets.ts` |
| **P8** | « Semaine 1 » = aucun objet déjà servi SOUS LE GABARIT *(`dejaServis` vide)*, pas le premier cycle de production. | `semaine1 = dejaServis.size === 0` ⇒ 2 objets neufs, sans borne de budget ; ensuite 1 en `tc_seul`, 2 en `hlp_seul` et `bi_classe` (`objetsNeufsParCycle`). | `objets.ts` |
| **P9** | L'entrée d'un objet neuf se juge sur la SÉQUENCE ENTIÈRE. | `minutes posées + réservées aux méthodes entamées + séquence ≤ plafond`, sinon `objet_entree_hors_budget` avec les trois nombres. La séquence = la somme des `dureeMin` des retenues de méthode de l'objet *(durées de doctrine, non touchées)*. | `objets.ts` |
| **P11** | PB1 ne joue plus entre objets sous le gabarit ; PB3 gagne l'objet ; PB2 dure ; le tirage garde le dernier mot. | Quand un candidat porte `ordre`, le tri est `rang` puis `change` (cran, mode, grain **et objet**), puis le tirage journalisé sur les ex æquo. Sans `ordre` : le code d'hier, à l'octet *(test « porte fermée »)*. | `semaine.ts` |
| **P12** | Le pull respecte « un par objet ouvert et par cycle ». | Le pull appelle `retenusPourLaPose` et `candidatsPour(…, compo.objets)` ; les déjà-posés de la reprise portent leur objet. Éprouvé en mémoire *(couture ⑤ : le second pull dit `objet_un_par_cycle`)*. ⚠️ `servirUnExerciceDePlus` lui-même n'a pas été appelé hors Next *(`next/navigation` par `utils/deroule/acces.ts`)*. | `bonus-serveur.ts` |
| **P21** | Les sondes de montée comptent dans « le moins mesuré ». | `compterLesMesuresParObservable` : toute mesure dont `observables[code]` porte une valeur ≠ `n/a`. | `objets.ts` |
| **S1** *(séance)* | **La sonde que le registre demande passe avant le cran à servir** *(piège 13 ne le disait pas)* : sur un objet ouvert, un candidat `porte: 'sonde'` prend le sous-rang 0, le cran non tenu le plus bas le sous-rang 1, une case au-dessus de la bande le sous-rang 2. | `objets.ts` — *sinon un élève qui stagne au 1 recevrait le 1 à chaque cycle.* |
| **S2** *(séance)* | **Un dépôt sans devoir connu est SON PROPRE devoir** : deux textes de la banque 1.4 sont deux textes, et le registre ne ferme pas une porte sur une absence. Une ligne de décor sans `reussitesDatees` compte comme hier. | `reussites.ts` |
| **S3** *(séance)* | **Un objet de méthode PAR COMPÉTENCE, dans l'ordre de la liste** *(`bornerLaMethode`)*. Mesuré au premier essai : `argument` + `objection`, tous deux d'Argumentation ⇒ **UN exercice posé sur 60 min**, PB2 bloquant la suite. Avec un objet par compétence : 5 exercices, deux objets. Le test L6 « deux objets au plus » reste vert. | `vivier.ts` |
| **S4** *(séance)* | **La méthode entamée passe avant les objets ouverts**, l'objet neuf après *(règle 3, et l'amendement du §5 : « seulement après avoir posé un exercice par objet déjà ouvert »)*. Rangs : 0 / 1 000 000 / 2 000 000 / 3 000 000 *(sondes des tenus)*. | `objets.ts` |
| **S5** *(séance)* | **Le journal** : `alternatives_ecartees.objet = { objet, etat, entree, palier, bande, crans_absents, cran_a_servir, semaine_1, observable: { code, competence, regle, mesures, motif }, ecartes: [{ objet, competence, motif, detail }], hors_instrument }` — sur chaque ligne ; aucune colonne. `sondes_retenues` intact. | `decision.ts`, `objets.ts` |
| **S6** *(séance)* | **Une clé sans observable, une instance sans clé, un code hors instrument** passent APRÈS, chacun avec son motif ; `hors_instrument` compté par compétence au journal. Mesuré : **0** des trois cas sur les 328 instances 1.5 du bac à sable. | `objets.ts` |

---

## 2. Les deux « pas fait » de C7-L6, fermés (décision de Louis, §⑤ du prompt)

- **(a) « Deux et deux » sur deux devoirs à un cycle d'écart** — `DepotPourLeRegistre.devoirs`, lu par `lireLesDepotsPourLeRegistre` *(`materiau_id` sur la même jointure, la souche de l'`id_import` au cran 2 par `devoirsDeLInstance`)* ; `LigneRegistre.reussitesDatees` ; `reussitesQuiComptent` *(une paire sur deux devoirs disjoints à ≥ 1 cycle — `cyclesEcoules`, jamais en jours)*. Le point (4) du « fait quand » de L6 est un test : même jour même devoir ⇒ fermé ; deux devoirs la même semaine ⇒ fermé ; même devoir à un cycle ⇒ fermé ; deux devoirs à un cycle ⇒ ouvert.
- **(b) Les sondes dès deux échecs, consécutifs ou non** — `cransOuLEleveStagne` : `filter` sur la série *(un succès entre deux ne remet plus à zéro)*.

Le `07-` peut porter ces deux fermetures à l'entrée `C7-L6`. *Reste « pas fait » de L6, hors de ce lot : le retour à un cycle après une erreur confiante.*

---

## 3. Ce que je n'ai pas tranché — questions pour Louis

**Les trois questions ont été posées à Louis le 06/09 (nuit), et tranchées — voici ce qu'il a dit, ce que le code en fait, et ce qui reste.**

**Q1 — PB2 contre la séquence de méthode.** *Louis : « La règle valait quand on mesurait des compétences au complet. Là on regarde des observables. Donc on peut transformer la règle : jamais deux fois de suite le même observable. L'esprit de PB2, c'était l'interleaving. »* ✅ **Fait** : `poserLaSemaine(…, { pb2: 'observable' })` sous le gabarit — la permutation à la couture ne joue plus sur la compétence ; un candidat qui redonnerait l'observable du précédent est écarté ; un candidat sans observable (banque 1.4) reste sous la règle d'hier ; à l'octet sans l'option *(`Candidat.observable`, posé par `candidatsPour`)*. ⚠️ **Ce qui reste, mesuré** : la séquence de méthode d'un objet porte UN SEUL observable *(même clé aux crans 1·3·2 : `objection` = `objection_traitee` aux trois)* — elle ne s'enchaîne donc que si un autre objet alterne. Semaine 2 : `objection` entre *(cran 1)* et son 3 attend ; le pull, avec 30 min de quota, ne sert rien *(« un tour complet sans qu'aucun exercice ne tienne »)*. ✅ **Tranché par Louis le 07/09 : la méthode est l'exception à PB2**, comme à l'espacement — *(et tous les exercices d'une même clé portent le même observable, pas seulement 1·3·2)*. Codé : un cran de la séquence de méthode qui suit le cran d'avant sur le même objet n'est pas « deux fois de suite la même chose » *(`ordre.methode`)* ; un objet ouvert reste sous PB2. **Mesuré** : semaine 2, `objection` 1 → 3 enchaînés *(le 2 fermé par la porte, Q4)*, 4 exercices ; le pull ne sert rien parce que les deux ouverts ont eu le leur et la méthode est posée entière — c'est le motif attendu.

**Q2 — La bande dure du palier C.** *Louis : « Il faut ouvrir ce que le 01 disait. Le fonctionnement des crans a complètement changé : voir ce qui fait du sens au vu du nouveau fonctionnement des crans et des principes pédagogiques. »* ✅ **Lecture de la séance, implémentée, à confirmer** : sous le gabarit, **la porte du registre est l'ordre dur, la bande du palier indexe** — elle dit ce qui est *tenu* *(`sous_la_bande ∪ centre`)*, où sont les *sondes* *(`au_dessus`)* et quelle *séquence de méthode* ; mais **le cran d'en dessous qu'une échelle exige pour tenir un cran de la bande se sert** *(`prerequisDeLaBande` : à C, bande 4·5·7, prérequis 1·3 ; rien de tenu ⇒ le 1 ; le 1 tenu ⇒ le 3 ; les deux ⇒ le 4)*, et la bande dure *(`entree.crans`)* laisse passer ce que le registre exige *(`ordre.rattrapage`)* — la séquence de méthode comprise *(C-B : 1·3·2)*. **Fondements** : `10-` §7 *(« la hiérarchie des crans n'a de sens que si on la respecte » ; « la distribution par palier choisit PARMI les crans débloqués »)* ; `01-` §4 règle 2 *(« le cran non tenu le plus bas »)* ; le rapport du 05/09 §6 *(fading Renkl / Atkinson : 3 → 5 → 7 et 1 → 4 → 9 SONT un fading ; Kalyuga : méthode courte pour C-B — c'est la séquence 1·3·2 du §5, pas une exemption)*. **Ce que ça ne change pas** : E-D *(aucun prérequis : la bande commence en bas des échelles)*, « tenu », les sondes. **Mesuré** aux vraies lettres *(C)* : semaine 1 `argument` 1·3 et `exemple` 1·3 ; semaine 2 les deux ouverts servis **au 1**, sur un autre devoir, motif « sous la bande, exigé par le registre ». ⛔ Le `01-` §4 *(« ne sortent pas d'E-D »)* est donc contredit sous le gabarit : **à porter au `01-` par une passe de conception**, je n'y ai rien marqué.

**Q3 — Un objet ouvert sans compétence élue.** *Louis : « Je ne crois pas que R2 dit une seule compétence du trio par cycle. Si j'ai tort, trouver un moyen pour que R2 serve plus de compétences du trio. »* **Mesuré dans le code et la source** : `elireR2` rend UNE cible *(« Cible = la plus faible du trio », `01-` §6)* ; les autres n'entrent que par la dette R5, à K ≈ 15 exercices. ✅ **Fait** : `listeDePriorite(…, { trioEntier: true })` sous le gabarit — après la plus faible, **le reste du trio éligible** *(R0, seuil du Questionnement)*, du plus en retard au moins en retard puis par l'ordre de levier, en `R2`, motif « trio entier sous le gabarit » ; journal `R2.trioEntier`. À l'octet sans l'option. **Mesuré** : semaine 2, liste `synthese(R3) → argumentation(R2) → structure(R2) → expression(R5)` — `exemple` *(Structure)* a eu son exercice.

**Q5 est fermée le 07/09** *(Louis : « tu peux faire le bornerLaMethode maintenant »)* : la séquence de méthode suit le palier de la compétence **du devoir retenu** — sous le gabarit, un devoir porte une clé, donc une compétence ; chaque devoir reçoit la séquence de sa compétence et c'est avec elle qu'on compte sa couverture *(`bornerLaMethode`, test « `exemple` : Structure D contre Expression B »)*. **Q4 reste** *(le cran 2 en méthode fermé par `CRANS_DE_LA_METHODE`)* — boîte de C7-L5. **Q6** *(le test de la fabrique)* — boîte de la fabrique.

---

## 4. Ce que la séance a mesuré

**Les deux bases, par requête (06/09 soir).** Bac à sable : `gabarit_actif` ON, `juge_documents_actif` ON, `routeur_actif` ON ; **328 exercices 1.5** tous `a_concevoir` *(crans 1 : 76 · 2 : 24 · 3 : 38 · 4 : 76 · 5 : 38 · 7 : 38 · 9 : 38 — aucun 6·8)*, 41 clés ; 0 `routeur_decisions`, 0 dépôt du routeur ; `exercices_problemes` 206, **19 sans observable**. Production : `gabarit_actif` OFF, `juge_documents_actif` OFF, `routeur_actif` ON ; 0 exercice 1.5 ; 480 décisions, toutes sur `2026-08-31` ; `exercices_problemes` 205, 19 sans observable. **La bascule de dimanche n'est pas jouée.**

**La dérivation.** `derive-instruments.py --verifie` disait DIVERGE *(la branche portait l'empreinte du `07-` 2.76)* : `--ecris` rejoué, version et empreinte seules *(2.80)*, IDENTIQUE. `derive-doctrine.py --verifie` : **16 tables sur 17 IDENTIQUE en bac à sable, `exercices_types_crans` DIVERGE** — 13 lignes, la `couverture_observables` des 13 objets **au cran 2** *(le cran 2 isole)* ; `--sql` rejoué en bac à sable *(une transaction)*, **17 / 17 IDENTIQUE** ; fixture régénérée, IDENTIQUE.

**Couture ①** *(328 instances 1.5, `lireLesInstances`)* : 328 avec clé, 41 clés, **328 avec observable, 0 sans, 0 hors instrument** ; par compétence : structure 159 · argumentation 97 · expression 56 · questionnement 16 ; par objet : transition 87 · argument 53 · paragraphe 52 · objection 52 · exemple 35 · phrase 25 · plan 24 ; `ciblables` = {compétence de l'observable} sur 328 / 328.

**Les durées de doctrine** *(`dureeExercice`)* : argument / objection / exemple / paragraphe / plan 1 : 8 · 2 : 20 · 3 : 17 · 4 : 8 ⇒ séquence E-D **53 min avec le 2, 33 sans** ; transition / phrase 1 : 3 · 2 : 10 · 3 : 8 · 4 : 3 ⇒ 24 / 14. Le prompt annonçait « une trentaine de minutes » aux durées réelles — les durées de doctrine, que je ne touche pas, disent le double pour les objets méso.

**Le banc en production** *(`banc-objet.mjs --prod --cycle 2026-08-31`, lecture seule)* : 480 décisions, 58 élèves, **4,38 objets par élève** *(3 à 5)* ; la règle aurait gardé **2 objets par élève** et posé **306 exercices** *(la séquence du palier, sans cran 2)* contre 261 réellement posés sur ces deux objets ; **218 exercices sur d'autres objets seraient tombés** *(transition 89 · phrase 41 · exemple 25 · argument 20 · paragraphe 17 · objection 17 · plan 9)*. Les compteurs ① et ② se remplissent à la première pose sous la règle.

**Les chemins du prompt qui n'étaient pas les bons** : `lireLesFiches` vit dans `utils/routeur/donnees.ts` *(pas `utils/moteur/donnees.ts`)* ; `candidatsPour` dans `vivier.ts` *(pas `semaine.ts`)*. Sans conséquence.

---

## 5. Ce qui va dans les boîtes (`PLAN_DE_CHANTIER.md` §5)

- **C7-L8** : `InstanceDuVivier.observable` *(code, compétence)* et `alternatives_ecartees.objet.observable` sont deux canaux prêts pour « la mesure sur la cible et les sondes seulement » et « le retour sur l'observable de la clé » — la chaîne lit déjà la décision par `exercices_depots.routeur_decision_id`.
- **C7-L5 · crans 6·8 au registre** : `etatDeLObjet` fait sortir de la bande les crans que la banque ne porte pas ; le jour où la 1.5 aura des 6·8, `issueDuDepot` doit les dériver, sinon aucun objet n'est jamais tenu à C, B, A. Plus Q4.
- **La fabrique** : Q6 *(le test du cran 2)* ; et la banque 1.5 ne porte **aucune** des 19 clés sans observable — rien à faire, à savoir.
- **La session des écrans** : Q4 ; la table `exercices_types_crans` du bac à sable a été re-dérivée *(cran 2 isole)*.

---

## 6. Conditions de reprise (au `SUIVI_tests_manuels.md`)

`C7L7-x` — la première pose réelle sous la règle, lundi 14/09 après 18:00 UTC, lue par `banc-objet.mjs --prod` *(suppose la branche sur `main` avant)* · le premier objet « tenu » en production *(suppose le juge ouvert : sans lui, aucune réussite au-dessus du 3)* · le pull réel au bouton · la lecture de Q2 confirmée par Louis et portée au `01-` · Q4, Q5.

---

## Annexe — ce qui est écrit au `01-routeur.md` (v5.12, 07/09, sur le « go » de Louis)

*(Porté dans l'arbre de travail du dépôt de conception, **non commité** — on n'y commite que sur demande. Deux endroits : le §4 couche 3, après « ce qui se mesure », et la règle 2 de l'amendement du 06/09 ; le texte porte aussi les deux décisions de la phase B — PB2 sur l'observable, la méthode exception — et le trio entier.)*

> ⭐⭐ **AMENDEMENT DU 07/09/2026 — SOUS LE GABARIT, LA BANDE INDEXE, LE REGISTRE ORDONNE** *(décision de Louis, sur la mesure de `C7-L7` : un élève à C ne reçoit jamais le 1, donc ne tient jamais le 4)*. La table ci-dessus reste la lecture en vigueur pour la banque servie sous l'ancien régime. **Sous le gabarit, un cran ne s'ouvre sur un objet que par le registre** *(`10-` §7 : le cran d'en dessous de son échelle — `1 → 4 → 9`, `3 → 5 → 7`, `2 → 6 → 8` — tenu par « deux et deux »)*, et la bande du palier de la compétence cible ne dit plus *quels crans l'élève peut recevoir* : elle dit **trois choses**. *(1)* **Ce qui doit être tenu pour que l'objet le soit** — `sous_la_bande ∪ centre` *(règle 5)*. *(2)* **Où sont les sondes de montée** — `au_dessus` *(M-b)*. *(3)* **Quelle séquence de méthode** s'applique *(§5 : E-D, C-B, A)*. **Le cran servi à un objet ouvert est le cran non tenu le plus bas — de la bande, ou de ce que ses échelles exigent en dessous** : un élève à C, dont la bande est `diagnostic_nomme · transformation_nommee · production_etayee · transformation_aveugle`, reçoit d'abord `diagnostic_guide` *(le 1, qu'exige le 4)*, puis `transformation_guidee` *(le 3, qu'exige le 5)*, puis sa bande — et **« `diagnostic_guide`, `production_guidee` et `transformation_guidee` ne sortent pas d'E-D » cesse de valoir sous le gabarit** : ils sortent, exactement là où le registre les exige, et nulle part ailleurs. *Ce que cela coûte, et c'est le prix de la maîtrise (Kulik 1990) : un élève à C qui réussit tient le 1 en deux cycles au moins (deux devoirs, un cycle d'écart), puis le 3, avant d'atteindre sa bande — de l'ordre de quatre cycles ; les sondes de montée du §8.8 ne raccourcissent ce chemin que sur deux échecs, pas sur deux réussites.* **Ce que cela ne change pas** : E-D *(sa bande commence en bas des trois échelles : aucun cran en dessous)*, « tenu », les sondes, et la distribution 80/20 du §7 — qui reste une préférence, jamais un filtre.

Et à la **règle 2** de l'amendement du 06/09 *(couche 3)*, remplacer *« sur le cran non tenu le plus bas »* par *« sur le cran non tenu le plus bas — de la bande, ou de ce que ses échelles exigent en dessous (amendement du 07/09) »*.


---

## 07/09 (soir) — l'objet neuf s'élit par la règle 4, le devoir se tire (`01-` v5.13)

**Mesuré** avant la décision : sous `bornerLaMethode` tel que codé, les 63 élèves de production auraient reçu lundi **les mêmes trois devoirs** — l'objet était le premier par l'alphabet de chaque compétence atteinte (`argument`, `exemple`, `phrase`), et le devoir le premier par identifiant à couverture égale (`garant-circulaire`, `attache-absente`, `tenue-sens-perdu`). Les 35 autres clés n'auraient servi à personne avant le 14/09. Louis : « il faut que l'objet soit décidé selon les mêmes grands principes que le reste ».

**Codé** *(branche `feat/ecrans-passation`)* : `bornerLaMethode` reçoit une `ElectionDeLaMethode` — `score` (la règle 4 sur les instances en méthode de l'objet : `scoreDeLObjetEnMethode`, `objets.ts`, journalisé `methode|<objet>` dans `journal.elections`) et `tirer` (le tirage de la décision, journalisé « methode ») ; à rang de compétence égal, le score puis le tirage ; le devoir, à couverture égale et jamais servi, se tire. `retenusPourLaPose` passe l'élection sous le gabarit seulement ; sans élection, l'ordre d'hier (nom, identifiant) reste — les tests d'hier passent inchangés, deux tests neufs (`vivier.test.ts`). `tsc` vert, 2423/2424 (le rouge est `verifie-import-gabarit.test.ts`, hors lot, connu).

**Ce que ça suppose** : plusieurs devoirs par clé. `vague-gabarit.py --encore --deja …` (conception, 07/09) fabrique le n-ième devoir d'une clé sur un sujet qu'elle n'a pas reçu, souche `gab-<objet>-<clé>-d<n>` ; vague v3 lancée le 07/09 au soir sur les 38 clés (`gabarit-v3.json`). ⚠️ Le cran 2 des devoirs neufs n'est pas fabriqué par l'outil (dérivation à part, comme le 06/09).

**Non codé, à trancher** : la séquence de méthode sur DEUX devoirs (1·3 sur l'un, 2·4 sur l'autre — Louis, 07/09 : « si on fait 1, 3, 2, l'élève ne va que recopier le 3 dans le 2 ») ; le `01-` §5 dit encore « la séquence entière sur un seul devoir ».

**07/09 (soir, suite) — la séquence sur DEUX devoirs, tranchée et codée** (`01-` v5.14 §5) : `partsDeMethode(palier, cran2Servi)` — E-D `{premier: [1,3], second: [4,2]}`, C-B `{[1,3], [2]}`, A `{[4], []}` ; `sequenceDeMethode` = les deux parts à la suite (E-D devient `1·3·4·2`). `bornerLaMethode` élit le premier devoir sur la couverture de la première part, puis un SECOND devoir, différent, sur la seconde part de la MÊME séquence (celle de la compétence du premier) ; sans second devoir, la seconde part n'est pas posée et l'écart dit « il manque un devoir ». `methode.devoir` porte le devoir de chaque instance, `methode.rang` l'index dans la séquence entière. Tests : le « devoir unique » réécrit en « deux devoirs », un test d'un seul devoir. `couture-c7l7.mjs` : l'attente « un seul devoir » devient « deux au plus ». `tsc` vert, 2423/2424.
