# RELEVÉ — C5 · L4 — Les onglets de la lecture

**Séance du 27 août 2026.** Aucune migration. Aucun interrupteur posé, allumé ni éteint.
`npx tsc --noEmit` vert · `npm test` **1673 / 1673, 0 échec** *(le seul rouge, antérieur au lot, a
été levé en fin de séance sur décision de Louis — §0)* · `npx eslint` **0 erreur** *(16
avertissements, tous dans des fichiers que ce lot ne touche pas)*.

| | Ce qu'il fallait | État | La preuve |
|---|---|---|---|
| **①** | les trois onglets prof — Livres · Exercices · Paramètres | **FERMÉ** | `components/nav/configModules.ts` ; 8 tests de `utils/codex-onglets/onglets-aletheia.test.ts` sur la config RÉELLE ; les 6 routes prof rendent 307 ou 404 au serveur de dev selon qu'elles existent ou non *(§2)* |
| **②** | les trois onglets élève — Livres · Exercices · Examens | **FERMÉ** | idem ; **le piège du segment dynamique est levé au navigateur** : `/eleve/modules/aletheia/nexistepas` rend **404** quand les deux routes neuves rendent **307** — `[livreId]` n'avale rien *(§2)*. Barre mobile **mesurée à 375 px** dans le vrai navigateur, avec la vraie police : 64 + 81 + 81 px, **aucun débordement**, **44 px de haut** les trois *(§2)* |
| **③** | les dix routes, chacune atteinte par un clic depuis un onglet | **FERMÉ** | §1 — inventaire AVANT / APRÈS ; les `href` sont **rendus par les lectures**, jamais tapés *(§5)* ; ⭐ **et les six allumages sont relevés À L'ÉCRAN, connecté** *(§11)* |
| **④** | la copie `abouti` qui dit ses compétences écartées | **FERMÉ** | **sur les 13 copies de PRODUCTION** : `13/13` servent leur motif, `0` se taisent *(avant : 13/13 se taisaient)* ; 7 tests purs et un aller-retour en base *(§4)* ; ⭐⭐ **et VU À L'ŒIL, avec son témoin muet à côté** *(§11)* |
| **⑤** | ⭐ **la couture** — l'exercice de lecture traversé d'un bout à l'autre, par exécution | **FERMÉ** | `scripts/recette/couture-c5l4.mjs` — **41 verts, 0 rouge**, décor semé et **retiré** *(idempotent : deux tours consécutifs)*, `--retire` au dépôt *(§5)* |

---

## §0 — Le contrôle d'entrée, et **UNE QUESTION POUR LOUIS**

**Les quatre pièces du manifeste existent et portent leur version attendue** : `07-Implementation.md`
**2.58** *(RELU ET VALIDÉ)* · `01-routeur.md` **5.7** *(VALIDÉ ET GELÉ → vaut « relu et validé »,
l'exigence explicite du manifeste)* · `02-exercices.md` **5.6** *(VALIDÉ ET GELÉ)* ·
`06-Palimpseste.md` **2.6** *(VALIDÉ ET GELÉ)*. **Aucun écart, aucune relecture d'en-tête déclenchée.**
La clause granulaire est sans objet : ce manifeste ne compte aucune fiche de compétence.

**Les trois dépendances sont en place, vérifiées par leur chemin :** `utils/generateur/`,
`app/prof/conception/reference/[id]/`, `app/prof/conception/textes/` *(C5-L1)* ·
`app/eleve/modules/aletheia/exercice/[depotId]/`, `app/deroule/PageDuDeroule.tsx`, l'option `atelier`
de `lireDepotMaison` *(C5-L2)* · `competencesDeLExercice` et `motifDesEcartees` dans
`utils/chaine/chaine.ts`, `scripts/recette/reception-c5l3.mjs` *(C5-L3)*. **Et les deux lots dont
j'ouvre les portes** : `utils/passation/`, `components/passation/EcranProf.tsx` et `EcranEleve.tsx`
*(C4-L4)* · `components/examens/EncartAConcevoir.tsx`, `SignalDeLancement.tsx`, `utils/examens/`
*(C4-L9)*. **Aucun chemin manquant.**

**Les quatre contrôles machine :**

1. ⚠️ **`npm test` N'ÉTAIT PAS VERT AVANT QUE JE COMMENCE — et le prompt m'interdit le geste qui le
   réparerait.** 1646 / 1647 ; l'unique rouge est `utils/chaine/instruments.test.ts:207` — *« les
   dérivés ont divergé de leurs sources »*. **J'ai caractérisé l'écart sans rien écrire** : en
   important `scripts/derive-instruments.py` et en rendant `fichiers_attendus()` dans un dossier
   d'essai hors du dépôt, puis en diffant. **La divergence entière tient en trois lignes**, et elle
   ne touche **aucune compétence** :

   ```
   utils/chaine/derive/MANIFESTE.ts       "version": "2.57"        →  attendu "2.58", puis "2.59"
                                          "empreinte": "b078bae…"  →  "440b903…", puis "fbecf94…"
   utils/chaine/derive/calame-retour.ts   "version_source": "2.57" →  idem
                                          "empreinte_source"       →  idem
   ```

   Le **gabarit Calame est identique à l'octet** *(3422 octets, comparé)*, et les six compétences sont
   intactes. **La divergence porte sur QUATRE VALEURS ET RIEN D'AUTRE** — vérifié par un `diff -ru`
   complet des dix fichiers dérivés contre ce que le script produirait.

   **Le rouge est né AVANT moi** : la complétion de l'entrée `C5-L4` au `07-` §2, en séance de
   fabrication de ce prompt, a fait passer le `07-` de 2.57 à **2.58**, et les dérivés portent sa
   version. ⚠️ **Et il a bougé AVEC moi** : mes deux amendements aux sections ouvertes *(§10)* le font
   passer en **2.59**. *Je le dis parce que c'est vrai : je n'ai pas créé ce rouge, mais j'en partage
   maintenant la cause.*

   > ⭐⭐ **LA QUESTION, ET JE NE LA TRANCHE PAS.** Le contrôle d'entrée n° 1 exige un `npm test`
   > **vert** avant d'écrire ; la convention *« D'ouverture de compétence »* du même prompt écrit
   > **⛔ « Ne joue pas `derive-instruments.py --ecris` »**. **Les deux ne peuvent pas être satisfaites
   > ensemble** — et le prompt m'autorise par ailleurs, **sans accord préalable**, les deux
   > amendements qui font bouger cette version-là. ⭐ **Le dépôt montre d'ailleurs que le geste normal
   > après un amendement du `07-` est de rejouer `--ecris`** : les dérivés étaient à jour en **2.57**,
   > donc quelqu'un l'a joué après les amendements de `C5-L3`. *L'interdiction du prompt vise
   > l'ouverture d'une compétence — son titre le dit — et non la métadonnée de version ; mais elle est
   > écrite sans réserve, et **ce n'est pas à moi de la lire largement**.*
   >
   > **J'ai pris l'option qui ne change rien** — je n'ai pas joué `--ecris` de moi-même — et j'ai
   > enregistré une base de départ exacte pour savoir ce que je casse. **La question est allée à
   > Louis, qui a répondu en fin de séance : rejouer.**

   ### ✅ Rejoué le 27/08, sur décision de Louis — et rien d'autre n'a bougé

   `python3 scripts/derive-instruments.py --ecris`. **Le script réécrit ses DIX fichiers ; `git status`
   n'en voit changer que DEUX**, et le diff entier tient en quatre lignes :

   ```
   MANIFESTE.ts        "version": "2.57" → "2.59"   ·  "empreinte": b078bae… → fbecf94…
   calame-retour.ts    "version_source"  → idem     ·  "empreinte_source"    → idem
   ```

   ⭐ **Les huit autres dérivés ont été réécrits À L'IDENTIQUE** — les six compétences gardent leur
   version et leur compte d'observables *(expression v3.2/9 · argumentation v4.3/9 · structure
   v3.3/8 · connaissance v2.2/8 · synthese v3.4/13 · questionnement v2.2/9)*, le **gabarit Calame est
   inchangé à l'octet** *(3347 caractères)*, `monitoring.ts` et `transcription.ts` non plus.
   `--verifie` rend **`INSTRUMENTS : IDENTIQUE (10 fichier(s) dérivé(s))`**.

   **`npm test` : 1673 / 1673, 0 échec. `npx tsc --noEmit` : vert.**

   ⭐ **La leçon, pour le prochain lot qui amende le `07-`** : *amender une section ouverte du `07-`
   fait mécaniquement diverger les dérivés ; le geste de clôture est de rejouer
   `derive-instruments.py --ecris`, et il ne touche que la métadonnée de version.* **C'est ce que le
   dépôt faisait déjà — les dérivés étaient à jour en 2.57 —, mais aucune convention ne l'écrivait.**

2. `npx tsc --noEmit` — **vert avant, vert après.**
3. **L'inventaire des routes** — exactement ce que le prompt annonçait : **cinq pages et un layout de
   chaque côté**, dix routes *(§1)*. **Aucun écart : aucun lot n'a bougé sous moi.**
4. **Les six interrupteurs, lus par requête, AVANT et APRÈS.** ⚠️ **Ils sont à ON en bac à sable ET
   EN PRODUCTION** — `exercices_actif`, `routeur_actif`, `competences_affichage_actif`,
   `fabrique_actif`, `chaine_actif`, `passation_classe_actif`, les six, des deux côtés. **Je
   RECONSTATE ce que C5-L2 avait relevé le 27/08, et je ne le répare pas** *(le `07-` §5 les dit « à
   OFF jusqu'à la recette » ; c'est un constat d'état, déjà déposé)*. **Je n'en ai allumé, éteint ni
   posé aucun** : relu en fin de séance, les six sont identiques.

---

## §1 — L'inventaire des routes, AVANT et APRÈS, avec le clic qui y mène

### AVANT — dix routes, et **trois d'entre elles sans porte d'onglet**

| Route | Avant : comment on y arrivait |
|---|---|
| `app/prof/aletheia/page.tsx` | onglet **Classe** *(la racine)* |
| `app/prof/aletheia/parametres/page.tsx` | onglet **Paramètres** |
| `app/prof/aletheia/eleve/[eleveId]/page.tsx` | racine → « Voir le détail → » · `app/prof/eleves/[eleveId]/page.tsx:82` · **`utils/integrite-preuve.ts:154`** *(`lienAnalyse`, fabriqué à la main)* |
| 🔴 `app/prof/aletheia/passation/[exerciceId]/page.tsx` | **UN SEUL lien dans tout le dépôt** : `app/prof/conception/[id]/page.tsx:258` |
| 🔴 `app/prof/aletheia/examen-diagnostique/[planifieId]/page.tsx` | **uniquement** par `EncartAConcevoir` *(href ligne 42)*, rendu **en tête de la racine prof** |
| `app/eleve/modules/aletheia/page.tsx` | **aucun onglet** — l'entrée « Modules → Aletheia » |
| `app/eleve/modules/aletheia/[livreId]/[semaine]/page.tsx` | racine → la ligne d'une séance |
| `app/eleve/modules/aletheia/[livreId]/capstone/page.tsx` | racine → « Voir la carte d'architecture → » *(sous `toutesDone`)* |
| `app/eleve/modules/aletheia/exercice/[depotId]/page.tsx` | racine → la `<section>` « Mes exercices de lecture » **posée par C5-L2** |
| 🔴 `app/eleve/modules/aletheia/passation/[depotId]/page.tsx` | **uniquement** par `SignalDeLancement`, noyé sur la racine sous les livres |

### APRÈS — treize routes, **chacune atteinte par un clic depuis un onglet**

| Route | Onglet | Le clic |
|---|---|---|
| `/prof/aletheia` | **Livres** *(racine)* | Modules → Aletheia · onglet Livres |
| `/prof/aletheia/eleve/<id>` | **Livres** | onglet Livres → tuile de classe → « Voir le détail → » |
| **`/prof/aletheia/exercices`** *(NEUVE)* | **Exercices** | onglet Exercices |
| `/prof/aletheia/passation/<exerciceId>` | **Exercices** | onglet Exercices → « Passations en classe » → « Ouvrir → » |
| `/prof/aletheia/examen-diagnostique/<planifieId>` | **Exercices** | onglet Exercices → encart « Examens diagnostiques à concevoir » → « Concevoir → » |
| `/prof/aletheia/parametres` | **Paramètres** | onglet Paramètres |
| `/eleve/modules/aletheia` | **Livres** *(racine)* | onglet Livres |
| `/eleve/modules/aletheia/<livreId>/<n>` | **Livres** | onglet Livres → la ligne d'une séance |
| `/eleve/modules/aletheia/<livreId>/capstone` | **Livres** | onglet Livres → « Voir la carte d'architecture → » |
| **`/eleve/modules/aletheia/exercices`** *(NEUVE)* | **Exercices** | onglet Exercices |
| `/eleve/modules/aletheia/exercice/<depotId>` | **Exercices** | onglet Exercices → la ligne de son exercice |
| **`/eleve/modules/aletheia/examens`** *(NEUVE)* | **Examens** | onglet Examens |
| `/eleve/modules/aletheia/passation/<depotId>` | **Examens** | onglet Examens → le signal de lancement |

**Deux renvois hors module, depuis l'onglet Exercices du professeur** — ils n'allument aucun onglet
d'Aletheia, et c'est juste : `/prof/conception/nouvelle?porte=aletheia` *(« Concevoir un exercice de
lecture → »)* et `/prof/conception/textes` *(« Les textes et leurs références → »)*, plus
`/prof/conception` *(« Tout ce qui a été conçu → »)*.

⭐ **Les trois routes 🔴 sont fermées.** ⛔ **Et aucune route n'est morte** : le balayage
route par route de `app/prof/aletheia/` et `app/eleve/modules/aletheia/` ne laisse **aucune route
sans clic**. Le script de couture le vérifie par exécution *(section F)* : chaque `href` du tableau
est **rendu par une lecture**, jamais tapé, et `ongletActifParRoute` dit sur lui l'onglet attendu.

**Ce qui a DÉMÉNAGÉ — trois blocs, et trois seulement :**

| Bloc | D'où | Vers |
|---|---|---|
| `EncartAConcevoir` *(C4-L9)* | tête de `app/prof/aletheia/page.tsx` | onglet **Exercices** prof |
| `<section>` « Mes exercices de lecture » + ses deux vides *(C5-L2)* | `app/eleve/modules/aletheia/page.tsx` | onglet **Exercices** élève |
| `SignalDeLancement` *(C4-L9)* | `app/eleve/modules/aletheia/page.tsx` | onglet **Examens** élève |

⛔ **Tout le reste n'a pas bougé** : les livres, les séances, le stepper, le capstone, la trajectoire
diagnostique, le sélecteur `classesAvecModule`, l'écran Paramètres et son voisin de
`app/prof/scriptorium/SectionParametresScriptorium.tsx` *(deux écrans, deux domiciles, une seule
table `aletheia_params` — non réunis)*. **`utils/deroule/`, `utils/chaine/`, `utils/examens/`,
`utils/generateur/` et tout le chemin du livre sortent de ce lot INCHANGÉS** — `git diff --stat` le
dit pour chacun. Le seul écart à cette règle est `utils/passation/` : **il est le §4**, et il était
demandé.

---

## §2 — Ce que `configModules.ts` porte désormais, et ce que j'ai vérifié de l'allumage

```
prof   : Livres  /prof/aletheia                (racine, aucun préfixe)
         Exercices  /prof/aletheia/exercices   prefixes: [ /prof/aletheia/passation,
                                                            /prof/aletheia/examen-diagnostique ]
         Paramètres /prof/aletheia/parametres

élève  : Livres  /eleve/modules/aletheia               (racine, aucun préfixe)
         Exercices  /eleve/modules/aletheia/exercices  prefixes: [ …/aletheia/exercice ]
         Examens    /eleve/modules/aletheia/examens    prefixes: [ …/aletheia/passation ]
```

**Trois onglets de chaque côté, et c'est le plafond** *(`AGENTS.md`, « un module = 2-3 onglets »)*.
⛔ **Pas de quatrième onglet « Diagnostic »** : la trajectoire E→A vit **sous Livres**, avec ce
qu'elle décrit. ⛔ **Aucun `?vue=`** : Aletheia est piloté par la ROUTE des deux côtés, et les deux
mécaniques ne se mélangent pas — un test l'assère, parce que `SousNavModuleMobile` choisit par
`onglets.some(o => !!o.vue)`.

⭐ **`/prof/aletheia/eleve/<id>` n'a délibérément PAS de préfixe** : la fiche d'un élève est ce que
l'onglet Livres décrit, et l'onglet racine l'attrape *(le plus long préfixe qui matche est
`/prof/aletheia` lui-même)*. Une ligne déclarée en plus n'aurait rien changé qu'un entretien.

### ⚠️⚠️ Le piège du segment dynamique à la racine — **vérifié au navigateur, pas au raisonnement**

`app/eleve/modules/aletheia/[livreId]/` est au même niveau que `exercice/`, `passation/` et mes deux
dossiers neufs. **Serveur de dev lancé, sondes au `curl` :**

```
307 → /login   /eleve/modules/aletheia              (route existante)
307 → /login   /eleve/modules/aletheia/exercices    ⭐ NEUVE — servie
307 → /login   /eleve/modules/aletheia/examens      ⭐ NEUVE — servie
307 → /login   /eleve/modules/aletheia/exercice/abc (le déroulé de C5-L2, intact)
307 → /login   /eleve/modules/aletheia/uuid-x/3     (la séance : [livreId]/[semaine], intacte)
404            /eleve/modules/aletheia/nexistepas   ⛔ RIEN NE L'AVALE
307 → /login   /prof/aletheia/exercices             ⭐ NEUVE — servie
404            /prof/aletheia/nexistepas
```

⭐ **Le 404 est la preuve** : un segment inconnu à ce niveau **n'est servi par personne** — il n'y a
pas de `[livreId]/page.tsx`, seulement `[livreId]/[semaine]` et `[livreId]/capstone`, qui exigent
deux segments. Mes deux routes statiques sont donc servies **par leur propre dossier**, et la séance
dynamique n'est pas cannibalisée. *(Next 16.2.9 ; les docs de `node_modules/next/dist/docs/` ne
documentent pas la précédence statique / dynamique pour l'App Router — d'où la sonde plutôt que la
citation.)*

### ⭐ Qu'une séance de lecture allume bien **Livres**

`ongletActifParRoute` prend **le plus long préfixe qui matche**. Sur
`/eleve/modules/aletheia/3f2504e0-4f89-11d3-9a0c-0305e82c3301/3` : la racine matche *(longueur 23)*,
`…/exercices` ne matche pas, `…/examens` non plus, et **aucun préfixe statique ne peut décrire un
UUID** → **Livres**. Idem pour `…/capstone`. **C'est ce qui rend l'ordre de Louis techniquement le
plus sûr** : Livres attrape par défaut, les deux autres se déclarent. *Tenu par un test nommé, sur
la config réelle.*

### ⚠️ Singulier / pluriel à un caractère près — **le test le plus important du fichier**

`…/aletheia/exercice/<id>` *(le déroulé, C5-L2, **il ne bouge pas**)* et `…/aletheia/exercices`
*(mon onglet)*. `ongletActifParRoute` s'en sort : `===` est faux, et `startsWith(base + '/')` aussi
**dans les deux sens** — le caractère qui suit `…/exercice` dans `…/exercices` est `s`, pas `/`.
**Un humain qui relit le fichier, lui, peut s'y tromper** : le commentaire le dit dans
`configModules.ts`, dans `regles.ts` et dans la page du déroulé, et **un test l'assère dans les deux
sens** *(il vérifie aussi que le pluriel ne tombe pas sur la racine par accident)*.

### La barre mobile à 375 px — **mesurée, pas estimée**

Le markup de `SousNavModuleMobile` a été injecté dans le vrai navigateur, à 375 px, **avec la police
réelle de l'application** *(Alegreya Sans, chargée par `app/layout.tsx`)* et les **couleurs réelles**
d'Aletheia :

```
largeurs : Livres 64 px · Exercices 81 px · Examens 81 px   (somme + gaps = 234 px)
hauteurs : 44 · 44 · 44        ⭐ la cible tactile de SousNavModuleMobile est tenue
scrollWidth 375 = clientWidth 375   ⛔ AUCUN débordement horizontal
```

**125 px de marge**, et le conteneur est `overflow-x-auto` de toute façon. *« Exercices » et
« Examens » commencent par les deux mêmes lettres ; à 81 px chacun, ils ne se tronquent ni l'un ni
l'autre.* ⚠️ **Ce n'est pas la page vivante** — une session Code ne s'authentifie pas : la vérification
à l'écran, connecté, va à la recette *(§8)*.

### Les tests

**19 tests neufs** dans `utils/codex-onglets/onglets-aletheia.test.ts`, sur la config **RÉELLE**,
jamais une copie. Et **deux tests de `onglets.test.ts` ont dû changer** : ils tenaient l'attente de
C4-L6 — *« Aletheia garde ses deux onglets prof et n'en gagne aucun côté élève (C5-L4) »* — qui était
une **garde de non-débordement**, pas une règle. Ils asserent désormais que **C5-L4 n'a touché aucun
onglet de Codex** *(libellés ET préfixes, les quatre listes)*, et le tableau des comptes passe
`aletheia: [2, 0]` → `[3, 3]`.

---

## §3 — La liste des passations de lecture : **paramétrée, jamais dupliquée**

⭐⭐ **Le geste est EXACTEMENT celui que C5-L2 avait fait sur `exercicesMaisonDeLEleve` : la cible
devient un paramètre, et le prédicat ne change pas d'un caractère.**

`passationsDeClasseCodex(admin)` → **`passationsDeClasse(admin, atelier = 'codex')`**
*(`utils/codex-onglets/liste.ts`)*. Le seul appelant existant — `app/prof/codex/page.tsx:113` — passe
désormais `'codex'` explicitement. Le `href` suit l'atelier par **`hrefDeLaPassationProf(atelier, id)`**
*(`utils/codex-onglets/regles.ts`)*, le pendant exact de `hrefDuDeroule` de l'autre côté de l'écran.
⛔ **Aucun second exemplaire : il aurait divergé au premier correctif** — et la règle qu'il porte est
précisément celle qu'il ne faut jamais laisser diverger.

⚠️ **Le dossier s'appelle `codex-onglets` et son contenu ne l'est plus.** Deux lectures sur trois
prennent leur atelier en paramètre ; le renommer coûterait plus de churn qu'il n'apporte de clarté,
et **la note vaut mieux que le déménagement** *(elle est en tête du fichier)*. ⛔ **Seul
`nombreAValiderCodex` reste attaché à Codex, et le motif est réel** : il lit `codex_travaux`, la
synthèse en classe, **dont il n'y a aucun équivalent côté lecture**. *Je n'ai fabriqué aucune file de
validation pour Aletheia, et je n'en propose pas : le retour d'un exercice de lecture est publié par
la chaîne.*

### ⭐⭐ La preuve que l'ordre ligne-de-plan-puis-mode est respecté — **par une contre-épreuve**

L'ordre ne se lit pas, il se **fait tomber**. Le décor du script de couture sème une passation de
classe qui **porte `composer`** *(`{ expression: ['composer'] }`)* et dont la **ligne de plan dit
`lecture`** — **c'est l'explication de texte**, mot pour mot le cas que le code nomme. **Si un jour
quelqu'un inverse les deux règles, ce décor le fait tomber immédiatement.** Constaté à l'exécution :

```
✓ ⭐⭐ L'ORDRE NE S'INVERSE PAS : l'explication de texte porte `composer`, sa LIGNE DE PLAN
     dit `lecture` — elle N'APPARAÎT PAS dans la liste de Codex
✓   ↳ et elle apparaît bien dans celle d'Aletheia : la ligne de plan a primé sur le mode
✓ ⛔ ni sous les Examens de CODEX — la ligne de plan `lecture` a primé sur `composer`
```

⚠️ **Et le script dit ce qu'il ne prouve pas** : sans plan validé sur la classe, il l'écrit et vérifie
alors la règle 2 *(à défaut de ligne de plan, l'atelier suit le mode)*. **Il choisit donc une classe
qui a un plan validé**, et le dit quand il n'y en a pas.

**Les trois pièges de supabase-js sont tenus** : la lecture ratée est **journalisée** *(elle ne lève
pas, elle rend `{ error }`)* et nomme désormais son atelier ; `fenetre_debut` est **NULLABLE** et le
tri départage sur **deux clés** *(l'instant, puis l'identifiant)* ; aucune ligne n'est comptée à la
main. ⚠️ **`passation_classe_actif` à OFF ne vide pas cet inventaire** — l'interrupteur garde
l'**écran**, et le commentaire le redit à l'appel.

---

## §4 — Ce que la copie `abouti` dit maintenant, la forme choisie, et son motif

### La forme : **l'état `abouti` porte son motif**. Pas de septième compteur.

C5-L3 nommait deux formes possibles. **J'ai pris la première, et voici pourquoi la seconde a été
écartée :** les six comptes de « LA FILE » sont **DISJOINTS et couvrent tout** — leur somme vaut le
nombre de copies, et un test l'assère. *« Aboutie avec écartées » n'est pas un septième état : c'est
la MÊME aboutie qui a quelque chose à dire.* Un compteur de plus aurait cassé l'invariant qui fait
la valeur de cette section, pour dire à l'altitude du lot ce que le « fait quand » demande **à
l'altitude de la copie** — *« une copie dont le traitement a abouti dit ses compétences écartées »*.
⚠️ **Un test neuf tient explicitement ce choix**, avec son motif, pour que la question ne se rouvre
pas par accident. *Ce que le professeur perd : sur treize copies toutes écartées, il lit
« 13 terminées · rien n'attend de geste de votre part » et doit déplier une copie pour voir le motif.
C'est le geste que l'écran enseigne déjà *(« dépliez-la ci-dessous »)*, mais je le nomme ici plutôt
que de le taire.*

### Ce qui a changé — **deux endroits, et le silence reste la règle**

`utils/passation/file-copie.ts` — la branche `if (c.aUnRetour)` rend désormais
`motif: ecarteesDuMessage(dernier)` au lieu de `motif: null`. ⛔ **La clé reste `abouti`, la phrase
reste « Traitement terminé. », et `relancable` reste `false`** : *une trace n'est pas un état* — je
**lis** `dernier_message` pour l'**afficher**, je n'en **déduis** rien. Même règle que pour
`sans_retour` : **c'est la dernière étape qui parle**, `retour_v1` après `mesure_v1`.

`components/passation/EcranProf.tsx` — `TraitementDeLaCopie` rendait `null` sur `abouti` ; il rend
`null` sur **`abouti` SANS motif**. ⚠️ Le ton reste neutre *(`grave` est faux)*, et **aucun bouton de
relance n'apparaît**.

Le lecteur s'ancre sur une marque **discriminante** : `\d+ écartée\(s\) — \S…`. **Deux autres textes
du dépôt disent « écartée(s) »** et **aucun ne matche** — l'alerte *« N compétence(s) sondée(s)
écartée(s) DU RETOUR »* de `chaine.ts:983` et le compte *« N sans copie remise, écartée(s) »* de
`app/passation/actions.ts:96` : ni l'un ni l'autre n'a le tiret cadratin. **Un test le tient sur les
deux chaînes réelles.**

### La preuve : **la PRODUCTION, 13 sur 13**

⛔ *L'exercice du bac à sable `473b2c25` n'élit que des couples non couverts : il tombe en
`sans_retour`, qui servait DÉJÀ son motif — le prouver sur lui ne prouve rien de neuf.* **Le cas
MIXTE n'existe qu'en production**, et c'est là que le script va le chercher, **en lecture seule**
*(PostgREST, `PROD_SUPABASE_*`)*. Il reconstitue exactement ce que l'écran reçoit — les jobs du
dépôt, la présence d'un retour, la présence d'une copie — et appelle `etatChaineDeLaCopie` :

```
· 13 job(s) de PRODUCTION portent la marque des écartées
· sur 13 dépôt(s), 13 sont en état `abouti`
✓ ⭐ LE CAS MIXTE EXISTE EN PROD : 13 copie(s) `abouti` dont le job nomme des compétences écartées
✓ ⭐⭐ ET ELLES LE DISENT TOUTES DÉSORMAIS : 13/13 servent leur motif (0 se taisent encore)
· ce que l'écran affiche maintenant, sur 0f379471… :
·   phrase : « Traitement terminé. »
·   motif  : « 2 écartée(s) — structure, argumentation : mode « expliquer » non couvert par
               l'instrument de structure, qui ne couvre que « composer »… »
·   relançable : false (rien n'attend le professeur — une trace n'est pas un état)
```

**Avant ce lot, ces treize copies affichaient treize fois « Traitement terminé. » et rien d'autre.**

### ⭐ Et **Codex en profite aussi** — dit ici parce qu'il fallait le dire

`utils/passation/file-copie.ts` et `components/passation/EcranProf` **servent les deux modules** : la
correction vaut pour `/prof/codex/passation/<id>` autant que pour
`/prof/aletheia/passation/<id>`. **La porte de mode de C5-L3 vaut pour les deux**, et l'écran de la
file aussi. ⚠️ **J'ai touché un écran qui n'est pas seulement le mien** : la section **C4-L4** du
`SUIVI_tests_manuels.md` a été rejouée *(`scripts/recette/passation-c4l4.mjs` — 56 verts, 1 rouge,
et **ce rouge est la latence connue**, §6)*.

---

## §5 — ⭐⭐ LA COUTURE, ET SON SCRIPT

> **Un exercice de lecture, de sa conception à son retour lu, se traverse-t-il d'un bout à l'autre
> en ne cliquant QUE sur des onglets ?**

**Cinq coutures nommées, chacune sous la seule forme qui la rend vérifiable — qui écrit · qui lit ·
un chemin réel y mène-t-il ?**

| | Qui ÉCRIT | Qui LIT | Le chemin réel |
|---|---|---|---|
| ① | la conception d'examen *(C4-L9)* écrit `exercices.exercice_planifie_id` | `passationsDeClasse(admin, 'aletheia')` | onglet **Exercices** prof → « Passations en classe » → « Ouvrir → » |
| ② | le plan d'évaluation écrit la ligne `a_concevoir` | `examensAConcevoir(admin, 'aletheia')` | onglet **Exercices** prof → encart → « Concevoir → » |
| ③ | le routeur / la conception écrivent le dépôt `maison` | `exercicesMaisonDeLEleve(…, 'aletheia')` | onglet **Exercices** élève → la ligne → le déroulé *(C5-L2)* |
| ④ | `ouvrirLesDepots()` *(C4-L4)* écrit `ouvert_par_prof_at` | `signauxDeLancement(…, 'aletheia')` | onglet **Examens** élève → le signal → la passation |
| ⑤ | `motifDesEcartees()` *(C5-L3)* écrit `exercices_jobs.dernier_message` | `etatDesJobs()` → `etatChaineDeLaCopie()` → `EcranProf` | onglet **Exercices** prof → la passation → section « LA FILE » |

**Le script :** `scripts/recette/couture-c5l4.mjs` — **41 verts, 0 rouge.**

```bash
node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON \
     --import ./scripts/register-calibration-resolver.mjs \
     scripts/recette/couture-c5l4.mjs
```

**Son mode de retrait** *(il l'appelle lui-même en fin de course, échec compris)* :

```bash
node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON \
     --import ./scripts/register-calibration-resolver.mjs \
     scripts/recette/couture-c5l4.mjs --retire
```

`--garde-le-decor` le laisse en place pour un smoke à l'écran ; le registre `.couture-c5l4.json` est
ce que `--retire` relit, avec un repli **par la marque** `COUTURE-C5L4` si le registre a disparu.

⛔ **Pas de lecture de code en guise de preuve.** Le script **appelle les lectures que les six
onglets appellent**, puis, pour chaque `href` qu'elles rendent, vérifie **deux choses que rien
d'autre ne vérifie ensemble** : que **la route SERT** *(`chargerVueProf`, `chargerVueEleve`,
`chargerConception`, `lireDepotMaison`)* et que **l'onglet S'ALLUME** *(`ongletActifParRoute` sur la
config réelle)*. **Il éprouve aussi la borne dans les deux sens** — la porte de Codex refuse le dépôt
de lecture *(journal serveur à l'appui)*, et celle d'Aletheia refuse l'écriture.

⭐ **La couture ⑤ est éprouvée deux fois, et la première est la vraie** : la **production en lecture
seule** *(§4)*, puis un **aller-retour en base** dans le bac à sable — le message est produit par le
**vrai écrivain** `motifDesEcartees()`, écrit dans une **vraie ligne** de `exercices_jobs` *(avec sa
`cle_idempotence`, qui est NOT NULL et ne s'invente pas)*, relu par le **vrai lecteur** `etatDesJobs()`,
et servi à `etatChaineDeLaCopie()`. ⛔ **Aucun appel de modèle n'est payé par ce script.**

⚠️ **Deux pièges du décor, trouvés à l'exécution et écrits dans le script :**
`exercices_jobs.cle_idempotence` est **NOT NULL** *(c'est `cleIdempotence(depotId, etape)`, la même
que `mettreEnFile` pose)* ; et **les lignes de plan partent EN DERNIER au retrait** —
`exercices.exercice_planifie_id` les référence, et les retirer d'abord rend `23503` en laissant le
décor à moitié en base. *L'ordre n'est pas une préférence.*

⛔ **Le script ne bascule AUCUN interrupteur** — il les **relève** et le dit. La sandbox a été
relue après : **aucune trace `COUTURE-C5L4`, aucun registre, les six interrupteurs identiques.**
Deux tours consécutifs donnent le même résultat.

---

## §6 — Les renvois de périmètre, **AVEC LEUR LISTE**

1. ⛔ **Le déménagement de `app/prof/conception/` — reporté par Louis le 27/08**, et il retourne à
   `IDEES_post_rentree.md` avec sa nouvelle date. **Son coût, pour que personne ne le retente en
   passant :** dix `revalidatePath` sur des chemins Aletheia dans six fichiers —
   `app/passation/actions.ts:47` et `:49` *(en `'layout'`)*, `app/deroule/actions.ts:48` *(`'layout'`)*,
   `app/eleve/modules/aletheia/actions.ts:49` et `:50`, `app/prof/aletheia/actions.ts:123` et `:203`,
   `app/prof/examens-diagnostiques/actions.ts:297`, `app/prof/scriptorium/actions.ts:1200` et `:1452` —
   plus `utils/integrite-preuve.ts:154`, `app/prof/corpus/page.tsx:237`,
   `components/examens/EcranConceptionExamen.tsx:102` et `:114`, et l'entrée « Conception » du
   Pilotage *(`components/nav/configNavigation.ts:37`)*. ⚠️ **Un `revalidatePath` sur un chemin mort
   ne lève aucune erreur.** ⭐ **Garder les racines aux onglets Livres est ce qui laisse les dix
   justes** : les trois `'layout'` couvrent le sous-arbre entier, et **aucune route n'a été déplacée**.

2. ⚠️ **Les trois `<main>` imbriqués de CODEX ne sont pas corrigés** — C4-L6 est clos, ils sont hors
   de mon périmètre : `app/eleve/modules/codex/passation/[depotId]/page.tsx`,
   `app/prof/codex/passation/[exerciceId]/page.tsx`,
   `app/prof/codex/examen-diagnostique/[planifieId]/page.tsx`. **J'ai corrigé les trois d'Aletheia**
   *(piège 24 n'en nommait qu'un ; les trois portent le même défaut, dans mon module, et je suis le
   lot des écrans de lecture)* : `<main>` → `<div>`, **classes inchangées**, seule la balise change.
   *La coquille du rôle rend déjà un `<main>` autour de tout le sous-arbre : deux repères « principal »
   dans une page qui n'en a qu'un s'entendent au lecteur d'écran.*

3. ⚠️ **Les six interrupteurs sont à ON en bac à sable ET EN PROD** — reconstaté par requête au
   début et à la fin de la séance. Le `07-` §5 les dit « à OFF jusqu'à la recette ». **Constat
   d'état, déjà déposé par C5-L2, non réparé ici** : ce lot n'en pose, n'en allume ni n'en éteint
   aucun. **Conséquence pour la recette : les six onglets seront visibles et pleins dès la première
   seconde.**

4. ⚠️ **Un résidu de recette bloque `examens-c4l9.mjs`** — et ce n'est pas moi : une ligne de plan
   `codex` / `ecriture` / `septembre` / `concu`, `note: "RECETTE C4-L9"`, créée le **2026-08-24T01:38**,
   sur le plan de la classe `Test` *(`6ff2d56b-0fbf-4d53-b187-464dd4233852`)*. Elle fait échouer le
   décor de sa propre recette sur `uk_exercices_diagnostic` **dès la section A** — le script s'arrête
   avant toute vérification. **Rien de ce lot ne la touche ; elle est à retirer par qui l'a semée.**

5. ⚠️ **Trois fichiers du dépôt sont modifiés par une AUTRE session que la mienne**, et je ne les
   commite pas : `SUIVI_SQL.md` *(une ligne pour `retrait_production_2026-08-27.sql`)*,
   `scripts/derive-doctrine.py` *(les deux corrections du `02-` §2.4 — le quart au grain micro, le
   doublement du diagnostique)*, et le fichier `retrait_production_2026-08-27.sql` lui-même.
   **Ils appartiennent au chantier de la banque, pas à C5-L4.**

6. ⚠️ **Le `07-` §5 dit les six interrupteurs « à OFF jusqu'à la recette » ; ils sont à ON.** ⛔ **Je
   ne marque PAS le `07-` `[faux]` pour autant** — le §5 décrit un **régime voulu**, et l'écart est
   un **état de la base**, pas une erreur de la source. *Une source fausse se marque ; une base qui
   s'écarte de sa source se répare ou se déclare.* **Elle est déclarée, deux fois.**

---

## §7 — Toute source trouvée fausse

**Aucune.** ⛔ **Aucune dette `D10` n'a été posée**, et le registre `INVENTAIRE_Non_Tranches.md` n'est
pas touché. Les quatre pièces du manifeste ont été lues aux sections que le manifeste nomme, et
**rien n'y est faux** : les deux points qui auraient pu l'être — l'ordre des onglets et le
déménagement de la conception — **avaient été tranchés par Louis avant la séance** et sont écrits à
l'entrée du `07-` §2.

**Trois commentaires DE CODE étaient devenus faux avec ce lot, et ils sont corrigés :**

- `components/nav/configModules.ts:73-77` — *« Aujourd'hui, seul ALETHEIA n'en a pas — ses onglets
  sont C5-L4. »* **Corrigé au passage, comme le prompt le demandait** : c'est le fichier qui fait
  foi, et un commentaire faux y coûte plus cher qu'un onglet mal placé. Il dit désormais que
  **Scriptorium est le seul module dont la face élève n'est pas pilotée par la route**.
- `utils/codex-onglets/regles.ts` *(`hrefDuDeroule`)*, `app/deroule/PageDuDeroule.tsx`,
  `app/eleve/modules/aletheia/exercice/[depotId]/page.tsx` — les trois disaient *« ce n'est pas un
  onglet, les onglets sont C5-L4 »*. Ils disent maintenant **ce qui les range**, c'est-à-dire le
  `prefixes[]` de l'onglet Exercices, et redisent le piège du singulier / pluriel.
- `app/prof/aletheia/passation/[exerciceId]/page.tsx` et
  `app/prof/aletheia/examen-diagnostique/[planifieId]/page.tsx` — les deux disaient *« cette page ne
  réorganise pas la navigation […] Les onglets sont C4-L6 et C5-L4 »*. Ils disent maintenant **quelle
  porte les atteint** et **pourquoi elles restent hors de la barre**.
- `app/deroule/PageDuDeroule.tsx` disait aussi que la passation élève d'Aletheia rouvre un `<main>` :
  **c'est réparé, et le commentaire le dit** — en nommant les trois jumelles de Codex qui le portent
  encore.

---

## §8 — Ce qui reste à jouer en recette, avec sa condition de reprise

*Tout ce qui suit part au `SUIVI_tests_manuels.md`, section C5 · L4.*

| | Ce qui reste | Condition de reprise |
|---|---|---|
| **C5L4-A** | **Le smoke PROF, connecté** : les trois onglets s'affichent et s'allument ; l'onglet Exercices porte ses trois renvois, l'encart et la liste ; une passation s'ouvre depuis la liste. | ⚠️ **Une session Code ne s'authentifie pas** — à jouer par Louis. Le décor est prêt : `couture-c5l4.mjs --garde-le-decor`. |
| **C5L4-B** | **Le smoke ÉLÈVE, connecté**, sur téléphone ou à 375 px : les trois onglets, la séance qui allume Livres, l'exercice qui allume Exercices, la passation qui allume Examens, et **un seul « ← Retour » par écran**. | idem. ⚠️ *Le défaut du double retour n'est visible qu'à l'œil : aucun test ne compte les liens d'une page.* |
| **C5L4-C** | **Les deux vides de l'onglet Exercices élève**, distingués : porte fermée *(« pas encore ouverts »)* vs rien à faire *(« aucun exercice pour le moment »)*. | ⚠️ **Demande d'éteindre `exercices_actif`** — un geste du professeur, à l'écran `/prof/allumage`. Non joué ici : ce lot ne bascule aucun interrupteur. |
| **C5L4-D** | **Le motif de la copie aboutie, VU À L'ÉCRAN**, sur `/prof/aletheia/passation/<exerciceId>`, section « LA FILE » et les lignes par élève. | ⚠️ **Prouvé par exécution sur les 13 copies de PROD** *(§4)*, **jamais vu à l'écran** : le smoke prof le lève. C'est là que C5-L3 avait constaté le silence. |
| **C5L4-E** | **La section C4-L4 rejouée à l'écran** — l'écran de la file est partagé avec Codex. | Le script est vert *(56/1, le rouge est la latence)* ; l'écran ne l'a pas été. |
| **C5L4-F** | **`examens-c4l9.mjs` rejoué** une fois le résidu du 24/08 retiré. | Le résidu est nommé au §6.4. **Le script s'arrête à sa section A** — il n'a rien vérifié, ni en bien ni en mal. |

**Et la case qui m'attendait :** **`C5L3-11-bis` est LEVÉE et cochée.** *L'écran montre le motif quand
tout va bien — sur `abouti`, quand il y a des écartées.* La preuve est celle du §4 ; la vue à l'écran
part en `C5L4-D`, avec sa condition.

---

## §9 — Ce qui a été rejoué, et ce que ça a coûté

| Script | Résultat | Lecture |
|---|---|---|
| `scripts/recette/couture-c5l4.mjs` ⭐ | **41 / 0** | le lot |
| `scripts/recette/lecture-c5l2.mjs` | **38 / 0** | C5-L2 intact |
| `scripts/recette/generateur-c5l1.mjs` | **27 / 0** | C5-L1 intact |
| `scripts/recette/passation-c4l4.mjs` | **56 / 1** | ⚠️ le rouge est **la latence connue** — `22,8 s pour une copie, le contrat est « quelques secondes »` —, déjà relevée à C4-L4 le 22/08 *(22,4 s)*. **Sans rapport avec ce lot.** |
| `scripts/recette/reception-c5l3.mjs` | **26 / 1** | ⚠️ le rouge est `⛔ la vf N'A ÉCRIT AUCUNE MESURE de plus`. **Ce lot ne touche pas une ligne de `utils/chaine/`** *(`git diff --stat utils/chaine/` est vide)* et ce script **n'importe aucun de mes fichiers**. Le tour porte par ailleurs `retour refusé : RR4` — le refus n'est **pas déterministe** *(leçon de C5-L2-bis)*. **À relancer avant d'enquêter.** |
| `scripts/recette/examens-c4l9.mjs` | **arrêté en section A** | ⚠️ bloqué par le résidu du 24/08, §6.4. **Il n'a rien vérifié.** |

⚠️ **Coût des rejeux payants** : **108 appels, 1,66 USD**, tous en bac à sable *(`api_couts`, depuis
20:00 UTC)* — `reception-c5l3` a tourné deux fois, `lecture-c5l2` et `generateur-c5l1` une fois.
⛔ **Le script de couture n'a rien payé.**

---

## §10 — Les deux amendements qui m'appartenaient

- **`07-Implementation.md` §2, inventaire des lots** — l'entrée `C5-L4` porte son **état de clôture**.
- **`07-Implementation.md` §5** — ce que j'ai appris de l'allumage : **l'état réel des six
  interrupteurs, reconstaté le 27/08**, et le fait qu'aucun septième n'a été créé pour six onglets.

⛔ **Rien d'autre n'a été écrit dans une source** : le §3, le §4, le §6 et la règle de manifeste du
§2 sont GELÉS, et le `01-`, le `02-` et le `06-` le sont entièrement.

---

## §11 — ⭐ LE SMOKE PROF, JOUÉ À L'ÉCRAN LE 27/08 (session ouverte par Louis)

*Ajouté après la clôture : Louis a ouvert une session professeur, et quatre restes de recette ont
été levés.* **`C5L4-A`, `C5L4-D`, `C5L4-E` et le nouveau `C5L4-E-bis` sont cochés au
`SUIVI_tests_manuels.md`.**

### ① Les deux barres, et les six allumages

**La barre MOBILE a été rendue la première** — le pane était étroit —, ce qui éprouve
`SousNavModuleMobile` **en page vivante**, et pas seulement la mesure géométrique du §2. Puis la
**Barre 2 desktop**, à 900 px. **Les allumages, relevés par `aria-current="page"` sur la config
réelle :**

| La route | L'onglet allumé |
|---|---|
| `/prof/aletheia` | **Livres** |
| `/prof/aletheia/eleve/<id>` | **Livres** — *sans préfixe déclaré : l'onglet racine l'attrape, comme le §2 l'annonçait* |
| `/prof/aletheia/exercices` | **Exercices** |
| `/prof/aletheia/passation/<id>` | **Exercices** |
| `/prof/aletheia/examen-diagnostique/<id>` | **Exercices** |
| `/prof/aletheia/parametres` | **Paramètres** |
| `/prof/codex/passation/<id>` | **Exercices** *(Codex, ses deux onglets intacts)* |

### ② ⭐⭐ Ce que la liste a révélé, et que rien d'autre n'aurait montré

L'onglet Exercices affiche **« PASSATIONS EN CLASSE · 4 »**, et **deux de ces quatre sont de VRAIES
passations de lecture du bac à sable** — *« Examen diagnostique — l'explication de texte »*, pour T5
et pour Test. **Elles existaient, et elles n'avaient qu'UNE SEULE porte dans tout le dépôt.** C'est
le 🔴 du piège 21 constaté sur des données réelles, et fermé.

⚠️ **L'encart des examens à concevoir est ABSENT en bac à sable**, et c'est juste : ses deux lignes
`lecture` y sont `concu`, pas `a_concevoir`. *Une page nue n'est pas la preuve qu'il est cassé* —
et **la capture de PRODUCTION le montre justement avec ses DEUX lignes** *(T5, 12 oct. et 4 janv.)*,
sous l'ancienne barre « Classe · Paramètres ». **C'est l'état AVANT, photographié.**

### ③ ⭐⭐ La copie qui parle et celle qui se tait, sur le même écran

⚠️ **Le bac à sable n'a aucun job mixte** — son seul exercice de porte de mode tombe en
`sans_retour`, qui servait déjà son motif. **Un mode `--decor-ecran` a donc été ajouté au script de
couture**, qui sème sur une même passation **deux copies abouties** : l'une dont le job porte les
écartées, l'autre non. *Le message est écrit par le VRAI écrivain (`motifDesEcartees`), jamais tapé.*

```
LA FILE   2 terminées · 0 en file · 0 pas encore en file · 0 sans retour · 0 en échec · 0 sans copie
          « Rien n'attend de geste de votre part. »

Nina Panaitescot   « Traitement terminé. »
                   2 écartée(s) — structure, argumentation : mode « expliquer » non couvert par
                   l'instrument de structure, qui ne couvre que « composer »…
                   → aucun bouton « Relancer »        (relevé au DOM : boutonRelancer: false)

Eléonore Delprat   (rien)                             (relevé au DOM : blocDeTraitement: false)
```

⭐ **Les six comptes restent disjoints et rien n'est réclamé au professeur** : c'est bien la MÊME
aboutie qui parle, pas un septième état — la forme choisie au §4 se vérifie à l'écran.
⛔ **Et le silence reste la règle** : l'exception est nommée, elle ne déborde pas.

### ④ ⚠️ Les `<main>` imbriqués, comptés à l'écran

`document.querySelectorAll('main').length`, sur les quatre écrans jumeaux :

| L'écran | Compte |
|---|---|
| `/prof/aletheia/passation/<id>` | **1** ✅ |
| `/prof/aletheia/examen-diagnostique/<id>` | **1** ✅ |
| `/prof/codex/passation/<id>` | **2** ⚠️ |
| `/prof/codex/examen-diagnostique/<id>` | **2** ⚠️ |

*Le correctif se vérifie par la mesure, et le renvoi de périmètre aussi : les jumelles de Codex
portent bien le défaut, et il se compte.*

### ⑤ Ce qui n'a PAS pu être joué, et pourquoi — la précision compte

⛔ **`C5L4-C` n'est pas bloqué par l'interrupteur, il est bloqué par le RÔLE.** Louis avait autorisé
la bascule de `exercices_actif` en bac à sable ; **elle n'a pas été jouée, et c'eût été un geste pour
rien** : les deux vides à distinguer sont sur un écran **ÉLÈVE**, et la session ouverte était une
session **PROFESSEUR**. *Basculer un interrupteur sans pouvoir en observer l'effet n'aurait rien
prouvé et aurait touché la base.* **`C5L4-B` et `C5L4-C` se lèvent ensemble, d'un seul passage, avec
une session élève.**

### ⑥ La sandbox, rendue comme trouvée

Décor retiré *(deux tours de `--retire` : le second ne trouve plus rien, y compris une instance
orpheline d'un premier essai, rattrapée **par la marque** et non par le registre)*. **Les six
interrupteurs sont identiques** à ce qu'ils étaient au contrôle d'entrée. **La couture rejouée
après le nettoyage : 41 verts, 0 rouge.**

---

## §12 — ⭐⭐ LE SMOKE ÉLÈVE, JOUÉ À L'ÉCRAN LE 27/08 (élève **Elo**, classe **Test**)

*`C5L4-B` et `C5L4-C` sont levés. **Il ne reste plus, des huit restes de recette, que `C5L4-F`
— bloqué par un résidu qui n'est pas de ce lot — et `C5L4-G`, qui est une décision.***

### ① Les six allumages, et les deux pièges levés en page vivante

| La route | L'onglet allumé | |
|---|---|---|
| `/eleve/modules/aletheia` | **Livres** | |
| `…/<livreId>/2` | **Livres** | ⭐⭐ **le piège 18** : aucun préfixe statique ne décrit un UUID, seule la racine la sert |
| `…/exercices` | **Exercices** | |
| `…/exercice/<depotId>` | **Exercices** | ⭐⭐ **le piège du singulier / pluriel** : le déroulé de C5-L2 allume l'onglet, **pas la racine** |
| `…/examens` | **Examens** | |
| `…/passation/<depotId>` | **Examens** | `mains: 1` — le correctif du `<main>` tient côté élève |

⭐ **UN SEUL « ← Retour » par écran**, compté au DOM (`nbRetours: 1`) sur les cinq écrans qui en
portent un. *Le défaut trouvé par le smoke du 27/08 ne s'est pas rouvert en déménageant les blocs —
et c'est exactement ce qu'aucun test ne pouvait dire.*

⭐ **La barre mobile à 375 px, en page vivante** : `Livres 63 · Exercices 82 · Examens 81`, **44 px
de haut les trois**, `scrollWidth = clientWidth = 375`. *La mesure isolée du §2 (64/81/81) est
confirmée à un pixel près.*

### ② ⭐⭐ Ce que l'onglet Examens a révélé

Il sert **DEUX passations en classe réellement OUVERTES** pour cet élève — *« Passation en classe
ouverte — Examen diagnostique — l'explication de texte »*. **Elles n'étaient atteignables que par le
signal noyé sur la racine, sous les livres.** C'est le 🔴 du piège 23, sur des données réelles, fermé.

*Avec les deux passations de lecture trouvées côté professeur (§11 ②), ce sont **quatre écrans
réels** que ce lot rend atteignables dans le seul bac à sable — quatre écrans qui existaient et que
personne ne pouvait ouvrir sans connaître un identifiant.*

### ③ ⭐ Les trois états de l'onglet Exercices, vus

`exercices_actif` a été **éteint puis remis** en bac à sable, sur autorisation explicite de Louis,
**par l'écrivain du lot** (`poserExercicesActifs`) — jamais par un `update` écrit pour l'occasion.
⛔ **La production n'a pas été touchée**, revérifiée après coup.

| La porte | Ce que l'onglet dit |
|---|---|
| **ON**, aucun exercice | *« Aucun exercice de lecture pour le moment. Ceux que ton professeur te donne apparaîtront ici. »* |
| **ON**, un exercice | la ligne, avec son état |
| **OFF** | *« Les exercices de lecture ne sont pas encore ouverts. Ton professeur t'indiquera quand ils commencent. »* |

⭐⭐ **Et le lien disparaît alors que le dépôt existe** *(`liens: []`)* : la porte ferme **la liste**,
pas l'onglet — *un lien qui mènerait à une page fermée est un lien qui promet une porte close*.
⭐ **Les trois onglets restent affichés dans les trois cas** : *« un vide expliqué, jamais un onglet
qui clignote »*, vérifié plutôt que supposé.

⭐ **C'est la leçon du §10 prise en flagrant délit d'être juste** : sur la racine, ce bloc se cachait ;
sous un onglet dédié, il parle — **et il dit deux choses différentes.**

### ④ La sandbox, rendue comme trouvée

Décor retiré *(deux tours de `--retire` ; le second ne trouve plus rien)*. **`exercices_actif` remis
à ON**, et **les six relus, identiques**, en bac à sable **comme en production**.

---

## §13 — LE RÉSIDU DU 24/08, RETIRÉ — et deux choses qu'il a fallu corriger

*Sur mandat explicite de Louis, 27/08 : « retire le résidu du 24/08 et rejoue examens-c4l9, mais
assure-toi que le résidu ne sert vraiment à rien ».*

### ① ⚠️⚠️ Le résidu n'était pas UNE ligne, c'était DEUX

Le même run `--garde-le-decor` du **24/08 à 01:38** avait laissé **les deux jumeaux**, à une seconde
d'intervalle : `6ff2d56b…` *(codex/ecriture)* et **`12c914c5…`** *(aletheia/lecture)*. ⭐ **La seconde
n'a été trouvée que par le contrôle final du retrait**, qui recompte les lignes portant la marque —
*une vérification qui se fie au retour de la suppression n'aurait rien vu.*

**Chacune portait une instance `assigne` et SEPT dépôts `ouvert`** : **quatorze signaux de lancement
vivants** pour sept élèves réels du bac à sable.

### ② Le pré-vol : la preuve que ça ne servait à rien

⛔ **Rien n'a été supprimé avant de l'avoir prouvé.** Le script de retrait **refusait de jouer** si
l'un de ces comptes avait été non nul, et une **sauvegarde JSON** a été écrite hors du dépôt avant
toute écriture :

- **le travail d'élève** : `transcription_v1`, `texte_v1`, `photos_v1`, `v1_remis_at`, `vf_remis_at`
  — **rien, sur les 14 dépôts** ;
- **les traces de chaîne** : `exercices_jobs`, `exercices_retours`, `exercices_squelettes`,
  `exercices_metacognition`, `competences_mesures`, `monitoring_mesures` — **zéro partout** ;
- **le coût payé** : `api_couts` — **zéro** ;
- **le routeur** : `routeur_decisions` — **zéro**.

⚠️ **Un comptage m'avait d'abord induit en erreur** : `exercices_cas` rendait « 4 », qui étaient en
fait **les quatre clés d'un objet d'erreur PostgREST** *(la table n'a pas de colonne `id` — sa clé
est `(exercice_id, ordre)`)*. **Le vrai compte est zéro.** *Un `len()` sur une réponse qu'on n'a pas
regardée compte ce qu'elle contient, pas ce qu'on croit.*

### ③ Le retrait est passé par la FONCTION DE PRODUCTION

`retirerExamenDiagnostique` *(`utils/examens/conception.ts`)*, dans l'ordre que le script dit
lui-même — **dépôts → statut `concu` → retrait → ligne de plan** —, **jamais par un `delete` brut** :
c'est elle qui fait **revenir la ligne de plan à `a_concevoir`** avant d'effacer l'instance, sans quoi
la ligne resterait `concu` sans instance. *Elle porte aussi la garde qui refuse une instance
`assigne` — « retirer une instance assignée laisserait les dépôts de ses élèves orphelins ».*

**Résultat : `⭐ AUCUNE trace de RECETTE C4-L9 ne survit`**, et `examens-c4l9.mjs` **va au bout** —
**126 verts, 6 rouges**, en se nettoyant lui-même cette fois.

### ④ ⚠️ UNE CORRECTION À CE QUE LES SMOKES AVAIENT CONCLU

Sur les **deux** « vraies passations de lecture » que j'avais célébrées aux §11 ② et §12 ②, **une
seule l'était** :

| | Ligne de plan | Note | Créée | Verdict |
|---|---|---|---|---|
| `41e66a82…` | `31575dae…` | **aucune** | 26/08 | ⭐ **vraie** — intacte, ses 7 dépôts avec |
| `97eb21da…` | `12c914c5…` | `RECETTE C4-L9` | 24/08 | ⛔ **décor**, retiré |

**Le fond tient** — la vraie passation n'avait, elle aussi, **qu'une seule porte** avant ce lot, et
c'est bien le 🔴 du piège 21 qui se ferme — **mais le compte était faux, et il est rectifié.**

### ⑤ ⚠️⚠️ Un défaut de `examens-c4l9.mjs`, trouvé en le rejouant

**Il ne remet pas les interrupteurs comme il les trouve : il les force à OFF.** À sa section I, il
ouvre les deux portes élève pour sa vérification, puis exécute **`await poserPortes(false, false)`**
— *une valeur en dur, pas l'état d'avant*. **Constaté deux fois par requête** : `exercices_actif` et
`passation_classe_actif` sont passés de **ON à OFF** à chaque rejeu, et ont dû être remis à la main.

⭐ **CORRIGÉ le 27/08, sur mandat de Louis, au patron de `decor-c4l6.mjs` et de `lecture-c5l2.mjs`**
— voir le §14.

### ⑥ Les six rouges sont le même fait, et c'est `C5L4-G`

Le script **encode le régime du `07-` §5 en assertion** : deux contrôles attendent les portes élève
**fermées**, quatre affirment les autres **à OFF**. **Les six sont à ON.** ⛔ **Ce n'est pas un défaut
de ce que C4-L9 construit** — ses 126 autres contrôles sont verts, y compris la garde
`uk_exercices_diagnostic` et les deux signaux qui entrent chacun par leur module. **C'est la
divergence base / source de `C5L4-G`, vue par un autre bout.** *(Reste `C5L4-F-ter`.)*

### ⑦ La sandbox, rendue comme trouvée

**Les six interrupteurs sont à ON**, relus par requête après la dernière remise. **La production n'a
jamais été touchée**, revérifiée. La vraie passation de lecture et ses sept dépôts sont intacts.

---

## §14 — ⭐ `examens-c4l9.mjs` CORRIGÉ — la restitution, et la part du constat

*Sur mandat de Louis, 27/08 : « corrige-le aussi, avec le patron de decor-c4l6 ».*
**Résultat : 128 vérifications passées, 0 en échec — contre 126 / 6 avant, dont aucun des six rouges
n'était un défaut.**

### ① La restitution — le commentaire était juste, le code avait tort

`poserPortes` portait déjà la bonne intention : *« la recette les bascule le temps d'une
vérification, et les REMET »*. **Le code, lui, écrivait `poserPortes(false, false)`** — une valeur
en dur. Tant que les six interrupteurs étaient à OFF, la différence ne se voyait pas. **Quatre
gestes**, au patron de `decor-c4l6.mjs` *(qui garde `porteAvant`)* et de `lecture-c5l2.mjs` :

- **`PORTES_TROUVEES`** est lu au démarrage, et **`restituerLesPortes()`** y revient ;
- ⭐ **le contrôle « portes FERMÉES, AUCUN signal » LES FERME LUI-MÊME** au lieu de le supposer.
  *Un contrôle qui AFFIRME un état au lieu de l'ÉTABLIR échoue quand le régime change, sans que rien
  ne soit cassé — c'est ce qui rendait deux des six rouges.*
- la fin de section I **restitue** au lieu de forcer ;
- ⚠️ **`process.on('SIGINT')` restitue aussi sur interruption** — un Ctrl-C entre l'ouverture et la
  restitution laissait deux portes ouvertes derrière lui.

### ② ⭐⭐ Éprouvé DANS LES DEUX RÉGIMES — un seul tour n'aurait rien prouvé

| Portes trouvées | Portes rendues | Résultat |
|---|---|---|
| **ON / ON** | **ON / ON** | 128 / 0 |
| **OFF / OFF** | **OFF / OFF** | 128 / 0 |

*C'est la différence entre une restitution et une constante : un rejeu à ON seul aurait aussi bien
validé un `poserPortes(true, true)` en dur.*

### ③ La part du constat — un compteur de recette peut mentir comme un écran

Le bloc final affirmait `v === false` sur les six : il transformait **un état de la base** en
**échec de la recette**. Il fait désormais la part des deux :

- **une ASSERTION** sur ce dont la recette répond — *« `exercices_actif` est remis COMME TROUVÉ »*,
  idem pour `passation_classe_actif` : les deux seules portes qu'elle bascule ;
- **un CONSTAT** sur les six, avec un avertissement nommé dès que l'un est à ON — *« le `07-` §5 les
  dit à OFF ; l'écart est un ÉTAT DE LA BASE, pas un défaut de ce lot ; il se répare à
  `/prof/allumage`, jamais ici »*.

⭐ *« Un écran qui compte autre chose que ce qu'il dit est un écran qui ment » — la leçon du smoke du
22/08, qui vaut aussi pour un compteur de recette.*

⚠️ **C'est un écart au périmètre, et il est assumé** : `examens-c4l9.mjs` appartient à **C4-L9,
clos**. Il est corrigé parce que **Louis l'a demandé explicitement**, et parce que le laisser armé
signifiait que chaque rejeu fermait deux portes aux élèves du bac à sable, en silence.
