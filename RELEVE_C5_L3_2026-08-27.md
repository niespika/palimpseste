# RELEVÉ — C5-L3 · Les mesures en réception

**27 août 2026.** Dépôt `palimpseste`. **Aucune migration, aucun interrupteur neuf, aucune ligne au
`SUIVI_SQL.md`** — tout se règle en code, sur des colonnes qui existent.

> ## ⭐⭐ TRENTE-NEUF COPIES RÉELLES NE PEUVENT PLUS RECEVOIR DE LETTRE FAUSSE
>
> **Vingt-six en production, treize en bac à sable.** En prod, un exercice `assigne` × `classe` à
> référence validée élit `argumentation: [expliquer]` **et** `structure: [expliquer]` — les deux
> compétences sans instrument réceptif — et porte **23 dépôts dont 13 au statut `v1_remis`** : **13 × 2
> = 26 lettres** que la chaîne aurait calculées **par l'instrument de COMPOSITION sur des copies de
> lecture**, rangées dans le signal de ciblage réceptif du routeur, **sans que rien ne le dise**. Les
> six interrupteurs sont à `ON` dans les deux bases, et le geste qui déclenche tout est **un clic**.
> *En bac à sable, trois exercices portent les mêmes couples : 9 dépôts ouverts, plus 2 assignés — soit
> 13 lettres de plus à l'échéance de leur remise.*
>
> ⭐ **Ce n'était pas une prédiction : `C5-L2` l'a exercé le 27/08, sur un dépôt réel.** Ce lot ferme
> la porte, et **elle dit ce qu'elle refuse**.

---

## 1. LES QUATRE CHOSES, PLUS LA COUTURE — développées

| | Le fait | État | Où il vit | La preuve | Ce que j'ai écarté, et pourquoi |
|---|---|---|---|---|---|
| **①** | **le verrou du manifeste** | **FERMÉ** | `07-Implementation.md` §2, ligne de manifeste de `C5-L3` *(dépôt conception)* | **décision de Louis, 27/08, en premier geste** — citée au §2 ci-dessous. Les sept en-têtes vérifiés à la main avant de poser la question. | Je n'ai **rien tranché seul**. Le prompt prescrivait la question ; je l'ai posée avec le précédent de C4-L5 sous les yeux, **et sa seconde moitié** — la clause « bancée en réception » du « fait quand », que l'amendement ne réglait pas. |
| **②** | **la porte de mode** | **FERMÉ** | déclaration : `modesCouverts` aux six branchements *(`utils/chaine/branchements/*.ts`)* · lecture : [`modeNonCouvert`](utils/chaine/instruments.ts) · contrôle au chargement : `refusDeModes` dans `verifierCoherence` · refus : [`competencesDeLExercice`](utils/chaine/chaine.ts:389) | **par le succès et par l'échec, sur LE MÊME dépôt réel** — `scripts/recette/reception-c5l3.mjs`, sections B et D · 9 vecteurs neufs à `utils/chaine/instruments.test.ts` | ⛔ **Je n'ai construit aucun instrument manquant** — ni prompt, ni enum, ni cascade, ni seuil. La porte **n'ouvre aucune compétence : elle en ferme deux.** |
| **③** | **les deux grilles qui existent** | **FERMÉ** | `utils/chaine/branchements/{synthese,questionnement}.ts` | **en base, après traversée payante** : `questionnement × expliquer` → **lettre E** · `synthese × restituer` → **lettre D**, `modes` justes, 6 lignes d'`api_couts`, 56,4 s | Je n'ai **pas touché aux cascades** : celle du Questionnement est la même pour les cinq modes *(acté à sa fiche §4)*, seul le référent change — et `prePhase2` le faisait déjà. |
| **④** | **la tranche de référence** | **FERMÉ** | [`utils/chaine/tranche.ts`](utils/chaine/tranche.ts) *(neuf)*, appelée dans `chaineDUneCompetence` **avant la sérialisation** | **sur une VRAIE référence en base** — 4 phrases sur 17 écartées en silence *(script, section C)* · 9 vecteurs à `utils/chaine/tranche.test.ts` | ⛔ Je n'ai **rien stocké** *(dette D8)*, **rien resegmenté**, **recopié aucune liste fermée** — elles s'**importent** de `verifie-reference.ts` — et **retiré aucune unité**. |
| **⑤** | ⭐ **la couture** *(convention du 27/08)* | **FERMÉ** — *éprouvée par exécution, script au dépôt* | [`scripts/recette/reception-c5l3.mjs`](scripts/recette/reception-c5l3.mjs) | **37 verts, 0 rouge.** Quatre coutures nommées et **traversées**, pas lues. | ⛔ Elle **ne répare pas ce qu'elle révèle** : les cinq constats partent au `SUIVI_tests_manuels.md` avec leur condition de reprise. |

### ⑤ — la couture, en détail : ce qu'elle demande, et ce qu'elle a rendu

**Le lot dépend de `C4-L5`**, et la question se nomme en une phrase :
*« **La chaîne sait-elle servir un instrument RÉCEPTIF de bout en bout, sur un dépôt réel, et écrire sa
mesure ?** »* — **oui.**

Les quatre coutures, **sous la seule forme qui les rend vérifiables** — *qui écrit · qui lit · un
chemin réel y mène-t-il ?* :

| | La colonne | Qui l'écrit | Qui la lit | Chemin réel ? |
|---|---|---|---|---|
| ① | `exercices.modes_par_competence` | l'écran de conception, et `MODES_MESURES` *(`utils/examens/types.ts`, arrêté au `01-` §10)* | `lireContexte` → `competencesDeLExercice` → **la porte de mode** | **OUI** — un exercice de prod l'élit, et 4 clés y descendent |
| ② | `exercices.reference_id` → `exercices_references.contenu` | `concevoirInstance`, depuis `referenceValidee` *(C5-L2)* | `lireContexte` → **la tranche** → `armatureDe` / `referenceDuContexte` | **OUI** — 3 références validées en sandbox, 2 en prod ; le **matériau** descend par la même jointure *(1166 caractères mesurés)* |
| ③ | `exercices_squelettes` *(extraction + jugement)* | `chaineDUneCompetence` *(C4-L5)* | le retour, et la relecture RR1-RR4 | **OUI** — 2 squelettes écrits, **2 avec leur verdict** |
| ④ | `competences_mesures.modes` | `ecrireMesure` *(C4-L5)* | `utils/routeur/donnees.ts:86` → `proportions.ts` → `ciblage.ts` | **OUI** — et c'est là que la porte empêche une mesure fausse d'entrer dans le groupe réceptif |

**Le script est laissé au dépôt, avec son mode de retrait**, et c'est lui — pas ce relevé — qui se
rejouera :

```bash
node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --import ./scripts/register-calibration-resolver.mjs scripts/recette/reception-c5l3.mjs --avec-chaine
```

⛔ **Mode de retrait** : sans `--garde-le-decor`, la section E retire **tout** ce que le script a semé
— classe, instance, dépôt, et les six tables filles — par requête, **et elle le fait aussi sur
interruption** (`SIGINT`). Le décor se reconnaît à sa classe, `nom LIKE 'RECETTE-C5L3%'`. ⚠️
`--avec-chaine` **paie 6 appels** ; sans lui, tout le reste se joue et **aucun modèle n'est appelé**.

⭐⭐ **Et la convention se démontre dans ce lot même** : la porte de mode manquante vivait **ENTRE**
`competencesDeLExercice` et les six branchements. `C4-L5` était juste à sa frontière — il sert ce que
le contexte porte ; les branchements étaient justes à la leur — ils traitent ce qu'on leur donne ; **et
le trou était entre eux.** `C5-L2` l'a rencontré **en traversant**, pas en lisant.

---

## 2. La réponse de Louis au verrou du manifeste

**Question posée en premier geste**, après avoir vérifié moi-même les sept en-têtes *(`07-` **2.56** ·
`03-` **2.1** · les six fiches **RELUES ET VALIDÉES**, aucune versée et bancée · `monitoring` **2.1** à
son plafond)*. Deux décisions, parce que l'amendement ne suffisait pas seul.

> **Q1 — le verrou.** *« Le manifeste de C5-L3 exige `competences/*.md` — versé et bancé. Aucune des
> six ne l'est […] Or la MÊME ligne, au manifeste de C4-L5, a été amendée le 22/08 en* relu et validé
> *[…] L'entrée de C5-L3 est le dernier endroit du corpus où un banc conditionne une lecture.
> J'amende ? »*
> → **« Oui — comme le 22/08 »**
>
> **Q2 — le « fait quand ».** *« Ton « fait quand » demande une lettre-équivalente conforme sur au
> moins une compétence **bancée en réception**. Le `03-` §7 écrit que le protocole du banc de réception
> « reste à écrire », et `copies-tests/lecture/` n'existe pas (vérifié). Une clause de recette ne se
> négocie pas en séance — c'est donc une décision, pas une interprétation. »*
> → **« Même règle : fais comme si »**

**Ce que j'en ai fait du `07-` §2** *(inventaire des lots — section **OUVERTE À L'IMPLÉMENTATION**,
amendée depuis ce relevé, sans accord préalable)* :

- **la ligne de manifeste** passe de `competences/*.md` — **versé et bancé** à **relu et validé**, avec
  le motif de C4-L5 **recopié** : *« c'était le SEUL endroit du chantier où le banc conditionnait une
  lecture — le `03-` §9 confie ses trois conditions au professeur, le `01-` §3 dit qu'il choisit sans
  automatisme. Un contrôle plus strict que sa source refuse du licite, en silence »*, augmenté du fait
  qui rendait ce cas plus grave que celui de C4-L5 : **le blocage granulaire prenait les quatre
  compétences d'un coup, c'est-à-dire tout le périmètre du lot** ;
- **le « fait quand »** porte la mention **ATTEINT le 27/08**, avec la levée de la clause « bancée en
  réception » **dite explicitement comme une décision de Louis**, et le motif — le banc n'existe pas ;
- **l'état de clôture** du lot, en six puces.

⚠️ **Le `03-competences.md` et les six fiches n'ont PAS été touchés** — ils sont GELÉS. Le seul geste
qui les concerne est le `[faux]` de la dette D9, qui **marque** sans corriger.

**Le `07-` passe donc de 2.56 à 2.57.** ⛔ **Le §4 n'a pas bougé.**

---

## 3. Où la porte de mode vit, et le motif exact qu'elle sert

**Deux endroits, deux rôles** — et c'est le patron des slots, appliqué à une donnée qui n'est pas de
chargement.

**(a) LA DÉCLARATION**, au branchement, à côté de `slotsFournis` : `modesCouverts: readonly Mode[]`,
**champ obligatoire** de `BranchementCompetence` *(donc tenu par le compilateur)*. C'est le seul endroit
du dépôt où *« ce que cet instrument couvre »* a un sens : la dérivation ne connaît que les phases
`P1`/`P2` et ne sait rien des modes ; la table dérivée `competences_modes_admis` parle de la
**compétence**, pas de son **outil**.

**(b) LE CONTRÔLE AU CHARGEMENT**, `refusDeModes` dans `verifierCoherence()` : une déclaration vide ou
qui nomme un mode inexistant **arrête le module**. ⛔ **Et fermé par défaut** : si la déclaration vide
passait quand même, `modeNonCouvert` **refuse tout** plutôt que de tout laisser passer — *une garde qui
existe contre un silence ne doit pas se taire quand sa propre déclaration manque.*

**(c) LE REFUS**, sur l'exercice réel, dans `competencesDeLExercice` — un troisième motif à côté du
statut `differee` et de la compétence non branchée. ⚠️ **Un mode est une donnée d'exercice, pas de
chargement** : `verifierCoherence()` ne peut pas le voir.

### Le motif servi, mot pour mot

> *mode « expliquer » non couvert par l'instrument de structure, qui ne couvre que « composer » — sa
> grille pour ce mode n'a rien de machine (ni prompt marqué, ni enum au bloc machine, ni cascade), et
> mesurer ici produirait un squelette de COMPOSITION sur une copie de lecture, rangé dans le signal de
> ciblage réceptif (`03-` §4 ; `01-` §3). La couverture se déclare au branchement (`modesCouverts`),
> quand le banc lecture aura validé l'étape de notation réceptive de la fiche.*

⭐ **Ce motif est SERVI** — le bilan d'un dépôt l'affiche — et *« un motif faux ne se lit pas comme un
commentaire faux : il se croit »*. Il nomme donc **les trois choses** : la compétence, le mode élu, et
ce qui manque. **Et il nomme sa condition de reprise**, pour que le lecteur sache quoi faire.

### La preuve PAR L'ÉCHEC — un dépôt écarté sans un appel payé

Sur le dépôt de recette *(section D du script, `--avec-chaine`)* :

```
`api_couts` : 6 ligne(s) — {"questionnement":3,"synthese":3}
✓ ZÉRO APPEL PAYÉ sur `argumentation` et `structure`
✓ 2 squelette(s) écrit(s), dont 2 avec leur VERDICT : questionnement, synthese
✓ et AUCUN squelette de composition n'a été écrit sur une copie de lecture
✓ et AUCUNE mesure fausse n'est entrée dans le signal de ciblage réceptif du routeur
```

⚠️ **La lecture est PRUDENTE, et elle se dit.** On refuse dès qu'**UN** mode élu n'est pas couvert, et
non « dès qu'aucun ne l'est » — parce que la mesure porterait le mode non couvert dans sa colonne
`modes`, donc entrerait dans le groupe de ciblage correspondant. C'est la même prudence que
`prePhase2`, qui se déclare réceptif dès que l'un des modes l'est *(`C4L10Q-18`)*. ⛔ **Je n'ai rien
tranché** : *mesuré en base — **504 couples (exercice × compétence) en bac à sable, 488 en prod, aucun
à plus d'un mode**.* L'arbitrage est **inerte aujourd'hui**, et il est écrit pour le jour où il ne le
sera plus. ⚠️ **Une liste VIDE ne refuse rien** — *mesuré : zéro couple à liste vide dans les deux
bases* — parce qu'inventer un `composer` implicite serait décider à la place de la donnée.

---

## 4. L'inventaire de l'écart, en base — admis contre couvert

**Les 13 couples admis** *(`competences_modes_admis`, table **dérivée**, vérifiée dans **les deux
bases** — identiques)* contre **ce que les instruments couvrent** :

| Compétence | Admis *(table dérivée)* | Couvert *(branchement)* | Écart |
|---|---|---|---|
| `expression` | `composer` | `composer` | — |
| `connaissance` | `composer` | `composer` | — |
| **`argumentation`** | `composer` · **`expliquer`** · **`évaluer`** | `composer` | ⛔ **2** |
| **`structure`** | `composer` · **`expliquer`** | `composer` | ⛔ **1** |
| `synthese` | `restituer` | `restituer` | — *(et il est **réceptif**)* |
| `questionnement` | les **cinq** | les **cinq** | — |

⭐ **L'écart vaut exactement TROIS couples**, et un test le tient
*(`utils/chaine/instruments.test.ts`)*.

### Les exercices et les dépôts concernés — mesurés le 27/08, en lecture seule

**PROD** *(`ucmngachkxvvlegntuwh`)* — **1 exercice sur 152** porte un mode réceptif :

| Exercice | Statut | Modes élus | Dépôts | Squelettes | Mesures |
|---|---|---|---|---|---|
| `b5f7a719-…` | `assigne` × `classe`, référence **validée**, `cible_primaire` **NULL** | `synthese:[restituer]` · `expression:[composer]` · **`structure:[expliquer]`** · **`argumentation:[expliquer]`** | **23**, dont **13 `v1_remis`** et 10 `ouvert` | **0** | **0** |

**BAC À SABLE** *(`aoakpxxlyvthzueaywna`)* — **3 exercices sur 164**, mêmes couples : deux `classe` à
référence validée *(7 dépôts `ouvert` chacun)*, un `maison` sans référence *(2 dépôts `assigne`)*.

**Les couples réellement élus, dans les deux bases** : `argumentation×composer` ·
**`argumentation×expliquer`** · `connaissance×composer` · `expression×composer` ·
`questionnement×composer` · `structure×composer` · **`structure×expliquer`** · `synthese×restituer`.
⭐ **Deux des trois couples de l'écart sont donc réellement élus** ; `argumentation × évaluer` est
admis et jamais instancié.

**Les mesures existantes, par (compétence × mode)** — *avant ce lot, et c'est le point* :
prod `{structure×composer: 49, expression×composer: 50, argumentation×composer: 50}` · sandbox
`{argumentation×composer: 2, expression×composer: 1}`. ⭐ **Zéro mesure portant un mode réceptif, dans
les deux bases** — le Questionnement n'avait **jamais** tourné en réception.

---

## 5. Ce que le Questionnement fait en mode réceptif

⭐⭐ **`C4L10Q-15` était décoché alors que sa condition de reprise était levée depuis le 23/08**
*(commit `4e865b5` : `FOURNISSEURS_NATIFS` porte six noms, `reference` et `source` compris)*, et
**personne ne l'avait relue** — son jumeau `C4L10SY-14` avait été coché, pas lui. **Je l'ai coché**, en
conservant l'énoncé d'origine pour mémoire du diagnostic.

**Ce qu'il fait, constaté en base** *(traversée payante, dépôt réel de lecture, texte de Descartes)* :

| | Valeur |
|---|---|
| squelette | **écrit**, extraction **et** verdict |
| `modes` en base | `["expliquer"]` |
| `lettre_equivalente` | **`E`** |
| `instrument_version` | `2.2` |
| référent servi | `armature.question_directrice` = *« Le doute porté sur toute chose laisse-t-il subsister une certitude, et de quelle nature ? »* |
| appels | **3** *(p1, p2, + sa part du retour)* |

⚠️ **Son verdict est `Absent`, et c'est une mesure, pas une panne** : l'alerte du branchement dit
*« `copie_sans_question` : aucune question au relevé — niveau Absent »*. La copie de recette pose une
interrogation indirecte *(« On peut se demander de quelle durée… »)* que le module n'a pas relevée comme
une question. **C'est un jugement de l'instrument sur une copie, exactement ce qu'on lui demande.**

### RR4 — refusé ou non, et combien de fois

⛔ **NON REFUSÉ, et le compte est de 1 sur 1.** Le retour a été **écrit et publié** *(registre
`descriptif`)*. ⚠️ **Un tirage ne fait pas une fréquence** : `C4L10Q-16` observait un refus *« une fois
sur deux tours »* quand le retour emploie « recadrage » ou « enjeu » — les deux seuls observables du
corpus qui soient des mots français ordinaires. **`C4L10Q-16` reste donc ouvert**, et il est reporté au
`SUIVI_tests_manuels.md` sous `C5L3-12` avec sa condition de reprise : *rejouer et compter.* ⛔ **Non
corrigé, et pas par moi** : RR4 est au `01-` §12, **source gelée et hors de mon manifeste**.

---

## 6. Ce que la Synthèse prouve comme mesure de RÉCEPTION

**La Synthèse est mono-mode `restituer`, et `restituer` est un mode réceptif** : *« à l'inverse la
Synthèse, mono-mode `restituer`, n'a QUE celle-là »* *(`03-` §4)* — **sa grille réceptive EST son
instrument unique**. Elle mesurait donc déjà en réception depuis C4-L10 ; ce que ce lot apporte est de
**le prouver comme tel**.

| | Valeur |
|---|---|
| squelette | **écrit**, extraction **et** verdict |
| `modes` en base | `["restituer"]` |
| `lettre_equivalente` | **`D`** |
| `instrument_version` | `3.4` |
| référent | **texte** — donc **deux** appels de P1 *(`p1a` relevé aveugle + `p1b` aligneur)*, plus P2 |
| appels | **3** |

⭐ **Le référent TEXTE est prouvé** — et c'est ce que son en-tête niait encore ce matin. `ctx.reference`
**et** `ctx.materiau` descendent tous deux par la même jointure *(1166 caractères de matériau mesurés)*,
l'aligneur reçoit `{reference_decomposee}`, et le pré-relevé mécanique a de quoi calculer sa compression
et ses recouvrements.

### Ce qui l'en sépare encore d'une lettre honnête

1. ⛔ **Le module lit un format qui n'existe pas.** `copies-tests/synthese/code.py` lit
   `reference["unites"]` et `moments[].unites` ; **ni l'une ni l'autre n'est au format canonique**.
   `normaliserReference()` comble l'écart **au harnais**, additivement. ⚠️ *Le contrat pose que « le
   parsage tolérant appartient au BANC, pas au module », et sa condition de reprise est le pré-vol du
   Run 1 (`C4L10SY-20`).* **Non déplacé, non « réparé ».**
2. ⚠️ **Elle n'a ni gold, ni copie, ni critère, ni run stocké** *(`VERSION_GOLDS_TESTEE = None`)* : la
   lettre `D` est **le produit d'un instrument non calibré**. C'est exactement ce que la clause « bancée
   en réception » demandait, et que Louis a levé pour ne pas bloquer le lot.
3. ⚠️ **Une alerte du juge subsiste** : *« la justification du juge contient un nombre — elle ne doit
   rien compter »*. Elle est **du ressort du banc**, pas du branchement.

---

## 7. La règle de lecture déclarée, par compétence — et le sort de `relance`

*« On ne passe à un consommateur que ce que sa règle lit »* *(`05-` §1)*. `valeursServies()` existait
depuis C5-L1, **éprouvée et sans aucun consommateur de production** — seuls ses tests et le script de
recette de C5-L1 l'appelaient. **Elle en a un.** La restriction s'applique dans `chaineDUneCompetence`,
**avant la sérialisation, donc avant tout prompt**.

| Compétence | Ce que sa règle LIT | Ce qu'elle ÉCARTE | D'où vient la règle |
|---|---|---|---|
| **`synthese`** | `defend_these` · `explique` · `illustre` *(fonctions de phrase)* ; **tous** les statuts d'énonciation ; **toutes** les fonctions de moment | **`relance`**, la quatrième fonction de phrase déclarée au `02-` | ⭐ **de SA PROPRE FICHE** — `bloc_machine.squelette.catalogue.fonctions_reference`, versé par la dérivation. *Recopier cette liste ici lui donnerait un second domicile, et « une liste fermée qui vit à deux endroits diverge ».* |
| **`questionnement`** | **rien** des unités — il ne lit qu'`armature.question_directrice`, qui n'est pas une unité et descend **intacte** | **toutes** les fonctions de phrase, **toutes** les fonctions de moment, **tous** les statuts | de sa fiche §4, en toutes lettres : *« c'est son champ `armature.question_directrice`, et **le module n'en lit aucun autre** »* |
| les **quatre** autres | **tout** — aucune règle déclarée | rien | *« Un champ absent veut dire : il lit tout ce que le `02-` déclare — on ne retranche que ce qu'un consommateur a NOMMÉ. »* Elles ne reparsent même pas la référence : leur donner une règle serait **déclarer une lecture qui n'existe pas**. |

### Le sort de `relance` — le cas que le `05-` §1 nomme lui-même

*« Une phrase dont `relance` est la SEULE fonction ne porte aucun contenu à restituer et n'est donc pas
une unité pour la Synthèse. »*

**Mesuré sur la vraie référence en base** *(Descartes, `ff46eabc`, validée)* : **17 phrases, dont 4 —
les n° 5, 11, 14 et 16 — dont `relance` est la seule fonction.**

⭐⭐ **Ce que la règle bouche, et comment.** Sans elle, chacune de ces quatre coûtait au module **DEUX
alertes** : *« fonction inconnue « relance » — écartée du calcul du statut »*, puis *« aucune fonction
lisible — unité sans statut, écartée des décomptes de couverture »*. **Et la première était FAUSSE** :
`relance` n'est pas inconnue — elle est **déclarée au `02-` et non lue par ce consommateur**. C'est
exactement la distinction que l'asymétrie de `valeursServies` porte :

- une valeur **déclarée au `02-`** que la règle ne lit pas → **écartée EN SILENCE** *(« une valeur
  nouvelle est INERTE tant qu'aucune règle ne la lit »)* ;
- une valeur **NON déclarée au `02-`** → **atteint quand même le consommateur, et lève une alerte**
  *(« c'est un vrai défaut, et il doit se voir »)*.

⛔ **Je n'ai pas symétrisé ces deux branches.** **Résultat** : de 2 alertes par phrase à **1**, et celle
qui reste est **juste** — cette unité n'a rien à restituer. *C'est la dette `C4L10SY-21`, bouchée sans
toucher à la fiche.*

⛔ **ET L'UNITÉ N'EST PAS RETIRÉE, c'est une décision.** La retirer de `phrases[]` aurait fait mentir
les **MOMENTS**, dont l'intervalle `de..a` est garanti *« contigu, sans trou ni chevauchement »* par le
validateur et que `normaliserReference` déplie en unités : un moment aurait réclamé une unité absente.
*Le format n'est pas à nous ; ce qui est à nous, ce sont les **valeurs** qu'on sert.* ⚠️ De même, une
fonction de **moment** écartée devient **absente**, jamais vide — le format la déclare au singulier, et
servir `[]` changerait son type sous le consommateur.

---

## 8. La dette `D9`, et les sources trouvées fausses en chemin

### D9 — `competences/structure.md` §3

| | |
|---|---|
| **Avant** | *« **Le bloc machine** — le volet `squelette` déclare **les listes fermées de cette grille** — vit à la fin du §4, avec les deux autres volets. »* — sous le titre « La grille réceptive — mode `expliquer` », donc : le bloc machine porterait les enums **réceptifs**. |
| **Après** *(rédaction proposée)* | celle de l'Argumentation, **mot pour mot** : *« le volet `squelette` déclare les listes fermées **du squelette de composition** […] **les enums de cette grille n'y entreront qu'avec l'étape de notation réceptive**, quand le banc lecture l'aura validée »*. |
| **Vérifié sur pièce** | le bloc `<!-- DEBUT-CONFIG -->` porte `roles`, `gestes`, `probleme_forme`, `statuts_composes`, `natures_couture` — **tous de composition** — et **ni `decoupage_present`, ni `decoupage_conforme`, ni `fonction_moments`**. *Re-vérifié sur l'instrument **dérivé**, qui est ce que la chaîne lit.* |
| **Ce que ça coûte** | ⚠️ **Ce n'est pas une coquille : l'énoncé trompe un lot.** Il fait croire qu'un instrument réceptif de la Structure existe côté machine, alors qu'il n'y a **ni prompt marqué, ni enum, ni cascade active**. |

⚠️ **Le `[faux]` est posé dans la PROSE du §3**, jamais dans le bloc `<!-- DEBUT-CONFIG -->`, qui est
**dérivé** — *y écrire une marque l'enverrait dans l'instrument*. C'est la règle qu'ont suivie **D2** et
**D5**. **Ligne ajoutée à la section DETTES de `INVENTAIRE_Non_Tranches.md`** *(le compte passe de 7 à
8)*, avec son avant / après.

⭐ **Et la re-dérivation le prouve** : après `derive-instruments.py --ecris`, **seule l'empreinte de
`competences/structure.md` a bougé** — aucun prompt, aucun enum, aucun observable. *C'est la leçon de
C5-L1, vue en direct : le `--verifie` d'une dérivation mord sur l'empreinte de sa source, pas sur son
contenu utile.* Idem pour la fixture de la doctrine : **identique à l'octet près hors `_derivation`** —
donc **aucune migration**, et aucun `--sql` à rejouer.

### Aucune autre source trouvée fausse

J'ai relu, en cherchant précisément cela : les §3, §4 et §5 des quatre fiches réceptives, le `03-` §4,
§5, §7 et §9, le `07-` §1, §2, §3, §5 et §6. **La formulation de l'Argumentation est juste**, celle de
la Structure ne l'était pas — c'est le seul écart.

---

## 9. Les renvois de périmètre, AVEC LEUR LISTE

*« Je ne fais pas X, c'est ton lot » n'est pas un dépôt* — chacun est nommé **avec son fichier, sa
ligne et sa condition de reprise**, et tous partent au `SUIVI_tests_manuels.md` *(C5L3-11 à C5L3-17)*.

1. ⛔ **L'instrument réceptif de l'ARGUMENTATION** — `competences/argumentation.md` §3 et §4.
   Manquent : le prompt marqué *(aucun marqueur réceptif sur les sept fiches ; la dérivation ne connaît
   que `P1`, `P2`, `P1A`/`P1B`)*, les enums au volet `squelette.catalogue` *(`garant`, `mode_discursif`,
   `objection_cible`, `charite`)*, la cascade, l'étape de notation *(déclarée inactive par la fiche)*.
   **Sa cascade réceptive part de `garant × mode_discursif × garant_valide` quand la composition part de
   `statut_du_lien` sur `unites[]` : rien ne se recouvre.** **Reprise** : le banc lecture valide →
   `modesCouverts` passe à `['composer', 'expliquer', 'évaluer']` à
   `utils/chaine/branchements/argumentation.ts`.
2. ⛔ **L'instrument réceptif de la STRUCTURE** — `competences/structure.md` §3 et §4. Manquent :
   `decoupage_present`, `decoupage_conforme`, `fonction_moments`. **Reprise** : idem, vers
   `['composer', 'expliquer']`. *C'est le couple que `C5-L2` a fait mesurer par l'instrument de
   composition.*
3. ⛔ **Le banc de calibration lecture** — `03-` §7 : *« Protocole du banc de réception : reste à
   écrire »* ; `copies-tests/lecture/` **absent** *(vérifié : `copies-tests/` en porte douze, dont les
   sept fiches)*. **Reprise** : une séance de conception. ⚠️ *C'est la clause du « fait quand » que
   Louis a levée le 27/08 ; elle ne se lève qu'une fois.*
4. ⚠️ **Le statut `evaluee` du Questionnement, posé sans son banc** — le `03-` §9 lui impose, **à lui
   seul**, quatre lots de banc dont **deux en modes réceptifs**, avant la bascule. Il est `evaluee`
   dans les deux bases depuis le 23/08. *« Une contrainte de protocole n'est appliquée par aucun
   code. »* **Reprise : décision de Louis.** ⛔ Le statut appartient au professeur.
5. ⚠️ **`competences_statut_recette` — SEPT lignes en bac à sable, CINQ en prod.** `connaissance` et
   `monitoring` **n'ont pas de ligne du tout** en production. *« Pas de ligne » n'est pas « pas
   d'objet ».* **Reprise : l'écran de la fabrique (C4-L8).** ⛔ C'est une **donnée**, pas un défaut de
   code.
6. ⚠️ **Les deux en-têtes de branchement étaient PÉRIMÉS — je les ai corrigés.** `synthese.ts` et
   `questionnement.ts` affirmaient tous deux *« LA CHAÎNE NE SERT PAS LA RÉFÉRENCE DÉCOMPOSÉE : son
   contexte porte QUATRE noms »* — **faux depuis le 23/08**. Corrigés, plus deux commentaires de
   `instruments.ts` *(l. 476 et 510)*, un commentaire de `prePhase2`, un de `prePhase1b`, et **le titre
   d'un test** qui disait « LA CHAÎNE NE SERT AUCUNE RÉFÉRENCE » alors qu'il éprouve **une garde**.
   *Ce sont des commentaires, pas des sources.*
7. ⛔ **`exercices.reference_id` reste sans écrivain à l'IMPORT**, qui y met toujours `null`.
   **Destinataire : `C4-L8`**, déjà déposé par C5-L2 — je le confirme, je ne le redouble pas.
8. ⚠️ **RR4** *(`C4L10Q-16`)* — voir §5. Non corrigé : `01-` §12, source **gelée et hors manifeste**.
9. ⚠️ **La question de source de `C5-L2`** *(règle 1 du gabarit contre RR3)* — voir §10. **Le §4 est
   GELÉ : je ne la tranche pas.** J'apporte **la donnée**.

---

## 9-bis. ⭐⭐ LE SMOKE PROF, JOUÉ — ET IL A TROUVÉ QUE MON MOTIF N'ALLAIT NULLE PART

*Joué le 27/08 sur session prof authentifiée (bac à sable), après que Louis a libéré le serveur.*

⛔ **LE DÉFAUT.** `competencesDeLExercice` porte depuis toujours la note *« ce motif-là est SERVI : le
bilan d'un dépôt l'affiche. Un motif faux ne se lit pas comme un commentaire faux — il se croit »* — et
**mon prompt la répétait en piège 14**. **Elle était fausse.** `competencesEcartees` ne vivait que dans
la valeur de retour **EN MÉMOIRE** de `traiterDepot` ; les deux seuls résumés qui persistent —
`resume()` *(`utils/deroule/mesure.ts`)* et `resumeBilan()` *(`utils/chaine/chaine.ts`)* — **ne le
mentionnaient pas**, et `exercices_jobs.dernier_message` est le seul canal durable qu'un écran lise.
**Ma porte refusait juste, et en silence.** *C'est « une trace n'est pas un état », pris par l'autre
bout : ici l'état existait, et il n'avait pas de trace.*

⭐ **CORRIGÉ, ET C'ÉTAIT DANS MON PÉRIMÈTRE** : le motif est la sortie de mon propre geste, et la
mission dit *« qu'elle refuse […] **et qu'elle le dise** »*. `motifDesEcartees()` porte les écartées
dans les deux résumés. **Éprouvé en base, par la file réelle** :

```
job mesure_v1 · abouti · 2 mesurée(s), 2 écrite(s), 0 déjà là, retour écrit, 6 appel(s), 56 s,
2 écartée(s) — structure, argumentation : mode « expliquer » non couvert par l'instrument de structure…
```

⚠️ **DEUX LEÇONS DE RECETTE, ET LA PREMIÈRE EST LA PLUS UTILE.**

1. **Une recette qui court-circuite le chemin de production ne prouve pas le chemin de production.** Mon
   script appelait `traiterDepot` **en direct** ; or `resume()` et `resumeBilan()` **ne sont appelés que
   par la file**. Le script passe désormais par `mettreEnFile` + `reclamerJobs` + `tourDeFile` — le
   patron exact de `app/api/chaine/route.ts`. *Sans ce changement, le correctif aurait été écrit et
   jamais éprouvé.*
2. **L'ordre des mots dans un champ borné se choisit.** Ma première version rendait
   `nom — motif | nom — motif`, bornée à 400 caractères : **la troncature mangeait le SECOND NOM**, et
   le professeur lisait « 2 écartée(s) » en n'en voyant qu'une. Les **noms passent devant, toujours
   entiers** ; le détail suit, borné. *Savoir LESQUELLES coûte trois mots ; savoir POURQUOI peut coûter
   trois écrans.*

⛔ **ET LA MOITIÉ QUI RESTE EST UN ÉCRAN — DÉPOSÉE À `C5-L4`, PAS RÉPARÉE.**
`etatChaineDeLaCopie()` *(`utils/passation/file-copie.ts:118`)* ne sert `motif` que sur **deux** états —
`echec` et `sans_retour`. Sur **`abouti`**, il rend `phrase: 'Traitement terminé.'` et **`motif:
null`**. ⚠️ **Conséquence sur l'exercice de prod** *(13 dépôts remis × 2 compétences écartées)* : le
professeur lira **treize fois « Traitement terminé. »** et **n'apprendra jamais que la moitié des
compétences élues n'a pas été mesurée**. *Une mesure qui n'a pas eu lieu ne se voit pas — c'est
exactement ce que la porte existait pour dire.* ⛔ **Mon entrée exclut « tout écran » en toutes
lettres** : item déposé dans la boîte de `C5-L4` *(`PLAN_DE_CHANTIER.md` §5)* et au
`SUIVI_tests_manuels.md` sous `C5L3-11-bis`. ⭐ *Le geste est petit — un état `abouti` qui porte son
motif, ou un sixième compteur à « LA FILE » ; le motif est déjà en base.*

⚠️ **L'ancrage du retour n'est PAS déterministe** — trois traversées : **1/1**, puis **2 copie / 0
texte_support**, puis **1/1**. *C5-L2 mesurait 4/4 sur la copie. Le canal `texte_support` est donc
atteignable en réception mais minoritaire, et un tirage ne tranche rien.*

---

## 9-ter. ⭐⭐ QUAND LA PORTE ÉCARTE TOUT — le cas que je n'avais pas éprouvé

**Il n'est pas théorique.** L'exercice `473b2c25` du bac à sable élit **uniquement**
`argumentation: [expliquer]` et `structure: [expliquer]` — les deux couples que la porte refuse. Il ne
reste alors **rien** à mesurer, et une porte neuve est exactement le genre de chose qui fait lever une
chaîne sur ce cas-là. **Éprouvé** *(section D-bis du script, dépôt réel)* :

| Ce qui est vérifié | Résultat |
|---|---|
| compétences qui passent la porte | **0** sur 2 |
| la chaîne lève-t-elle ? | **non** — bilan rendu |
| lignes d'`api_couts` sur tout le dépôt | **0** — *un exercice de lecture qui n'élit que des couples non couverts ne coûte plus rien* |
| mesures écrites | **0** |
| la chaîne le dit-elle ? | **oui** — *« aucun squelette : aucune compétence n'est entrée dans la chaîne — pas de retour engendré (clause granulaire) »* |
| ce que l'écran demande à l'élève | **rien** — aucune confiance réclamée, ce qui est juste |
| le message persisté | *« 0 mesurée(s) […] retour non écrit […] **2 écartée(s) — structure, argumentation** : mode « expliquer » non couvert… »* |

⭐⭐ **Et ce cas précise le dépôt fait à `C5-L4`** : sans retour, l'état de la copie est `sans_retour`,
**qui sert `motif`** — le professeur voit donc bien pourquoi cette copie n'a rien reçu. ⛔ **C'est le cas
MIXTE qui se tait, et c'est celui de la prod** : deux compétences passent, le retour s'écrit, l'état
devient `abouti`, et les deux écartées disparaissent de l'écran.

---

## 9-quater. ⭐⭐ LES TROIS TESTS DEMANDÉS PAR LOUIS — joués le 27/08

### (1) La `vf` — la porte tient-elle quand la chaîne ne rejoue QUE la cible ? **OUI**

*Je ne l'avais éprouvée qu'en `v1`, et je l'avais dit.* En `vf`, `competencesFroides = [cible]`
*(`01-` §11)*, et la cible vient de `cibleDuRetour(ctx, mesurees)` — **`mesurees` excluant déjà les
écartées, une compétence non couverte ne peut structurellement pas devenir la cible.** *Structurellement
n'est pas éprouvé.* Éprouvé *(section D-ter)* :

| Ce qui est vérifié | Résultat |
|---|---|
| la cible en `vf` | **`questionnement`** — jamais une écartée |
| la porte tourne-t-elle en `vf` ? | **oui** — 2 écartées, `structure` et `argumentation` |
| compétences rejouées | **1** — la vf ne rejoue que la cible |
| mesures écrites en `vf` | **0** — toujours 2 au dépôt *(`07-` §1.2 : « la vf n'écrit jamais dans les mesures »)* |
| appels payés sur les écartées, `vf` comprise | **0** *(3 appels à la vf, tous sur la cible)* |

⚠️ **Un fait attendu, et déjà connu** : `delta_v1_vf` sort **NULL**, avec son alerte nommée — *« le
branchement de la compétence n'en déclare pas le calcul, et NULL n'est pas 0 »*. C'est la dette assumée
du `07-` §2 v2.41 *(registre des ouverts, item 47)*, **pas un défaut de ce lot**.

### (2) RR4 — **0 refus sur 10 tours comptés** *(13 traversées en tout)*

`C4L10Q-16` observait un refus *« une fois sur deux tours »* en **composition**, quand le retour emploie
« recadrage » ou « enjeu ». **En RÉCEPTION, sur 10 tours comptés à copie différente : zéro.**
⭐ *Une explication plausible, et je ne la tranche pas : le retour de lecture parle du texte et de la
question directrice, pas du « recadrage » ni de l'« enjeu » — le vocabulaire qui déclenche RR4 est celui
de la problématisation, donc de la composition.* ⛔ **`C4L10Q-16` reste ouvert** : 10 tours en réception
ne disent rien de la composition, où le défaut a été vu.

⚠️⚠️ **MAIS TROIS RETOURS ONT ÉTÉ REFUSÉS POUR UNE AUTRE RAISON, et ma première instrumentation ne
l'a pas vue.** Elle ne comptait que RR4 : deux refus sur cinq sont passés **sans motif imprimé**. Le
motif, capté ensuite et au tour de `vf` : *« retour refusé : `points[1].ancrage.citation` texte trop
court (0 < 1) »* — une **citation VIDE**, donc un refus de **forme**, pas de RR4. ⭐ **La leçon vaut
mieux que le chiffre : compter les refus d'UNE règle fait manquer ceux de toutes les autres.** *Le
script imprime désormais le motif de tout refus, quel qu'il soit.*

### (3) L'ancrage du retour de réception — **32 % au texte support**

*C'est la donnée que la question de source de `C5-L2` attend, et elle penche nettement.*

| | Points « copie » | Points « texte_support » | Total | % au texte |
|---|---|---|---|---|
| **C5-L2**, en composition *(pour mémoire)* | 4 | **0** | 4 | **0 %** |
| **C5-L3**, en réception — 13 traversées | **15** | **7** | 22 | **32 %** |

⭐⭐ **Le canal `texte_support` n'est donc PAS mort** : `ancrage.source` cessait d'être *« un enum à deux
valeurs dont une seule est employée »* dès qu'on mesure **en réception**. ⚠️ **Il est aussi très
variable** — deux séries de cinq tours ont donné **17 %** puis **40 %**. *Le tirage compte autant que le
mode.* ⛔ **Je ne tranche pas la question de source** : la règle 1 du gabarit *(`07-` §4, **GELÉ**)* fait
ancrer « sur une citation du squelette », et le squelette est fait de la copie. **J'apporte le compte.**

---

## 9-quinquies. ⭐⭐⭐ LE SMOKE PROD — la porte tient, et il a trouvé un défaut que le bac à sable NE POUVAIT PAS montrer

*Joué le 27/08 sur la prod, session prof authentifiée. **13 copies réelles**, exercice
`b5f7a719`, classe de 23 élèves.*

### Ce qui est prouvé, en base

| Ce qui est vérifié | Résultat |
|---|---|
| mesures écrites | **26** — exactement **2 par copie** : `expression×composer` (13) et `synthese×restituer` (13) |
| lettres | Expression `B C C D D D D D D D D D E` · Synthèse `C C D D D D D D D D D E E` |
| mesures sur `argumentation` / `structure` | **0** ✓ |
| squelettes | `{expression: 13, synthese: 13}` — **aucun de composition sur une copie de lecture** |
| **appels payés sur `argumentation` / `structure`** | **0** ✓ — 80 appels de mesure, tous sur les deux couvertes |
| jobs dont `dernier_message` nomme les écartées | **13 / 13** ✓ — *le correctif du smoke bac à sable tient en prod* |
| retours engendrés | **13**, aucun publié *(geste du professeur)* |
| ancrage | 46 « copie » / **8 « texte_support »** sur 54 points — **15 % au texte** |

⭐⭐ **Un effet de bord que personne n'avait prévu, et il va dans le bon sens.** `cible_primaire` est
`NULL` sur cet exercice, donc le retour tombe sur le **repli alphabétique**. *Avant* la porte, les
mesurées auraient été `argumentation, expression, structure, synthese` → l'alphabétique élisait
**`argumentation`**, celle dont l'instrument réceptif n'existe pas *(c'est le piège 13)*. *Après*, il ne
reste que `expression` et `synthese` → **c'est l'Expression qui commande le retour.**

### ⛔⛔ ET LE DÉFAUT QUE LA PROD A TROUVÉ — `competences_couvertes[]` portait les deux écartées

**J'avais affirmé le contraire, et j'avais amendé le `07-` §1.4 là-dessus.** C'était faux.

⚠️ **Pourquoi mon contrôle de recette était vert** : il portait sur les lignes de `monitoring_mesures`
du dépôt — et **il n'y en avait aucune** en bac à sable, les deux drapeaux du Monitoring
(`optin_se_juger`, `optin_confiance_remise`) étant des gestes de la **passation en classe**, quand mon
décor était à la maison. **`.every()` sur une liste vide est vrai.** ⭐⭐ **Prouver une négation sur un
ensemble vide ne prouve rien** — et c'est la leçon la plus chère de la séance.

⚠️ **La cause est plus large que ma porte.** Les entrées de la calibration viennent de
**`ctx.confianceDeclaree`** — ce que l'**ÉLÈVE** a déclaré à sa remise —, et `competencesQuiComptent()`
ne filtrait que sur le **statut de recette**. Les quatre compétences étant `evaluee`, les deux écartées
entraient dans `competences_couvertes[]` **avec un `niveau` à `null`** : la calibration comparait une
confiance déclarée à un niveau qui n'existait pas, et la ligne prétendait couvrir ce qu'elle n'avait
pas mesuré. ⚠️ *Ces élèves ont déclaré leur confiance le 25/08, **avant que la porte existe** — l'écran
leur demandait les quatre. Il n'en demande plus que deux depuis, mais **une remise antérieure à un
changement de couverture porte plus de déclarations que de mesures**, et c'est une propriété
permanente, pas un accident de calendrier.*

⭐ **Corrigé** : `competencesQuiComptent()` prend désormais les niveaux obtenus et écarte, **avec son
motif nommé**, toute compétence que la chaîne n'a pas mesurée. ⭐⭐ **Le discriminant est la PRÉSENCE DE
LA CLÉ, jamais sa valeur** — une compétence mesurée sans lettre porte sa clé à `null`, une compétence
écartée n'y est pas du tout : *comparer la valeur confondrait « mesurée sans lettre » et « jamais
mesurée ».* Quatre vecteurs à `utils/chaine/monitoring-calcul.test.ts`, **sur la fonction**, où
l'ensemble n'est jamais vide. Et le contrôle de la recette **refuse désormais de se déclarer vert sur
un ensemble vide** : il dit que le cas n'est pas couvert.

⚠️⚠️ **HUIT LIGNES DE PROD PORTENT ENCORE LA TRACE** — les 8 `monitoring_mesures` de sous-dimension
`calibration_confiance` écrites avant le correctif listent quatre compétences quand deux ont compté.
⛔ **Non réparées : c'est une écriture sur des données de prod, et c'est la décision de Louis.**
*Rien d'aval ne les lit encore — la table de conversion n'est pas écrite, et le principe est
« collecter d'abord, convertir ensuite ».*

---

## 10. Cinq faits que la séance a fait apparaître

**(1) ⛔ LA SOURCE *VEUT* `argumentation × expliquer` — et la porte ne la contredit pas.**
`MODES_MESURES` *(`utils/examens/types.ts:90`, arrêté au `01-` §10 et **recopié, jamais dérivé**)* élit,
pour l'explication de texte : `expression: [composer]`, **`argumentation: [expliquer]`**,
**`structure: [expliquer]`**, `synthese: [restituer]`. **C'est l'écrivain de l'exercice qui attend en
prod.** ⭐ *La porte ne retire donc rien à l'élection : elle refuse **la mesure** que l'instrument ne
sait pas faire, et elle le dit. Le jour où les deux instruments existeront, rien ne changera de ce
côté-là — seule la ligne `modesCouverts` bougera.*

**(2) ⚠️⚠️ `competences_mesures` N'A PAS DE COLONNE `version` — ET LE PIÈGE A MORDU DANS CE LOT MÊME.**
La version vit sur le **squelette**, jamais sur la mesure *(cohérent avec « la vf n'écrit jamais dans
les mesures »)*. Mon premier script de recette interrogeait `.eq('version', 'v1')` : PostgREST a rendu
**`42703`**, **`supabase-js` n'a pas levé**, `data` est sorti `null` — et **la recette a annoncé « 0
mesure » sur un tour où deux étaient écrites, avec leurs lettres.** ⭐ *Le lot qui connaissait le piège
est tombé dedans.* **Corrigé** : tout `select` du script passe désormais par un helper `lire()` qui
**surface l'erreur en rouge**. *C'est la moitié invisible de « supabase-js ne lève pas » : ce n'est pas
seulement qu'une écriture échoue en silence — c'est qu'une **lecture mal posée se lit comme une réponse
négative**.* **Porté au `07-` §1.2.**

**(3) ⭐ L'ÉCRAN FILTRE DÉJÀ PAR LA PORTE, ET PERSONNE NE L'AVAIT PRÉVU.** `chargerLeDeroule` ne demande
la confiance de l'élève que sur `synthese, questionnement` — **pas sur les deux compétences que la
chaîne va écarter**. *La cohérence vient de ce que l'écran lit la même fonction ; c'est une propriété
gagnée, pas construite, et elle méritait d'être constatée.*

**(4) ⭐ LA DONNÉE QUE LA QUESTION DE SOURCE DE `C5-L2` ATTEND — et elle penche dans l'autre sens.** Sur
la première traversée d'un retour de **RÉCEPTION**, l'ancrage s'est réparti **1 point « copie », 1 point
« texte_support »** sur 2. ⭐ **C'est la première fois que le canal `texte_support` est employé** :
C5-L2 mesurait *quatre points sur quatre* ancrés sur la copie, et concluait que `ancrage.source` était
« un enum à deux valeurs dont une seule est employée ». **Ce n'est plus vrai en réception.** ⚠️ **Un
tirage ne tranche pas une question de source** — la règle 1 du gabarit *(§4, **GELÉ**)* fait ancrer
*« sur une citation du squelette »*, et le squelette est fait de la copie. **Je compte et je dis** ;
la décision est de source, et le §4 ne bouge pas.

**(5) ⚠️ L'ÉTAT DE LA CHAÎNE APRÈS MESURE RESTE MUET, ET C'EST ATTENDU.** Les deux mesures ont produit
l'alerte *« état de X non réécrit — sans lettre, et aucune ANCRE : sa première lettre vient de sa
première ancre (§9). Le cold start, lui, se pose à la semaine 1 »*. **Les gardes d'aval fonctionnent** —
c'est exactement ce que C5-L2 avait observé. *Ce qui manquait était en amont, et c'était ce lot.*

---

## 11. Ce qui reste à jouer en recette

**Tout est au `SUIVI_tests_manuels.md`, section « C5 · L3 »**, avec sa condition de reprise nommée —
*un reste de recette qui ne vit que dans un relevé ne se rappelle à personne.* En résumé :

| | Ce qui reste | Condition de reprise |
|---|---|---|
| `C5L3-11` | ⛔ **le smoke prof de la file, sur l'exercice de PRODUCTION** — le refus n'a **jamais été vu à l'écran** | une session prof **authentifiée** ; *une session Code ne s'authentifie pas*. ⚠️ **Ce geste PAIE** |
| `C5L3-12` | RR4 : **1 tour sur 1 sans refus** — un tirage ne fait pas une fréquence | rejouer le script `--avec-chaine` et compter |
| `C5L3-13` | l'ancrage du retour de réception : **1 / 1** sur `texte_support` | accumuler des tirages, porter le compte à Louis |
| `C5L3-14` | les **deux instruments réceptifs absents** | le banc lecture valide l'étape de notation réceptive |
| `C5L3-15` | le **banc de calibration lecture** lui-même | une séance de conception |
| `C5L3-16` | le Questionnement `evaluee` **sans son banc** | décision de Louis |
| `C5L3-17` | `competences_statut_recette` : **5 lignes en prod** | l'écran de la fabrique *(C4-L8)* |

---

## 12. Les contrôles machine, aux deux bouts

| Contrôle | À l'ouverture | À la clôture |
|---|---|---|
| `npm test` | **1625 / 1625**, `fail 0` *(le prompt annonçait 1611 : C5-L2-bis en avait ajouté 14)* | **1643 / 1643**, `fail 0` — **+18 vecteurs** |
| les cinq étalons | **156**, verts *(le prompt annonçait 142)* | verts, **+18** |
| `derive-instruments.py --verifie` | `IDENTIQUE` | `IDENTIQUE` *(après `--ecris`, en geste séparé — deux sources ont bougé : le `[faux]` de D9 et la version 2.57 du `07-`)* |
| `derive-doctrine.py --verifie` | `FIXTURE : IDENTIQUE` | `FIXTURE : IDENTIQUE` *(après `--fixture`, en geste séparé ; **contenu identique à l'octet près hors `_derivation`** → **aucun `--sql`, aucune migration**)* |
| `derive-generateur.py --verifie` | `IDENTIQUE` | `IDENTIQUE` — *je n'ai pas touché au générateur* |
| `npx tsc --noEmit` | silencieux | silencieux |
| `scripts/recette/reception-c5l3.mjs --avec-chaine` | — | **37 verts, 0 rouge**, 56,4 s, 6 appels |

⚠️ **Deux écarts avec le prompt, tous deux dans le bon sens, constatés au contrôle d'entrée** :
`main` **était à jour avec `origin/main`** *(le prompt annonçait « deux commits en avance » : C5-L2 **et
tout C5-L2-bis** ont été poussés depuis la fabrication)*, et les compteurs avaient monté.

⚠️ **Une modification non commitée préexistait à `PLAN_DE_CHANTIER.md`** — le vidage de la boîte aux
lettres de C5-L3 et la convention de couture, laissés par la session de fabrication du prompt. **Je ne
l'ai pas commitée avec mon lot** : *on ne pousse jamais le travail d'un autre lot avec le sien.*
