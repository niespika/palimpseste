# RELEVÉ — C4-L7 · La recette du flux (24/08/2026)

> **Le lot qui regarde.** Ce lot ne construit rien. Sa réussite ne se mesure pas à un diff : elle se
> mesure à **ce qu'il a fait traverser**, et à **ce qu'il a coché ou requalifié** au suivi des tests
> manuels.
>
> **Diff de la séance** : `SUIVI_tests_manuels.md` *(la convention de clôture)* et **un** fichier
> neuf, `scripts/recette/traversee-c4l7.mjs` — l'instrument de la recette, dix-neuvième du dossier
> —, plus son registre de décor. **Aucune ligne au `SUIVI_SQL.md`. Aucune migration. Aucun `[faux]`
> posé dans une source.** *Zéro diff hors des scripts de recette et du suivi, comme la mission
> l'exige.*

---

## 1. Le contrôle d'entrée

**Les six pièces du manifeste : PASSENT.** `01-routeur.md` **v5.5** *(VALIDÉ ET GELÉ)* ·
`02-exercices.md` **v5.4** *(VALIDÉ ET GELÉ)* · `06-Palimpseste.md` **v2.6** *(VALIDÉ ET GELÉ)* ·
`07-Implementation.md` **v2.44** *(RELU ET VALIDÉ, régimes mêlés)* · les deux journaux.

**La clause de dérive du `07-`, appliquée.** Le numéro a bougé *(2.43 → 2.44, amendement de
C4-L14)*. Ce qui décide est le **diff des trois blocs de l'entrée C4-L7** — manifeste, mission,
« fait quand » : ils sont **identiques au mot près** à ceux recopiés au prompt. *Le numéro qui a
changé ne me concerne pas.*

⚠️ **Deux pièces gelées portent du travail NON COMMITÉ** dans `palimpseste-conception`. Vérifié :
le commité est `01-` **v5.4** et `06-` **v2.5** ; **c'est le non-commité qui porte les versions
exigées**. Les hunks sont terminologiques *(« passations » → « examens » diagnostiques)* et tombent
**hors de §11/§12 du `01-`** et **hors de §1/§2/§3 du `06-`**. Rien à signaler sur le fond.

### ⚠️ Quelqu'un a joué après la fabrication du prompt — trois fois, et je l'ai lu avant tout

| Commit | Ce qu'il a fait | Effet sur ce lot |
|---|---|---|
| `fa47bf4` | **« Les recettes rejouées vertes »** — répare les assertions périmées par la pose des statuts, **et** écrit deux outils : `relecture-rr-c4l5.mjs`, `vf-c4l3.mjs` | **Le piège 81 est largement soldé** avant mon entrée ; `scripts/recette/` porte **18** `.mjs`, pas 16 |
| `fc30441` | Naissance de `C4-L12`/`C4-L13` au `07-` §2 ; `derive-instruments --ecris` rejoué par Louis | **`C4L14-11` s'est refermé** |
| `b648740` | **C4-L14**, section datée du **24/08** au suivi et au `SUIVI_SQL` | **Trois entrées neuves me nomment** : `C4L14-12`, `C4L14-14`, `C4L14-15` |

Conséquence : **`C4L3-14` était déjà coché** à mon entrée — `vf-c4l3.mjs` avait produit un dépôt à
deux squelettes et un retour final citant un progrès réel.

### Les deux points d'arrêt — constatés par requête, pas par mémoire

**A.** Les **six** compétences sont `evaluee`, posées le 23/08 entre 21:18:59 et 21:19:56 ;
`monitoring` reste `mesuree_silencieusement`. Le statut vit au **domicile global**
*(`competences_statut_recette`, 7 lignes)*, lu par `utils/statut-recette.ts` et par lui seul ; les
deux colonnes retirées sont **absentes** *(0 et 0)*.

**B.** Le verrou du routeur est bien **la LETTRE**, pas le statut. Vérifié : `routeur_decisions`
**0 ligne** ; **0 dépôt sur 53** ne porte de `routeur_decision_id` ; `poserLaSemaine`
*(`utils/routeur/semaine.ts:123`)* **aucun appelant hors de ses tests** ; `candidatsPour` **aucun
producteur** ; `competences_niveaux` **aucun écrivain dans tout le dépôt** — **0 lettre sur 102
lignes d'état**.

### Les bornes

| | Entrée | Clôture |
|---|---|---|
| `npm test` | **1 260 / 1 260, 0 échoué** | **1 260 / 1 260, 0 échoué** |
| `npx tsc --noEmit` | rien | rien |
| `derive-doctrine.py --verifie` | **12 `IDENTIQUE`** | **12 `IDENTIQUE`** |
| `derive-instruments.py --verifie` | **`IDENTIQUE`** (10 dérivés) | **`IDENTIQUE`** (10 dérivés) |
| les six interrupteurs | **OFF** | **OFF** |
| décor de C4-L6 | **absent** | **absent** |

*Le chiffre du prompt (1 230) était périmé : C4-L14 a ajouté 26 tests, et le geste de Louis a
refermé `C4L14-11`.*

---

## 2. Ce qui a traversé

### La voie de la CLASSE — **57 contrôles, 0 échec** — clause SATISFAITE

Examen diagnostique **conçu** *(Codex, fenêtre `decembre` — `septembre` était prise par le décor de
C4-L9 laissé en base le 23/08, et **on ne sème pas sur le décor d'un autre lot**)* → assigné à
7 élèves → ouvert → transcrit → remis → mesuré → corrigé → **publié** → **lu**.

⭐ **L'ancre est produite** : les trois mesures portent `lieu = classe` **et `forme = sommatif`**,
lue **là où la chaîne l'écrit** *(`competences_mesures.forme`)*, chacune avec sa lettre-équivalente
*(`A`, `B`, `C`)*. Le retour est **publié** puis **lu** *(`lu_at` posé)*, et le dépôt s'arrête à
**`retour_publie`** — *il n'y a pas de version finale en classe*, tenu.

### La voie de la MAISON — **47 contrôles, 0 échec** — puis un mur

Instance conçue à la fabrique → assignation → déroulé → **les trois gestes de la remise dans
l'ordre** *(remise prématurée REFUSÉE ; condition hors des trois valeurs REFUSÉE)* → remise →
`mesurerMaintenant` *(en file **puis** déclenché)* → mesure `lieu = maison`, `forme = formatif`
*(sans ligne de plan — **pas un repli**)*, lettre `C` → version finale → **deux squelettes** →
**retour final engendré** → `delta_v1_vf` **NULL avec son alerte nommée**, motif **lu** et exact.

⛔ **Puis : rien ne peut publier ce retour.** Voir §3.

### La reprise après expiration — `C4L5-3`

Job tué **après P1** *(attente de la première ligne `api_couts` en `p1`, jamais supposée)*, bail
expiré, réclamé par un second ouvrier, chaîne rejouée entière. **Au bout : UN squelette, UNE mesure,
UN job** — et **trois** passages complets ont eu lieu *(9 lignes d'`api_couts`)*, donc les gardes
ont tenu sous plus dur que le scénario écrit.

### La route `/api/chaine` — `C4L11-B`

`{"reclames":1,"traites":1,"arret":"file vide","tuesEnVol":0}` sur une **vraie invocation avec file
chargée**. Les **trois portes** éprouvées dans l'ordre, dont **deux par l'échec** : sans `Bearer`
→ 401 · mauvais secret → 401 · portes fermées → 200 `{gate: …}` **sans réclamer un seul job**.

### La latence

**48,8 s** et **49,5 s** à **trois** chaînes en parallèle ; **31,8 s** / **40,4 s** à une, maison ;
**30,4 s** par la route. Contrat : moins de 180 s. **Il tient avec plus de trois fois la marge**, et
la courbe reste plate. ⚠️ Les mesures à trois sont sur la voie **classe**, où le contrat ne porte
pas ; la variance du fournisseur reste du même ordre que l'effet. **L'extrapolation à six chaînes
reste à faire.**

---

## 3. Les défauts — relevés, non corrigés

### ⛔⛔ Le défaut central : un retour de la MAISON ne peut jamais atteindre l'élève

*Aucun des dix lots qui précèdent ne pouvait le voir : chacun est juste à sa frontière, et le trou
est **entre** eux.*

Établi sur pièces, exhaustivement :

1. la chaîne **n'écrit jamais** `published_at` — aucune occurrence dans `utils/chaine/` ;
2. `publier()` *(`utils/passation/retours.ts:186`)* n'a que **deux appelants de production**, tous
   deux dans le flux de classe ;
3. la pile de correction **exclut la maison par construction** — `utils/passation/vues.ts:49` :
   `if (d.exercice.lieu !== 'classe') return null`, commenté *« un dépôt de maison n'a rien à faire
   ici »* ;
4. **aucun défaut de colonne, aucune fonction, aucun trigger** ne le pose ;
5. `utils/deroule/vue.ts:614` **saute tout retour sans `published_at`**.

⭐ **Vérifié à l'écran de l'élève** : `chargerLeDeroule` charge normalement sur un dépôt `vf_remis`
portant **deux retours réels en base**, et rend **`chaud = ABSENT · final = ABSENT`**.

⚠️ **Ce n'est pas le piège 18.** Le §1.2 interdit l'**édition** d'un retour maison — le trigger
`trg_retour_maison_non_edite` la tient. **La publication est un autre geste**, et le « fait quand »
la présuppose : *« il suppose un `published_at`, qui est la case que coche le professeur »*.

⚠️ **La question qui reste est une décision de SOURCE, pas de code**, et elle appartient à Louis :
publier automatiquement un retour maison en fin de chaîne *(ce que le contrat de latence
suggère — « un retour maison relu un par un est un goulot qui tue la fréquence »)*, ou ouvrir la
pile de correction aux dépôts maison *(ce que le §1.2 écarte)* ?

### Les autres

- **`bilan.appels` peut sous-compter** — 7 annoncés, 8 lignes en base, sur un tour ; **non
  reproduit** au suivant. Origine non établie ; ce qui est écarté sur pièces est dit à `C4L7-7`.
  ⭐ **Le plafond est sauf** : `appelsDuDepot` lit **la base**, pas le bilan.
- **Deux assertions de `deroule-c4l3.mjs` sont périmées par C4-L14** *(l.929 et l.963, sur
  `vue.correctionDuPremierCas`, champ remplacé par `corrections[]` — `vue.ts:116` le dit mot pour
  mot)*. **Le comportement est juste ; ce sont les assertions qui figent un monde d'avant.** Non
  corrigées : ce serait réécrire la recette d'un autre lot.
- **`C4L10-14/15/16` existent dans TROIS sections** — la renumérotation du 23/08 n'a pas couvert ce
  trio, et le prompt affirmait l'unicité des identifiants. Les trois ont été amendées.
- **`SOURCES` et `FIXTURE` disent `DIVERGE`** alors que les douze tables sont `IDENTIQUE` : réduit à
  sa cause — **2 empreintes sur 15**, deux fiches non commitées dans le dépôt conception, **aucune
  ligne dérivée ne diffère**. Rien n'a été rejoué.
- ⚠️⚠️ **Un défaut de la recette elle-même, trouvé et réparé en séance** — et c'est le patron exact
  que ce lot traque ailleurs : mon premier nettoyage supprimait les dépôts **sans leurs enfants**,
  et `competences_mesures.depot_id` est en **`on delete set null`** → **trois mesures `sommatif`
  orphelines**, invisibles à tout contrôle qui compte par dépôt. Constatées, supprimées, **les deux
  chemins de nettoyage corrigés**. *« Un retour ignoré ment » — un `on delete set null` aussi.*

---

## 4. La boîte aux lettres — ce qui est coché, ce qui est requalifié

**Cochés avec leur preuve (9)** : `C4L4-5` et `C4L3-15` *(« se juger » réellement servi, éprouvé par
l'échec)* · `C4L4-6` *(la confiance de remise sert enfin un **objet NON vide** — 3 valeurs, une par
compétence `evaluee` mesurée, comptées en base)* · `C4L4-8` *(les sujets des passations
diagnostiques — la voie est parcourue)* · `C4L5-3` *(la reprise après expiration)* · `C4L11-B` *(le
compteur de la garde de budget)* · `C4L10-15` × **3** *(épuisée : les six sont ouvertes)* ·
`C4L14-11` *(le contrôle de dérivation, refermé par le geste de Louis)*.

**Requalifiés avec leur motif et leur NOUVELLE condition de reprise (8)** :

| Entrée | Ce qui a changé |
|---|---|
| `C4L2-12` | **Condition RÉÉCRITE** : son texte mentait deux fois. Ce n'est plus le statut qui bloque, **c'est la LETTRE** → `C4-L12`, 4ᵉ geste |
| `C4L2-11` | **Chiffré** *(≈ 12 appels)* et **déclaré NON JOUÉ**, avec les deux motifs — dont : l'escalade observée serait déclenchée par des mesures fabriquées par la recette |
| `C4L5-22` | L'alerte du delta **vue tomber** sur une vraie vf, motif **lu** — mais les trois défauts nommés restent entiers : **non cochée** |
| `C4L10SY-18` | Cinq tours de plus ; **l'extrapolation à six chaînes reste à faire** |
| `C4L4-4` | **Le décor est laissé en base exprès**, et le chemin d'écran est écrit pas à pas |
| `C4L3-21` | **Toujours pas de fautes** : condition précisée — *une copie de recette écrite AVEC des fautes*, qu'aucun des 19 scripts ne sème |
| `C4L14-12`, `C4L14-14` | Non levées : **contrainte de rôle** *(une session Code ne saisit pas d'identifiants)* + **aucune instance de cran 1 en base** |
| `C4L14-15` | **Condition CORRIGÉE** : elle demandait un banc de test, donc du code neuf — *« C4-L7 ne construit rien »*. Sa vraie condition est la seconde branche |
| `C4L5-1bis` | Relecture **rejouée** sur les retours neufs : **20 contrôles, 0 échec**, et **le défaut de l'item 49 ne s'est pas reproduit** |

**Non touchés, à dessein** : `C4L2-15` *(tranché le 23/08 — « ce qui reste ici : rien » ; ce n'est
pas à moi de solder une entrée dont le geste part ailleurs)* · `C4L5-4` *(requalifié par Louis, ne
me concerne plus)* · `C4L3-16` *(décision de source — n'invente pas la cinquième colonne)* · les six
entrées neuves du Questionnement et de la Synthèse *(aucune ne m'appartient)*.

---

## 5. Ce que je laisse, et comment le retirer

**En base** : le décor de smoke de la voie classe — ligne de plan `4770f701` *(note
`RECETTE C4-L7`)*, instance `0f6c3fb4`, **7 dépôts**, dont un porte 3 mesures `sommatif` et un
retour **publié et lu**. *Laissé exprès pour que Louis coche `C4L4-4` à l'œil.*

**Le registre** : `scripts/recette/.traversee-c4l7-registre.json`.

**Le retrait** :

```
node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON \
     --import ./scripts/register-calibration-resolver.mjs \
     scripts/recette/traversee-c4l7.mjs --retire
```

**Tout le reste est retiré** : les décors maison, de reprise et de route sont partis dans le run
même, les mesures orphelines sont supprimées, et **les six interrupteurs sont re-constatés à OFF**.

---

## 6. Ce qui attend une décision de Louis

1. **Le défaut de publication maison** *(§3)* — publication automatique en fin de chaîne, ou
   ouverture de la pile de correction aux dépôts maison ? **Le « fait quand » de C4-L7 présuppose un
   `published_at` que rien ne peut poser.**
2. **`C4L2-11`** — des mesures fabriquées par la recette valent-elles « des données réelles » pour
   observer une escalade ?
3. **`C4L7-9`** — renuméroter `C4L10-14/15/16` dans les trois sections.
