# RELEVÉ — Session Code C4-L4 · La passation en classe *(22/08/2026)*

> Ce relevé porte **ce que la séance a construit**, **ce qu'elle a tranché et pourquoi**,
> **ce qu'elle a amendé au `07-`**, **ce qu'elle a mesuré**, et **les questions qu'elle laisse
> ouvertes à Louis**. Le journal SQL porte les migrations ; `SUIVI_tests_manuels.md` porte ce qui
> reste à jouer en recette.

---

## 1. Le contrôle d'entrée — les deux temps

**Les quatre pièces du manifeste, aux versions attendues :**

| Pièce | Attendue par le prompt | Constatée le 22/08 | |
|---|---|---|---|
| `07-Implementation.md` §1 | VERSION 2.23 · RELU ET VALIDÉ | **2.23** · RELU ET VALIDÉ | ✅ *(passée à **2.24** par l'amendement de cette séance, §4 ci-dessous)* |
| `02-exercices.md` §5 et §6.D | VERSION 5.4 · VALIDÉ ET GELÉ | **5.4** · VALIDÉ ET GELÉ | ✅ |
| `06-Palimpseste.md` §1, §4, §7 | VERSION 2.5 · VALIDÉ ET GELÉ | **2.5** · VALIDÉ ET GELÉ | ✅ |
| prompt de transcription | à `copies-tests/`, fait foi tel quel | **3 343 o**, md5 `d5369bb5…` | ✅ **identique à l'octet** à la copie de la racine du dépôt `palimpseste` |

**Les dépendances, vérifiées PAR REQUÊTE et non par confiance :**

- **C4-L1 joué** — les **33 colonnes** d'`exercices_depots` y sont, toutes celles que le prompt
  nomme, et **aucune colonne `note`**. `exercices.optin_se_juger` et `optin_confiance_remise`
  existent, `boolean not null default false`. `exercices_retours` porte `published_at`, `lu_at`,
  `points_ids`. `profiles` porte `mode_saisie_force` *(et `exception_expression`,
  `exception_orthographe`)*, **et aucune colonne de motif**.
- **C4-L5 joué** — `exercices_retours.texte` et `texte_edite_par_prof` sont en `jsonb` sous
  `retours_texte_segmente_chk`, **appelée à deux arguments** :
  `retour_segmente_bien_forme(texte, true) AND retour_segmente_bien_forme(texte_edite_par_prof, false)`.
  ✅ **Un seul domicile** — le second exemplaire a bien été retiré le 21/08.
- **C4-L8 joué** — `exercices_routes` porte **3 264** lignes, `exercices_types_crans` **117**.
- **Les gardes de C4-L1, éprouvées et non supposées** : `garde_depot_lieu` refuse `vf_remis` **et
  les cinq champs de version finale** ; `photos_bien_formees` contrôlait le **type** des quatre
  clés ; `depots_confiance_chk` exige un **objet**.
- **⭐ Les cinq interrupteurs à OFF** — `exercices_actif`, `routeur_actif`,
  `competences_affichage_actif`, `fabrique_actif`, `chaine_actif` : **tous à `f`**, et le sixième
  que cette séance a posé aussi *(§4)*.
- **⭐ La borne du moment, confirmée** : `python3 scripts/derive-instruments.py --resume` rend
  **« compétences OUVERTES à la chaîne : AUCUNE »** — les six fiches sont *relues et validées*,
  pas *versées et bancées* ; `utils/chaine/derive/competences/` est **vide** ; en base,
  `competences_niveaux.statut_recette` ne porte que **`mesuree_silencieusement`** (Expression,
  Synthèse) et **aucune `evaluee`**.
- **⚠️ RLS — constat par requête, et il est plus fermé que la convention** : sur les **37 tables**
  du moteur, **une seule policy par table**, `*_prof_all`, `for all`, réservée au rôle `prof`.
  **Zéro policy élève.** Ce lot est le premier à faire écrire un élève dans ces tables : **toutes
  ses écritures passent par le serveur**, par le client admin, dans des actions qui vérifient que
  l'élève est bien celui du dépôt. **Aucune policy élève n'a été ajoutée.**

---

## 2. Ce que la séance a construit

**Une migration** — `c4_l4_passation.sql` *(+ son rollback, écrit AVANT et présent sur le disque)*,
jouée en sandbox le 22/08, répétition à blanc d'abord *(corps seul)* et retour arrière **vérifié par
requête**. Trois gestes : l'interrupteur propre à OFF, la colonne
`exercices_depots.transcription_v1_doutes` et sa garde, et le **domaine** de `rotation` resserré au
quart de tour.

**Le prompt de transcription est désormais DÉRIVÉ** — `scripts/derive-instruments.py` le lit à
`copies-tests/`, le verse dans `utils/chaine/derive/transcription.ts`, et `--verifie` dit
**IDENTIQUE** ou **DIVERGE**. Il est découpé en **trois parts** — l'énoncé d'usage, le corps
verrouillé, la règle d'anonymisation —, ce qui **nomme** les deux réglages ouverts sans en décider
aucun. *Il n'a ni ligne VERSION ni statut : son **empreinte** tient lieu de version.*

**Onze modules** sous `utils/passation/` : `acces` *(l'interrupteur propre)*, `garde` *(qui entre)*,
`config`, `chemins` *(où vivent les photos)*, `photos` **(pur)**, `transcription-calcul` **(pur)**,
`revelation` **(pur)**, `transcription` *(les deux passes)*, `ouvrier` *(le job)*, `depots` *(les
dix-huit étapes)*, `metacognition` *(les deux drapeaux)*, `retours` *(la correction)*, `vues`.
**48 tests purs neufs** ; `npm test` : **476/476**.

**Un correctif à la file de C4-L5** — `reclamerJobs` accepte désormais un filtre `etapes` *(ne pas
réclamer ce qu'on ne peut pas servir : la réclamation **brûle une tentative**)* et un filtre
`depotId` *(§7.2 bis)*. `tourDeFile` **dispatche par étape** : une transcription passée à
`traiterDepot` aurait été traitée comme une mesure de v1.

**La vision, ajoutée au fournisseur unique** — `AppelIA` porte un champ `images` **facultatif** :
sans image, les deux adaptateurs sont **strictement inchangés**. Et `Forme` porte un
`texte_brut` — le prompt de transcription impose **son propre format de réponse**, et son corps est
verrouillé : lui demander du JSON serait réécrire une règle qu'on n'a pas le droit de toucher.

**Deux écrans, un seul jeu de composants**, posés dans **les deux modules** —
`/prof/{codex,aletheia}/passation/[exerciceId]` et
`/eleve/modules/{codex,aletheia}/passation/[depotId]`. *« C'est le même flux dans deux modules, et
ce qui commande le comportement est le `lieu`, jamais le module. »*

**Une recette** — `scripts/recette/passation-c4l4.mjs`, patron de `chaine-c4l5.mjs` : **56
contrôles, 55 verts**, le seul rouge étant **la latence** *(§7.1)*, qui est un constat et non un
défaut de construction. Elle joue le flux entier **par le même code que les écrans**, sème son
décor et **le retire**, et **remet les interrupteurs exactement comme elle les a trouvés**.

⭐ **Et elle a trouvé un vrai défaut, que seul l'échelle pouvait montrer** — voir §7.2 bis.

---

## 3. Ce que la séance a TRANCHÉ, et pourquoi

Chaque point ci-dessous est un endroit où le prompt demandait de trancher et de le dire.

### 3.1 Piège 2 — file PLUS déclenchement immédiat, et non appel direct hors file

**Tranché : la file, plus le déclenchement immédiat par le dépôt lui-même.** L'étape
`transcription_v1` a été ajoutée à `EtapeChaine`, « ici même » comme son commentaire l'annonçait.

**Pourquoi la file, alors qu'un appel direct serait plus court :**

- **l'idempotence.** Un élève qui tape deux fois sur « envoyer » paierait deux transcriptions, donc
  **quatre appels** — la clé `dépôt:étape` la rend gratuite. *Éprouvé : la seconde mise en file rend
  `deja = true`.*
- **la reprise.** Un appel direct qui échoue est perdu, et l'élève est devant un écran vide
  **pendant l'heure de cours**. Le bail et `tentatives_max` rendent la reprise automatique.
- **la visibilité.** `echec_definitif` est un état que l'écran interroge ; un appel direct n'a pas
  d'état, il a un silence.
- **le filet.** *« Une tâche planifiée est due dans les deux cas »* — sans file, il n'y a rien à
  reprendre.

**Pourquoi le déclenchement immédiat** : la seconde voie du §1.1 *(« une tâche planifiée à la
minute »)* **ne tient pas la seconde**, et la transcription doit revenir *« en quelques secondes,
pendant l'heure de cours »*. Le dépôt appelle donc son propre ouvrier, en ligne.

**⚠️ Et les deux étapes n'ont pas la même porte.** `chaine_actif` gouverne **la mesure** ;
`passation_classe_actif` gouverne **la transcription**. Les lier avait un mode de panne précis : la
coupure automatique de facture bascule `chaine_actif` *(C4-L5)*, et **une classe entière se
retrouverait sans écran, pendant l'heure de cours, parce que la facture du mois a coupé**. La route
`/api/chaine` draine donc **les deux étapes, sous deux portes**, et **ne réclame que ce qu'elle peut
servir** — parce que `reclamerJobs` **incrémente `tentatives` à la prise** : réclamer un job qu'on
va reposer lui brûle un essai.

### 3.2 Piège 12 — `rotation` est un quart de tour, et la garde le dit

**Tranché : oui, bornée à {0, 90, 180, 270}.** Motif : l'écran ne propose que la rotation par
quarts, et une valeur libre rendrait la **somme de contrôle** inutile — deux dépôts de la même page
à 3° près ne se reconnaîtraient plus. La garde `photos_bien_formees` est resserrée en conséquence.
**Sans risque** : `exercices_depots` portait 14 lignes de recette, **aucune avec des photos**.

**Et « dire qu'une page manque, VRAIMENT »** : `marqueurPageManquante(ordre)` fabrique une entrée
**sans fichier qui tient son rang**. Une copie de quatre pages dont la troisième est illisible se
dépose en **quatre** entrées, dont une vide — et non en trois, qui diraient faussement une copie de
trois pages. `refuserPhotos` refuse un **trou** dans l'ordre : *une page qui manque se DÉCLARE*.

### 3.3 Piège 17 — les deux réglages du prompt : PROPOSÉS, non arrêtés → **question à Louis, §6**

### 3.4 Piège 27 — quatre crans de révélation

Le prompt tranchait la structure *(par copie, graduée, aucun interrupteur global)* ; les **échelons**
appartiennent à la session *(« la forme appartient à la session Code »)*. Ils suivent la structure du
retour segmenté et rien d'autre :

| Cran | Ce qu'il montre |
|---|---|
| **0 · Masqué** | rien — *« il juge avant de voir la machine »* |
| **1 · Le compte** | combien de points, par compétence, et de quelle nature. **Aucun texte, aucune citation.** |
| **2 · Les points** | le texte de chaque point. **Aucune citation.** |
| **3 · Le détail** | les ancrages, l'action de révision, le feed-forward |

**Le cran vit dans l'état de l'écran, par copie, et ne se persiste NULLE PART** : rien en base ne se
souvient qu'une copie a été révélée, et **rouvrir l'écran la remasque**. L'identifiant stable
survit à tous les crans — sans quoi la porte de la contestation *(piège 33)* se fermerait.

### 3.5 Piège 37 — le collage : bloqué au clavier, **pas** sur le champ de transcription

**Tranché, et voici le motif.** Le refus du collage **est posé** sur le champ de rédaction au
clavier de l'élève **exempté** — là où l'enjeu de validité est réel, puisque sa copie est une
**ancre**. Il **n'est pas posé** sur le champ d'édition de la transcription : l'élève y recopie **sa
propre copie**, sous les yeux du professeur, et **la copie papier est ramassée**. Un refus l'y
empêcherait de remettre en ordre un paragraphe mal replacé sans rien protéger que le terrain ne
protège déjà — les garde-fous y sont **de terrain**, la source le dit.

**La journalisation est une TRACE SERVEUR, pas un signal.** *« La journalisation d'une tentative
alimente le faisceau, qui ne regarde que la maison »* : rien n'est écrit dans
`integrite_signalements` — le canal existant **ne nomme ni ce module ni ce type**, et un lot le
réutilise, il n'en crée pas un second.

### 3.6 Piège 19 — la `phase` de la transcription est NULL

Écrit **`phase` NULL avec le `depot_id` renseigné**, comme le prompt le tranche : la contrainte en
base *(`api_couts_phase_chk`, vérifiée le 22/08)* n'admet que `p1`, `p2`, `retour` ou NULL, et la
transcription **est dans un exercice sans être un étage de la chaîne**. ✅ Éprouvé : les deux lignes
d'une copie portent `phase = NULL` et leur `depot_id`. **Si un jour la transcription mérite sa
propre valeur d'étage, c'est une décision de source, pas de séance.**

### 3.7 La passe retenue est la PREMIÈRE — un point que la source ne tranchait pas

`06-` §4 dit que `confiance_ocr` **EST** le désaccord des deux passes ; il ne dit pas **laquelle des
deux se conserve**. Retenir la première est **le seul choix qui n'introduise aucun jugement** —
élire « la meilleure » demanderait un critère de qualité, donc un juge, ce que
*« calculé en code, sans modèle de jugement »* exclut. De toute façon, *« la mesure porte sur la
version corrigée »* par l'élève.

### 3.8 Le modèle de la transcription — configuration, et défaut au modèle fort

Le **régime de modèle** *(§6)* suit **la mesure** et ne connaît que **trois étages**, dont la
transcription n'est aucun. **Rien de ce lot n'atteint `utils/chaine/modele.ts`** : « ta part est de
ne rien forcer ». Le modèle de transcription vit à la configuration —
`PASSATION_MODELE_TRANSCRIPTION`, défaut `claude-sonnet-4-6`. **Le défaut est le modèle fort, et
c'est un choix qui se dit** : une faute de transcription ne se rattrape nulle part en aval
*(« c'est sur elle que l'Expression se mesure »)*, et le §6 note déjà que la transcription
**surestime l'Expression, et davantage chez les élèves faibles** — le modèle économique creuserait
un biais connu. **→ à confirmer par Louis, §6.**

### 3.9 Piège 24 — la `comparaison_squelette` N'EST PAS faite ici, et c'est dit

Elle *« suppose le squelette »*, donc elle vient **après** le traitement en lot ; et pour une
passation en classe **elle n'a pas de destinataire écrit** — le déroulé de la maison est C4-L3.
**Elle n'est donc pas faite**, plutôt que dupliquée : *« un calcul dupliqué diverge, un calcul
absent se voit »*. `exercices_metacognition.comparaison_squelette` reste NULL sur ces dépôts.

### 3.9 bis · Piège 61 — la lecture applicative qui plafonne à mille lignes

**Ce lot ne lit aucune des douze tables de doctrine** — ni `exercices_routes`, ni les banques de
consignes : il n'en a pas besoin. Les seules tables de doctrine qu'il effleure sont
`exercices_crans` *(**9** lignes, la table des neuf crans)* et `competences_correspondance`
*(**51** lignes)* : **aucune n'approche le plafond**. La convention a quand même été jouée avant de
lire : `python3 scripts/derive-doctrine.py --verifie` → **IDENTIQUE** *(13 objets · 9 crans · 3 264
routes · fixture IDENTIQUE)*.

⭐ **Mais la panne a été traitée comme une leçon, pas comme une exemption.** Les deux lectures de ce
lot qui **peuvent** grossir sont **paginées et confrontées au décompte que la base annonce** : les
dépôts d'une instance *(`lireDepotsDeLInstance` — et **le lot REFUSE de partir** si les deux comptes
diffèrent, plutôt que d'oublier des copies en silence)*, et la collecte des photos à l'effacement
*(`utils/effacement.ts` — une purge qui s'arrête à mille laisse des fichiers d'élèves derrière
elle, et `error` reste nul)*.

### 3.10 Piège 41-42 — l'effacement, ce qui est fait et ce qui ne l'est pas

**Le chemin choisi est `passation/<élève>/<dépôt>/…` dans le bucket `codex`** — aucun bucket neuf,
le §1 dit « le bucket existant ». **La classe n'est PAS dans le chemin**, délibérément : un élève
qui change de classe rendrait les fichiers introuvables par le geste censé les emporter. Les trois
gestes l'atteignent — par élève, le préfixe suffit ; par classe,
`exercices_depots` → `exercices.classe_id` résout en une requête.

✅ **`utils/effacement.ts` collecte désormais ces photos**, paginé *(supabase-js plafonne à mille
lignes sans rien signaler ; une purge qui s'arrête à mille laisse des fichiers derrière elle)*.

⚠️ **CE QUI N'EST PAS FAIT, ET QUI EST UNE DETTE — voir §5.** `effacer_classe()` **ne touche pas
`exercices_depots`** : les tables de C4-L1 sont nées après elle. Les **fichiers** partent ; les
**lignes** de dépôt d'une classe effacée survivraient.

**Piège 42 — le régime des écritures du professeur sur des lignes d'élève.** `commentaire_general`,
`corrige_par`, `texte_edite_par_prof` sont sur **des lignes d'élève** : elles **partent avec lui**,
c'est la lecture que ce lot retient. **Si Louis veut l'autre régime, c'est une décision de source.**

---

## 4. Ce que la séance a AMENDÉ au `07-` — §1, ouvert à l'implémentation depuis la v2.23

*« Une session Code l'amende depuis son relevé, sans accord préalable ; la modification se dit au
relevé et n'attend rien. »* Deux amendements, et **le fichier passe de 2.23 à 2.24**.

1. **§1.1, la ligne « par version »** — la **rotation est un quart de tour**, et la garde le tient.
2. **§1.1, une ligne neuve** — **les endroits que la machine a peiné à lire**, à côté de la
   `confiance_ocr`. Motif : *« un écran n'affiche un nombre que si ce nombre compte quelque chose »*
   et **un scalaire ne sait pas MONTRER un passage**, or la règle 2 des quatre règles de lisibilité
   veut que la confiance de transcription **dise ce que la machine a peiné à lire**. ⚠️ Le texte de
   l'amendement dit expressément que **ce n'est ni une transcription brute, ni un diff de l'édition
   de l'élève, ni un drapeau d'écart** *(piège 13)* : ce sont **deux lectures machine de la même
   photo**, et rien de l'élève n'y entre.
3. **§1.5** — **un lot peut avoir son propre interrupteur, et il naît à OFF au même emplacement.**
   La section n'en nommait que trois ; trois autres existent déjà en base *(`chaine_actif`,
   `fabrique_actif`, et `passation_classe_actif` posé par ce lot)*, et **ils ne se confondent pas**.

**Aucun `[faux]` posé.** Rien dans les sources ne s'est révélé **faux** — seulement incomplet, ce
que le droit d'amendement couvre.

---

## 5. Les dettes constatées — pour le lot de correctifs, PAS pour cette séance

1. **⚠️ `effacer_classe()` ne touche pas `exercices_depots`.** Les fichiers partent *(fait, §3.10)* ;
   les **lignes** survivraient. C'est un `alter`/`create or replace` sur une fonction
   `security definer` d'un **flux existant** — **protocole renforcé**, et un geste qui ne se fait
   pas en passant. **Le geste à faire** : ajouter au corps de `effacer_classe` la suppression des
   dépôts, jobs, retours, métacognition et squelettes des élèves de la classe, sur les instances de
   cette classe ; et le miroir dans `retirer_inscription`. ⚠️ **Sans emporter le journal des coûts
   ni le dossier d'intégrité**, qui *« survivent délibérément »* *(`06-` §7)*.
2. **La cadence et `maxDuration`** — voir §7 : le constat est fait, **le geste part au lot de
   correctifs** *(piège 46)*.
3. **La garde de budget de la route** — *un dernier job tué en vol à chaque tour* : elle ne réserve
   pas la durée du traitement qu'elle engage, et `reclamerJobs` **incrémente `tentatives` à la
   prise**. ⭐ **Ce lot n'a pas aggravé le cas — il l'a contourné là où il pouvait** : la route ne
   réclame que les étapes qu'elle peut servir. **Le compte à cent quarante est au §7.**
4. **`cible_primaire`** — la colonne n'existe pas *(vérifié le 22/08 : aucune colonne de ce nom sur
   `exercices`, aucune occurrence dans le code)*. Le repli alphabétique sert, et **il se dit**.
5. **⚠️ Le chemin absolu de la racine dans la dérivation** — connu, écrit, au lot de correctifs.
   **Ce lot ne l'a pas aggravé** : le prompt de transcription rejoint le **manifeste existant**, qui
   portait déjà `racine_conception`. Aucun second chemin absolu n'est né.
6. **La copie du prompt de transcription à la racine du dépôt `palimpseste`** est désormais **doublement
   morte** : rien ne la lisait, et le code lit maintenant le **dérivé**. Elle ne dérive plus en
   silence *(le contrôle le dirait)*, mais elle n'a plus de raison d'être.

---

## 6. LES QUESTIONS OUVERTES — ce que la séance n'a PAS tranché

### Q1 · ⚠️ L'ÉNONCÉ D'USAGE du prompt de transcription — **due avant la première passation**

Le prompt annonce en tête : *« Cette transcription servira de copie test dans un corpus
d'évaluation »* — **ce qui n'est plus le cas**. Le manifeste dit qu'il **se règle avant la première
passation**. *C'est un texte de source : une session Code ne l'arrête pas.*

**Rédaction proposée**, aussi proche que possible de l'original :

> Tu transcris la photo d'une copie manuscrite d'élève, rédigée en classe. Cette transcription est
> le texte sur lequel le travail de l'élève sera lu : sa seule valeur est sa fidélité absolue à ce
> qui est écrit, fautes comprises.

*Ce qui change : « servira de copie test dans un corpus d'évaluation » → « est le texte sur lequel
le travail de l'élève sera lu », et l'ajout de « rédigée en classe ». Le reste de la phrase — « sa
seule valeur est sa fidélité absolue à ce qui est écrit, fautes comprises » — ne bouge pas.*

### Q2 · ⚠️ LA RÈGLE D'ANONYMISATION (règle 9) — **due avant la première passation**

La source la marque *« supprimer cette règle si inutile »*. Elle remplace le nom de l'élève en
en-tête par `[nom]`.

**Ce que la séance a constaté**, sans décider : la copie réelle utilisée au test de charge **porte
bien un en-tête nominatif**, et le transcripteur l'a rendu. **Deux lectures possibles** :

- **la garder** — la loi 25 ne l'exige pas *(l'élève est identifié par son compte, pas par
  l'en-tête)*, mais elle **retire un nom d'un envoi au sous-traitant** sans rien coûter ;
- **la supprimer** — l'en-tête est parfois **la seule preuve d'appariement papier ↔ compte**, et
  l'effacer complique la restitution des copies *(étape 18)*.

**Recommandation** : la garder. L'appariement est déjà réglé par le compte *(`06-` §1)*, et le nom
sur la photo part de toute façon au sous-traitant — mais il n'a aucune raison d'entrer dans le
**texte** que douze autres traitements reliront.

### Q3 · Le modèle de la transcription — **à confirmer**

Défaut posé : **`claude-sonnet-4-6`** *(le modèle fort)*. Motif au §3.8. **Coût mesuré : 0,033 $ par
copie** *(deux passes)*, soit **≈ 4,60 $ pour 140 copies**, deux à trois fois l'an → **≈ 14 $ par
an**. `PASSATION_MODELE_TRANSCRIPTION` n'est **pas posée** : elle tourne sur le défaut.

### Q4 · Piège 42 — le sort des écritures du professeur sur les lignes d'élève

Ce lot retient : **elles partent avec l'élève** *(ce sont des lignes d'élève)*. L'autre régime —
détacher le commentaire du professeur — est une **décision de source**.

### Q5 · La table de traitement de la lettre d'information — **une ligne à ajouter**

*« Si ton lot crée une donnée d'élève que cette table ne nomme pas, dis-le au relevé — c'est une
ligne à ajouter à la lettre, pas une donnée à taire »* *(piège 44)*.

**La donnée neuve est : les endroits que la machine a peiné à lire** *(`transcription_v1_doutes`)*.
La table nomme déjà « photo de copie » et « transcription ». **Proposition de ligne** : *finalité* —
attirer l'œil de l'élève sur un passage à relire ; *qui la lit* — l'élève et son professeur ;
*sous-traitant* — aucun *(calculée en code)* ; *conservation* — tant que la classe existe ;
*effacement* — avec le dépôt.

### Q6 · ⚠️ LA PAIRE DE DIAGNOSTIC EN CLASSE — « un exercice en DEUX TEMPS », et la classe n'en a qu'un

**Ouverte le 22/08 par le test de la crédence sur paire** *(§7 ter, C4L4-K)*. **La forme est
désormais juste — deux crédences, chacune avec ses candidats ; c'est la SÉQUENCE qui reste à
décider.**

`02-` §2.3.1 a : *« un premier cas traité sur indication, puis un cas neuf de la même famille, traité
seul. **La correction du premier cas est servie avant le second** — c'est elle qui rend l'écart des
deux crédences interprétable, et le transfert mesurable. »*

⚠️ **Or le flux de classe n'a pas ce moment.** Les dix-huit étapes du `02-` §6.D vont de l'ouverture
du dépôt à la publication du retour : **rien n'y sert une correction EN COURS d'exercice**. En
classe, l'élève écrit **toute** sa copie à la main, la photographie, la relit — et les deux crédences
se collectent donc **au même instant, après la copie entière**. Elles sont bien déclarées *« avant de
savoir s'il a raison »* ; mais **l'écart qu'elles produisent n'est pas celui que la source définit** :
il ne mesure pas le déplacement de confiance **après une correction**, puisqu'aucune correction n'a
été servie entre les deux. *L'écart existe et se calcule ; ce qu'il mesure n'est pas le transfert.*

**La question** : une **paire de diagnostic en CLASSE** se déroule-t-elle en deux temps séparés par
une correction — et alors **par quelle voie cette correction est-elle servie**, l'écran ou le
professeur, oralement ou sur papier ? — ou bien la paire est-elle un objet du **formatif à la
maison** *(C4-L3, où les six temps existent)*, la classe n'en recevant qu'une **forme dégradée
assumée**, dont il faudrait alors dire ce que le Monitoring a le droit d'en conclure ?

**Une session Code ne l'arrête pas**, et rien ne presse : ⚠️ **aucune passation diagnostique n'est
concevable aujourd'hui** *(C4L4-8 — les deux types seedés sont vides, la doctrine ne les connaît
pas)*. **Cette question et celle-là se répondent ensemble**, et la séance dédiée aux diagnostiques
est leur domicile naturel.

**Ce qui est construit tient dans les deux cas** : deux crédences, chacune avec ses propres
candidats, chacune identifiée par son `cas`. **Décor laissé en place** pour cette décision — deux
paires de classe chez Alice, `ca5fcb89-…` au cran **guidé** *(dépôt `c3b1eaee-…`)* et `19d1a439-…`
au cran **nommé** *(dépôt `3dcf9e9d-…`)*.

---

## 7. CE QUE LA SÉANCE A MESURÉ — le « fait quand »

*Chiffres du 22/08, sur la sandbox, avec le modèle `claude-sonnet-4-6` et **deux passes par copie**.*

### 7.1 La latence — ⚠️ **LE CONTRAT N'EST PAS TENU**

| | Mesure |
|---|---|
| **une copie, seule** | **21,0 s** puis **23,3 s** *(deux passages)* |
| **le contrat** | *« quelques secondes par copie, pendant l'heure de cours »* *(`02-` §6.D)* |

⚠️ **Vingt-et-une secondes ne sont pas « quelques secondes ».** La recette lit le contrat **à dix
secondes** et **rend un rouge** : elle le mesure, elle ne le déplace pas. *Une recette qui se donne
trente secondes de marge ne mesure plus le contrat.*

**Ce que le chiffre recouvre, et qui n'est pas une excuse** : les deux passes partent **en
parallèle** *(la latence d'une copie est celle d'un appel, pas de deux)*, et la copie éprouvée est
**dense** — une page pleine d'écriture cursive avec ratures, ~1 500 jetons de sortie.

**Les leviers, pour Louis, aucun n'étant à ce lot** : un modèle plus rapide pour la transcription
*(Q3)* · une seule passe *(mais `confiance_ocr` disparaît, et c'est une règle de source)* · accepter
que « quelques secondes » veuille dire une demi-minute *(décision de source)*.

### 7.2 Le test de charge à **cent quarante** — ✅ **LA FILE TIENT**

*Cent quarante copies, la même photo réelle, lancées **par vagues de trente-cinq** — le chiffre de
la simultanéité d'une salle —, par **le chemin réel de l'écran de l'élève**.*

| Ce que le « fait quand » demande | Mesuré |
|---|---|
| **140 copies traversent SANS INTERVENTION** | **140/140 transcrites, 0 en échec** |
| aucun `echec_definitif` | **0** |
| aucune copie oubliée en file | **0** — et **140/140 jobs clos `abouti`** |
| aucun job repris à la main | **0 job à plus d'une tentative** |
| alertes de repli alphabétique | **0** |
| le découpage à l'échelle | **4 blocs sur les 140**, aucune copie en un seul bloc |
| la facture | **280 lignes, 4,54 $** — exactement **2,0 appels par copie** |
| une vague de 35 | **≈ 25 s**, les quatre vagues à moins d'une seconde d'écart |
| les 140 de bout en bout | **99 s** |

⭐ **Une salle entière revient en vingt-cinq secondes, et la file ne bronche pas à cent quarante.**
C'est le **risque n° 1 du corpus**, et il est levé — *« une file qui tient à trente peut s'effondrer
à cent quarante »* : celle-ci ne s'effondre pas. Ce qui n'est pas tenu est le contrat **par copie**
*(§7.1)*, pas la tenue de la file.

### 7.2 bis · ⭐⭐ CE QUE LE TEST DE CHARGE A TROUVÉ — et qui a été réparé en séance

Au **premier** passage : les 140 copies étaient bien transcrites, **mais leurs 140 jobs restaient en
file**. Le rouge était juste, et il a fait remonter mieux qu'un défaut de recette — **un défaut de
conception**.

**`reclamerJobs` trie par `created_at ASC`.** Sans filtre, *« le dépôt appelle lui-même le
déclencheur »* réclamait donc **le job du PREMIER dépôt de la file**, pas le sien : à trente-cinq
élèves qui photographient en même temps, **un seul écran aurait répondu**, et les trente-quatre
autres auraient attendu la tâche planifiée — celle qui *« ne tient pas la seconde »*, et qui **n'est
même pas posée** *(§7.3)*. Le défaut ne se voyait pas à un dépôt à la fois ; il ne se voyait qu'à
l'échelle.

**Réparé** : `reclamerJobs` accepte un `depotId` et filtre **en SQL**. Second passage : **0 copie
oubliée, 140/140 jobs clos, 0 reprise.** *Le test de charge a payé pour lui-même.*

### 7.3 La cadence que l'offre d'hébergement autorise — **piège 46, la part entière de ce lot**

**Constaté d'abord dans le dépôt** : `vercel.json` déclare **un seul cron**,
`/api/scriptorium/synthese-hebdo` *(`0 9 * * 1`)*. **Rien ne vise `/api/chaine`** — la file se
remplirait normalement et **ne se viderait jamais**.

**La cadence autorisée**, lue à la documentation de l'hébergeur *(Vercel, pages « Usage & Pricing
for Cron Jobs » et « Vercel Functions Limits », consultées le 22/08)* :

| | crons par projet | **intervalle minimal** | précision |
|---|---|---|---|
| **Hobby** | 100 | **une fois par jour** | à l'heure *(±59 min)* |
| **Pro** | 100 | **une fois par minute** | à la minute |

⭐ **Louis étant passé au plan payant le 21/08, la cadence à la minute que le §1.1 réclame EST
autorisée.** *(Constat lu à la documentation ; il n'y a sur cette machine ni CLI ni jeton Vercel
pour lire le plan du compte lui-même.)*

⭐ **Et une trouvaille au passage, qui confirme le piège 46 :** `maxDuration = 60` n'a **jamais** été
un plafond d'hébergement. La documentation donne, avec fluid compute : **Hobby 300 s** par défaut
**et** maximum ; **Pro 300 s** par défaut, **800 s** maximum, **1 800 s** en maximum étendu *(bêta)*.
Le commentaire de `app/api/chaine/route.ts` disait « le plafond du plan Vercel Hobby » : **il était
faux, et il est corrigé dans le fichier**. **Le geste — poser la cadence et monter `maxDuration` —
part au lot de correctifs, pas ici.**

### 7.4 Ce qui attend la première fiche *versée et bancée*

**La chaîne ouvre ZÉRO compétence, et aucune compétence n'est `evaluee`.** Conséquences, toutes
constatées en séance :

- **le retour affiché ne s'éprouve pas avec un vrai retour** : la chaîne n'en engendre aucun. La
  recette **pose une fixture** à la forme exacte que C4-L5 écrira — et le dit ;
- **« se juger » n'est pas servi** : *« aucune compétence de cet exercice n'est au statut
  `evaluee` »*. Le drapeau est nécessaire, pas suffisant ;
- **la confiance de remise est SERVIE et ne collecte rien** : l'étape existe, elle sert un objet
  vide. **Construite quand même** — c'est la collecte qu'une année manquée ne rattrape pas ;
- **aucune mesure n'est écrite**, et **aucune valeur de confiance de remise** n'est collectée.

### 7.5 La crédence — le constat du piège 25, revérifié par requête

Les **deux types diagnostiques** seedés par C4-L1 — `diagnostic_essai` et
`diagnostic_explication_texte` — sont toujours **SANS CRAN** : `crans_admis` vaut `{}`, et
`exercices_types_crans` ne porte **aucune ligne** pour eux. **Il n'y a donc, au 22/08, aucun écran
de crédence à servir pour eux.** L'écran est construit et se sert aux six crans de `diagnostiquer`
et de `transformer` — dont la forme suit le cran, et dont les crans **guidés** exigent **quatre
candidats** *(trois distracteurs plus la réponse attendue, plancher de trois en banque)*.

### 7.6 « Les sujets des deux passations diagnostiques sont chargés »

**Partiellement.** En base au 22/08 : **un sujet** *(« Peut-on douter de tout ? », `cours_etat`
`generique`, statut `valide`)* et **deux textes**. **Aucune instance `lieu = classe` n'existe** — les
cinq instances en base sont toutes `maison` et toutes sur le type `argument`. **Les deux passations
diagnostiques ne sont donc PAS montées** : c'est un geste de **fabrique** *(C4-L8, joué)*, pas de ce
lot, et il reste à faire. *Décoché à `SUIVI_tests_manuels.md`.*

---

## 7 bis. ⭐ LE SMOKE TEST EN VRAI NAVIGATEUR — joué le 22/08, et il a payé

*Joué dans Chrome (élève) et dans le navigateur local (professeur — vérifié au préalable qu'aucun
`confirm()` natif n'y vit, ce que la règle d'or interdit d'y tester), sur `localhost`, décor monté
sur la classe `Test` : une instance `lieu = classe`, sept dépôts créés à l'assignation.*

**Le flux entier passe** : ouverture manuelle → dépôt de la vraie copie manuscrite avec **une page
déclarée manquante** → transcription → correction libre → validation → déclenchement du lot →
révélation cran par cran → édition → commentaire → validation → **case de publication** → **lecture
validée par l'élève**. Vérifié à l'écran ET en base.

### ⚠️⚠️ Et il a trouvé un défaut que rien d'autre ne pouvait trouver

**LE CRLF.** La soumission d'un formulaire HTML **normalise la valeur d'un `<textarea>` en CRLF** —
c'est la spécification. Le texte validé par l'élève arrivait donc en `\r\n` et s'y stockait ; or
`blocs()` cherche `\n[ \t]*\n`, et `\r\n\r\n` **ne matche pas** — le `\r` n'est ni une espace ni
une tabulation.

    blocs() sur le texte RÉELLEMENT STOCKÉ  : 1
    blocs() si l'on normalisait le CRLF     : 4

C'est **mot pour mot** la panne que les sources annoncent : *« une transcription qui fusionne deux
paragraphes fabrique une copie sans architecture, et la copie est lue en défaillance forte »*
*(`06-` §4)*. **La Structure de toute copie validée depuis un navigateur aurait planché** — sans
erreur, sans trace, sans que rien ne le dise.

⚠️ **Ni la recette, ni les 476 tests, ni `tsc`, ni `eslint` ne pouvaient le voir** : ils appellent
les fonctions serveur **depuis Node, avec des `\n`**. La normalisation du navigateur n'existe que
dans le navigateur. *C'est la règle d'or du `SUIVI_tests_manuels.md`, écrite le 24/07, qui a payé —
et c'est la première fois qu'elle paie sur ce lot.* **Leçon à retenir pour C4-L3, qui construira le
champ de rédaction de la maison : le même piège l'attend.**

**Deux autres défauts, d'affichage :** l'écran comptait `corrige_at` au lieu de `v1_remis_at` et
annonçait « 0 copie validée sur 7 » quand une copie était remise *(le serveur, lui, comptait juste)* ;
et `capitalize` portait sur toute la ligne du sommaire. Les trois sont corrigés et revérifiés dans
le navigateur.

### Ce que le smoke test n'a PAS pu prouver, et pourquoi

- **« se juger » et la confiance de remise ne servent rien** — les deux drapeaux **sont** levés,
  mais aucune compétence n'est `evaluee` : l'écran n'affiche donc rien. C'est correct, et c'est
  silencieux. ⚠️ **Un professeur qui lève un drapeau et ne voit rien se produire n'a aucun moyen de
  savoir pourquoi** : son écran ne le dit pas. Ce n'est pas un défaut au sens des sources — mais
  c'est une question de forme à trancher, et elle est ici.
- **le retour affiché est une FIXTURE** — la chaîne ouvre zéro compétence et n'engendre aucun
  retour. Ce qui est prouvé est **l'écran**, pas la mesure.

---

## 7 ter. LA SECONDE SÉANCE DE TESTS — 22/08, et deux défauts de plus

Quatre restes joués : l'effacement d'une classe, l'élève exempté, la crédence aux deux formes, et
la tentative sur les sujets diagnostiques.

### ⚠️ C4L4-G · La crédence était IMPOSSIBLE À ENREGISTRER

J'écrivais `credence` comme un **objet** — `{ forme, saisies }`. La garde de C4-L1,
`metacog_credence_chk`, exige un **TABLEAU** :

    CHECK ((credence IS NULL) OR (jsonb_typeof(credence) = 'array'))

Et **elle a raison** : elle met en œuvre le §1.2, *« la `credence` […] **plusieurs valeurs par
dépôt** — il y en a une par diagnostic, donc **deux sur une paire** »*. L'élève répartissait ses
jetons, cliquait, et recevait une `23514` **brute à l'écran**. Corrigé : une **liste**, une entrée
par cas, chacune portant sa forme ; et la `23514` est traduite, comme elle l'est déjà pour le retour.

⚠️ **C'est ma faute, et elle est nommable** : j'ai lu la garde du retour et celle des photos, **pas
celle de la crédence**. Le prompt m'avait pourtant prévenu au piège 28 — « deux précisions qui
t'éviteront une `23514` en pleine correction ». J'avais appliqué la leçon à un endroit et pas à
l'autre.

⚠️ **Et la recette ne pouvait pas le voir** : son décor n'avait **aucun cran à crédence** — ce que
le piège 25 annonçait déjà (les deux types diagnostiques sont sans cran). Il fallait fabriquer une
instance à cran de diagnostic, ce que seul le smoke test a fait.

### ✅ C4L4-H · Deux règles de la même source se contredisaient — TRANCHÉ PAR LOUIS le 22/08

`06-` §1 exige de **refuser le collage par « menu contextuel »** ; le même §1 exige que **« le
correcteur orthographique du navigateur reste actif — il n'est pas désactivé »**. Or, dans Chrome,
**la seule façon d'appliquer une suggestion orthographique est le menu contextuel**. Le bloquer
garde les soulignements rouges et retire la possibilité d'agir dessus.

⭐ **DÉCISION DE LOUIS, 22/08** : *« Tu as bien fait de garder les soulignements et d'enlever la
possibilité d'agir dessus »*, et *« bloque le collage au raccourci et au glisser-déposer »*. **Les
trois vecteurs restent donc bloqués** — le correcteur SIGNALE, il ne corrige plus à la place de
l'élève. C'est cohérent avec ce que la règle protège : sur une copie qui est une **ancre**, le
texte doit être celui de l'élève, corrections comprises.

*Conséquence portée au code* : `spellCheck` est désormais **explicite** sur le champ de rédaction au
clavier, et non laissé au défaut du navigateur — une règle que la source nomme ne se confie pas à un
défaut d'implémentation qui peut changer.

**Les trois vecteurs sont éprouvés** : `raccourci` au presse-papiers réel du système,
`menu-contextuel` au clic droit réel, `glisser-deposer` par un `DragEvent` porteur d'un vrai
`DataTransfer` — `defaultPrevented: true`, valeur inchangée.

### ⚠️ C4L4-I · « Journalisée » n'était pas un journal — LES TROIS SE RAPPORTENT AU PROFESSEUR

⭐ **RELEVÉ PAR LOUIS, 22/08** : *« Il me semble qu'on avait dit que les 3 devaient être rapportés au
prof. Tu dis qu'ils ne lèvent pas de signal d'intégrité. »* **Les deux choses étaient vraies, et
c'est la seconde qui masquait la première.**

**Ce que je disais était exact, et insuffisant.** Aucun signalement d'intégrité n'est levé, et le
motif tient — le §7 de la SPEC ne fait **jamais** d'un signal isolé un drapeau : c'est la
**convergence** qui part au professeur, par `signalerEnAttenteIA`, avec confirmation humaine ; et
*« la journalisation d'une tentative alimente le faisceau, qui ne regarde que la maison »*
*(`06-` §6 ; piège 37)*. **Mais `06-` §1 exige que « chaque tentative de collage bloquée soit
JOURNALISÉE », et ce que j'avais posé était un `console.warn`** — que personne ne lit, qui expire
avec les journaux de l'hébergeur, et **que le professeur ne voit jamais**. Ce n'est pas un journal.
*Entre « pas un signalement » et « pas un journal » il y avait toute la place d'une exigence non
tenue, et c'est elle que la question de Louis a trouvée.*

⚠️ **UN MOTIF FAUX QUI PROTÉGEAIT LA BONNE DÉCISION.** Le commentaire de `depots.ts` justifiait le
silence par *« le canal existant ne nomme ni ce module ni ce type »*. **Vérifié par requête le
22/08 : c'est faux.** `integrite_signalements_module_check` **nomme** `codex` et `aletheia`, et
`type` **n'a aucune contrainte** — le canal aurait accepté la ligne. *(Il n'aurait toutefois gardé
qu'UNE ligne par rendu : `unique (eleve_id, module, rendu_ref)`, quand il en faut N.)* **C'est la
doctrine qui l'interdit, pas la forme de la table** — et un motif faux qui protège la bonne décision
reste un motif faux. Rectifié dans le code, dans la migration et ici.

**Ce qui est construit** — `c4_l4_collage_journal.sql`, joué en sandbox le 22/08, six drapeaux à
`t` : `exercices_depots.collages_bloques` *(une entrée par tentative : `{moyen, at}`)*, sa garde
`depots_collages_chk` **au domaine fermé aux trois vecteurs de la source**, et
`journaliser_collage()` — **l'ajout est ATOMIQUE**, parce qu'un lire-modifier-écrire perdrait une
tentative sur deux `Cmd+V` rapprochés, *et c'est exactement ce qu'on veut compter*. L'instant est
celui du **serveur** : l'horloge du navigateur de l'élève n'est pas une source.

**La colonne vit sur le DÉPÔT — et C4-L3 y écrira aussi.** C'est la copie qui est en cause, et le
champ de rédaction de la maison est le même champ : *« un lot le réutilise, il n'en crée pas un
second »*.

**Ce que l'écran du professeur rend, mot pour mot** *(éprouvé au navigateur le 22/08)* :

> **3 tentatives de collage bloquées — 1 au raccourci clavier, 1 au glisser-déposer, 1 au menu
> contextuel.** Le collage est refusé dans le champ de rédaction ; ces tentatives n'ont rien inséré.
> C'est une information, pas un verdict : rien n'a été signalé, et le blocage est côté navigateur
> seulement — il arrête le geste paresseux, pas l'élève déterminé.

⚠️ **INFORMER N'EST PAS ACCUSER, et l'écran le tient par sa forme autant que par ses mots** : ton
d'attention, aucune couleur de faute, **aucune action offerte** — le professeur n'a rien à valider
ni à signaler là. ⚠️ **La réserve de la source est écrite À L'ÉCRAN**, pas seulement dans le code :
sans elle, un professeur lirait un zéro comme une garantie. **D'où : à zéro tentative, l'écran
n'affiche RIEN** — *« un écran n'affiche un nombre que si ce nombre compte quelque chose »*
*(`06-` §5)*.

### ⚠️ C4L4-J · La copie de l'élève exempté était INVISIBLE — des deux côtés

**Trouvé en construisant C4L4-I, sur le même chemin, et c'est plus grave que ce qui m'y amenait.**

**(1) Côté PROFESSEUR** : l'écran de correction ne lisait que `transcription_v1`. La copie d'un
élève exempté vit dans **`texte_v1`** — elle **n'apparaissait nulle part**. Le professeur corrigeait
un retour **sans pouvoir lire la copie**, alors que la chaîne, elle, la lisait bien
*(`production()` lit `texte_v1` **ou** `transcription_v1`)*. **L'aménagement rendait la copie
invisible à celui qui la corrige** — exactement l'élève que l'exemption protège.

**(2) Côté ÉLÈVE** : le champ s'initialisait sur `vue.transcription`, toujours NULL au clavier.
L'élève qui **rouvrait sa page après avoir validé** trouvait un champ **vide et verrouillé** : sa
copie avait disparu de son écran.

⚠️ **Pourquoi le smoke test du 22/08 ne pouvait pas le voir** : il a tapé et validé **d'un seul
trait, sans jamais recharger**, et il n'est pas retourné sur l'écran du professeur après. *La leçon
n'est pas « tester au navigateur » — c'était déjà fait — mais **rouvrir** : un écran ne ment pas
pendant qu'on le remplit, il ment quand on y revient.*

✅ **Les deux corrigés et éprouvés** : l'élève retrouve ses 244 caractères en lecture seule ; le
professeur lit « La copie (3 paragraphes · **tapée au clavier (aménagement)**) », texte compris.

### ⚠️ C4L4-K · LA PAIRE NE COLLECTAIT QU'UNE CRÉDENCE SUR DEUX

**Demandé par Louis le 22/08 ; le décor a été monté pour la première fois, et il a trouvé.**

**Ce qui n'allait pas.** `offreCredence` ne rendait qu'**une** forme et **un** jeu de candidats, tirés
du **premier** cas ; `actionCredence` poussait `{ cas: 1 }` **en dur**. Sur une vraie paire — un
exercice, `paire_diagnostic = true`, **deux** `exercices_cas` aux distracteurs différents — l'écran
servait **une seule crédence**, celle du cas 1, et la seconde **n'existait pas**.

⚠️ **Et c'est la moitié qui porte la mesure qui manquait.** *« Il y en a une par diagnostic, donc
DEUX sur une paire »* *(`07-` §1.2)* ; et le motif est au `02-` §2.3.1 a : *« l'ÉCART entre les deux
dit si la confiance s'est déplacée juste après la correction »*. **Une crédence unique sur une paire
ne mesure aucun transfert** — elle rend un chiffre là où il faut une différence.

**Établi sur pièce, par requête**, avant correction : `[{"cas": 1, …}]` — une entrée, sur une paire.

**Ce qui est corrigé.** L'offre porte désormais **une liste de cas** — `cas: [{ordre, consigne,
candidats}]`, plus `paire` — construite depuis `exercices.paire_diagnostic` et les rangs
d'`exercices_cas` *(la base les bornait déjà à `{1, 2}`)*. ⚠️ **Chaque cas sert SES PROPRES
candidats** : le second temps est *« un cas NEUF de la même famille »*, et lui resservir les
candidats du premier ferait de la seconde crédence **une copie de la première**. L'écran rend un
bloc par cas, **étiqueté par la consigne de ce cas** *(une paire en porte deux — `exercices_paire_chk`
l'exige)*, et le formulaire préfixe chaque champ par l'ordre : `j:2:…`, `pourcentage:2`.

⭐ **ABSENT N'EST PAS ZÉRO — et c'est la correction la moins visible et la plus importante.** Au
cran nommé, un champ manquant lu comme `0` aurait enregistré *« 0 % de chances »* : **une crédence
INVENTÉE, que le Monitoring aurait crue déclarée**. C'est la même règle que *« `delta_v1_vf` NULL
n'est pas 0 »* *(`06-` §6)*. L'action ne pousse donc **rien** pour un cas muet, et la garde refuse en
**nommant le cas qui manque**.

**Éprouvé au navigateur, les deux formes, sur deux vraies paires** :

| | ce que l'écran sert | ce que la base reçoit |
|---|---|---|
| `diagnostic_guide` | **8 champs**, `j:1:…` et `j:2:…`, **quatre candidats DIFFÉRENTS par cas** | deux entrées, `70` puis `40` sur la bonne réponse — **l'écart existe** |
| `diagnostic_nomme` | **2 champs**, `pourcentage:1` et `pourcentage:2`, aucun candidat | deux entrées, `85 %` puis `35 %` |

Et les deux gardes le disent juste : « **Cas 2** : les jetons doivent totaliser 100 (ils font 40). »,
« **Une paire demande une crédence par cas : il manque celle du cas 2.** »
✅ Recette rejouée : **44 verts, 0 rouge** — le chemin à un seul cas n'a pas bougé.

⚠️ **CE TEST A OUVERT UNE QUESTION DE SOURCE — elle est portée au §6, en Q6**, avec les cinq
autres : la paire est *« un exercice en DEUX TEMPS »*, séparés par la correction du premier cas, et
**le flux de classe n'a pas ce moment**. La forme est juste ; la séquence reste à décider. *Elle vit
au §6 et non ici : une question rangée dans un journal de tests n'est pas une question posée.*
**Décor laissé en place** pour elle — deux paires de classe sur la classe `Test`, chez Alice,
`ca5fcb89-…` au cran **guidé** *(dépôt `c3b1eaee-…`)* et `19d1a439-…` au cran **nommé** *(dépôt
`3dcf9e9d-…`)*, toutes deux `paire_diagnostic = true`, deux cas, deux consignes.

### ⚠️ C4L4-8 · Les passations diagnostiques ne sont pas concevables — question ouverte

Établi par requête : les deux types seedés sont **vides** (0 cran, 0 mode, aucun grain,
`nature = 'complet'`), et **la doctrine ne connaît que les treize objets** — ils n'y sont pas.
L'écran de conception de C4-L8 dérivant ses choix de la doctrine, **il ne peut pas produire une
passation diagnostique**. Ce n'est un défaut ni de C4-L4 ni de C4-L8 : une passation diagnostique
n'est pas *un objet à un cran*, c'est **une copie entière**. **Par quelle voie naît-elle ?**

⭐ **DÉCISION DE LOUIS, 22/08 : une SÉANCE DÉDIÉE traitera les diagnostiques.** La quatrième ligne du
« fait quand » de C4-L4 sort donc de sa recette : elle ne s'y jouera pas, et ce n'est plus un reste
de ce lot mais l'objet d'une séance à venir. *Ce que cette séance trouvera écrit ici : les deux
types sont seedés vides, la doctrine ne les connaît pas, et le flux de C4-L4 les accueillera sans
rien changer dès qu'une instance `lieu = classe` existera — c'est le `lieu` qui commande, jamais le
type.*

### Ce que la séance a prouvé

- **l'élève exempté** : bascule clavier, **rappel de lisibilité absent** (piège 34 tenu), refus du
  collage **prouvé au presse-papiers réel** sur deux vecteurs, traces serveur, **zéro signalement
  d'intégrité**, correcteur orthographique actif, `texte_v1` écrit et **0 CR / 3 blocs** ;
- **la crédence** aux deux formes, avec la garde des 100 jetons qui tient et le dit ;
- **l'effacement** : fichiers partis, instance **détachée**, journal des coûts survivant — et le
  travail d'élève qui survit, ce qui chiffre la dette du §5.1.

---

## 7 quater. LA VÉRIFICATION DE CLÔTURE — sur l'ÉTAT FUSIONNÉ, C4-L2 comprise

**Pourquoi elle existe** : C4-L2 et C4-L4 ont été construits **en parallèle, sans jamais se voir**,
et ils touchent le même arbre — `utils/passation/`, `utils/chaine/derive/`, `SUIVI_SQL.md`. Aucun
des deux lots ne s'était éprouvé **après** la fusion de l'autre. Jouée sur `main` à `48282e8`.

| | résultat |
|---|---|
| `tsc --noEmit` | **propre**, zéro erreur sur tout le dépôt |
| `npm test` | **743 / 743**, 0 échec |
| Recette C4-L4 *(`--sans-appel`)* | **44 verts, 0 rouge** |
| Recette C4-L2 *(lecture seule)* | **tout passe**, les huit sections |
| Les six interrupteurs | **tous à `false`**, re-constatés APRÈS les deux recettes |

### ESLint — **17 signalements : 2 erreurs, 15 avertissements**

⚠️ **AUCUNE des deux ERREURS n'appartient à C4-L2 ni à C4-L4**, et c'est vérifié, pas supposé
*(`git log --name-only` sur les trois commits de la fusion : le fichier n'y figure pas)*.

| fichier | ce qu'il rend | à qui |
|---|---|---|
| `handoff_en_tete/support.js` | **2 erreurs** — `ReactDOM.render` déprécié *(React 18)* et une affectation à `module` *(`@next/next/no-assign-module-variable`)* — plus **7 avertissements** | **préexistant**, commité en `9d02454` ; fichier de **handoff de design**, hors application |
| `app/eleve/modules/fragments-erudition/page.tsx` | 2 avertissements *(variables inutilisées)* | **préexistant**, hors C4 |
| `utils/synthese-semestre.ts` | 2 avertissements *(`dateDebut`, `dateFin` inutilisées)* | **préexistant**, hors C4 |
| `scripts/recette/routeur-c4l2-decor.mjs` | 1 avertissement | **C4-L2** |
| `scripts/recette/passation-c4l4.mjs` | **3 avertissements** — `lireDepotsDeLInstance`, `transcrireDepot`, `marqueurPageManquante` importés et jamais appelés | ⚠️ **CE LOT** |

⚠️ **DETTE NOMMÉE, ET ELLE EST À MOI** : les trois imports morts de la recette de C4-L4 sont venus
avec le script d'origine et **ont survécu à toutes les passes de la séance** — `tsc` ne les voit pas
*(ce sont des liaisons valides)*, et `npm test` ne lit pas ce fichier. **Sans conséquence
d'exécution** ; c'est du bruit qui masquerait un vrai import mort le jour où il y en aurait un.
*Non corrigé ici parce que la séance se clôt : à prendre au lot de correctifs.*

⭐ **Ce que ce relevé NE dit pas** : que le dépôt est propre au linter. Il ne l'est pas, il ne l'était
pas avant ce lot, et **deux erreurs y dorment depuis `9d02454`**. Les nommer ici est le seul moyen
qu'elles ne se fassent pas attribuer, un jour, à la passation en classe.

---

## 8. Le périmètre — ce que ce lot n'a PAS fait, délibérément

- **aucune mesure, aucun registre élu, aucune lettre agrégée** — la chaîne est C4-L5, appelée par
  une **écriture en file** ;
- **aucun déroulé à six temps** *(C4-L3)*, **aucune fabrique** *(C4-L8)*, **aucun pilotage**
  *(C4-L2)*, **aucun écran d'assiduité** *(C4-L2)* ;
- **aucune réorganisation de la navigation** *(C4-L6, C5-L4)* : les écrans se posent dans Codex et
  Aletheia **tels qu'ils sont**, et la porte est **un lien** sur l'écran de conception, où le
  professeur vient déjà d'assigner. **Aucun sous-onglet n'a été ajouté** ;
- **aucune table créée** ; **aucun interrupteur allumé** ; **aucune policy élève ouverte** ;
- **aucun second sous-traitant** — l'appel passe par `utils/chaine/appel.ts`, donc par le
  fournisseur d'intelligence artificielle, et par lui seul ;
- **aucune expiration, aucun TTL, aucune purge planifiée** — *« rien n'expire, tout s'efface à la
  main »* ;
- **aucune ligne de dépôt créée** — elles naissent **à l'assignation**, et c'est C4-L8 qui le fait
  déjà *(`assignerALaClasse`)* ;
- **aucune question à l'élève sur ce qu'il n'a pas compris** ;
- **aucun signal d'intégrité, aucun seuil « photo suspecte »** — l'EXIF est purgé **parce que la loi
  25 l'exige**, pas pour en tirer un signal.

**Une seule découverte hors périmètre a été portée à `IDEES_post_rentree.md`** *(voir le fichier)*.
